# M5.4 代码审核 — 第 2 轮

> 第 1 轮修复后的全面复审

## 第 1 轮修复验证

两项修复均已验证正确：
1. PoolRunner 接口扩展 — 所有实现者（mockPoolRunner、cancelPoolRunner）已更新，Pool 原生实现完整
2. 超时校验补全 — triage_e2e 已加入 validate.go 的超时校验循环，worker_queue_config.go 映射正确

## 发现问题

### Critical (0)

无。

### Important (2)

**[Important-1] `container.go:297-301` — buildContainerCmd 的 triage_e2e case 变为死代码**

Processor 现在直接通过 `RunWithCommandAndStdin` 传入 cmd + prompt，不再走 `pool.Run` → `buildContainerCmd` 路径。该 case 虽不会被生产路径触发，但保留作为 fallback 有合理性（未来可能有测试/调试工具直接调 Pool）。

**修复**：更新注释，明确标注"生产路径不经过此函数"，降低维护误解风险。

**[Important-2] `enqueue.go:430` — handleMergedE2ERegression 入队失败仅 Warn 级日志**

设计文档明确要求 void return（不阻断 gen_tests），但入队失败意味着该 webhook 的 triage 触发永久丢失。Warn 级日志在高日志量环境中容易被忽略。

**修复**：将 Warn 改为 Error 级，并在日志中追加 PR 号和"触发将丢失"提示。

### Minor (3)

1. sanitizeTriageText 字节截断 — 同第 1 轮，与项目模式一致
2. triage_modules metadata 无总量上限 — 实际受 `maxE2EScanDirs=30` 约束，风险极低
3. Cancel-and-Replace `FindActiveTasksByModule` 错误被 `_` 吞掉 — 符合 fail-open 设计，无需修改

## 额外验证通过的项目

- 迁移 v23 CHECK 约束与 v22 一致 ✓
- selectGiteaToken 默认返回 review token ✓  
- serve.go/serve_deps.go 装配完整 ✓
- buildContainerEnv 注入 BASE_REF ✓
- 链式入队事务性：单模块失败不阻断其他模块 ✓
- 错误脱敏覆盖 triage_e2e ✓
