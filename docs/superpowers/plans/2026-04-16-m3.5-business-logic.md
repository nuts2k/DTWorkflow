# M3.5 修复业务逻辑层实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 M3.4 基础设施上实现 Issue 自动修复到 PR 创建的完整闭环——Claude 在执行容器中分析 + 修复 + 测试 + push，`fix.Service` 通过 Gitea API 创建 PR，Issue 评论同步 PR 链接。

**Architecture:** 单容器全托管执行模型 + 容器外 PR 创建（混合模式）。`fix.Service.Execute` 按 `payload.TaskType` 分流：`analyze_issue` 走既有 `executeAnalysis`，`fix_issue` 走新 `executeFix`（前置信息不足检查 → 采集上下文 → 构造修复 prompt → 容器执行 → 解析 FixOutput → 创建 PR + 回写 Issue 评论）。新增 `PRClient` / `FixStaleChecker` 两个窄接口（ServiceOption 注入），`store.GetLatestAnalysisByIssue` 支撑前置检查。

**Tech Stack:** Go, Gitea REST API（CreatePullRequest/GetRepo），SQLite（历史分析结果查询），Docker（执行镜像），Claude Code CLI（stdin prompt + --output-format json）

**设计文档:** `docs/superpowers/specs/2026-04-16-m3.4-m3.5-fix-execution-design.md` 第 4 章

**M3.4 前置已交付:**
- `TaskTypeFixIssue` 路由已注册（`queue/client.go` AsynqTypeFixIssue、`serve.go` Mux）
- `entrypoint.sh fix_issue)` case 写模式就绪（credential cache + git identity + Maven 缓存重定向）
- `worker/container.go` `buildContainerEnv` 已为 `fix_issue` 注入 `ISSUE_NUMBER` / `ISSUE_TITLE` / `ISSUE_REF`
- `PoolConfig.ImageFull` + `resolveImage` 已为 `fix_issue` 选择执行镜像
- `EnqueueManualFix` + `HandleIssueLabel` 已按 `TaskType` 分流入队
- `fix.Service.Execute` 已含 switch 分流骨架（`fix_issue` 目前返回 not-supported 错误）
- `Processor.ProcessTask` M3.4 仅路由 `analyze_issue` 到 `fixService`，`fix_issue` 走默认 `pool.Run`（本里程碑接管）
- `adaptFixResult` / `handleSkipRetryFailure` / `sendCompletionNotification` 签名已支持 `*fix.FixResult`

---

## 文件改动总览

| 操作 | 文件路径 | 职责 |
|------|---------|------|
| Modify | `internal/fix/result.go` | 新增 `FixOutput` / `TestResults` / `RefKind` 结构体；`FixResult` 扩展 `Fix` / `PRNumber` / `PRURL` / `RefKind`；新增 `ErrInfoInsufficient` / `ErrFixFailed` 等错误常量 |
| Modify | `internal/fix/prompt.go` | 新增修复 prompt 常量（fixPreamble/fixInstructions/fixConstraints/fixJSONSchema）+ `buildFixPrompt` + `buildFixCommand` |
| Modify | `internal/fix/service.go` | 新增 `PRClient` / `FixStaleChecker` 窄接口 + `WithPRClient` / `WithFixStaleChecker` Option；`validateRef` 返回 `RefKind`；新增 `executeFix` + `checkPreviousAnalysis` + `createPR` 流程；`Execute` switch 分流到 `executeFix` |
| Modify | `internal/fix/formatter.go` | 新增 `FormatFixPRBody` / `FormatFixSuccessComment` / `FormatFixFailureComment` / `FormatFixInfoInsufficientComment` / `FormatFixPushButNoPRComment` |
| Modify | `internal/store/store.go` | Store 接口新增 `GetLatestAnalysisByIssue` |
| Modify | `internal/store/sqlite.go` | SQLite 实现 `GetLatestAnalysisByIssue`（查最新 `analyze_issue` succeeded 任务，按 created_at 倒序） |
| Modify | `internal/store/sqlite_test.go` | `GetLatestAnalysisByIssue` 测试 |
| Modify | `internal/queue/processor.go` | `ProcessTask` switch 新增 `fix_issue → fixService`；`buildNotificationMessage` 修复成功 case 注入 `MetaKeyPRURL` / `MetaKeyPRNumber` / `MetaKeyModifiedFiles`；`adaptFixResult` 对 `Fix.Success` 状态判断 |
| Modify | `internal/notify/notifier.go` | 新增 `MetaKeyFixPRURL` / `MetaKeyFixPRNumber` / `MetaKeyModifiedFiles` 常量 |
| Modify | `internal/cmd/serve.go` | `fix.NewService` 新增 `WithPRClient(deps.GiteaClient)` 和 `WithFixStaleChecker(deps.Store)` 装配 |
| Modify | `configs/dtworkflow.example.yaml` | 可选：fix 模式相关说明 |

---

## 验证标准（来自设计文档 4.7 / §5 M3.5）

实施过程中每个 Task 步骤后需回顾：

1. `fix-to-pr` 标签触发 → Claude 分析 + 修复 + 测试 + push → `fix.Service` 创建 PR → Issue 收到 PR 链接评论
2. PR 描述含根因分析、修复方案、修改文件列表、测试结果；PR 关联 Issue（`fixes #N`）
3. 修复 PR 自动进入 Phase 2 评审（Gitea Webhook 自然触发，零代码）
4. 无前序分析 → Claude 隐式分析后修复（不阻拦）
5. 前序分析 `info_sufficient=false` → `fix-to-pr` 触发时 Issue 评论提醒 + 不启动容器
6. 容器执行失败（Claude 返回 `success=false`）→ Issue 评论报告失败 + SkipRetry
7. Push 成功但 PR 创建失败 → Issue 评论提示 + 允许 asynq 重试
8. 分支已存在（重试场景）→ prompt 指示 `git push --force-with-lease` 覆盖
9. Issue Ref 为 tag → PR `Base` 改用仓库默认分支，PR 描述中注明
10. 飞书通知：修复开始（蓝）/ 成功含 PR 链接（绿）/ 失败（红）三场景

---

## Task 1: FixOutput / TestResults 数据结构 + 错误常量

**Files:**
- Modify: `internal/fix/result.go`
- Test: `internal/fix/result_test.go`（如不存在则创建）

- [ ] **Step 1: 写失败测试**

在 `internal/fix/result_test.go` 追加：

```go
package fix

import (
	"encoding/json"
	"testing"
)

func TestFixOutput_JSONUnmarshal(t *testing.T) {
	raw := `{
		"success": true,
		"info_sufficient": true,
		"branch_name": "auto-fix/issue-15",
		"commit_sha": "abc123",
		"modified_files": ["src/a.java", "src/b.java"],
		"test_results": {"passed": 10, "failed": 0, "skipped": 1, "all_passed": true},
		"analysis": "根因是 NPE",
		"fix_approach": "添加空值检查"
	}`
	var out FixOutput
	if err := json.Unmarshal([]byte(raw), &out); err != nil {
		t.Fatalf("unmarshal 失败: %v", err)
	}
	if !out.Success {
		t.Error("Success 应为 true")
	}
	if out.BranchName != "auto-fix/issue-15" {
		t.Errorf("BranchName = %q, 期望 auto-fix/issue-15", out.BranchName)
	}
	if out.TestResults == nil || !out.TestResults.AllPassed || out.TestResults.Passed != 10 {
		t.Errorf("TestResults 字段不正确: %+v", out.TestResults)
	}
	if len(out.ModifiedFiles) != 2 {
		t.Errorf("ModifiedFiles 长度 = %d, 期望 2", len(out.ModifiedFiles))
	}
}

func TestFixOutput_InfoInsufficient(t *testing.T) {
	raw := `{
		"success": false,
		"info_sufficient": false,
		"missing_info": ["缺少错误堆栈", "缺少复现步骤"],
		"analysis": "信息不足无法修复"
	}`
	var out FixOutput
	if err := json.Unmarshal([]byte(raw), &out); err != nil {
		t.Fatalf("unmarshal 失败: %v", err)
	}
	if out.Success || out.InfoSufficient {
		t.Error("Success 和 InfoSufficient 均应为 false")
	}
	if len(out.MissingInfo) != 2 {
		t.Errorf("MissingInfo 长度 = %d, 期望 2", len(out.MissingInfo))
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/fix/ -run TestFixOutput -v`
Expected: 编译错误 `undefined: FixOutput`

- [ ] **Step 3: 实现 FixOutput / TestResults / RefKind / FixResult 扩展**

`internal/fix/result.go` 完全替换为：

```go
package fix

import (
	"errors"

	"otws19.zicp.vip/kelin/dtworkflow/internal/model"
)

// ErrIssueNotOpen Issue 不处于 open 状态时返回此错误，
// Processor 层据此跳过重试（同 review.ErrPRNotOpen 模式）。
var ErrIssueNotOpen = errors.New("Issue 不处于 open 状态")

// ErrMissingIssueRef Issue 未设置关联分支或 tag 时返回此错误，
// Processor 层据此跳过重试（同 ErrIssueNotOpen 模式）。
var ErrMissingIssueRef = errors.New("Issue 未设置关联分支或 tag")

// ErrInvalidIssueRef Issue 关联的分支或 tag 不存在时返回此错误，
// Processor 层据此跳过重试（同 ErrIssueNotOpen 模式）。
var ErrInvalidIssueRef = errors.New("Issue 关联的分支或 tag 不存在")

// ErrInfoInsufficient M3.5: 前序分析报告"信息不足"，阻断 fix_issue。
// Processor 层据此跳过重试（用户补充信息后重新添加标签再触发）。
var ErrInfoInsufficient = errors.New("前序分析报告信息不足，无法修复")

// ErrFixFailed M3.5: Claude 返回 success=false（测试失败、分支未创建等），
// 确定性失败，跳过重试。
var ErrFixFailed = errors.New("修复执行失败")

// ErrFixParseFailure M3.5: FixOutput JSON 解析失败或不变量校验失败。
// Processor 层根据剩余重试次数决定重试或降级评论。
var ErrFixParseFailure = errors.New("修复结果解析失败")

// RefKind 标识 Issue 关联 ref 的类型，PR 创建时需据此决定 Base 字段。
type RefKind int

const (
	RefKindUnknown RefKind = iota
	RefKindBranch          // 分支名，PR Base 直接使用
	RefKindTag             // tag 名，PR Base 需改用仓库默认分支
)

// AnalysisOutput Issue 分析输出 JSON schema（M3.1-M3.3）
type AnalysisOutput struct {
	InfoSufficient bool       `json:"info_sufficient"`
	MissingInfo    []string   `json:"missing_info,omitempty"`
	RootCause      *RootCause `json:"root_cause,omitempty"`
	Analysis       string     `json:"analysis"`
	FixSuggestion  string     `json:"fix_suggestion,omitempty"`
	Confidence     string     `json:"confidence"`
	RelatedFiles   []string   `json:"related_files,omitempty"`
}

// RootCause 根因定位
type RootCause struct {
	File        string `json:"file"`
	Function    string `json:"function,omitempty"`
	StartLine   int    `json:"start_line,omitempty"`
	EndLine     int    `json:"end_line,omitempty"`
	Description string `json:"description"`
}

// FixOutput M3.5: Issue 自动修复输出 JSON schema
type FixOutput struct {
	Success        bool         `json:"success"`
	InfoSufficient bool         `json:"info_sufficient"`
	MissingInfo    []string     `json:"missing_info,omitempty"`
	BranchName     string       `json:"branch_name,omitempty"`
	CommitSHA      string       `json:"commit_sha,omitempty"`
	ModifiedFiles  []string     `json:"modified_files,omitempty"`
	TestResults    *TestResults `json:"test_results,omitempty"`
	Analysis       string       `json:"analysis,omitempty"`
	FixApproach    string       `json:"fix_approach,omitempty"`
	FailureReason  string       `json:"failure_reason,omitempty"`
}

// TestResults M3.5: 容器内测试运行结果
type TestResults struct {
	Passed    int  `json:"passed"`
	Failed    int  `json:"failed"`
	Skipped   int  `json:"skipped"`
	AllPassed bool `json:"all_passed"`
}

// FixResult 是 Service.Execute 的返回值
type FixResult struct {
	IssueContext   *IssueContext   // 采集到的 Issue 上下文
	RawOutput      string          // Claude CLI 原始 stdout
	ExitCode       int             // worker 原始退出码
	CLIMeta        *model.CLIMeta  // CLI 执行元数据
	Analysis       *AnalysisOutput // 分析模式结果（analyze_issue）
	Fix            *FixOutput      // M3.5: 修复模式结果（fix_issue）
	ParseError     error           // JSON 解析失败时非 nil
	WritebackError error           // 回写失败时非 nil
	RefKind        RefKind         // M3.5: ref 类型，PR 创建时决定 Base 字段
	PRNumber       int64           // M3.5: 创建的 PR 编号（0 = 未创建）
	PRURL          string          // M3.5: PR 页面链接
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/fix/ -run TestFixOutput -v`
Expected: PASS

- [ ] **Step 5: 运行全量 fix 包测试确认无回归**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/fix/ -v`
Expected: 全部 PASS

- [ ] **Step 6: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/fix/result.go internal/fix/result_test.go
git commit -m "feat(fix): 新增 FixOutput/TestResults 数据结构 + 修复相关错误常量

M3.5: FixOutput JSON schema（含 success/info_sufficient/branch/commit/tests）；
RefKind 枚举标识分支/tag；FixResult 扩展 Fix/PRNumber/PRURL/RefKind；
新增 ErrInfoInsufficient/ErrFixFailed/ErrFixParseFailure 错误常量。"
```

---

## Task 2: store.GetLatestAnalysisByIssue 查询方法

**Files:**
- Modify: `internal/store/store.go`（接口）
- Modify: `internal/store/sqlite.go`（实现）
- Test: `internal/store/sqlite_test.go`

- [ ] **Step 1: 写失败测试**

`internal/store/sqlite_test.go` 追加：

```go
func TestSQLiteStore_GetLatestAnalysisByIssue(t *testing.T) {
	s := newTestSQLiteStore(t)
	ctx := context.Background()

	repo := "owner/repo"
	issueNum := int64(42)

	// 场景 1：无任何分析任务 → 返回 (nil, nil)
	got, err := s.GetLatestAnalysisByIssue(ctx, repo, issueNum)
	if err != nil {
		t.Fatalf("无分析任务应返回 nil 而非错误: %v", err)
	}
	if got != nil {
		t.Errorf("无分析任务应返回 nil, got %+v", got)
	}

	// 场景 2：创建 3 个 analyze_issue 任务，时间递增，只有最新的 succeeded
	base := time.Now().Add(-3 * time.Hour)
	for i := 0; i < 3; i++ {
		rec := &model.TaskRecord{
			ID:           fmt.Sprintf("analyze-%d", i),
			TaskType:     model.TaskTypeAnalyzeIssue,
			Status:       model.TaskStatusSucceeded,
			RepoFullName: repo,
			DeliveryID:   fmt.Sprintf("d-%d", i),
			CreatedAt:    base.Add(time.Duration(i) * time.Hour),
			UpdatedAt:    base.Add(time.Duration(i) * time.Hour),
			Payload: model.TaskPayload{
				TaskType:     model.TaskTypeAnalyzeIssue,
				RepoFullName: repo,
				IssueNumber:  issueNum,
			},
		}
		if err := s.CreateTask(ctx, rec); err != nil {
			t.Fatalf("CreateTask[%d]: %v", i, err)
		}
	}

	got, err = s.GetLatestAnalysisByIssue(ctx, repo, issueNum)
	if err != nil {
		t.Fatalf("查询失败: %v", err)
	}
	if got == nil {
		t.Fatal("应返回最新任务，got nil")
	}
	if got.ID != "analyze-2" {
		t.Errorf("应返回 analyze-2（最新）, got %s", got.ID)
	}

	// 场景 3：另一个 Issue 的 analyze 不应匹配
	got, err = s.GetLatestAnalysisByIssue(ctx, repo, 999)
	if err != nil {
		t.Fatalf("查询失败: %v", err)
	}
	if got != nil {
		t.Errorf("不同 Issue 应返回 nil, got %+v", got)
	}

	// 场景 4：fix_issue 类型不应匹配
	fixRec := &model.TaskRecord{
		ID: "fix-1", TaskType: model.TaskTypeFixIssue,
		Status: model.TaskStatusSucceeded, RepoFullName: repo,
		DeliveryID: "fd-1",
		CreatedAt:  time.Now(), UpdatedAt: time.Now(),
		Payload: model.TaskPayload{
			TaskType: model.TaskTypeFixIssue, RepoFullName: repo, IssueNumber: issueNum,
		},
	}
	if err := s.CreateTask(ctx, fixRec); err != nil {
		t.Fatalf("CreateTask fix: %v", err)
	}
	got, err = s.GetLatestAnalysisByIssue(ctx, repo, issueNum)
	if err != nil {
		t.Fatalf("查询失败: %v", err)
	}
	if got == nil || got.ID != "analyze-2" {
		t.Errorf("应仍返回 analyze-2（fix 类型应被过滤）, got %+v", got)
	}
}
```

注意：`newTestSQLiteStore` 是既有的测试辅助函数，参照 `sqlite_test.go` 中既有测试找到实际名称（可能是 `setupTestStore` / `newTestStore` 等）。

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/store/ -run TestSQLiteStore_GetLatestAnalysisByIssue -v`
Expected: 编译错误 `s.GetLatestAnalysisByIssue undefined`

- [ ] **Step 3: 扩展 Store 接口**

`internal/store/store.go` 第 47-49 行 `FindActiveIssueTasks` 定义之后追加：

```go
	// GetLatestAnalysisByIssue 查询指定 Issue 最新一次成功的 analyze_issue 任务。
	// M3.5: fix_issue 前置检查用——若返回任务的结果中 info_sufficient=false，
	// 则 fix_issue 直接阻断并在 Issue 评论提醒用户补充信息。
	// 未找到时返回 (nil, nil)，不视为错误。
	GetLatestAnalysisByIssue(ctx context.Context, repoFullName string, issueNumber int64) (*model.TaskRecord, error)
```

- [ ] **Step 4: 实现 SQLite 版本**

`internal/store/sqlite.go` 在 `FindActiveIssueTasks` 函数（约第 665 行起）之后追加：

```go
// GetLatestAnalysisByIssue 查询指定 Issue 最新一次成功的 analyze_issue 任务。
// 按 created_at DESC 排序，返回第一条。未找到时返回 (nil, nil)。
func (s *SQLiteStore) GetLatestAnalysisByIssue(ctx context.Context, repoFullName string, issueNumber int64) (*model.TaskRecord, error) {
	if repoFullName == "" || issueNumber <= 0 {
		return nil, nil
	}
	query := `
		SELECT ` + taskColumns + `
		FROM tasks
		WHERE repo_full_name = ?
		  AND task_type = ?
		  AND status = ?
		  AND json_extract(payload, '$.issue_number') = ?
		ORDER BY created_at DESC
		LIMIT 1
	`
	row := s.db.QueryRowContext(ctx, query,
		repoFullName, string(model.TaskTypeAnalyzeIssue),
		string(model.TaskStatusSucceeded), issueNumber)
	rec, err := s.scanTask(row)
	if errors.Is(err, sql.ErrNoRows) {
		return nil, nil
	}
	if err != nil {
		return nil, fmt.Errorf("查询最新 analyze_issue 任务失败: %w", err)
	}
	return rec, nil
}
```

注意：
- `taskColumns` 和 `scanTask`（或 `scanTaskRow`）是 `sqlite.go` 中既有辅助，需先确认实际名称。若既有代码直接写 `"SELECT id, task_type, ..."`，则保持一致写法
- `json_extract(payload, '$.issue_number')`：依赖 SQLite JSON1 扩展，modernc.org/sqlite 默认带此功能；既有 `FindActiveIssueTasks` 若已使用 JSON1，复用相同写法；否则需改为冗余列（需先考察既有实现）

**验证既有写法**：
```bash
# 在 sqlite.go 中检查 FindActiveIssueTasks 如何查询 issue_number
cd /Users/kelin/Work/DTWorkflow && grep -n "issue_number" internal/store/sqlite.go
```

如果既有实现使用 `json_extract` 则沿用，否则查看是否已有 `issue_number` 冗余列（M2.4 为 PR 引入过 `pr_number` 列的先例）。

- [ ] **Step 5: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/store/ -run TestSQLiteStore_GetLatestAnalysisByIssue -v`
Expected: PASS

- [ ] **Step 6: 确认既有 store 测试无回归**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/store/ -v`
Expected: 全部 PASS

- [ ] **Step 7: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/store/store.go internal/store/sqlite.go internal/store/sqlite_test.go
git commit -m "feat(store): 新增 GetLatestAnalysisByIssue 查询方法

M3.5 fix_issue 前置检查用：查询指定 Issue 最新一次成功的 analyze_issue
任务，服务层据此判断前序分析是否标记为信息不足。"
```

---

## Task 3: PRClient + FixStaleChecker 窄接口 + ServiceOption

**Files:**
- Modify: `internal/fix/service.go`

- [ ] **Step 1: 新增窄接口定义**

`internal/fix/service.go` 在 `RefClient` 定义（约第 24-27 行）之后追加：

```go
// PRClient M3.5 窄接口，仅暴露修复模式创建 PR 所需的 Gitea API。
// *gitea.Client 通过 CreatePullRequest 和 GetRepo 满足此接口。
type PRClient interface {
	CreatePullRequest(ctx context.Context, owner, repo string,
		opts gitea.CreatePullRequestOption) (*gitea.PullRequest, *gitea.Response, error)
	// GetRepo 用于 Tag-as-Ref 场景下获取仓库默认分支作为 PR Base。
	GetRepo(ctx context.Context, owner, repo string) (*gitea.Repository, *gitea.Response, error)
}

// FixStaleChecker M3.5 窄接口，查询前序分析结果以支持"信息不足"前置检查。
// *store.SQLiteStore 通过 GetLatestAnalysisByIssue 满足此接口。
type FixStaleChecker interface {
	GetLatestAnalysisByIssue(ctx context.Context, repoFullName string,
		issueNumber int64) (*model.TaskRecord, error)
}
```

- [ ] **Step 2: 新增 ServiceOption + Service 字段**

`internal/fix/service.go` 在 `WithRefClient` 定义（约第 58-61 行）之后追加：

```go
// WithPRClient M3.5: 注入创建 PR 所需的 Gitea API 客户端（可选）。
// 仅在 fix_issue 修复模式执行 PR 创建时使用；analyze_issue 无需。
func WithPRClient(c PRClient) ServiceOption {
	return func(s *Service) { s.prClient = c }
}

// WithFixStaleChecker M3.5: 注入前序分析查询接口（可选）。
// 仅在 fix_issue 模式下用于"信息不足"前置检查。未注入时跳过检查（Claude 自行判断）。
func WithFixStaleChecker(c FixStaleChecker) ServiceOption {
	return func(s *Service) { s.staleChecker = c }
}
```

`Service` 结构体（约第 64-70 行）新增字段：

```go
type Service struct {
	gitea        IssueClient
	pool         FixPoolRunner
	cfgProv      FixConfigProvider
	refClient    RefClient
	prClient     PRClient        // M3.5: fix_issue 创建 PR 使用
	staleChecker FixStaleChecker // M3.5: fix_issue 前置检查使用
	logger       *slog.Logger
}
```

- [ ] **Step 3: 编译验证**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./internal/fix/`
Expected: 编译通过（`model.TaskRecord` 已在 service.go 通过 `internal/model` 导入；如未导入则需追加 `"otws19.zicp.vip/kelin/dtworkflow/internal/model"`）

- [ ] **Step 4: 运行全量 fix 测试无回归**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/fix/ -v`
Expected: 全部 PASS

- [ ] **Step 5: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/fix/service.go
git commit -m "feat(fix): 新增 PRClient/FixStaleChecker 窄接口与 ServiceOption

M3.5: PRClient 暴露 CreatePullRequest+GetRepo（Tag-as-Ref 需默认分支）；
FixStaleChecker 暴露 GetLatestAnalysisByIssue（信息不足前置检查）；
两者均为可选依赖，未注入时优雅降级。"
```

---

## Task 4: 修复 prompt 常量 + buildFixPrompt

**Files:**
- Modify: `internal/fix/prompt.go`
- Test: `internal/fix/prompt_test.go`

- [ ] **Step 1: 写失败测试**

`internal/fix/prompt_test.go` 追加：

```go
func TestBuildFixPrompt_FourSegments(t *testing.T) {
	issue := &gitea.Issue{
		Number: 15,
		Title:  "登录页面崩溃",
		Body:   "用户输入空密码时报 NPE",
	}
	ctx := &IssueContext{
		Issue:    issue,
		Comments: nil,
		Ref:      "main",
	}
	s := &Service{}
	prompt := s.buildFixPrompt(ctx)

	// 关键段落必须出现
	checks := []struct{ needle, reason string }{
		{"#15", "Issue 编号"},
		{"登录页面崩溃", "Issue 标题"},
		{"main", "ref 信息"},
		{"auto-fix/issue-15", "分支命名规范"},
		{"force-with-lease", "force push 指令（重试场景）"},
		{"mvn test", "Java 测试指令"},
		{"npm test", "前端测试指令"},
		{"missing_info", "JSON schema info_sufficient 字段"},
		{"branch_name", "JSON schema branch_name 字段"},
		{"commit_sha", "JSON schema commit_sha 字段"},
		{"test_results", "JSON schema test_results 字段"},
	}
	for _, c := range checks {
		if !strings.Contains(prompt, c.needle) {
			t.Errorf("fix prompt 应包含 %q（%s）", c.needle, c.reason)
		}
	}

	// 不应含只读模式独有文本
	if strings.Contains(prompt, "READ-ONLY code analysis mode") {
		t.Error("fix prompt 不应包含 READ-ONLY 只读约束（修复需要写权限）")
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/fix/ -run TestBuildFixPrompt -v`
Expected: 编译错误 `s.buildFixPrompt undefined`

- [ ] **Step 3: 实现修复 prompt 常量**

`internal/fix/prompt.go` 在既有 `analysisJSONSchemaInstruction` 常量之后追加（不要修改既有 analyze 相关常量）：

```go
// ==================== M3.5: fix_issue 修复模式 prompt ====================

// fixPreamble 修复模式前言（替代 analyze 的 readOnlyConstraint + analysisPreamble）。
// fix 模式允许运行 Bash（需执行 mvn test / npm test），但仍禁止网络访问。
const fixPreamble = `你是一位资深软件工程师，正在修复项目中报告的 Issue。
你拥有完整的文件读写和 Bash 执行能力，可以：
- 修改源代码文件
- 运行 mvn test / npm test / npx vitest 等测试命令
- 使用 git 创建分支、commit、push

安全约束（非常重要）：
- 禁止调用任何外部 HTTP API / 网络服务（curl、wget 等）
- 禁止向 Gitea 提交评审、评论或状态更新（由系统外部处理）
- 除运行测试外，不要执行任何与修复无关的系统命令

你的目标：分析 Issue → 修改代码 → 补充测试 → 运行测试 → push 代码，并输出 JSON 结果。
`

// fixInstructions 修复流程指令（六步）
const fixInstructions = `
## 修复流程

### 第一步：理解问题
- 阅读 Issue 描述和评论，判断信息是否充分
- **如果 Issue 评论中存在之前的分析报告**，优先参考其根因定位和修复建议
- 信息不充分时，输出 info_sufficient=false + missing_info 后终止，不要启动修复

### 第二步：创建修复分支
执行（分支已存在时用 force-with-lease 覆盖，确保重试幂等）：
  git checkout -b auto-fix/issue-<id> 2>/dev/null || git checkout auto-fix/issue-<id>

### 第三步：修改代码
- 最小必要范围原则：只修改与 Issue 直接相关的文件
- 不修改无关代码格式、不删除现有测试
- 参考前序分析（如有）定位的文件和方法

### 第四步：补充测试
- 为修复点新增或调整单元测试，验证修复有效
- Java 模块用 JUnit 5 + Mockito；Vue/前端模块用 Vitest
- 测试风格与既有测试保持一致（查看现有测试文件的命名、断言库选择）

### 第五步：运行测试
- 自动检测：pom.xml 存在 → 运行 mvn test；package.json 存在 → 运行 npm test 或 npx vitest
- 全部通过才能继续。测试失败时根据错误信息修正代码或测试，重试最多 3 轮
- 3 轮后仍失败，输出 success=false + failure_reason 后终止

### 第六步：Commit + Push
- commit message 格式：fix: #<issue_id> <简要描述>
- push：git push --force-with-lease origin auto-fix/issue-<id>
- 若 push 被拒（例如他人已推送同分支），输出 success=false + failure_reason
`

// fixConstraints 修复约束段
const fixConstraints = `
## 修复约束

- 【禁止】修改与 Issue 无关的文件
- 【禁止】删除或禁用现有测试用例
- 【禁止】跳过测试（--skip-tests / -DskipTests）
- 【禁止】force push 到 main/master/主分支
- 【必须】commit message 使用 "fix: #<issue_id> <描述>" 格式
- 【必须】分支名严格为 auto-fix/issue-<issue_id>
- 【必须】对所有修改的代码路径覆盖测试

Git identity 已由容器 entrypoint 设置（DTWorkflow Bot），直接 git commit 即可。
`

// fixJSONSchemaInstruction 修复输出格式定义
const fixJSONSchemaInstruction = `
## 输出格式

你必须将修复结果输出为如下结构的单个 JSON 对象。
不要用 markdown 代码块包裹，只输出原始 JSON。
**analysis / fix_approach / failure_reason 字段必须使用中文书写。**

成功场景示例：
{
  "success": true,
  "info_sufficient": true,
  "branch_name": "auto-fix/issue-15",
  "commit_sha": "abc123def456",
  "modified_files": ["src/main/java/com/example/LoginController.java", "src/test/java/com/example/LoginControllerTest.java"],
  "test_results": {
    "passed": 12,
    "failed": 0,
    "skipped": 1,
    "all_passed": true
  },
  "analysis": "根因分析（中文）",
  "fix_approach": "修复方案描述（中文）"
}

信息不足场景：
{
  "success": false,
  "info_sufficient": false,
  "missing_info": ["缺少错误堆栈信息", "缺少复现步骤"],
  "analysis": "根据现有描述的初步判断..."
}

修复失败场景（测试未通过、分支 push 失败等）：
{
  "success": false,
  "info_sufficient": true,
  "branch_name": "auto-fix/issue-15",
  "test_results": {"passed": 5, "failed": 3, "skipped": 0, "all_passed": false},
  "analysis": "根因分析（中文）",
  "fix_approach": "尝试的修复方案（中文）",
  "failure_reason": "3 个单元测试未通过：... "
}
`
```

- [ ] **Step 4: 实现 buildFixPrompt**

`internal/fix/prompt.go` 在既有 `buildPrompt`（分析模式，约第 138-184 行）之后追加：

```go
// buildFixPrompt M3.5: 按四段式构造修复 prompt。
// 与 buildPrompt（分析模式）结构相似但内容不同：
// - 没有 READ-ONLY 约束（修复需要 Bash + 文件写）
// - 包含修复流程、分支命名、测试运行、push 指令
// - JSON schema 使用 FixOutput
func (s *Service) buildFixPrompt(issueCtx *IssueContext) string {
	var b strings.Builder

	// 1. 修复前言（含安全约束）
	b.WriteString(fixPreamble)

	// 2. 任务上下文
	b.WriteString(fmt.Sprintf("\n## 任务上下文\n\n你正在修复仓库中的 Issue #%d。\n", issueCtx.Issue.Number))
	if issueCtx.Ref != "" {
		b.WriteString(fmt.Sprintf("当前代码基于 ref：%s\n", issueCtx.Ref))
	}
	b.WriteString(fmt.Sprintf("Issue 标题：%s\n", issueCtx.Issue.Title))
	if issueCtx.Issue.Body != "" {
		b.WriteString(fmt.Sprintf("Issue 描述：\n%s\n", truncate(issueCtx.Issue.Body, 5000)))
	}
	if len(issueCtx.Comments) > 0 {
		b.WriteString(fmt.Sprintf("\n评论（共 %d 条，若含前序分析报告请优先参考其根因定位）：\n", len(issueCtx.Comments)))
		const maxCommentTotalRunes = 20000
		commentRunes := 0
		for i, c := range issueCtx.Comments {
			author := commentAuthor(c)
			body := ""
			if c != nil {
				body = truncate(c.Body, 2000)
			}
			if commentRunes+len([]rune(body)) > maxCommentTotalRunes {
				b.WriteString(fmt.Sprintf("（剩余 %d 条评论因长度限制被省略）\n", len(issueCtx.Comments)-i))
				break
			}
			commentRunes += len([]rune(body))
			b.WriteString(fmt.Sprintf("--- 评论 #%d（%s）---\n%s\n",
				i+1, author, body))
		}
	}

	// 3. 修复指令 + 约束
	b.WriteString(fixInstructions)
	b.WriteString(fixConstraints)

	// 4. 输出格式
	b.WriteString(fixJSONSchemaInstruction)

	return b.String()
}
```

- [ ] **Step 5: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/fix/ -run TestBuildFixPrompt -v`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/fix/prompt.go internal/fix/prompt_test.go
git commit -m "feat(fix): 新增修复模式 prompt 四段式构造

M3.5: buildFixPrompt 构造 fix_issue prompt，包含前言（非只读）+ Issue 上下文 +
修复六步流程（含 force-with-lease 和测试重试）+ 约束段 + FixOutput JSON schema。"
```

---

## Task 5: buildFixCommand — 容器命令构造

**Files:**
- Modify: `internal/fix/prompt.go`
- Test: `internal/fix/prompt_test.go`

- [ ] **Step 1: 写失败测试**

`internal/fix/prompt_test.go` 追加：

```go
func TestBuildFixCommand_NoDisallowedBash(t *testing.T) {
	s := &Service{}
	cmd := s.buildFixCommand()

	// 必须是 claude -p - --output-format json 起始
	if len(cmd) < 5 {
		t.Fatalf("fix 命令长度 = %d，期望至少 5 个参数", len(cmd))
	}
	if cmd[0] != "claude" || cmd[1] != "-p" || cmd[2] != "-" {
		t.Errorf("命令前缀 = %v，期望 [claude -p -]", cmd[:3])
	}

	// 必须有 --output-format json
	joined := strings.Join(cmd, " ")
	if !strings.Contains(joined, "--output-format json") {
		t.Error("fix 命令必须含 --output-format json")
	}

	// 禁止带分析模式的 --disallowedTools（fix 需要 Bash）
	if strings.Contains(joined, "--disallowedTools") {
		t.Errorf("fix 命令不应含 --disallowedTools（需要 Bash 和 Write 权限运行测试）: %s", joined)
	}
}

func TestBuildFixCommand_WithModelAndEffort(t *testing.T) {
	s := &Service{cfgProv: stubCfgProvider{model: "claude-sonnet-4-5", effort: "HIGH"}}
	cmd := s.buildFixCommand()
	joined := strings.Join(cmd, " ")
	if !strings.Contains(joined, "--model claude-sonnet-4-5") {
		t.Errorf("应透传 --model, got: %s", joined)
	}
	if !strings.Contains(joined, "--effort high") {
		t.Errorf("effort 应小写归一化, got: %s", joined)
	}
}

// stubCfgProvider 参照 prompt_test.go 既有 stub 风格；如已有则复用，否则新建
type stubCfgProvider struct {
	model  string
	effort string
}

func (s stubCfgProvider) GetClaudeModel() string  { return s.model }
func (s stubCfgProvider) GetClaudeEffort() string { return s.effort }
```

注意：如果 `internal/fix/prompt_test.go` 既有 stub，复用即可。

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/fix/ -run TestBuildFixCommand -v`
Expected: 编译错误 `s.buildFixCommand undefined`

- [ ] **Step 3: 实现 buildFixCommand**

`internal/fix/prompt.go` 在既有 `buildCommand`（分析模式，约第 194-209 行）之后追加：

```go
// buildFixCommand M3.5: 构造 fix_issue 容器执行命令。
// 与 buildCommand（分析模式）的关键区别：
// - 不加 --disallowedTools（修复需要 Edit/Write/Bash 执行测试）
// - 其他一致：stdin prompt（claude -p -）、--output-format json、透传模型和 effort
func (s *Service) buildFixCommand() []string {
	cmd := []string{
		"claude", "-p", "-",
		"--output-format", "json",
	}
	if s.cfgProv != nil {
		if model := s.cfgProv.GetClaudeModel(); model != "" {
			cmd = append(cmd, "--model", model)
		}
		if effort := s.cfgProv.GetClaudeEffort(); effort != "" {
			cmd = append(cmd, "--effort", strings.ToLower(strings.TrimSpace(effort)))
		}
	}
	return cmd
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/fix/ -run TestBuildFixCommand -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/fix/prompt.go internal/fix/prompt_test.go
git commit -m "feat(fix): 新增 buildFixCommand 容器执行命令

M3.5: 修复模式不加 --disallowedTools（需 Bash 运行测试），
stdin 传入 prompt，保留 --output-format json 和 model/effort 透传。"
```

---

## Task 6: validateRef 返回 ref 类型（RefKind）

**Files:**
- Modify: `internal/fix/service.go`
- Test: `internal/fix/service_test.go`

目的：PR 创建时需要区分 ref 是分支还是 tag（tag 不能作为 PR Base）。既有 `validateRef` 只返回 error，需扩展返回值为 `(RefKind, error)`。

- [ ] **Step 1: 写失败测试**

`internal/fix/service_test.go` 追加（若已有 ref 测试则调整其期望）：

```go
func TestValidateRef_ReturnsRefKind_Branch(t *testing.T) {
	rc := &stubRefClient{
		branchOK: map[string]bool{"feature/x": true},
	}
	s := &Service{refClient: rc}
	kind, err := s.validateRef(context.Background(), "o", "r", "feature/x")
	if err != nil {
		t.Fatalf("expected nil error, got %v", err)
	}
	if kind != RefKindBranch {
		t.Errorf("kind = %v, want RefKindBranch", kind)
	}
}

func TestValidateRef_ReturnsRefKind_Tag(t *testing.T) {
	rc := &stubRefClient{
		tagOK: map[string]bool{"v1.0.0": true},
	}
	s := &Service{refClient: rc}
	kind, err := s.validateRef(context.Background(), "o", "r", "v1.0.0")
	if err != nil {
		t.Fatalf("expected nil error, got %v", err)
	}
	if kind != RefKindTag {
		t.Errorf("kind = %v, want RefKindTag", kind)
	}
}

func TestValidateRef_StripsRefsHeadsPrefix(t *testing.T) {
	rc := &stubRefClient{branchOK: map[string]bool{"main": true}}
	s := &Service{refClient: rc}
	kind, err := s.validateRef(context.Background(), "o", "r", "refs/heads/main")
	if err != nil || kind != RefKindBranch {
		t.Fatalf("refs/heads/main 应解析为分支，got kind=%v err=%v", kind, err)
	}
}

func TestValidateRef_NotFound(t *testing.T) {
	rc := &stubRefClient{} // 全部返回 NotFound
	s := &Service{refClient: rc}
	_, err := s.validateRef(context.Background(), "o", "r", "does-not-exist")
	if !errors.Is(err, ErrInvalidIssueRef) {
		t.Errorf("expected ErrInvalidIssueRef, got %v", err)
	}
}
```

**注意**：如果 `service_test.go` 中已有 `stubRefClient` 或类似 mock，沿用既有实现；否则参照既有 M3.1 测试风格新建。既有 M3.1 测试的 `TestValidateRef_*` 需同步更新（返回值变为两个），在本 Task 内一并完成以避免编译失败。

- [ ] **Step 2: 修改 validateRef 签名**

`internal/fix/service.go` 第 277-295 行的 `validateRef` 替换为：

```go
// validateRef M3.5: 验证 Issue 关联的 ref 是否存在，并返回 ref 类型。
// 先查分支再查 tag；RefKind 用于 PR 创建时决定 Base 字段（tag 不能直接作为 Base）。
func (s *Service) validateRef(ctx context.Context, owner, repo, ref string) (RefKind, error) {
	bare := stripRefPrefix(ref)
	_, _, err := s.refClient.GetBranch(ctx, owner, repo, bare)
	if err == nil {
		return RefKindBranch, nil
	}
	if !gitea.IsNotFound(err) {
		return RefKindUnknown, fmt.Errorf("检查分支 %q 失败: %w", bare, err)
	}
	_, _, err = s.refClient.GetTag(ctx, owner, repo, bare)
	if err == nil {
		return RefKindTag, nil
	}
	if !gitea.IsNotFound(err) {
		return RefKindUnknown, fmt.Errorf("检查 tag %q 失败: %w", bare, err)
	}
	return RefKindUnknown, ErrInvalidIssueRef
}
```

- [ ] **Step 3: 调整 executeAnalysis 中的 validateRef 调用**

`internal/fix/service.go` 第 136-146 行既有调用改为接收两个返回值：

```go
	// 4. ref 有效性检查
	if s.refClient != nil {
		if _, err := s.validateRef(ctx, owner, repo, effectiveRef); err != nil {
			if errors.Is(err, ErrInvalidIssueRef) {
				if commentErr := s.commentRefInvalid(ctx, owner, repo, issueNum, effectiveRef); commentErr != nil {
					return nil, fmt.Errorf("Issue #%d ref=%q: 回写 ref 无效提示失败: %w", issueNum, effectiveRef, commentErr)
				}
				return nil, fmt.Errorf("Issue #%d ref=%q: %w", issueNum, effectiveRef, ErrInvalidIssueRef)
			}
			return nil, fmt.Errorf("验证 ref %q 失败: %w", effectiveRef, err)
		}
	}
```

分析模式不关心 kind，使用 `_` 丢弃即可。

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/fix/ -v`
Expected: 全部 PASS（新增 RefKind 测试 + 既有 validateRef 测试）

- [ ] **Step 5: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/fix/service.go internal/fix/service_test.go
git commit -m "feat(fix): validateRef 返回 RefKind（分支/tag 区分）

M3.5: 为 PR 创建 Tag-as-Ref 边界处理做准备——tag 不能作为 PR Base，
需回退到仓库默认分支。既有 executeAnalysis 调用点同步更新。"
```

---

## Task 7: FixOutput 双层 JSON 解析（parseResult 分流）

**Files:**
- Modify: `internal/fix/service.go`
- Test: `internal/fix/service_test.go`

目的：`parseResult` 既有实现只解析 `AnalysisOutput`。新增 `parseFixResult` 或参数化 `parseResult`，解析 `FixOutput`。

- [ ] **Step 1: 写失败测试**

`internal/fix/service_test.go` 追加：

```go
func TestService_ParseFixResult_Success(t *testing.T) {
	envelope := `{"type":"result","subtype":"success","duration_ms":12000,"total_cost_usd":0.05,"is_error":false,"num_turns":8,"result":"{\"success\":true,\"info_sufficient\":true,\"branch_name\":\"auto-fix/issue-15\",\"commit_sha\":\"abc123\",\"modified_files\":[\"a.java\"],\"test_results\":{\"passed\":5,\"failed\":0,\"skipped\":0,\"all_passed\":true},\"analysis\":\"x\",\"fix_approach\":\"y\"}","session_id":"s1"}`
	s := &Service{}
	result := s.parseFixResult(envelope)
	if result.ParseError != nil {
		t.Fatalf("解析不应失败: %v", result.ParseError)
	}
	if result.Fix == nil {
		t.Fatal("Fix 不应为 nil")
	}
	if !result.Fix.Success || result.Fix.BranchName != "auto-fix/issue-15" {
		t.Errorf("Fix 字段不正确: %+v", result.Fix)
	}
	if result.CLIMeta == nil || result.CLIMeta.CostUSD != 0.05 {
		t.Errorf("CLIMeta 不正确: %+v", result.CLIMeta)
	}
}

func TestService_ParseFixResult_SuccessInvariantViolation(t *testing.T) {
	// success=true 但 branch_name 为空 → 视为解析异常
	envelope := `{"type":"result","result":"{\"success\":true,\"info_sufficient\":true,\"analysis\":\"x\"}"}`
	s := &Service{}
	result := s.parseFixResult(envelope)
	if result.ParseError == nil {
		t.Error("success=true 时 branch_name 为空应视为解析异常")
	}
}

func TestService_ParseFixResult_InfoInsufficient(t *testing.T) {
	envelope := `{"type":"result","result":"{\"success\":false,\"info_sufficient\":false,\"missing_info\":[\"需要堆栈\"]}"}`
	s := &Service{}
	result := s.parseFixResult(envelope)
	if result.ParseError != nil {
		t.Fatalf("解析不应失败: %v", result.ParseError)
	}
	if result.Fix == nil || result.Fix.InfoSufficient {
		t.Errorf("InfoSufficient 应为 false, got Fix=%+v", result.Fix)
	}
}

func TestService_ParseFixResult_CLIError(t *testing.T) {
	envelope := `{"type":"error","subtype":"context_limit","is_error":true}`
	s := &Service{}
	result := s.parseFixResult(envelope)
	if result.ParseError == nil {
		t.Error("CLI 错误时应有 ParseError")
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/fix/ -run TestService_ParseFixResult -v`
Expected: 编译错误 `s.parseFixResult undefined`

- [ ] **Step 3: 实现 parseFixResult**

`internal/fix/service.go` 在既有 `parseResult`（第 210-241 行）之后追加：

```go
// parseFixResult M3.5: 双层 JSON 解析 — 外层 CLI 信封 → 内层 FixOutput。
// 与 parseResult（分析模式）的区别仅在于内层 schema；外层解析逻辑相同。
// 成功场景不变量：success=true 时 BranchName 和 CommitSHA 必须非空，否则视为 ParseError。
func (s *Service) parseFixResult(output string) *FixResult {
	result := &FixResult{}

	// 外层 CLI JSON 信封
	var cliResp CLIResponse
	if err := json.Unmarshal([]byte(output), &cliResp); err != nil {
		result.ParseError = fmt.Errorf("CLI JSON 解析失败: %w", err)
		return result
	}
	result.CLIMeta = &model.CLIMeta{
		CostUSD:    cliResp.EffectiveCostUSD(),
		DurationMs: cliResp.DurationMs,
		IsError:    cliResp.IsExecutionError(),
		NumTurns:   cliResp.NumTurns,
		SessionID:  cliResp.SessionID,
	}

	if cliResp.IsExecutionError() {
		result.ParseError = fmt.Errorf("Claude CLI 报告错误: type=%s, subtype=%s", cliResp.Type, cliResp.Subtype)
		return result
	}

	// 内层 FixOutput JSON
	jsonText := extractJSON(cliResp.Result)
	var fix FixOutput
	if err := json.Unmarshal([]byte(jsonText), &fix); err != nil {
		result.ParseError = fmt.Errorf("FixOutput JSON 解析失败: %w", err)
		return result
	}

	// 成功不变量校验：success=true 必须有 branch_name 和 commit_sha
	if fix.Success {
		if fix.BranchName == "" || fix.CommitSHA == "" {
			result.ParseError = fmt.Errorf("FixOutput 不变量违反：success=true 但 branch_name=%q commit_sha=%q",
				fix.BranchName, fix.CommitSHA)
			return result
		}
	}

	result.Fix = &fix
	return result
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/fix/ -run TestService_ParseFixResult -v`
Expected: 全部 PASS

- [ ] **Step 5: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/fix/service.go internal/fix/service_test.go
git commit -m "feat(fix): 新增 parseFixResult 双层 JSON 解析

M3.5: 解析 CLI 信封 + FixOutput；不变量校验 success=true 要求
branch_name 和 commit_sha 非空；CLI 错误正确透传 ParseError。"
```

---

## Task 8: checkPreviousAnalysis — "信息不足"前置检查

**Files:**
- Modify: `internal/fix/service.go`
- Test: `internal/fix/service_test.go`

目的：容器启动前查询最新 `analyze_issue` 任务，若其结果 `info_sufficient=false` 则阻断修复。

- [ ] **Step 1: 写失败测试**

`internal/fix/service_test.go` 追加：

```go
// stubFixStaleChecker 返回预设的 TaskRecord
type stubFixStaleChecker struct {
	record *model.TaskRecord
	err    error
}

func (s *stubFixStaleChecker) GetLatestAnalysisByIssue(ctx context.Context, repo string, issueNum int64) (*model.TaskRecord, error) {
	return s.record, s.err
}

func TestCheckPreviousAnalysis_NoPreviousAnalysis(t *testing.T) {
	s := &Service{staleChecker: &stubFixStaleChecker{}}
	sufficient, err := s.checkPreviousAnalysis(context.Background(), "o/r", 15)
	if err != nil {
		t.Fatalf("无前序分析应返回 nil error, got %v", err)
	}
	if !sufficient {
		t.Error("无前序分析应视为充分（不阻拦，让 Claude 隐式分析）")
	}
}

func TestCheckPreviousAnalysis_InfoInsufficient(t *testing.T) {
	// 构造一个 analyze_issue TaskRecord，其 Result 是 info_sufficient=false 的 CLI 信封
	rawResult := `{"type":"result","result":"{\"info_sufficient\":false,\"missing_info\":[\"缺少堆栈\"],\"analysis\":\"x\",\"confidence\":\"low\"}"}`
	rec := &model.TaskRecord{
		ID:       "a-1",
		TaskType: model.TaskTypeAnalyzeIssue,
		Result:   rawResult,
	}
	s := &Service{staleChecker: &stubFixStaleChecker{record: rec}}
	sufficient, err := s.checkPreviousAnalysis(context.Background(), "o/r", 15)
	if err != nil {
		t.Fatalf("error 应为 nil, got %v", err)
	}
	if sufficient {
		t.Error("info_sufficient=false 应视为不充分（阻断）")
	}
}

func TestCheckPreviousAnalysis_InfoSufficient(t *testing.T) {
	rawResult := `{"type":"result","result":"{\"info_sufficient\":true,\"analysis\":\"x\",\"confidence\":\"high\"}"}`
	rec := &model.TaskRecord{
		ID:       "a-1",
		TaskType: model.TaskTypeAnalyzeIssue,
		Result:   rawResult,
	}
	s := &Service{staleChecker: &stubFixStaleChecker{record: rec}}
	sufficient, err := s.checkPreviousAnalysis(context.Background(), "o/r", 15)
	if err != nil || !sufficient {
		t.Errorf("info_sufficient=true 时应放行: sufficient=%v err=%v", sufficient, err)
	}
}

func TestCheckPreviousAnalysis_ParseError_FailOpen(t *testing.T) {
	// 前序分析记录存在但解析失败 → 降级为放行（fail-open），日志记录
	rec := &model.TaskRecord{
		ID:       "a-1",
		TaskType: model.TaskTypeAnalyzeIssue,
		Result:   `不是 JSON`,
	}
	s := &Service{staleChecker: &stubFixStaleChecker{record: rec}}
	sufficient, err := s.checkPreviousAnalysis(context.Background(), "o/r", 15)
	if err != nil {
		t.Fatalf("解析失败应 fail-open, got err %v", err)
	}
	if !sufficient {
		t.Error("解析失败应放行（fail-open），不误阻断")
	}
}

func TestCheckPreviousAnalysis_NoStaleCheckerInjected(t *testing.T) {
	s := &Service{staleChecker: nil}
	sufficient, err := s.checkPreviousAnalysis(context.Background(), "o/r", 15)
	if err != nil || !sufficient {
		t.Errorf("未注入 staleChecker 应放行: sufficient=%v err=%v", sufficient, err)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/fix/ -run TestCheckPreviousAnalysis -v`
Expected: 编译错误 `s.checkPreviousAnalysis undefined`

- [ ] **Step 3: 实现**

`internal/fix/service.go` 末尾新增：

```go
// checkPreviousAnalysis M3.5: 查询最新 analyze_issue 结果，判断信息是否充分。
// 返回 (true, nil) 表示可继续修复；(false, nil) 表示前序分析明确标记为信息不足。
// 错误和解析失败一律 fail-open（返回 true），避免因辅助检查失败而阻断主流程。
func (s *Service) checkPreviousAnalysis(ctx context.Context, repoFullName string, issueNumber int64) (bool, error) {
	if s.staleChecker == nil {
		return true, nil // 未注入时跳过检查
	}
	rec, err := s.staleChecker.GetLatestAnalysisByIssue(ctx, repoFullName, issueNumber)
	if err != nil {
		s.logger.WarnContext(ctx, "查询前序分析失败，fail-open 继续修复",
			"repo", repoFullName, "issue", issueNumber, "error", err)
		return true, nil
	}
	if rec == nil || rec.Result == "" {
		return true, nil // 无前序分析，放行
	}
	// 解析 rec.Result：外层 CLI 信封 → 内层 AnalysisOutput
	var cliResp CLIResponse
	if err := json.Unmarshal([]byte(rec.Result), &cliResp); err != nil {
		s.logger.WarnContext(ctx, "前序分析 CLI 信封解析失败，fail-open",
			"task_id", rec.ID, "error", err)
		return true, nil
	}
	if cliResp.IsExecutionError() {
		return true, nil // 前序分析本身就失败，无有效结论，放行让 Claude 重新分析
	}
	var analysis AnalysisOutput
	if err := json.Unmarshal([]byte(extractJSON(cliResp.Result)), &analysis); err != nil {
		s.logger.WarnContext(ctx, "前序分析内层 JSON 解析失败，fail-open",
			"task_id", rec.ID, "error", err)
		return true, nil
	}
	if !analysis.InfoSufficient {
		s.logger.InfoContext(ctx, "前序分析标记为信息不足，阻断修复",
			"task_id", rec.ID, "missing_info", analysis.MissingInfo)
		return false, nil
	}
	return true, nil
}

// latestMissingInfo 读取最新分析的 missing_info 列表，用于生成"信息不足"评论。
// 解析失败时返回 nil。
func (s *Service) latestMissingInfo(ctx context.Context, repoFullName string, issueNumber int64) []string {
	if s.staleChecker == nil {
		return nil
	}
	rec, err := s.staleChecker.GetLatestAnalysisByIssue(ctx, repoFullName, issueNumber)
	if err != nil || rec == nil {
		return nil
	}
	var cliResp CLIResponse
	if err := json.Unmarshal([]byte(rec.Result), &cliResp); err != nil {
		return nil
	}
	var analysis AnalysisOutput
	if err := json.Unmarshal([]byte(extractJSON(cliResp.Result)), &analysis); err != nil {
		return nil
	}
	return analysis.MissingInfo
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/fix/ -run TestCheckPreviousAnalysis -v`
Expected: 全部 PASS

- [ ] **Step 5: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/fix/service.go internal/fix/service_test.go
git commit -m "feat(fix): 新增 checkPreviousAnalysis 信息不足前置检查

M3.5: 查询最新 analyze_issue 任务结果，info_sufficient=false 时阻断修复。
失败场景全部 fail-open（查询失败、解析失败、CLI 错误），避免误阻断。
配套 latestMissingInfo 读取缺失信息列表供评论使用。"
```

---

## Task 9: PR 描述 + 修复结果评论 formatter

**Files:**
- Modify: `internal/fix/formatter.go`
- Test: `internal/fix/formatter_test.go`

目的：生成修复 PR 的 body 和 Issue 评论（成功/失败/信息不足/push但PR失败四种场景）。

- [ ] **Step 1: 写失败测试**

`internal/fix/formatter_test.go` 追加：

```go
func TestFormatFixPRBody_Success(t *testing.T) {
	fix := &FixOutput{
		Success:       true,
		BranchName:    "auto-fix/issue-15",
		CommitSHA:     "abc123",
		ModifiedFiles: []string{"src/a.java", "test/a_test.java"},
		TestResults:   &TestResults{Passed: 12, Failed: 0, Skipped: 1, AllPassed: true},
		Analysis:      "NPE 由未校验空密码引起",
		FixApproach:   "在 login 前添加 isEmpty 判断",
	}
	body := FormatFixPRBody(fix, 15, RefKindBranch, "")

	checks := []string{"fixes #15", "NPE 由未校验空密码引起", "src/a.java", "12", "DTWorkflow"}
	for _, s := range checks {
		if !strings.Contains(body, s) {
			t.Errorf("PR body 应包含 %q", s)
		}
	}
}

func TestFormatFixPRBody_TagRef_NotesBaseBranchFallback(t *testing.T) {
	fix := &FixOutput{Success: true, BranchName: "auto-fix/issue-15", CommitSHA: "abc",
		ModifiedFiles: []string{"x.go"}, TestResults: &TestResults{AllPassed: true}}
	body := FormatFixPRBody(fix, 15, RefKindTag, "main")
	if !strings.Contains(body, "tag") || !strings.Contains(body, "main") {
		t.Errorf("Tag 场景 PR body 应注明 base 为默认分支 main, got:\n%s", body)
	}
}

func TestFormatFixSuccessComment(t *testing.T) {
	body := FormatFixSuccessComment(42, "https://gitea/owner/repo/pulls/42", 3)
	checks := []string{"#42", "https://gitea/owner/repo/pulls/42", "3 个文件"}
	for _, s := range checks {
		if !strings.Contains(body, s) {
			t.Errorf("成功评论应包含 %q", s)
		}
	}
}

func TestFormatFixFailureComment(t *testing.T) {
	fix := &FixOutput{
		Success:       false,
		FailureReason: "3 个测试未通过",
		TestResults:   &TestResults{Passed: 5, Failed: 3, AllPassed: false},
		Analysis:      "根因是边界条件",
	}
	body := FormatFixFailureComment(fix, 2.5, 0.1)
	if !strings.Contains(body, "3 个测试未通过") {
		t.Error("失败评论应包含 failure_reason")
	}
	if !strings.Contains(body, "根因是边界条件") {
		t.Error("失败评论应包含分析说明帮助用户定位")
	}
}

func TestFormatFixInfoInsufficientComment(t *testing.T) {
	body := FormatFixInfoInsufficientComment([]string{"缺少堆栈", "缺少复现步骤"})
	checks := []string{"信息不足", "缺少堆栈", "缺少复现步骤", "auto-fix", "fix-to-pr"}
	for _, s := range checks {
		if !strings.Contains(body, s) {
			t.Errorf("评论应包含 %q", s)
		}
	}
}

func TestFormatFixPushButNoPRComment(t *testing.T) {
	body := FormatFixPushButNoPRComment("auto-fix/issue-15", "gitea API 返回 500")
	if !strings.Contains(body, "auto-fix/issue-15") || !strings.Contains(body, "PR 创建失败") {
		t.Errorf("push 成功但 PR 失败的评论应包含分支名和说明: %s", body)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/fix/ -run "TestFormatFix" -v`
Expected: 编译错误 `undefined: FormatFixPRBody` 等

- [ ] **Step 3: 实现 formatter**

`internal/fix/formatter.go` 末尾追加：

```go
// ==================== M3.5: fix_issue 修复模式格式化 ====================

// FormatFixPRBody M3.5: 构造修复 PR 的 body。
// refKind=RefKindTag 时，baseBranch 为仓库默认分支，body 中注明 base 差异。
func FormatFixPRBody(fix *FixOutput, issueNum int64, refKind RefKind, baseBranch string) string {
	var sb strings.Builder
	sb.WriteString("## 关联 Issue\n\n")
	sb.WriteString(fmt.Sprintf("fixes #%d\n\n", issueNum))

	if refKind == RefKindTag && baseBranch != "" {
		sb.WriteString("> ⚠️ 原 Issue 指定的 ref 为 tag，Gitea PR 不支持 tag 作为 base 分支。\n")
		sb.WriteString(fmt.Sprintf("> PR base 已改用仓库默认分支 `%s`，修复代码仍基于 tag 对应的 commit。\n\n", escapeMarkdown(baseBranch)))
	}

	if fix.Analysis != "" {
		sb.WriteString("## 根因分析\n\n")
		sb.WriteString(escapeMarkdown(fix.Analysis))
		sb.WriteString("\n\n")
	}

	if fix.FixApproach != "" {
		sb.WriteString("## 修复方案\n\n")
		sb.WriteString(escapeMarkdown(fix.FixApproach))
		sb.WriteString("\n\n")
	}

	if len(fix.ModifiedFiles) > 0 {
		sb.WriteString("## 修改文件\n\n")
		for _, f := range fix.ModifiedFiles {
			sb.WriteString(fmt.Sprintf("- `%s`\n", escapeMarkdown(f)))
		}
		sb.WriteString("\n")
	}

	if fix.TestResults != nil {
		sb.WriteString("## 测试结果\n\n")
		tr := fix.TestResults
		sb.WriteString(fmt.Sprintf("通过 **%d** / 失败 **%d** / 跳过 **%d**（全部通过：%v）\n\n",
			tr.Passed, tr.Failed, tr.Skipped, tr.AllPassed))
	}

	sb.WriteString("---\n")
	sb.WriteString("🤖 由 DTWorkflow 自动生成")

	body := sb.String()
	if len(body) > bodyMaxLen {
		body = truncateString(body, bodyMaxLen)
	}
	return body
}

// FormatFixSuccessComment M3.5: 修复成功后发送给 Issue 的评论。
func FormatFixSuccessComment(prNumber int64, prURL string, modifiedFileCount int) string {
	return fmt.Sprintf(
		"✅ 已创建修复 PR [#%d](%s)\n\n"+
			"修改了 **%d 个文件**，测试全部通过。\n"+
			"PR 将自动进入评审流程。\n\n"+
			"---\n_由 DTWorkflow 自动生成_",
		prNumber, prURL, modifiedFileCount)
}

// FormatFixFailureComment M3.5: 修复失败（测试未通过、分支未创建等）。
func FormatFixFailureComment(fix *FixOutput, durationSec, costUSD float64) string {
	var sb strings.Builder
	sb.WriteString("## DTWorkflow Issue 修复失败\n\n")

	if fix.FailureReason != "" {
		sb.WriteString("### 失败原因\n\n")
		sb.WriteString(escapeMarkdown(fix.FailureReason))
		sb.WriteString("\n\n")
	}

	if fix.Analysis != "" {
		sb.WriteString("### 尝试的分析\n\n")
		sb.WriteString(escapeMarkdown(fix.Analysis))
		sb.WriteString("\n\n")
	}

	if fix.TestResults != nil {
		tr := fix.TestResults
		sb.WriteString("### 测试结果\n\n")
		sb.WriteString(fmt.Sprintf("通过 %d / 失败 %d / 跳过 %d\n\n",
			tr.Passed, tr.Failed, tr.Skipped))
	}

	sb.WriteString("---\n")
	sb.WriteString("**建议**：人工检查失败原因，补充信息或修正代码后重新触发修复。\n\n")
	sb.WriteString("---\n")
	sb.WriteString(fmt.Sprintf("_由 DTWorkflow 自动生成 | 耗时 %.0fs | 费用 $%.4f_", durationSec, costUSD))

	body := sb.String()
	if len(body) > bodyMaxLen {
		body = truncateString(body, bodyMaxLen)
	}
	return body
}

// FormatFixInfoInsufficientComment M3.5: 前序分析信息不足时的提醒。
func FormatFixInfoInsufficientComment(missingInfo []string) string {
	var sb strings.Builder
	sb.WriteString("## DTWorkflow Issue 修复前置检查\n\n")
	sb.WriteString("> 之前的 `auto-fix` 分析表明 Issue 信息不足以自动修复。\n\n")

	if len(missingInfo) > 0 {
		sb.WriteString("### 缺失信息\n\n")
		for _, info := range missingInfo {
			sb.WriteString(fmt.Sprintf("- %s\n", escapeMarkdown(info)))
		}
		sb.WriteString("\n")
	}

	sb.WriteString("---\n\n")
	sb.WriteString("**补充信息后**，请移除 `auto-fix` 标签再重新添加以更新分析；\n")
	sb.WriteString("若新的分析确认信息充分，再添加 `fix-to-pr` 标签触发自动修复。\n\n")
	sb.WriteString("---\n")
	sb.WriteString("_由 DTWorkflow 自动生成_")

	body := sb.String()
	if len(body) > bodyMaxLen {
		body = truncateString(body, bodyMaxLen)
	}
	return body
}

// FormatFixPushButNoPRComment M3.5: 容器 push 成功但容器外 PR 创建失败。
func FormatFixPushButNoPRComment(branchName, apiError string) string {
	return fmt.Sprintf(
		"⚠️ 修复分支 `%s` 已推送成功，但 PR 创建失败。\n\n"+
			"错误：%s\n\n"+
			"系统将自动重试。如持续失败，请人工从 `%s` 分支手动创建 PR。\n\n"+
			"---\n_由 DTWorkflow 自动生成_",
		escapeMarkdown(branchName),
		escapeMarkdown(apiError),
		escapeMarkdown(branchName))
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/fix/ -run "TestFormatFix" -v`
Expected: 全部 PASS

- [ ] **Step 5: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/fix/formatter.go internal/fix/formatter_test.go
git commit -m "feat(fix): 新增修复模式 formatter（PR body + 4 种 Issue 评论）

M3.5: FormatFixPRBody 含 Tag-as-Ref 提示；
FormatFixSuccessComment/FailureComment/InfoInsufficientComment/PushButNoPRComment
分别处理成功/失败/信息不足/push 但 PR 创建失败四场景。
全部走 Markdown 转义 + 长度截断。"
```

---

## Task 10: executeFix 主流程（含 PR 创建）

**Files:**
- Modify: `internal/fix/service.go`
- Test: `internal/fix/service_test.go`

目的：实现 `executeFix` 方法，串联前置检查 → 容器执行 → PR 创建 → 评论回写。

- [ ] **Step 1: 写失败测试**（先覆盖几个关键分支）

`internal/fix/service_test.go` 追加：

```go
func TestExecuteFix_InfoInsufficientPreCheck(t *testing.T) {
	// 前序分析 info_sufficient=false → Issue 评论提醒 + 返回 ErrInfoInsufficient
	analysisRec := &model.TaskRecord{
		ID:     "a-1",
		Result: `{"type":"result","result":"{\"info_sufficient\":false,\"missing_info\":[\"堆栈\"]}"}`,
	}
	issueClient := &stubIssueClient{
		getIssue: &gitea.Issue{Number: 15, State: "open", Ref: "main", Title: "t"},
	}
	s := &Service{
		gitea:        issueClient,
		pool:         &stubPoolRunner{}, // 不应被调用
		staleChecker: &stubFixStaleChecker{record: analysisRec},
		logger:       slog.Default(),
	}

	payload := model.TaskPayload{
		TaskType: model.TaskTypeFixIssue, RepoOwner: "o", RepoName: "r",
		RepoFullName: "o/r", IssueNumber: 15,
	}
	_, err := s.Execute(context.Background(), payload)
	if !errors.Is(err, ErrInfoInsufficient) {
		t.Errorf("应返回 ErrInfoInsufficient, got %v", err)
	}
	// 容器池不应被调用
	if issueClient.createCommentCount == 0 {
		t.Error("应发出 Issue 评论提醒用户补充信息")
	}
}

func TestExecuteFix_SuccessCreatesPR(t *testing.T) {
	// 场景：有效分支 + Claude 返回 success=true + PRClient 成功
	issueClient := &stubIssueClient{
		getIssue: &gitea.Issue{Number: 15, State: "open", Ref: "main", Title: "t"},
	}
	fixEnvelope := `{"type":"result","duration_ms":1000,"total_cost_usd":0.01,"result":"{\"success\":true,\"info_sufficient\":true,\"branch_name\":\"auto-fix/issue-15\",\"commit_sha\":\"abc\",\"modified_files\":[\"a.go\"],\"test_results\":{\"passed\":5,\"failed\":0,\"skipped\":0,\"all_passed\":true},\"analysis\":\"x\",\"fix_approach\":\"y\"}"}`
	pool := &stubPoolRunner{
		result: &worker.ExecutionResult{ExitCode: 0, Output: fixEnvelope},
	}
	prClient := &stubPRClient{
		createResult: &gitea.PullRequest{Number: 42, HTMLURL: "https://g/o/r/pulls/42"},
	}
	s := &Service{
		gitea: issueClient, pool: pool, prClient: prClient,
		refClient: &stubRefClient{branchOK: map[string]bool{"main": true}},
		logger:    slog.Default(),
	}
	result, err := s.Execute(context.Background(),
		model.TaskPayload{TaskType: model.TaskTypeFixIssue, RepoOwner: "o", RepoName: "r",
			RepoFullName: "o/r", IssueNumber: 15, IssueRef: "main"})
	if err != nil {
		t.Fatalf("执行失败: %v", err)
	}
	if result == nil || result.Fix == nil || !result.Fix.Success {
		t.Fatalf("Fix 结果不正确: %+v", result)
	}
	if result.PRNumber != 42 || result.PRURL == "" {
		t.Errorf("PRNumber=%d PRURL=%q, 期望 42 + 非空", result.PRNumber, result.PRURL)
	}
	// PR 创建调用应发生
	if prClient.createCalls != 1 {
		t.Errorf("CreatePullRequest 应调用 1 次, 实际 %d", prClient.createCalls)
	}
	// Issue 评论应发出（成功通知）
	if issueClient.createCommentCount != 1 {
		t.Errorf("应发出 1 条 Issue 评论, 实际 %d", issueClient.createCommentCount)
	}
}

func TestExecuteFix_PushSuccessButPRCreateFailed(t *testing.T) {
	// Claude 成功 push 但 PR 创建失败 → 发送提醒评论 + 返回 error 允许重试
	fixEnvelope := `{"type":"result","result":"{\"success\":true,\"info_sufficient\":true,\"branch_name\":\"auto-fix/issue-15\",\"commit_sha\":\"abc\",\"modified_files\":[\"a.go\"],\"test_results\":{\"all_passed\":true}}"}`
	issueClient := &stubIssueClient{
		getIssue: &gitea.Issue{Number: 15, State: "open", Ref: "main", Title: "t"},
	}
	pool := &stubPoolRunner{result: &worker.ExecutionResult{Output: fixEnvelope}}
	prClient := &stubPRClient{createErr: fmt.Errorf("gitea 500")}

	s := &Service{gitea: issueClient, pool: pool, prClient: prClient,
		refClient: &stubRefClient{branchOK: map[string]bool{"main": true}},
		logger:    slog.Default()}

	_, err := s.Execute(context.Background(),
		model.TaskPayload{TaskType: model.TaskTypeFixIssue, RepoOwner: "o", RepoName: "r",
			RepoFullName: "o/r", IssueNumber: 15, IssueRef: "main"})
	if err == nil {
		t.Fatal("PR 创建失败应返回可重试错误")
	}
	if issueClient.createCommentCount == 0 {
		t.Error("应发出 push-成功-PR-失败 评论")
	}
}

func TestExecuteFix_ClaudeReturnsFailure(t *testing.T) {
	// success=false → Issue 评论失败报告 + SkipRetry（ErrFixFailed）
	fixEnvelope := `{"type":"result","result":"{\"success\":false,\"info_sufficient\":true,\"failure_reason\":\"测试未通过\",\"analysis\":\"x\",\"test_results\":{\"passed\":5,\"failed\":3,\"all_passed\":false}}"}`
	issueClient := &stubIssueClient{
		getIssue: &gitea.Issue{Number: 15, State: "open", Ref: "main", Title: "t"},
	}
	pool := &stubPoolRunner{result: &worker.ExecutionResult{Output: fixEnvelope}}

	s := &Service{gitea: issueClient, pool: pool,
		refClient: &stubRefClient{branchOK: map[string]bool{"main": true}},
		logger:    slog.Default()}

	_, err := s.Execute(context.Background(),
		model.TaskPayload{TaskType: model.TaskTypeFixIssue, RepoOwner: "o", RepoName: "r",
			RepoFullName: "o/r", IssueNumber: 15, IssueRef: "main"})
	if !errors.Is(err, ErrFixFailed) {
		t.Errorf("应返回 ErrFixFailed, got %v", err)
	}
	if issueClient.createCommentCount == 0 {
		t.Error("应发出失败报告评论")
	}
}
```

注意：`stubIssueClient` / `stubPoolRunner` / `stubPRClient` 等 mock 对象参照既有测试文件风格。如果既有 stub 结构不同，调整为一致即可。

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/fix/ -run TestExecuteFix -v`
Expected: 编译错误或 executeFix 未实现

- [ ] **Step 3: 实现 executeFix + createPR**

修改 `internal/fix/service.go` 的 `Execute` 方法（约第 94-103 行）：

```go
// Execute 按 TaskType 分发到分析或修复流程。
func (s *Service) Execute(ctx context.Context, payload model.TaskPayload) (*FixResult, error) {
	switch payload.TaskType {
	case "", model.TaskTypeAnalyzeIssue:
		return s.executeAnalysis(ctx, payload)
	case model.TaskTypeFixIssue:
		return s.executeFix(ctx, payload)
	default:
		return nil, fmt.Errorf("fix.Service 不支持任务类型: %s", payload.TaskType)
	}
}
```

`internal/fix/service.go` 末尾新增 `executeFix` + `createPR` + 辅助函数：

```go
// executeFix M3.5: 修复模式主流程。
func (s *Service) executeFix(ctx context.Context, payload model.TaskPayload) (*FixResult, error) {
	owner, repo, issueNum := payload.RepoOwner, payload.RepoName, payload.IssueNumber

	// 1. 前置校验
	if issueNum <= 0 {
		return nil, fmt.Errorf("无效的 Issue 编号: %d", issueNum)
	}

	// 2. Issue 状态校验
	issue, _, err := s.gitea.GetIssue(ctx, owner, repo, issueNum)
	if err != nil {
		return nil, fmt.Errorf("获取 Issue #%d 信息失败: %w", issueNum, err)
	}
	if issue.State != "open" {
		return nil, fmt.Errorf("Issue #%d 状态为 %s: %w", issueNum, issue.State, ErrIssueNotOpen)
	}

	effectiveRef := issue.Ref
	if effectiveRef == "" {
		effectiveRef = payload.IssueRef
	}

	// 3. ref 空值检查
	if effectiveRef == "" {
		if commentErr := s.commentRefMissing(ctx, owner, repo, issueNum); commentErr != nil {
			return nil, fmt.Errorf("Issue #%d: 回写 ref 缺失提示失败: %w", issueNum, commentErr)
		}
		return nil, fmt.Errorf("Issue #%d: %w", issueNum, ErrMissingIssueRef)
	}

	// 4. ref 有效性检查，并获取 RefKind
	refKind := RefKindUnknown
	if s.refClient != nil {
		kind, err := s.validateRef(ctx, owner, repo, effectiveRef)
		if err != nil {
			if errors.Is(err, ErrInvalidIssueRef) {
				if commentErr := s.commentRefInvalid(ctx, owner, repo, issueNum, effectiveRef); commentErr != nil {
					return nil, fmt.Errorf("Issue #%d ref=%q: 回写 ref 无效提示失败: %w", issueNum, effectiveRef, commentErr)
				}
				return nil, fmt.Errorf("Issue #%d ref=%q: %w", issueNum, effectiveRef, ErrInvalidIssueRef)
			}
			return nil, fmt.Errorf("验证 ref %q 失败: %w", effectiveRef, err)
		}
		refKind = kind
	}

	// 5. "信息不足"前置检查
	sufficient, err := s.checkPreviousAnalysis(ctx, payload.RepoFullName, issueNum)
	if err != nil {
		// checkPreviousAnalysis 实际上 fail-open，不会返回 error；防御性处理
		s.logger.WarnContext(ctx, "前序分析检查异常，继续修复", "error", err)
	}
	if !sufficient {
		missing := s.latestMissingInfo(ctx, payload.RepoFullName, issueNum)
		body := FormatFixInfoInsufficientComment(missing)
		if _, _, commentErr := s.gitea.CreateIssueComment(ctx, owner, repo, issueNum,
			gitea.CreateIssueCommentOption{Body: body}); commentErr != nil {
			return nil, fmt.Errorf("Issue #%d: 回写信息不足提示失败: %w", issueNum, commentErr)
		}
		return nil, fmt.Errorf("Issue #%d: %w", issueNum, ErrInfoInsufficient)
	}

	// 6. 采集上下文
	issueCtx, err := s.collectContext(ctx, owner, repo, issue)
	if err != nil {
		return nil, fmt.Errorf("采集 Issue #%d 上下文失败: %w", issueNum, err)
	}
	issueCtx.Ref = effectiveRef
	payload.IssueRef = effectiveRef

	s.logger.InfoContext(ctx, "开始修复 Issue",
		"issue", issueNum, "ref", effectiveRef, "ref_kind", refKind,
		"comments", len(issueCtx.Comments))

	// 7. 构造修复 prompt + 容器执行
	prompt := s.buildFixPrompt(issueCtx)
	cmd := s.buildFixCommand()
	execResult, err := s.pool.RunWithCommandAndStdin(ctx, payload, cmd, []byte(prompt))
	if err != nil {
		return &FixResult{
			IssueContext: issueCtx, RawOutput: safeOutput(execResult),
			RefKind: refKind,
		}, fmt.Errorf("容器执行失败: %w", err)
	}
	if execResult == nil {
		return &FixResult{IssueContext: issueCtx, RefKind: refKind},
			fmt.Errorf("容器执行结果为空")
	}
	if execResult.ExitCode != 0 {
		s.logger.ErrorContext(ctx, "fix worker 非零退出",
			"issue", issueNum, "exit_code", execResult.ExitCode)
		return &FixResult{
			IssueContext: issueCtx, RawOutput: execResult.Output,
			ExitCode: execResult.ExitCode, RefKind: refKind,
		}, fmt.Errorf("容器退出码 %d", execResult.ExitCode)
	}

	// 8. 解析 FixOutput
	result := s.parseFixResult(execResult.Output)
	result.IssueContext = issueCtx
	result.RawOutput = execResult.Output
	result.RefKind = refKind

	// 解析失败 → 上层 Processor 决定重试或降级
	if result.ParseError != nil {
		return result, fmt.Errorf("%w: %v", ErrFixParseFailure, result.ParseError)
	}
	fix := result.Fix
	if fix == nil {
		return result, fmt.Errorf("%w: FixOutput 为 nil", ErrFixParseFailure)
	}

	// 9. Claude 自行判断信息不足 → 发送提醒，跳过重试
	if !fix.InfoSufficient {
		body := FormatFixInfoInsufficientComment(fix.MissingInfo)
		if _, _, commentErr := s.gitea.CreateIssueComment(ctx, owner, repo, issueNum,
			gitea.CreateIssueCommentOption{Body: body}); commentErr != nil {
			result.WritebackError = fmt.Errorf("回写信息不足提示失败: %w", commentErr)
		}
		return result, fmt.Errorf("Issue #%d: %w", issueNum, ErrInfoInsufficient)
	}

	// 10. success=false → 失败报告评论，跳过重试
	if !fix.Success {
		var durationSec, costUSD float64
		if result.CLIMeta != nil {
			durationSec = float64(result.CLIMeta.DurationMs) / 1000.0
			costUSD = result.CLIMeta.CostUSD
		}
		body := FormatFixFailureComment(fix, durationSec, costUSD)
		if _, _, commentErr := s.gitea.CreateIssueComment(ctx, owner, repo, issueNum,
			gitea.CreateIssueCommentOption{Body: body}); commentErr != nil {
			result.WritebackError = fmt.Errorf("回写修复失败评论失败: %w", commentErr)
		}
		return result, fmt.Errorf("Issue #%d: %w", issueNum, ErrFixFailed)
	}

	// 11. success=true → 创建 PR（容器已完成 push）
	if s.prClient == nil {
		return result, fmt.Errorf("未注入 PRClient，无法创建修复 PR（success=true 但基础设施未就绪）")
	}
	prNum, prURL, prErr := s.createFixPR(ctx, payload, fix, refKind, issue.Title)
	if prErr != nil {
		// push 成功但 PR 失败 → 发送提醒评论 + 允许重试
		body := FormatFixPushButNoPRComment(fix.BranchName, prErr.Error())
		if _, _, commentErr := s.gitea.CreateIssueComment(ctx, owner, repo, issueNum,
			gitea.CreateIssueCommentOption{Body: body}); commentErr != nil {
			result.WritebackError = fmt.Errorf("回写 PR 创建失败提醒失败: %w", commentErr)
		}
		return result, fmt.Errorf("创建修复 PR 失败: %w", prErr)
	}
	result.PRNumber = prNum
	result.PRURL = prURL

	// 12. 修复成功评论
	body := FormatFixSuccessComment(prNum, prURL, len(fix.ModifiedFiles))
	if _, _, commentErr := s.gitea.CreateIssueComment(ctx, owner, repo, issueNum,
		gitea.CreateIssueCommentOption{Body: body}); commentErr != nil {
		result.WritebackError = fmt.Errorf("回写修复成功评论失败: %w", commentErr)
	}
	return result, nil
}

// createFixPR M3.5: 通过 Gitea API 创建修复 PR。
// refKind=RefKindTag 时需回退到仓库默认分支作为 Base（tag 不能作为 PR Base）。
func (s *Service) createFixPR(ctx context.Context, payload model.TaskPayload, fix *FixOutput,
	refKind RefKind, issueTitle string) (int64, string, error) {

	base := stripRefPrefix(payload.IssueRef)
	if refKind == RefKindTag {
		// Tag 不能作为 Base，改用默认分支
		repoInfo, _, err := s.prClient.GetRepo(ctx, payload.RepoOwner, payload.RepoName)
		if err != nil {
			return 0, "", fmt.Errorf("获取仓库默认分支失败: %w", err)
		}
		base = repoInfo.DefaultBranch
		if base == "" {
			return 0, "", fmt.Errorf("仓库默认分支为空，无法创建 PR")
		}
	}

	title := fmt.Sprintf("fix: #%d %s", payload.IssueNumber, issueTitle)
	// PR 标题 Gitea 上限约 255，安全截断
	if len(title) > 250 {
		title = title[:250]
	}

	body := FormatFixPRBody(fix, payload.IssueNumber, refKind, base)

	pr, _, err := s.prClient.CreatePullRequest(ctx, payload.RepoOwner, payload.RepoName,
		gitea.CreatePullRequestOption{
			Head:  fix.BranchName,
			Base:  base,
			Title: title,
			Body:  body,
		})
	if err != nil {
		return 0, "", err
	}
	if pr == nil {
		return 0, "", fmt.Errorf("Gitea API 返回空 PR")
	}
	return pr.Number, pr.HTMLURL, nil
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/fix/ -run TestExecuteFix -v`
Expected: 全部 PASS

- [ ] **Step 5: 运行全量 fix 测试无回归**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/fix/ -v`
Expected: 全部 PASS

- [ ] **Step 6: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/fix/service.go internal/fix/service_test.go
git commit -m "feat(fix): executeFix 主流程 — 修复 + PR 创建 + 评论回写

M3.5: 12 步主流程（校验 → 信息不足检查 → 采集 → 容器执行 → 解析
→ 分流处理）+ createFixPR（Tag-as-Ref 改用默认分支）。
完整覆盖成功/失败/push-但-PR-失败/信息不足四大场景。"
```

---

## Task 11: Processor 路由 fix_issue 到 fixService

**Files:**
- Modify: `internal/queue/processor.go`
- Test: `internal/queue/processor_test.go`

目的：M3.4 预留 fix_issue 仍走 `pool.Run()` 默认路径，M3.5 需改由 `fixService.Execute` 处理。

- [ ] **Step 1: 写失败测试**

`internal/queue/processor_test.go` 追加：

```go
func TestProcessor_RouteFixIssueToFixService(t *testing.T) {
	called := false
	fixSvc := &stubFixExecutor{
		executeFn: func(ctx context.Context, p model.TaskPayload) (*fix.FixResult, error) {
			called = true
			if p.TaskType != model.TaskTypeFixIssue {
				t.Errorf("fixSvc 收到 TaskType=%v, 期望 fix_issue", p.TaskType)
			}
			return &fix.FixResult{
				Fix: &fix.FixOutput{Success: true, BranchName: "auto-fix/issue-1", CommitSHA: "abc"},
				PRNumber: 42, PRURL: "https://g/o/r/pulls/42",
			}, nil
		},
	}

	// 使用既有 test helper 构造 Processor（参照同文件其他测试）
	p := newTestProcessorWithFix(t, fixSvc)
	// ... 触发 fix_issue 任务 → 断言 fixSvc.Execute 被调用，pool.Run 未被调用
	_ = p
	_ = called
}
```

注意：此测试框架依赖既有 processor_test.go 的 helper。先查找同文件中 review_service 注入测试的写法，仿照构造 fix_issue 测试。

- [ ] **Step 2: 修改 ProcessTask 路由 switch**

`internal/queue/processor.go` 第 229-242 行的 switch 替换为：

```go
	// M3.5 路由：review_pr → reviewService；analyze_issue 和 fix_issue 均 → fixService。
	switch {
	case payload.TaskType == model.TaskTypeReviewPR && p.reviewService != nil:
		reviewResult, runErr = p.reviewService.Execute(ctx, payload)
		if reviewResult != nil {
			result = adaptReviewResult(reviewResult)
		}
	case (payload.TaskType == model.TaskTypeAnalyzeIssue || payload.TaskType == model.TaskTypeFixIssue) && p.fixService != nil:
		fixResult, runErr = p.fixService.Execute(ctx, payload)
		if fixResult != nil {
			result = adaptFixResult(fixResult)
		}
	default:
		result, runErr = p.pool.Run(ctx, payload)
	}
```

- [ ] **Step 3: 扩展 ErrInfoInsufficient / ErrFixFailed 处理**

在既有 `errors.Is(runErr, fix.ErrMissingIssueRef)` 附近（约第 262-267 行），追加 M3.5 的失败模式：

```go
	if errors.Is(runErr, fix.ErrInfoInsufficient) {
		return p.handleSkipRetryFailure(ctx, record, runErr, nil, fixResult, "前序分析信息不足，跳过修复")
	}
	if errors.Is(runErr, fix.ErrFixFailed) {
		return p.handleSkipRetryFailure(ctx, record, runErr, nil, fixResult, "Claude 返回 success=false，跳过重试")
	}
```

注意：`ErrFixParseFailure` 不直接 SkipRetry，让默认的 retrying 路径处理（给 Claude 重试机会）。

- [ ] **Step 4: 运行全量 queue 测试**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/queue/ -v`
Expected: 全部 PASS（新测试 + 既有测试）

- [ ] **Step 5: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/queue/processor.go internal/queue/processor_test.go
git commit -m "feat(queue): Processor 路由 fix_issue 到 fixService

M3.5: analyze_issue 和 fix_issue 共用 fix.Service；
新增 ErrInfoInsufficient/ErrFixFailed 的 SkipRetry 处理。"
```

---

## Task 12: 完成通知注入修复 PR 元数据

**Files:**
- Modify: `internal/notify/notifier.go`
- Modify: `internal/queue/processor.go`
- Test: `internal/queue/processor_test.go`

**背景：** 既有 `buildNotificationMessage` 签名为 `(..., _ *fix.FixResult)`（fixResult 被丢弃），`TaskTypeFixIssue` 成功分支的 metadata 仅含 `issue_url`，飞书卡片无法展示 PR 跳转按钮和修改文件数。设计文档 §4.8 要求修复成功注入 `pr_url` / `pr_number` / `modified_files`。

- [ ] **Step 1: 写失败测试**

`internal/queue/processor_test.go` 追加（参照既有 `TestBuildNotificationMessage_ReviewPR_*` 风格）：

```go
func TestBuildNotificationMessage_FixIssue_SuccessInjectsPRMetadata(t *testing.T) {
	p := newTestProcessorWithGiteaBaseURL(t, "https://gitea.example.com")
	now := time.Now()
	record := &model.TaskRecord{
		ID:     "fix-1",
		Status: model.TaskStatusSucceeded,
		Payload: model.TaskPayload{
			TaskType:     model.TaskTypeFixIssue,
			RepoOwner:    "org",
			RepoName:     "repo",
			RepoFullName: "org/repo",
			IssueNumber:  15,
		},
		StartedAt:   &now,
		CompletedAt: &now,
	}
	fixResult := &fix.FixResult{
		Fix: &fix.FixOutput{
			Success:       true,
			BranchName:    "auto-fix/issue-15",
			CommitSHA:     "abc",
			ModifiedFiles: []string{"a.java", "b.java", "c.java"},
		},
		PRNumber: 42,
		PRURL:    "https://gitea.example.com/org/repo/pulls/42",
	}

	msg, ok := p.buildNotificationMessage(record, nil, fixResult)
	if !ok {
		t.Fatal("buildNotificationMessage 应返回 ok=true")
	}
	if msg.EventType != notify.EventFixIssueDone {
		t.Errorf("EventType = %q, 期望 EventFixIssueDone", msg.EventType)
	}
	if got := msg.Metadata[notify.MetaKeyPRURL]; got != fixResult.PRURL {
		t.Errorf("MetaKeyPRURL = %q, 期望 %q", got, fixResult.PRURL)
	}
	if got := msg.Metadata[notify.MetaKeyPRNumber]; got != "42" {
		t.Errorf("MetaKeyPRNumber = %q, 期望 42", got)
	}
	if got := msg.Metadata[notify.MetaKeyModifiedFiles]; got != "3" {
		t.Errorf("MetaKeyModifiedFiles = %q, 期望 3", got)
	}
}

func TestBuildNotificationMessage_FixIssue_FailureOmitsPRMetadata(t *testing.T) {
	p := newTestProcessorWithGiteaBaseURL(t, "https://gitea.example.com")
	record := &model.TaskRecord{
		ID:     "fix-2",
		Status: model.TaskStatusFailed,
		Error:  "测试未通过",
		Payload: model.TaskPayload{
			TaskType: model.TaskTypeFixIssue, RepoOwner: "org", RepoName: "repo",
			RepoFullName: "org/repo", IssueNumber: 15,
		},
	}
	msg, ok := p.buildNotificationMessage(record, nil, &fix.FixResult{})
	if !ok {
		t.Fatal("failed 状态也应生成通知")
	}
	if _, exists := msg.Metadata[notify.MetaKeyPRURL]; exists {
		t.Error("失败场景不应注入 pr_url")
	}
}
```

注意：`newTestProcessorWithGiteaBaseURL` 是既有 helper；若名称不同，沿用既有同类测试的构造方式。

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/queue/ -run TestBuildNotificationMessage_FixIssue -v`
Expected: 断言 `MetaKeyPRNumber` / `MetaKeyModifiedFiles` 不存在或未注入的失败

- [ ] **Step 3: 新增 notifier 常量**

`internal/notify/notifier.go` 在 `MetaKeyDuration` 之后追加：

```go
	MetaKeyPRNumber      = "pr_number"       // M3.5: 修复 PR 编号（字符串化 int64）
	MetaKeyModifiedFiles = "modified_files"  // M3.5: 修复修改文件数（字符串化 int）
```

**设计说明**：沿用 review 的 `MetaKeyPRURL`，不单独定义 `MetaKeyFixPRURL`（review 和 fix 的 PR 链接语义一致，飞书卡片按钮逻辑复用）。

- [ ] **Step 4: 修改 buildNotificationMessage 签名与实现**

`internal/queue/processor.go` 第 528 行签名改为 `fixResult *fix.FixResult`；`TaskTypeFixIssue` 的 succeeded 分支补充 PR 元数据注入：

```go
		case model.TaskStatusSucceeded:
			if fixResult != nil && fixResult.Fix != nil && fixResult.PRNumber > 0 {
				metadata[notify.MetaKeyPRURL] = fixResult.PRURL
				metadata[notify.MetaKeyPRNumber] = fmt.Sprintf("%d", fixResult.PRNumber)
				metadata[notify.MetaKeyModifiedFiles] = fmt.Sprintf("%d", len(fixResult.Fix.ModifiedFiles))
			}
			msg = notify.Message{
				EventType: notify.EventFixIssueDone,
				Severity:  notify.SeverityInfo,
				Target:    issueTarget,
				Title:     "Issue 自动修复任务完成",
				Body:      body,
				Metadata:  metadata,
			}
```

- [ ] **Step 5: 运行全量 queue 测试**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/queue/ -v`
Expected: 全部 PASS（含 Task 11 + Task 12 新测试）

- [ ] **Step 6: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/notify/notifier.go internal/queue/processor.go internal/queue/processor_test.go
git commit -m "feat(notify): 完成通知注入修复 PR 元数据

M3.5: 新增 MetaKeyPRNumber/MetaKeyModifiedFiles 常量；
buildNotificationMessage 使用 fixResult 在 fix_issue 成功分支注入
pr_url/pr_number/modified_files，供飞书卡片展示跳转按钮和修复摘要。"
```

---

## Task 13: 飞书卡片样式 — 修复/分析事件配色与按钮文案

**Files:**
- Modify: `internal/notify/feishu_card.go`
- Test: `internal/notify/feishu_card_test.go`

**背景：** 设计文档 §4.8 要求修复成功为绿色卡片（含 PR 跳转按钮），分析成功为蓝色。当前 `resolveHeaderStyle` 没有 `EventFixIssueDone` / `EventIssueAnalyzeDone` / `EventIssueFixStarted` / `EventIssueAnalyzeStarted` 专用 case，全部落到 default 的 "blue"（与设计不符）。

- [ ] **Step 1: 写失败测试**

`internal/notify/feishu_card_test.go` 追加：

```go
func TestFormatFeishuCard_FixIssueDone_GreenWithPRButton(t *testing.T) {
	msg := Message{
		EventType: EventFixIssueDone,
		Target:    Target{Owner: "org", Repo: "repo", Number: 15},
		Title:     "Issue 自动修复任务完成",
		Metadata: map[string]string{
			MetaKeyPRURL:         "https://gitea/org/repo/pulls/42",
			MetaKeyPRNumber:      "42",
			MetaKeyModifiedFiles: "3",
			MetaKeyIssueURL:      "https://gitea/org/repo/issues/15",
		},
	}
	card, err := FormatFeishuCard(msg)
	if err != nil {
		t.Fatalf("FormatFeishuCard: %v", err)
	}
	// 标题颜色应为绿色
	header := card["card"].(map[string]any)["header"].(map[string]any)
	if header["template"] != "green" {
		t.Errorf("fix 成功卡片应为绿色, got %v", header["template"])
	}
	// 按钮应指向 PR URL（resolveButtonURL 优先 pr_url）
	elements := card["card"].(map[string]any)["elements"].([]any)
	hasPRButton := false
	for _, el := range elements {
		if m, ok := el.(map[string]any); ok && m["tag"] == "action" {
			actions := m["actions"].([]any)
			btn := actions[0].(map[string]any)
			if btn["url"] == "https://gitea/org/repo/pulls/42" {
				hasPRButton = true
			}
			txt := btn["text"].(map[string]string)["content"]
			if !strings.Contains(txt, "PR") {
				t.Errorf("修复成功按钮文案应包含 PR, got %q", txt)
			}
		}
	}
	if !hasPRButton {
		t.Error("修复成功卡片应含跳转到 PR 的按钮")
	}
}

func TestFormatFeishuCard_IssueAnalyzeStarted_Blue(t *testing.T) {
	msg := Message{
		EventType: EventIssueAnalyzeStarted,
		Target:    Target{Owner: "o", Repo: "r", Number: 10},
		Title:     "Issue 自动分析开始",
		Metadata:  map[string]string{MetaKeyIssueURL: "https://gitea/o/r/issues/10"},
	}
	card, _ := FormatFeishuCard(msg)
	header := card["card"].(map[string]any)["header"].(map[string]any)
	if header["template"] != "blue" {
		t.Errorf("分析开始应为蓝色, got %v", header["template"])
	}
}

func TestFormatFeishuCard_IssueFixStarted_Blue(t *testing.T) {
	msg := Message{
		EventType: EventIssueFixStarted,
		Target:    Target{Owner: "o", Repo: "r", Number: 10},
		Title:     "Issue 自动修复开始",
		Metadata:  map[string]string{MetaKeyIssueURL: "https://gitea/o/r/issues/10"},
	}
	card, _ := FormatFeishuCard(msg)
	header := card["card"].(map[string]any)["header"].(map[string]any)
	if header["template"] != "blue" {
		t.Errorf("修复开始应为蓝色, got %v", header["template"])
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/notify/ -run "TestFormatFeishuCard_FixIssue|TestFormatFeishuCard_IssueAnalyze|TestFormatFeishuCard_IssueFix" -v`
Expected: 颜色断言失败（当前走 default blue，但 fix 成功期望 green）

- [ ] **Step 3: 扩展 resolveHeaderStyle + resolveButtonStyle**

`internal/notify/feishu_card.go` 的 `resolveHeaderStyle` switch 新增 case：

```go
	case EventIssueAnalyzeStarted, EventIssueFixStarted:
		return msg.Title, "blue"
	case EventIssueAnalyzeDone:
		return "Issue 分析完成", "blue"
	case EventFixIssueDone:
		// M3.5: 修复成功 = 绿色（含 PR 跳转）
		return "Issue 修复 PR 已创建", "green"
```

`resolveButtonStyle` switch 新增 case：

```go
	case EventFixIssueDone:
		return "查看修复 PR", "primary"
	case EventIssueAnalyzeDone, EventIssueAnalyzeStarted, EventIssueFixStarted:
		return "查看 Issue", "default"
```

`resolveButtonURL` 无需改动（既有逻辑已优先 `MetaKeyPRURL` → 回退 `MetaKeyIssueURL`）。

- [ ] **Step 4: 运行测试确认通过 + 既有回归**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/notify/ -v`
Expected: 全部 PASS

- [ ] **Step 5: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/notify/feishu_card.go internal/notify/feishu_card_test.go
git commit -m "feat(notify): 飞书卡片适配 fix/analyze 事件样式

M3.5: EventFixIssueDone 绿色卡 + '查看修复 PR' 按钮（含 PR 跳转）；
EventIssueAnalyzeStarted/Done 和 EventIssueFixStarted 蓝色 + Issue 按钮。
按钮 URL 复用既有 pr_url → issue_url 回退逻辑。"
```

---

## Task 14: serve.go 装配 PRClient + FixStaleChecker

**Files:**
- Modify: `internal/cmd/serve.go`
- Test: `internal/cmd/serve_test.go`（可选，若已有 fix.NewService 装配断言则更新）

**背景：** `cmd/serve.go:226-232` 当前 `fix.NewService` 只注入 `WithRefClient`，未注入 M3.5 新增的 `WithPRClient` 和 `WithFixStaleChecker`。Task 10 的 `executeFix` 在第 11 步（PR 创建）依赖 `s.prClient != nil`，未注入则修复成功永远降级为 "PRClient 未就绪" 错误。

- [ ] **Step 1: 确认 *gitea.Client 满足 PRClient 接口**

Run 一次性验证（不提交，仅心智检查）：

```bash
cd /Users/kelin/Work/DTWorkflow
grep -nE "func \(c \*Client\) (CreatePullRequest|GetRepo)" internal/gitea/
```

预期看到：
- `pulls.go`: `CreatePullRequest(ctx, owner, repo, opts) (*PullRequest, *Response, error)`
- `repos.go`: `GetRepo(ctx, owner, repo) (*Repository, *Response, error)`

签名需与 Task 3 的 `PRClient` 接口一致。若 Gitea 客户端的 `CreatePullRequest` 签名不同（比如多一个参数），调整 Task 3 的接口定义使其与既有实现一致。

- [ ] **Step 2: 修改 serve.go 装配**

`internal/cmd/serve.go:226` 的 `fix.NewService` 调用扩展为：

```go
		// M3.2: 装配 fix.Service；M3.5 追加 PRClient + FixStaleChecker
		fixSvc := fix.NewService(
			deps.GiteaClient,
			deps.Pool,
			fix.WithServiceLogger(slog.Default()),
			fix.WithConfigProvider(cfgAdapter),
			fix.WithRefClient(deps.GiteaClient),
			fix.WithPRClient(deps.GiteaClient),   // M3.5: 修复 PR 创建
			fix.WithFixStaleChecker(deps.Store),  // M3.5: 前序分析"信息不足"检查
		)
```

- [ ] **Step 3: 编译验证**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./...`
Expected: 编译通过。若 `deps.Store` 类型（`*store.SQLiteStore` 或 `store.Store` 接口）不满足 `FixStaleChecker`，回到 Task 2 确认 `Store` 接口已包含 `GetLatestAnalysisByIssue`（接口方式）或 `FixStaleChecker` 签名匹配具体类型。

- [ ] **Step 4: 运行全量测试**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./... -count=1`
Expected: 全部 PASS

- [ ] **Step 5: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/cmd/serve.go
git commit -m "feat(serve): fix.Service 装配 PRClient + FixStaleChecker

M3.5: 注入 Gitea 客户端作为 PRClient（用于创建修复 PR）；
注入 SQLiteStore 作为 FixStaleChecker（用于前序分析信息不足检查）。"
```

---

## Task 15: 全量回归构建 + 交叉编译验证

**Files:** 无

**背景：** Task 1-14 修改了 fix/notify/queue/store/cmd 五个包，需在合并里程碑前做完整回归。项目要求 `GOOS=linux GOARCH=amd64 go build` 可直接交叉编译（见 CLAUDE.md），修复代码不能引入 CGO 依赖。

- [ ] **Step 1: 全量单元测试**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./... -count=1 -timeout=5m`
Expected: 全部 PASS

- [ ] **Step 2: make build（服务端 + dtw 双二进制）**

Run: `cd /Users/kelin/Work/DTWorkflow && make build`
Expected: 生成 `bin/dtworkflow` 和 `bin/dtw`，无错误

- [ ] **Step 3: 交叉编译验证**

Run:

```bash
cd /Users/kelin/Work/DTWorkflow
GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -o /tmp/dtworkflow-linux ./cmd/dtworkflow
GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -o /tmp/dtw-linux ./cmd/dtw
file /tmp/dtworkflow-linux /tmp/dtw-linux
```

Expected: 两个二进制均为 `ELF 64-bit LSB executable, x86-64`，无 CGO 依赖链接错误

- [ ] **Step 4: golangci-lint（若配置存在）**

Run: `cd /Users/kelin/Work/DTWorkflow && golangci-lint run ./... 2>&1 | head -50`
Expected: 无新增警告（允许历史遗留 issue）

- [ ] **Step 5: （可选）提交一个空占位 commit 标记里程碑进入验收阶段**

如果上述全部通过，无代码改动则跳过此步；否则：

```bash
cd /Users/kelin/Work/DTWorkflow
git status  # 应为 clean
```

---

## Task 16: E2E 手动验收 + 文档同步

**Files:**
- Modify: `docs/ROADMAP.md`
- Modify: `docs/PRD.md`（若 M3.5 状态标记）
- Modify: `CLAUDE.md`（若需要补充运行方式）

**背景：** 设计文档 §5 M3.5 列出 10+ 条验证标准，需在测试服务器（`companytest`）按 `changyu/ai_workflow_test` 仓库走一次完整 E2E。参考既有 `memory/MEMORY.md` 中 Phase 2 E2E 验证的记录格式。

### 16A：E2E 验收清单（逐项记录在 commit message 或单独的验收报告）

- [ ] **准备环境**

  - 本机启动：`docker compose up -d redis --pull=never && ./bin/dtworkflow serve --config ./dtworkflow.yaml`
  - 或测试服务器：`ssh companytest` → 部署到 `/opt/dtworkflow`
  - 确认 `dtworkflow.yaml` 中 `worker.image_full: dtworkflow-worker-full:latest` 已配置
  - 确认 Gitea 测试仓库存在 `auto-fix` 和 `fix-to-pr` 两个标签

- [ ] **验收 1：fix-to-pr 完整闭环**

  场景：在 `changyu/ai_workflow_test` 创建含明确 bug 描述的 Issue → 打 `fix-to-pr` 标签

  断言：
  - 容器启动使用 `worker-full` 镜像（查 `docker ps` 或 `bin/dtworkflow` 日志）
  - Claude 完成：分析 + 代码修改 + 测试运行 + git push
  - `fix.Service` 创建 PR，`fixes #N` 关联 Issue
  - Issue 收到 "✅ 已创建修复 PR #X" 评论
  - PR 描述含根因分析、修复方案、修改文件列表、测试结果

- [ ] **验收 2：修复 PR 自动触发评审**

  断言：步骤 1 创建的 PR 被 Gitea Webhook 触发 `review_pr` 任务（Phase 2 零代码自动接入）

- [ ] **验收 3：无前序分析 → Claude 隐式分析后修复**

  场景：新 Issue 直接打 `fix-to-pr`（未先经过 `auto-fix` 分析）

  断言：容器正常启动，Claude 在 prompt 驱动下先理解问题再修复

- [ ] **验收 4：前序分析"信息不足" → 阻断**

  场景：Issue 先打 `auto-fix` 得到 `info_sufficient=false` 的分析 → 再打 `fix-to-pr`

  断言：
  - 容器**不**启动（查 `docker ps` 无新容器 + 任务 logger 输出 "前序分析标记为信息不足"）
  - Issue 收到 "## DTWorkflow Issue 修复前置检查" 评论
  - 任务状态 failed + `ErrInfoInsufficient`

- [ ] **验收 5：测试失败 → Claude 返回 success=false**

  场景：故意构造无法修复的 Issue（要求删除关键逻辑）

  断言：
  - Issue 收到 "## DTWorkflow Issue 修复失败" 评论，含 `failure_reason`
  - 任务状态 failed + `ErrFixFailed`，不重试

- [ ] **验收 6：Push 成功但 PR 创建失败**

  场景：（难以自然复现）临时在 serve.go 中注入一个 mock `PRClient` 返回 error；或使用无 pulls 写权限的 Token

  断言：
  - Issue 收到 "⚠️ 修复分支 ... 已推送成功，但 PR 创建失败" 评论
  - 任务允许 asynq 重试

- [ ] **验收 7：分支已存在 → force-with-lease 覆盖**

  场景：手动在目标仓库创建 `auto-fix/issue-X` 分支 → 触发 `fix-to-pr` 修复同一 Issue

  断言：Claude 执行 `git push --force-with-lease` 覆盖，PR 正常创建

- [ ] **验收 8：Issue Ref 为 tag**

  场景：Issue 的 "Reference" 字段设为 tag（如 `v1.0.0`）→ 触发 `fix-to-pr`

  断言：
  - PR 的 Base 为仓库默认分支（非 tag）
  - PR 描述注明 "原 Issue 指定的 ref 为 tag，Gitea PR 不支持 tag 作为 base 分支"

- [ ] **验收 9：飞书通知三场景**

  - 蓝色 "Issue 自动修复开始"（任务入队时）
  - 绿色 "Issue 修复 PR 已创建"（成功时，带 "查看修复 PR" 按钮）
  - 红色 "Issue 自动修复任务失败"（失败时）

- [ ] **验收 10：dtw fix-issue --fix 远程触发**

  Run: `./bin/dtw fix-issue --owner changyu --repo ai_workflow_test --issue N --fix`

  断言：服务端创建 `fix_issue` 任务，执行路径与标签触发完全一致

- [ ] **验收 11：并发修复 Cancel-and-Replace**（若 M3.4 已接入此能力，否则标注延后）

  场景：连续两次触发同一 Issue 的 `fix-to-pr`

  断言：旧任务被取消，只有最新任务执行

### 16B：文档同步

- [ ] **更新 ROADMAP.md**

  在 `##### M3.5 修复业务逻辑层` 章节（约第 512 行）：
  - 标题追加 "（已完成 YYYY-MM-DD）"（参照 M3.4 的 "已完成 2026-04-16"）
  - 所有 `- [ ]` 改为 `- [x]`
  - 追加 "完成说明" 块，简述交付物（同 M3.1-M3.3 完成说明的格式）

- [ ] **更新 PRD.md**

  `第二轮（M3.4-M3.5，自动修复与 PR 创建）` 章节（约第 365 行）：如果 PRD 有状态标记则同步为"已完成"；纯需求描述则无需改动

- [ ] **更新 CLAUDE.md**

  检查是否需要新增 "Issue 自动修复" 功能的运行方式说明（`fix-to-pr` 标签触发、`dtw fix-issue --fix` 命令、两级镜像选择）。参照 Phase 1/2 的记录体例，在合适章节追加一段

- [ ] **更新 memory/MEMORY.md（可选）**

  参照既有 Phase 1/M2.1/M2.6/Phase 2 E2E 的记录格式，新增 "M3.5 Issue 自动修复与 PR 创建 - 已完成（YYYY-MM-DD）" 章节，记录：
  - 核心架构（单容器全托管 + 容器外 PR 创建）
  - 关键类型（FixOutput / RefKind / PRClient / FixStaleChecker）
  - 数据流（12 步主流程概要）
  - E2E 验收过程中修复的 bug（如有）
  - 基线数据（修复耗时、费用、成功率）

### 16C：提交

- [ ] **Step 1: 提交文档同步**

```bash
cd /Users/kelin/Work/DTWorkflow
git add docs/ROADMAP.md docs/PRD.md CLAUDE.md
git commit -m "docs: 同步 M3.5 实现状态到 ROADMAP/PRD/CLAUDE.md

M3.5 Issue 自动修复与 PR 创建业务逻辑层全部交付；
E2E 验收 10 项清单验证通过；
飞书通知三场景卡片正常。"
```

- [ ] **Step 2: （可选）提交 memory 更新**

  memory 目录为用户本地持久化，不入 git；若有更新直接写入文件即可。

---

## 里程碑完成判定

所有 Task 1-16 完成且满足以下全部条件方可宣告 M3.5 收口：

1. `go test ./... -count=1` 全绿
2. `make build` + `GOOS=linux go build` 均成功
3. E2E 验收清单（16A）至少覆盖到验收 1、3、4、5、9、10（核心闭环 + 失败分支 + 通知 + 远程触发）
4. ROADMAP.md 的 M3.5 章节所有 `- [ ]` 已勾选
5. 测试服务器部署后在 `changyu/ai_workflow_test` 仓库完成一次真实修复 PR（含自动评审链路）

---
