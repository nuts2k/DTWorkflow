# M4.2.1 混合仓库多框架支持 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 整仓模式自动发现 depth=1 子目录中的可测试模块，多模块拆分为独立 gen_tests 任务，各自产出独立 PR。

**Architecture:** 入队层 `EnqueueManualGenTests` 调用 `ScanRepoModules` 扫描仓库根和 depth=1 子目录的 `pom.xml`/`package.json`，将发现的模块拆分为独立子任务入队。每个子任务复用现有完整 `test.Service.Execute` 管线。分支命名通过 `BuildAutoTestBranchName(module, framework)` 的 variadic framework 参数支持同目录双框架场景。Cancel-and-Replace 按 `(module, framework)` 二元组聚合，避免同 module 不同 framework 的任务互相取消。

**Tech Stack:** Go, Gitea REST API (Contents endpoint), asynq task queue

**设计文档:** `docs/superpowers/specs/2026-04-29-m4.2.1-mixed-framework-design.md`

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| `internal/gitea/repos.go` | 修改 | 新增 `ListDirContents` 方法（Gitea Contents API 列目录） |
| `internal/gitea/repos_test.go` | 修改 | `ListDirContents` 单测 |
| `internal/test/service.go` | 修改 | `RepoFileChecker` 接口新增 `ListDir` 方法；Execute 构造 `PromptContext` 时用 resolved framework |
| `internal/test/scan.go` | **新建** | `DiscoveredModule` 类型 + `ScanRepoModules` 函数 |
| `internal/test/scan_test.go` | **新建** | `ScanRepoModules` 全场景单测 |
| `internal/test/prompt.go` | 修改 | `BuildAutoTestBranchName` 增加 variadic `framework`；`PromptContext` 增 `Framework` 字段；`writeHeader` 传入 framework |
| `internal/test/prompt_test.go` | 修改 | `BuildAutoTestBranchName` 新参数组合测试 |
| `internal/test/service_test.go` | 修改 | `mockFileChecker`（约 line 72）添加 `ListDir` 桩方法 |
| `internal/worker/container.go` | 修改 | 新增 `moduleKeyForContainerWithFramework`；`buildContainerEnv` 适配 |
| `internal/worker/container_test.go` | 修改 | 一致性对照测试 + `buildContainerEnv` framework 注入测试 |
| `internal/queue/enqueue.go` | 修改 | 返回值改 `[]EnqueuedTask`；提取 `enqueueSingleGenTests`；`cleanupAllAutoTestBranches`；整仓拆分逻辑；`listActiveGenTestsTasksByBranchKey` 增加 framework 参数；`shouldSkipAutoTestReview` 适配 framework 后缀分支 |
| `internal/queue/enqueue_test.go` | 修改 | 适配返回值 + 新增拆分路径测试 + shouldSkipAutoTestReview framework 场景测试 |
| `internal/api/test_handler.go` | 修改 | 响应体适配多任务返回 |
| `internal/cmd/gen_tests.go` | 修改 | 服务端 CLI 输出适配多任务 + 装配层注入 `WithModuleScanner` / `WithPRClient` |
| `internal/dtw/cmd/gen_tests.go` | 修改 | 瘦客户端输出适配 |
| `internal/cmd/adapter.go` | 修改 | `giteaRepoFileChecker` 实现 `ListDir` |
| `internal/cmd/adapter_test.go` | 修改 | `ListDir` 适配器单测 |
| `internal/cmd/serve.go` | 修改 | 装配层注入 `WithModuleScanner` / `WithPRClient` |

---

### Task 1: Gitea ListDirContents API 方法

**Files:**
- Modify: `internal/gitea/repos.go`
- Modify: `internal/gitea/repos_test.go`

- [ ] **Step 1: 写失败测试 — ListDirContents 正常返回目录条目**

在 `internal/gitea/repos_test.go` 新增：

```go
func TestListDirContents(t *testing.T) {
	ts := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		if r.Method != "GET" {
			t.Errorf("期望 GET，实际 %s", r.Method)
		}
		wantPath := "/api/v1/repos/owner/repo/contents/"
		if r.URL.Path != wantPath {
			t.Errorf("期望路径 %s，实际 %s", wantPath, r.URL.Path)
		}
		if r.URL.Query().Get("ref") != "main" {
			t.Errorf("期望 ref=main，实际 %s", r.URL.Query().Get("ref"))
		}
		resp := []ContentsResponse{
			{Name: "backend", Path: "backend", Type: "dir"},
			{Name: "frontend", Path: "frontend", Type: "dir"},
			{Name: "README.md", Path: "README.md", Type: "file"},
		}
		w.Header().Set("Content-Type", "application/json")
		json.NewEncoder(w).Encode(resp)
	}))
	defer ts.Close()

	client, _ := NewClient(ts.URL, WithToken("test-token"))
	entries, _, err := client.ListDirContents(context.Background(), "owner", "repo", "", "main")
	if err != nil {
		t.Fatalf("ListDirContents 返回错误: %v", err)
	}
	if len(entries) != 3 {
		t.Fatalf("期望 3 个条目，实际 %d", len(entries))
	}
	if entries[0].Name != "backend" || entries[0].Type != "dir" {
		t.Errorf("第一个条目不符合预期: %+v", entries[0])
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/gitea/ -run TestListDirContents -v`
Expected: FAIL — `client.ListDirContents` 未定义

- [ ] **Step 3: 实现 ListDirContents**

在 `internal/gitea/repos.go` 末尾新增：

```go
// ListDirContents 列出仓库指定目录下的条目（文件和子目录）。
// dir 为空字符串时列出仓库根目录。
// GET /api/v1/repos/{owner}/{repo}/contents/{dir}?ref={ref}
// 注意：dir 不做 PathEscape，Gitea 的 contents 端点使用通配符路径，期望原始斜杠
func (c *Client) ListDirContents(ctx context.Context, owner, repo, dir, ref string) ([]ContentsResponse, *Response, error) {
	p := fmt.Sprintf("/api/v1/repos/%s/%s/contents/%s",
		url.PathEscape(owner), url.PathEscape(repo), dir)
	params := url.Values{}
	if ref != "" {
		params.Set("ref", ref)
	}
	req, err := c.newRequestWithQuery(ctx, "GET", p, params, nil)
	if err != nil {
		return nil, nil, err
	}

	var result []ContentsResponse
	resp, err := c.doRequest(req, &result)
	if err != nil {
		return nil, resp, err
	}
	return result, resp, nil
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/gitea/ -run TestListDirContents -v`
Expected: PASS

- [ ] **Step 5: 写 404 场景测试**

```go
func TestListDirContents_NotFound(t *testing.T) {
	ts := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		w.WriteHeader(http.StatusNotFound)
		w.Write([]byte(`{"message":"Not Found"}`))
	}))
	defer ts.Close()

	client, _ := NewClient(ts.URL, WithToken("test-token"))
	_, _, err := client.ListDirContents(context.Background(), "owner", "repo", "nonexist", "")
	if err == nil {
		t.Fatal("期望返回错误")
	}
	if !IsNotFound(err) {
		t.Errorf("期望 NotFound 错误，实际: %v", err)
	}
}
```

- [ ] **Step 6: 运行全部 gitea 包测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/gitea/ -v -count=1`
Expected: ALL PASS

- [ ] **Step 7: 提交**

```bash
git add internal/gitea/repos.go internal/gitea/repos_test.go
git commit -m "feat(m4.2.1): 新增 Gitea ListDirContents API 方法

用于 ScanRepoModules 列出仓库目录条目，支持 depth=1 子目录扫描。"
```

---

### Task 2: RepoFileChecker 接口扩展 + 适配器实现

**Files:**
- Modify: `internal/test/service.go:56-61` — `RepoFileChecker` 接口新增 `ListDir`
- Modify: `internal/cmd/adapter.go:172-195` — `giteaRepoFileChecker` 实现 `ListDir`
- Modify: `internal/cmd/adapter_test.go` — 新增 `ListDir` 测试
- Modify: `internal/test/service_test.go` — `mockFileChecker`（约 line 72）添加 `ListDir` 桩

**需要添加 `ListDir` 桩的 mock 精确位置**（用 `grep -rn "HasFile" internal/test/` 确认）：
- `internal/test/service_test.go` 中的 `mockFileChecker`（唯一实现 `RepoFileChecker` 的测试桩）

- [ ] **Step 1: 扩展 RepoFileChecker 接口**

在 `internal/test/service.go` 的 `RepoFileChecker` 接口中新增 `ListDir` 方法：

```go
type RepoFileChecker interface {
	HasFile(ctx context.Context, owner, repo, ref, module, relPath string) (bool, error)
	// ListDir 返回指定目录下的子目录名列表（不含文件，不递归）。
	// dir 为空字符串时列出仓库根目录。M4.2.1 用于 ScanRepoModules depth=1 扫描。
	ListDir(ctx context.Context, owner, repo, ref, dir string) ([]string, error)
}
```

- [ ] **Step 2: 编译检查——确认哪些实现需要修复**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./... 2>&1 | head -20`
Expected: FAIL — 列出所有缺少 `ListDir` 的实现

- [ ] **Step 3: 修复 giteaRepoFileChecker — 实现 ListDir**

在 `internal/cmd/adapter.go` 的 `giteaRepoFileChecker` 上添加：

```go
func (c *giteaRepoFileChecker) ListDir(ctx context.Context, owner, repo, ref, dir string) ([]string, error) {
	if c == nil || c.client == nil {
		return nil, fmt.Errorf("giteaRepoFileChecker: client 不能为空")
	}
	entries, _, err := c.client.ListDirContents(ctx, owner, repo, dir, ref)
	if err != nil {
		return nil, fmt.Errorf("ListDir(%s): %w", dir, err)
	}
	var dirs []string
	for _, e := range entries {
		if e.Type == "dir" {
			dirs = append(dirs, e.Name)
		}
	}
	return dirs, nil
}
```

- [ ] **Step 4: 修复 service_test.go 中 mockFileChecker**

在 `internal/test/service_test.go` 的 `mockFileChecker` 结构体上添加 `ListDir` 桩（返回 nil, nil 即可——Service.Execute 不直接调用 ListDir，只有 ScanRepoModules 使用）：

```go
func (m *mockFileChecker) ListDir(_ context.Context, _, _, _, _ string) ([]string, error) {
	return nil, nil
}
```

- [ ] **Step 5: 编译确认所有实现已修复**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./...`
Expected: PASS

- [ ] **Step 6: 写 adapter_test.go 中 ListDir 测试**

在 `internal/cmd/adapter_test.go` 新增：

```go
func TestGiteaRepoFileChecker_ListDir(t *testing.T) {
	ts := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		resp := []gitea.ContentsResponse{
			{Name: "backend", Type: "dir"},
			{Name: "frontend", Type: "dir"},
			{Name: "README.md", Type: "file"},
			{Name: ".gitignore", Type: "file"},
		}
		w.Header().Set("Content-Type", "application/json")
		json.NewEncoder(w).Encode(resp)
	}))
	defer ts.Close()

	client, _ := gitea.NewClient(ts.URL, gitea.WithToken("test"))
	checker := &giteaRepoFileChecker{client: client}
	dirs, err := checker.ListDir(context.Background(), "owner", "repo", "main", "")
	if err != nil {
		t.Fatalf("ListDir 返回错误: %v", err)
	}
	if len(dirs) != 2 {
		t.Fatalf("期望 2 个子目录，实际 %d: %v", len(dirs), dirs)
	}
	if dirs[0] != "backend" || dirs[1] != "frontend" {
		t.Errorf("子目录不符合预期: %v", dirs)
	}
}
```

- [ ] **Step 7: 运行测试确认全部通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/cmd/ -run TestGiteaRepoFileChecker -v && go test ./internal/test/ -v -count=1`
Expected: ALL PASS

- [ ] **Step 8: 提交**

```bash
git add internal/test/service.go internal/test/service_test.go internal/cmd/adapter.go internal/cmd/adapter_test.go
git commit -m "feat(m4.2.1): RepoFileChecker 接口新增 ListDir 方法

适配器通过 Gitea Contents API 列目录实现，仅返回 type=dir 的子目录名。
service_test.go mockFileChecker 同步添加桩实现。"
```

---

### Task 3: ScanRepoModules 函数（TDD）

**Files:**
- Create: `internal/test/scan.go`
- Create: `internal/test/scan_test.go`

- [ ] **Step 1: 写失败测试 — 根目录单框架（仅 pom.xml）**

在 `internal/test/scan_test.go`：

```go
package test

import (
	"context"
	"fmt"
	"testing"
)

type mockScanChecker struct {
	files map[string]bool
	dirs  map[string][]string
	errs  map[string]error
}

func (m *mockScanChecker) HasFile(_ context.Context, _, _, _, module, relPath string) (bool, error) {
	key := module + "/" + relPath
	if module == "" {
		key = relPath
	}
	if e, ok := m.errs[key]; ok {
		return false, e
	}
	return m.files[key], nil
}

func (m *mockScanChecker) ListDir(_ context.Context, _, _, _, dir string) ([]string, error) {
	if e, ok := m.errs["listdir:"+dir]; ok {
		return nil, e
	}
	return m.dirs[dir], nil
}

func TestScanRepoModules_RootSingleFrameworkJava(t *testing.T) {
	checker := &mockScanChecker{
		files: map[string]bool{"pom.xml": true},
	}
	modules, err := ScanRepoModules(context.Background(), checker, "owner", "repo", "main")
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	if len(modules) != 1 {
		t.Fatalf("期望 1 个模块，实际 %d", len(modules))
	}
	if modules[0].Path != "" || modules[0].Framework != FrameworkJUnit5 {
		t.Errorf("模块不符合预期: %+v", modules[0])
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/test/ -run TestScanRepoModules_RootSingleFrameworkJava -v`
Expected: FAIL — `ScanRepoModules` 未定义

- [ ] **Step 3: 实现 scan.go**

创建 `internal/test/scan.go`：

```go
package test

import (
	"context"
	"log/slog"
)

// DiscoveredModule 扫描发现的可测试模块。
type DiscoveredModule struct {
	Path      string    // 子目录相对路径（如 "backend"、"frontend"），根级为 ""
	Framework Framework // 检测到的框架
}

const maxScanDirs = 30

// ScanRepoModules 扫描仓库根和 depth=1 子目录，发现可测试模块。
func ScanRepoModules(ctx context.Context, checker RepoFileChecker,
	owner, repo, ref string) ([]DiscoveredModule, error) {

	rootHasPom, errPom := checker.HasFile(ctx, owner, repo, ref, "", "pom.xml")
	if errPom != nil {
		return nil, errPom
	}
	rootHasPkg, errPkg := checker.HasFile(ctx, owner, repo, ref, "", "package.json")
	if errPkg != nil {
		return nil, errPkg
	}

	if rootHasPom || rootHasPkg {
		var result []DiscoveredModule
		if rootHasPom {
			result = append(result, DiscoveredModule{Path: "", Framework: FrameworkJUnit5})
		}
		if rootHasPkg {
			result = append(result, DiscoveredModule{Path: "", Framework: FrameworkVitest})
		}
		return result, nil
	}

	subdirs, err := checker.ListDir(ctx, owner, repo, ref, "")
	if err != nil {
		return nil, err
	}

	if len(subdirs) > maxScanDirs {
		slog.Warn("ScanRepoModules: 子目录数量超过上限，截断处理",
			"total", len(subdirs), "max", maxScanDirs, "owner", owner, "repo", repo)
		subdirs = subdirs[:maxScanDirs]
	}

	var result []DiscoveredModule
	for _, dir := range subdirs {
		hasPom, err := checker.HasFile(ctx, owner, repo, ref, dir, "pom.xml")
		if err != nil {
			slog.Warn("ScanRepoModules: 检查子目录框架标记文件失败，跳过",
				"dir", dir, "file", "pom.xml", "error", err)
			continue
		}
		hasPkg, err := checker.HasFile(ctx, owner, repo, ref, dir, "package.json")
		if err != nil {
			slog.Warn("ScanRepoModules: 检查子目录框架标记文件失败，跳过",
				"dir", dir, "file", "package.json", "error", err)
			continue
		}
		if hasPom {
			result = append(result, DiscoveredModule{Path: dir, Framework: FrameworkJUnit5})
		}
		if hasPkg {
			result = append(result, DiscoveredModule{Path: dir, Framework: FrameworkVitest})
		}
	}

	if len(result) == 0 {
		return nil, ErrNoFrameworkDetected
	}
	return result, nil
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/test/ -run TestScanRepoModules_RootSingleFrameworkJava -v`
Expected: PASS

- [ ] **Step 5: 写全场景测试**

在 `internal/test/scan_test.go` 补充：

```go
func TestScanRepoModules_RootSingleFrameworkVue(t *testing.T) {
	checker := &mockScanChecker{
		files: map[string]bool{"package.json": true},
	}
	modules, err := ScanRepoModules(context.Background(), checker, "o", "r", "main")
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	if len(modules) != 1 || modules[0].Framework != FrameworkVitest {
		t.Errorf("期望单条 vitest，实际: %+v", modules)
	}
}

func TestScanRepoModules_RootDualFramework(t *testing.T) {
	checker := &mockScanChecker{
		files: map[string]bool{"pom.xml": true, "package.json": true},
	}
	modules, err := ScanRepoModules(context.Background(), checker, "o", "r", "main")
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	if len(modules) != 2 {
		t.Fatalf("期望 2 个模块，实际 %d", len(modules))
	}
	if modules[0].Framework != FrameworkJUnit5 || modules[1].Framework != FrameworkVitest {
		t.Errorf("框架不符合预期: %+v", modules)
	}
}

func TestScanRepoModules_Depth1MultiModule(t *testing.T) {
	checker := &mockScanChecker{
		files: map[string]bool{
			"backend/pom.xml":       true,
			"frontend/package.json": true,
		},
		dirs: map[string][]string{"": {"backend", "frontend", "docs"}},
	}
	modules, err := ScanRepoModules(context.Background(), checker, "o", "r", "main")
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	if len(modules) != 2 {
		t.Fatalf("期望 2 个模块，实际 %d", len(modules))
	}
	if modules[0].Path != "backend" || modules[0].Framework != FrameworkJUnit5 {
		t.Errorf("第一个模块不符合预期: %+v", modules[0])
	}
	if modules[1].Path != "frontend" || modules[1].Framework != FrameworkVitest {
		t.Errorf("第二个模块不符合预期: %+v", modules[1])
	}
}

func TestScanRepoModules_NoFrameworkDetected(t *testing.T) {
	checker := &mockScanChecker{
		dirs: map[string][]string{"": {"docs", "scripts"}},
	}
	_, err := ScanRepoModules(context.Background(), checker, "o", "r", "main")
	if err != ErrNoFrameworkDetected {
		t.Errorf("期望 ErrNoFrameworkDetected，实际: %v", err)
	}
}

func TestScanRepoModules_ListDirError(t *testing.T) {
	checker := &mockScanChecker{
		errs: map[string]error{"listdir:": fmt.Errorf("network error")},
	}
	_, err := ScanRepoModules(context.Background(), checker, "o", "r", "main")
	if err == nil {
		t.Fatal("期望返回错误")
	}
}

func TestScanRepoModules_SubdirHasFileError_SkipAndContinue(t *testing.T) {
	checker := &mockScanChecker{
		files: map[string]bool{"good/pom.xml": true},
		dirs:  map[string][]string{"": {"bad", "good"}},
		errs:  map[string]error{"bad/pom.xml": fmt.Errorf("timeout")},
	}
	modules, err := ScanRepoModules(context.Background(), checker, "o", "r", "main")
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	if len(modules) != 1 || modules[0].Path != "good" {
		t.Errorf("期望跳过 bad 保留 good，实际: %+v", modules)
	}
}

func TestScanRepoModules_ExceedMaxDirs_Truncate(t *testing.T) {
	dirs := make([]string, 40)
	files := map[string]bool{}
	for i := range dirs {
		dirs[i] = fmt.Sprintf("mod%d", i)
		files[fmt.Sprintf("mod%d/pom.xml", i)] = true
	}
	checker := &mockScanChecker{files: files, dirs: map[string][]string{"": dirs}}
	modules, err := ScanRepoModules(context.Background(), checker, "o", "r", "main")
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	if len(modules) != maxScanDirs {
		t.Errorf("期望截断到 %d，实际 %d", maxScanDirs, len(modules))
	}
}

func TestScanRepoModules_SubdirDualFramework(t *testing.T) {
	checker := &mockScanChecker{
		files: map[string]bool{
			"mono/pom.xml":      true,
			"mono/package.json": true,
		},
		dirs: map[string][]string{"": {"mono"}},
	}
	modules, err := ScanRepoModules(context.Background(), checker, "o", "r", "main")
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	if len(modules) != 2 {
		t.Fatalf("期望 2 个模块（同目录双框架），实际 %d", len(modules))
	}
}
```

- [ ] **Step 6: 运行全部 scan 测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/test/ -run TestScanRepoModules -v -count=1`
Expected: ALL PASS

- [ ] **Step 7: 提交**

```bash
git add internal/test/scan.go internal/test/scan_test.go
git commit -m "feat(m4.2.1): 新增 ScanRepoModules 多模块扫描函数

支持根目录检测、depth=1 子目录扫描、同目录双框架拆分、
子目录数量上限截断（maxScanDirs=30）、单目录 HasFile 失败跳过。"
```

---

### Task 4: BuildAutoTestBranchName 增加 variadic framework 参数（TDD）

**Files:**
- Modify: `internal/test/prompt.go:483-487`
- Modify: `internal/test/prompt_test.go`

- [ ] **Step 1: 写失败测试**

在 `internal/test/prompt_test.go` 新增：

```go
func TestBuildAutoTestBranchName_WithFramework(t *testing.T) {
	tests := []struct {
		name      string
		module    string
		framework string
		want      string
	}{
		{"单框架 module=backend 不传 framework", "backend", "", "auto-test/backend"},
		{"单框架 module=空 不传 framework", "", "", "auto-test/all"},
		{"双框架 module=空 framework=junit5", "", "junit5", "auto-test/all-junit5"},
		{"双框架 module=空 framework=vitest", "", "vitest", "auto-test/all-vitest"},
		{"双框架 module=mono framework=junit5", "mono", "junit5", "auto-test/mono-junit5"},
		{"双框架 module=mono framework=vitest", "mono", "vitest", "auto-test/mono-vitest"},
		{"扫描拆出 module=backend 单框架", "backend", "", "auto-test/backend"},
		{"扫描拆出 module=frontend 单框架", "frontend", "", "auto-test/frontend"},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			var got string
			if tt.framework != "" {
				got = BuildAutoTestBranchName(tt.module, tt.framework)
			} else {
				got = BuildAutoTestBranchName(tt.module)
			}
			if got != tt.want {
				t.Errorf("BuildAutoTestBranchName(%q, %q) = %q, want %q",
					tt.module, tt.framework, got, tt.want)
			}
		})
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/test/ -run TestBuildAutoTestBranchName_WithFramework -v`
Expected: FAIL — 签名不匹配

- [ ] **Step 3: 修改 BuildAutoTestBranchName 签名**

在 `internal/test/prompt.go` 修改：

```go
func BuildAutoTestBranchName(module string, framework ...string) string {
	key := ModuleKey(module)
	if len(framework) > 0 && framework[0] != "" {
		key = key + "-" + framework[0]
	}
	return "auto-test/" + key
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/test/ -run TestBuildAutoTestBranchName -v`
Expected: ALL PASS（新 + 旧测试均通过，variadic 向后兼容）

- [ ] **Step 5: 确认全包编译通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./...`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add internal/test/prompt.go internal/test/prompt_test.go
git commit -m "feat(m4.2.1): BuildAutoTestBranchName 新增 variadic framework 参数

多框架拆分场景追加 framework 后缀（如 auto-test/all-junit5），
单框架场景不传 framework 行为不变（向后兼容）。"
```

---

### Task 5: PromptContext.Framework 字段 + writeHeader / Execute 适配

**Files:**
- Modify: `internal/test/prompt.go:363-382` — `PromptContext` 新增 `Framework`
- Modify: `internal/test/prompt.go:450-462` — `writeHeader` 传入 framework
- Modify: `internal/test/service.go` — Execute 构造 `PromptContext` 时填充 `Framework`

- [ ] **Step 1: PromptContext 新增 Framework 字段**

在 `PromptContext` struct 的 `MaxRetryRounds` 字段之前新增：

```go
	// Framework 当前任务的 resolved 框架标识（如 "junit5"、"vitest"）。
	// 多框架拆分场景由入队层显式设置 payload.Framework，resolveFramework 直接返回；
	// 单框架场景由 resolveFramework 检测得出。
	// 用于 writeHeader 中 BuildAutoTestBranchName 的 framework 参数。
	Framework string
```

- [ ] **Step 2: writeHeader 传入 framework**

修改 `writeHeader` 中分支名行：

```go
b.WriteString(fmt.Sprintf("工作分支：%s\n", sanitize(BuildAutoTestBranchName(ctx.Module, ctx.Framework), 120)))
```

- [ ] **Step 3: Execute 构造 PromptContext 时填充 Framework**

在 `internal/test/service.go` 的 `Execute` 方法中，找到构造 `PromptContext` 的代码块。此时 `resolveFramework` 已返回 `framework` 变量（类型 `Framework`）。使用 **resolved 后的 framework**（非原始 `payload.Framework`）：

```go
Framework: string(framework),
```

**关键点**：使用 `string(framework)` 而非 `payload.Framework`。原因：
- 多框架拆分场景：`payload.Framework` 已被入队层显式设置，`resolveFramework` 走"显式配置优先"分支直接返回，两者一致。
- 单框架检测场景：`payload.Framework` 为空，但 `framework` 由 `resolveFramework` 检测得出（如 `FrameworkJUnit5`）。使用 resolved 值确保 prompt 中分支名正确。

- [ ] **Step 4: 编译 + 测试确认无回归**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./... && go test ./internal/test/ -v -count=1`
Expected: ALL PASS

- [ ] **Step 5: 提交**

```bash
git add internal/test/prompt.go internal/test/service.go
git commit -m "feat(m4.2.1): PromptContext 新增 Framework 字段

writeHeader 传入 resolved framework 生成正确分支名，
Execute 从 resolveFramework 结果填充。"
```

---

### Task 6: moduleKeyForContainerWithFramework + 一致性测试

**Files:**
- Modify: `internal/worker/container.go:120-138`
- Modify: `internal/worker/container.go:104-114` — `buildContainerEnv` MODULE_SANITIZED
- Modify: `internal/worker/container_test.go`

- [ ] **Step 1: 写失败测试 — moduleKeyForContainerWithFramework**

在 `internal/worker/container_test.go` 新增：

```go
func TestModuleKeyForContainerWithFramework(t *testing.T) {
	tests := []struct {
		module    string
		framework string
		want      string
	}{
		{"backend", "", "backend"},
		{"", "", "all"},
		{"", "junit5", "all-junit5"},
		{"", "vitest", "all-vitest"},
		{"mono", "junit5", "mono-junit5"},
		{"mono", "vitest", "mono-vitest"},
		{"svc/api", "", "svc-api"},
		{"svc/api", "junit5", "svc-api-junit5"},
	}
	for _, tt := range tests {
		t.Run(tt.module+"_"+tt.framework, func(t *testing.T) {
			got := moduleKeyForContainerWithFramework(tt.module, tt.framework)
			if got != tt.want {
				t.Errorf("got %q, want %q", got, tt.want)
			}
		})
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/worker/ -run TestModuleKeyForContainerWithFramework -v`
Expected: FAIL

- [ ] **Step 3: 实现函数**

在 `internal/worker/container.go` 中 `moduleKeyForContainer` 之后新增：

```go
func moduleKeyForContainerWithFramework(module, framework string) string {
	key := moduleKeyForContainer(module)
	if framework != "" {
		key = key + "-" + framework
	}
	return key
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/worker/ -run TestModuleKeyForContainerWithFramework -v`
Expected: PASS

- [ ] **Step 5: 写一致性对照测试**

在 `internal/worker/container_test.go` 新增（测试文件可 import internal/test）：

```go
import testgen "otws19.zicp.vip/kelin/dtworkflow/internal/test"

func TestModuleKeyConsistencyWithTestPackage(t *testing.T) {
	cases := []struct{ module, framework string }{
		{"", ""}, {"", "junit5"}, {"", "vitest"},
		{"backend", ""}, {"backend", "junit5"},
		{"frontend", "vitest"}, {"mono", "junit5"}, {"mono", "vitest"},
		{"svc/api", ""}, {"svc/api", "junit5"},
		{"a..b", ""}, {"--special--", "vitest"}, {"   ", ""},
	}
	for _, tt := range cases {
		t.Run(tt.module+"_"+tt.framework, func(t *testing.T) {
			workerKey := moduleKeyForContainerWithFramework(tt.module, tt.framework)
			var testBranch string
			if tt.framework != "" {
				testBranch = testgen.BuildAutoTestBranchName(tt.module, tt.framework)
			} else {
				testBranch = testgen.BuildAutoTestBranchName(tt.module)
			}
			testKey := strings.TrimPrefix(testBranch, "auto-test/")
			if workerKey != testKey {
				t.Errorf("一致性失败: worker=%q, test=%q", workerKey, testKey)
			}
		})
	}
}
```

- [ ] **Step 6: 修改 buildContainerEnv MODULE_SANITIZED 注入**

在 `buildContainerEnv` 的 gen_tests 分支中：

```go
// 旧：
env = append(env, fmt.Sprintf("MODULE_SANITIZED=%s", moduleKeyForContainer(payload.Module)))
// 新：
env = append(env, fmt.Sprintf("MODULE_SANITIZED=%s", moduleKeyForContainerWithFramework(payload.Module, payload.Framework)))
```

- [ ] **Step 7: 运行 worker 包全部测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/worker/ -v -count=1`
Expected: ALL PASS

- [ ] **Step 8: 提交**

```bash
git add internal/worker/container.go internal/worker/container_test.go
git commit -m "feat(m4.2.1): worker 新增 moduleKeyForContainerWithFramework

buildContainerEnv MODULE_SANITIZED 注入适配 framework 后缀，
新增 worker ↔ test 一致性对照测试保障双实现同步。"
```

---

### Task 7: 提取 enqueueSingleGenTests + EnqueueHandler 新增依赖

**Files:**
- Modify: `internal/queue/enqueue.go`

- [ ] **Step 1: 定义 EnqueuedTask 结构体和新依赖**

在 `internal/queue/enqueue.go` 新增：

```go
// EnqueuedTask 多模块入队结果条目。
type EnqueuedTask struct {
	TaskID    string
	Module    string
	Framework string
}

// genTestsPRClient 全量清理所需的 PR 操作窄接口。
// 注意：与 test.PRClient 同名但属不同包，不会冲突。
type genTestsPRClient interface {
	ListRepoPullRequests(ctx context.Context, owner, repo string,
		opts gitea.ListPullRequestsOptions) ([]*gitea.PullRequest, *gitea.Response, error)
	ClosePullRequest(ctx context.Context, owner, repo string, index int64) error
}
```

在 `EnqueueHandler` struct 新增字段：

```go
prClient      genTestsPRClient     // M4.2.1: cleanupAllAutoTestBranches
moduleScanner test.RepoFileChecker // M4.2.1: ScanRepoModules
```

新增 option 函数：

```go
func WithPRClient(c genTestsPRClient) EnqueueOption {
	return func(h *EnqueueHandler) { h.prClient = c }
}

func WithModuleScanner(c test.RepoFileChecker) EnqueueOption {
	return func(h *EnqueueHandler) { h.moduleScanner = c }
}
```

- [ ] **Step 2: 提取 enqueueSingleGenTests 函数**

从 `EnqueueManualGenTests` 提取单任务入队逻辑，返回 `(EnqueuedTask, error)`：

```go
func (h *EnqueueHandler) enqueueSingleGenTests(ctx context.Context, payload model.TaskPayload, triggeredBy string) (EnqueuedTask, error) {
	payload.TaskType = model.TaskTypeGenTests
	payload.DeliveryID = generateManualDeliveryID()

	activeTasks := h.listActiveGenTestsTasksByBranchKey(ctx, payload.RepoFullName, payload.Module, payload.Framework)

	if len(activeTasks) > 0 && h.branchCleaner != nil {
		branchName := test.BuildAutoTestBranchName(payload.Module, payload.Framework)
		if err := h.branchCleaner.CleanupAutoTestBranch(ctx, payload.RepoOwner, payload.RepoName, branchName); err != nil {
			h.logger.WarnContext(ctx, "清理旧 auto-test 分支失败，继续入队新任务",
				"repo", payload.RepoFullName, "module", payload.Module,
				"branch", branchName, "error", err)
		}
	}

	h.cancelTasks(ctx, activeTasks)

	record := &model.TaskRecord{
		TaskType:     model.TaskTypeGenTests,
		Priority:     model.PriorityNormal,
		RepoFullName: payload.RepoFullName,
		DeliveryID:   payload.DeliveryID,
		TriggeredBy:  triggeredBy,
	}

	if err := h.enqueueTask(ctx, payload, record); err != nil {
		return EnqueuedTask{}, err
	}

	h.logger.InfoContext(ctx, "gen_tests 任务已入队",
		"task_id", record.ID, "repo", payload.RepoFullName,
		"module", payload.Module, "framework", payload.Framework,
		"triggered_by", triggeredBy)
	return EnqueuedTask{TaskID: record.ID, Module: payload.Module, Framework: payload.Framework}, nil
}
```

- [ ] **Step 3: 修改 listActiveGenTestsTasksByBranchKey 增加 framework 参数**

**关键修复**：Cancel-and-Replace 必须考虑 framework 维度，否则同 module 不同 framework 的任务会互相取消。

```go
// listActiveGenTestsTasksByBranchKey 查找会落到同一 stable branch 的活跃 gen_tests 任务。
// framework 非空时 target key 为 ModuleKey(module) + "-" + framework，
// 确保同 module 不同 framework 的任务不互相取消。
func (h *EnqueueHandler) listActiveGenTestsTasksByBranchKey(ctx context.Context,
	repoFullName, module, framework string) []*model.TaskRecord {

	targetKey := test.ModuleKey(module)
	if framework != "" {
		targetKey = targetKey + "-" + framework
	}

	modules, err := h.store.ListActiveGenTestsModules(ctx, repoFullName)
	if err != nil {
		h.logger.WarnContext(ctx, "查询活跃 gen_tests module 列表失败，回退到原始 module 精确匹配",
			"repo", repoFullName, "module", module, "module_key", targetKey, "error", err)
		return h.listActiveGenTestsTasks(ctx, repoFullName, module)
	}

	seenModule := make(map[string]struct{}, len(modules))
	seenTask := make(map[string]struct{})
	var merged []*model.TaskRecord
	for _, candidate := range modules {
		candidateKey := test.ModuleKey(candidate)
		// 当 framework 为空时（旧行为），按 ModuleKey 匹配
		// 当 framework 非空时，需要精确匹配到 key+framework
		// 但 store 中 module 字段不含 framework，所以 framework 非空时
		// 只匹配 ModuleKey(candidate) == ModuleKey(module) 的任务，
		// 再通过任务的 payload.Framework 做二次过滤
		if candidateKey != test.ModuleKey(module) {
			continue
		}
		if _, ok := seenModule[candidate]; ok {
			continue
		}
		seenModule[candidate] = struct{}{}

		tasks, tErr := h.store.FindActiveGenTestsTasks(ctx, repoFullName, candidate)
		if tErr != nil {
			h.logger.WarnContext(ctx, "按 stable branch key 回查活跃 gen_tests 任务失败，跳过该 module",
				"repo", repoFullName, "candidate_module", candidate, "error", tErr)
			continue
		}
		for _, task := range tasks {
			if task == nil {
				continue
			}
			if _, ok := seenTask[task.ID]; ok {
				continue
			}
			// framework 非空时，二次过滤：只匹配相同 framework 的任务
			if framework != "" && task.Payload.Framework != framework {
				continue
			}
			seenTask[task.ID] = struct{}{}
			merged = append(merged, task)
		}
	}
	return merged
}
```

- [ ] **Step 4: 重写 EnqueueManualGenTests 使用 enqueueSingleGenTests**

暂时保持 `(string, error)` 返回值（下一个 Task 改为 `[]EnqueuedTask`），先让现有调用点编译通过：

```go
func (h *EnqueueHandler) EnqueueManualGenTests(ctx context.Context, payload model.TaskPayload, triggeredBy string) (string, error) {
	if payload.RepoFullName == "" || payload.CloneURL == "" {
		return "", fmt.Errorf("payload 数据不完整: RepoFullName 或 CloneURL 为空")
	}
	result, err := h.enqueueSingleGenTests(ctx, payload, triggeredBy)
	if err != nil {
		return "", err
	}
	return result.TaskID, nil
}
```

- [ ] **Step 5: 编译 + 测试确认无回归**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./... && go test ./internal/queue/ -v -count=1`
Expected: ALL PASS

- [ ] **Step 6: 提交**

```bash
git add internal/queue/enqueue.go
git commit -m "refactor(m4.2.1): 提取 enqueueSingleGenTests + framework 感知 Cancel-and-Replace

新增 EnqueuedTask / genTestsPRClient / WithPRClient / WithModuleScanner。
listActiveGenTestsTasksByBranchKey 增加 framework 参数，
同 module 不同 framework 的任务不互相取消。"
```

---

### Task 8: cleanupAllAutoTestBranches + shouldSkipAutoTestReview 适配

**Files:**
- Modify: `internal/queue/enqueue.go` — 新增 `cleanupAllAutoTestBranches`；修改 `shouldSkipAutoTestReview`
- Modify: `internal/queue/enqueue_test.go` — 两者的单测

- [ ] **Step 1: 实现 cleanupAllAutoTestBranches**

```go
func (h *EnqueueHandler) cleanupAllAutoTestBranches(ctx context.Context, owner, repo, repoFullName string) {
	// 步骤 1：取消所有活跃 gen_tests 任务
	modules, err := h.store.ListActiveGenTestsModules(ctx, repoFullName)
	if err != nil {
		h.logger.WarnContext(ctx, "cleanupAll: 查询活跃 gen_tests module 列表失败",
			"repo", repoFullName, "error", err)
	} else {
		for _, mod := range modules {
			tasks, tErr := h.store.FindActiveGenTestsTasks(ctx, repoFullName, mod)
			if tErr != nil {
				h.logger.WarnContext(ctx, "cleanupAll: 查询活跃 gen_tests 任务失败",
					"repo", repoFullName, "module", mod, "error", tErr)
				continue
			}
			for _, task := range tasks {
				h.logger.InfoContext(ctx, "cleanupAll: 即将取消任务",
					"task_id", task.ID, "module", mod)
			}
			h.cancelTasks(ctx, tasks)
		}
	}

	// 步骤 2：关闭所有 auto-test/* PR + 删除远程分支
	if h.prClient == nil {
		h.logger.WarnContext(ctx, "cleanupAll: prClient 未注入，跳过 PR 关闭和分支删除",
			"repo", repoFullName)
		return
	}

	prs, _, prErr := h.prClient.ListRepoPullRequests(ctx, owner, repo, gitea.ListPullRequestsOptions{
		ListOptions: gitea.ListOptions{Page: 1, PageSize: 50},
		State:       "open",
	})
	if prErr != nil {
		h.logger.WarnContext(ctx, "cleanupAll: 列出 open PR 失败",
			"repo", repoFullName, "error", prErr)
		return
	}

	for _, pr := range prs {
		if pr.Head == nil || !strings.HasPrefix(pr.Head.Ref, autoTestBranchPrefix) {
			continue
		}
		h.logger.InfoContext(ctx, "cleanupAll: 即将关闭 PR 并删除分支",
			"repo", repoFullName, "pr", pr.Index, "branch", pr.Head.Ref)

		if closeErr := h.prClient.ClosePullRequest(ctx, owner, repo, pr.Index); closeErr != nil {
			h.logger.WarnContext(ctx, "cleanupAll: 关闭 PR 失败",
				"repo", repoFullName, "pr", pr.Index, "error", closeErr)
		}
		if h.branchCleaner != nil {
			_ = h.branchCleaner.CleanupAutoTestBranch(ctx, owner, repo, pr.Head.Ref)
		}
	}
}
```

- [ ] **Step 2: 修改 shouldSkipAutoTestReview 适配 framework 后缀分支**

**关键修复**：`auto-test/all-junit5` 分支的 PR 也应被拦截。当前逻辑 `moduleKey = strings.TrimPrefix(head, autoTestBranchPrefix)` 得到 `"all-junit5"`，但 `test.ModuleKey("")` 返回 `"all"`，比较不等。

修改匹配逻辑为前缀匹配：

```go
func (h *EnqueueHandler) shouldSkipAutoTestReview(ctx context.Context, event webhook.PullRequestEvent) bool {
	head := event.PullRequest.HeadRef
	if !strings.HasPrefix(head, autoTestBranchPrefix) {
		return false
	}
	branchKey := strings.TrimPrefix(head, autoTestBranchPrefix)
	if branchKey == "" {
		return false
	}

	candidates, err := h.store.ListActiveGenTestsModules(ctx, event.Repository.FullName)
	if err != nil {
		h.logger.WarnContext(ctx, "查询活跃 gen_tests module 失败，auto-test PR 仍按评审流程放行",
			"repo", event.Repository.FullName, "module_key", branchKey,
			"pr", event.PullRequest.Number, "head", head,
			"delivery_id", event.DeliveryID, "error", err)
		return false
	}
	for _, candidate := range candidates {
		candidateKey := test.ModuleKey(candidate)
		// 精确匹配（无 framework 后缀）或前缀匹配（有 framework 后缀）
		// 例如 branchKey="all-junit5" 匹配 candidateKey="all"（因为 "all-junit5" 以 "all-" 开头）
		// 而 branchKey="all" 精确匹配 candidateKey="all"
		if candidateKey == branchKey || strings.HasPrefix(branchKey, candidateKey+"-") {
			h.logger.InfoContext(ctx, "拦截 auto-test PR 评审入队：存在活跃 gen_tests 任务",
				"repo", event.Repository.FullName, "module_key", branchKey,
				"pr", event.PullRequest.Number, "head", head,
				"delivery_id", event.DeliveryID)
			return true
		}
	}
	return false
}
```

- [ ] **Step 3: 写 shouldSkipAutoTestReview framework 场景测试**

在 `internal/queue/enqueue_test.go` 新增：

```go
func TestShouldSkipAutoTestReview_FrameworkSuffixedBranch(t *testing.T) {
	// mock store: ListActiveGenTestsModules 返回 [""]（空 module = 整仓）
	// event: HeadRef = "auto-test/all-junit5"
	// 期望: shouldSkipAutoTestReview 返回 true
	// （"all-junit5" 以 "all-" 为前缀，匹配活跃 module ""→ModuleKey="all"）
}

func TestShouldSkipAutoTestReview_FrameworkSuffixedBranch_NoMatch(t *testing.T) {
	// mock store: ListActiveGenTestsModules 返回 ["backend"]
	// event: HeadRef = "auto-test/all-junit5"
	// 期望: shouldSkipAutoTestReview 返回 false
	// （"all-junit5" 不以 "backend-" 为前缀）
}
```

- [ ] **Step 4: 写 cleanupAllAutoTestBranches 单测**

验证：取消活跃任务、关闭 auto-test/* PR、store 失败不 panic、prClient 为 nil 不 panic。

- [ ] **Step 5: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/queue/ -v -count=1`
Expected: ALL PASS

- [ ] **Step 6: 提交**

```bash
git add internal/queue/enqueue.go internal/queue/enqueue_test.go
git commit -m "feat(m4.2.1): cleanupAllAutoTestBranches + shouldSkipAutoTestReview 适配 framework 后缀

全量清理方法用于整仓拆分前清理旧 auto-test/* 资源。
shouldSkipAutoTestReview 改用前缀匹配，正确拦截 auto-test/all-junit5 等
framework 后缀分支的 PR 评审。"
```

---

### Task 9: EnqueueManualGenTests 返回 []EnqueuedTask + 整仓拆分逻辑

**Files:**
- Modify: `internal/queue/enqueue.go` — 返回值改 `[]EnqueuedTask`；整仓拆分流程
- Modify: `internal/queue/enqueue_test.go` — 适配返回值 + 拆分路径测试
- Modify: `internal/api/test_handler.go` — 适配新返回值
- Modify: `internal/cmd/gen_tests.go` — 适配新返回值

- [ ] **Step 1: 修改 EnqueueManualGenTests 签名和实现**

```go
func (h *EnqueueHandler) EnqueueManualGenTests(ctx context.Context, payload model.TaskPayload, triggeredBy string) ([]EnqueuedTask, error) {
	if payload.RepoFullName == "" || payload.CloneURL == "" {
		return nil, fmt.Errorf("payload 数据不完整: RepoFullName 或 CloneURL 为空")
	}

	// 整仓模式：module 空 + framework 空 + moduleScanner 已注入 + baseRef 非空
	if payload.Module == "" && payload.Framework == "" && h.moduleScanner != nil && payload.BaseRef != "" {
		discovered, err := test.ScanRepoModules(ctx, h.moduleScanner,
			payload.RepoOwner, payload.RepoName, payload.BaseRef)
		if err != nil {
			h.logger.WarnContext(ctx, "ScanRepoModules 失败，回退到单任务逻辑",
				"repo", payload.RepoFullName, "error", err)
			result, singleErr := h.enqueueSingleGenTests(ctx, payload, triggeredBy)
			if singleErr != nil {
				return nil, singleErr
			}
			return []EnqueuedTask{result}, nil
		}

		if len(discovered) >= 2 {
			h.cleanupAllAutoTestBranches(ctx, payload.RepoOwner, payload.RepoName, payload.RepoFullName)
		}

		var results []EnqueuedTask
		for _, mod := range discovered {
			subPayload := payload
			subPayload.Module = mod.Path
			subPayload.Framework = string(mod.Framework)
			result, subErr := h.enqueueSingleGenTests(ctx, subPayload, triggeredBy)
			if subErr != nil {
				h.logger.WarnContext(ctx, "子任务入队失败",
					"repo", payload.RepoFullName, "module", mod.Path,
					"framework", mod.Framework, "error", subErr)
				continue
			}
			results = append(results, result)
		}
		if len(results) == 0 {
			return nil, fmt.Errorf("所有子任务入队均失败")
		}

		h.logger.InfoContext(ctx, "整仓拆分 gen_tests 入队完成",
			"repo", payload.RepoFullName, "discovered", len(discovered),
			"enqueued", len(results), "triggered_by", triggeredBy)
		return results, nil
	}

	// 非整仓模式或未注入 scanner：单任务入队
	result, err := h.enqueueSingleGenTests(ctx, payload, triggeredBy)
	if err != nil {
		return nil, err
	}
	return []EnqueuedTask{result}, nil
}
```

- [ ] **Step 2: 修复所有调用点**

**`internal/api/test_handler.go`**（约 line 126）：

```go
	results, err := h.deps.EnqueueHandler.EnqueueManualGenTests(ctx, payload, triggeredBy)
	if err != nil {
		h.deps.Logger.Error("入队 gen_tests 失败",
			"owner", owner, "repo", repo, "module", req.Module, "error", err)
		Error(c, http.StatusInternalServerError, ErrCodeInternalError, "入队 gen_tests 失败")
		return
	}

	type taskInfo struct {
		TaskID    string `json:"task_id"`
		Module    string `json:"module"`
		Framework string `json:"framework"`
	}
	tasks := make([]taskInfo, 0, len(results))
	for _, r := range results {
		tasks = append(tasks, taskInfo{TaskID: r.TaskID, Module: r.Module, Framework: r.Framework})
	}

	resp := gin.H{
		"split": len(results) > 1,
		"tasks": tasks,
	}
	if len(results) == 1 {
		resp["task_id"] = results[0].TaskID
		resp["message"] = fmt.Sprintf("gen_tests 任务已入队（module=%q, ref=%q）", req.Module, baseRef)
	} else {
		resp["message"] = fmt.Sprintf("整仓扫描拆分为 %d 个子任务", len(results))
	}
	Success(c, http.StatusAccepted, resp)
```

**`internal/cmd/gen_tests.go`**（约 line 184）：

```go
	results, err := enqueueHandler.EnqueueManualGenTests(ctx, payload, triggeredBy)
	// ... 适配 results 输出，详见 Task 10
```

（此步先让编译通过，输出格式在 Task 10 细化）

- [ ] **Step 3: 修复 enqueue_test.go 中所有断言**

搜索测试中所有 `EnqueueManualGenTests` 调用，将返回值从 `string` 改为 `[]EnqueuedTask`，断言改为 `len(results) > 0 && results[0].TaskID != ""`。

- [ ] **Step 4: 新增整仓拆分路径测试**

```go
func TestEnqueueManualGenTests_SplitMultiModule(t *testing.T) {
	// mock scanner 返回 [{Path:"backend", Framework:junit5}, {Path:"frontend", Framework:vitest}]
	// 验证：返回 2 个 EnqueuedTask，Module/Framework 正确
}

func TestEnqueueManualGenTests_ScanFail_FallbackSingle(t *testing.T) {
	// mock scanner 返回 error
	// 验证：回退到单任务入队，返回 1 个 EnqueuedTask
}

func TestEnqueueManualGenTests_ModuleNonEmpty_SkipScan(t *testing.T) {
	// payload.Module = "backend"
	// 验证：不调用 scanner，直接单任务入队
}

func TestEnqueueManualGenTests_BaseRefEmpty_SkipScan(t *testing.T) {
	// payload.BaseRef = ""
	// 验证：不调用 scanner，直接单任务入队
}
```

- [ ] **Step 5: 编译 + 测试**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./... && go test ./internal/queue/ -v -count=1`
Expected: ALL PASS

- [ ] **Step 6: 提交**

```bash
git add internal/queue/enqueue.go internal/queue/enqueue_test.go internal/api/test_handler.go internal/cmd/gen_tests.go
git commit -m "feat(m4.2.1): EnqueueManualGenTests 返回 []EnqueuedTask + 整仓拆分

整仓模式自动调用 ScanRepoModules 拆分子任务；多模块时先全量清理；
扫描失败 fail-open 回退单任务；API 响应包含 split/tasks 结构。"
```

---

### Task 10: 服务端 CLI + dtw 瘦客户端输出适配

**Files:**
- Modify: `internal/cmd/gen_tests.go` — 输出 + 装配层注入 `WithModuleScanner` / `WithPRClient`
- Modify: `internal/dtw/cmd/gen_tests.go` — 响应解析适配

- [ ] **Step 1: 修改服务端 CLI gen_tests.go 输出**

```go
	results, err := enqueueHandler.EnqueueManualGenTests(ctx, payload, triggeredBy)
	if err != nil {
		return fmt.Errorf("入队 gen_tests 任务失败: %w", err)
	}

	if jsonOutput {
		return printJSON(results)
	}

	if len(results) == 1 {
		fmt.Printf("gen_tests 任务已入队: %s\n", results[0].TaskID)
	} else {
		fmt.Printf("整仓扫描发现 %d 个可测试模块，已拆分入队：\n", len(results))
		for i, r := range results {
			fmt.Printf("  [%d] module=%-20s framework=%-8s task_id=%s\n",
				i+1, r.Module, r.Framework, r.TaskID)
		}
	}
```

- [ ] **Step 2: 服务端 CLI gen_tests.go 装配层注入 WithModuleScanner + WithPRClient**

**关键修复**：`buildGenTestsEnqueueOptions` 必须也注入 `WithModuleScanner` 和 `WithPRClient`，否则 CLI 触发路径不会启用多模块扫描。

在 `internal/cmd/gen_tests.go` 的 `buildGenTestsEnqueueOptions` 中（约 line 234-241），在现有 `WithBranchCleaner` 之后添加：

```go
func buildGenTestsEnqueueOptions(gc *gitea.Client) []queue.EnqueueOption {
	if gc == nil {
		return nil
	}
	return []queue.EnqueueOption{
		queue.WithBranchCleaner(queue.NewBranchCleaner(gc, slog.Default())),
		queue.WithModuleScanner(&giteaRepoFileChecker{client: gc}),
		queue.WithPRClient(gc),
	}
}
```

**注意**：`buildGenTestsEnqueueOptions` 在 `serve.go` 和 `gen_tests.go` 中都被调用。如果 `buildGenTestsEnqueueOptions` 定义在共享位置（如 `gen_tests.go`），只需改一处。否则需确认 `serve.go` 的调用路径也覆盖到。

先用 `grep -rn "buildGenTestsEnqueueOptions" internal/cmd/` 确认该函数的定义位置和调用点。

- [ ] **Step 3: 修改 dtw 瘦客户端 gen_tests.go**

```go
	var result struct {
		Split  bool   `json:"split"`
		TaskID string `json:"task_id"`
		Tasks  []struct {
			TaskID    string `json:"task_id"`
			Module    string `json:"module"`
			Framework string `json:"framework"`
		} `json:"tasks"`
	}

	path := fmt.Sprintf("/api/v1/repos/%s/%s/gen-tests", owner, repo)
	if err := client.Do(cmd.Context(), "POST", path, body, &result); err != nil {
		return fmt.Errorf("提交 gen_tests 失败: %w", err)
	}

	if result.Split && len(result.Tasks) > 1 {
		if genTestsNoWait {
			if flagJSON {
				return printer.PrintJSON(result)
			}
			printer.PrintHuman("整仓扫描发现 %d 个可测试模块，已拆分入队：", len(result.Tasks))
			for i, t := range result.Tasks {
				printer.PrintHuman("  [%d] module=%-20s framework=%-8s task_id=%s",
					i+1, t.Module, t.Framework, t.TaskID)
			}
			return nil
		}
		if !flagJSON {
			printer.PrintHuman("已拆分为 %d 个子任务，请使用 `dtw task list` 查看各任务进度",
				len(result.Tasks))
			for i, t := range result.Tasks {
				printer.PrintHuman("  [%d] module=%-20s framework=%-8s task_id=%s",
					i+1, t.Module, t.Framework, t.TaskID)
			}
		} else {
			return printer.PrintJSON(result)
		}
		return nil
	}

	// 单任务场景：保持现有行为
	taskID := result.TaskID
	if taskID == "" && len(result.Tasks) > 0 {
		taskID = result.Tasks[0].TaskID
	}
	// ... 后续 no-wait / wait 逻辑使用 taskID，保持原样
```

- [ ] **Step 4: 编译确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./...`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add internal/cmd/gen_tests.go internal/dtw/cmd/gen_tests.go
git commit -m "feat(m4.2.1): CLI + dtw 瘦客户端适配多任务拆分

服务端 CLI buildGenTestsEnqueueOptions 注入 WithModuleScanner + WithPRClient，
dtw 瘦客户端解析 split/tasks 响应结构。"
```

---

### Task 11: serve.go 装配层注入

**Files:**
- Modify: `internal/cmd/serve.go` — 确认 `buildGenTestsEnqueueOptions` 已覆盖 WithModuleScanner + WithPRClient

- [ ] **Step 1: 检查 serve.go 中 EnqueueHandler 构造**

如果 `serve.go` 调用的是同一个 `buildGenTestsEnqueueOptions`（Task 10 已修改），则无需额外改动。

如果 `serve.go` 有独立的 option 构造逻辑，则需在 `NewEnqueueHandler` 调用前补充：

```go
// M4.2.1: 确保 moduleScanner 和 prClient 已注入
// 如果 buildGenTestsEnqueueOptions 已统一处理，此处无需重复
```

用 `grep -rn "NewEnqueueHandler" internal/cmd/serve.go` 确认实际装配方式。

- [ ] **Step 2: 编译 + 全项目测试**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./... && go test ./... -count=1 -timeout 5m`
Expected: ALL PASS

- [ ] **Step 3: 提交（如有修改）**

```bash
git add internal/cmd/serve.go
git commit -m "feat(m4.2.1): serve.go 装配层确认 moduleScanner + prClient 注入"
```

---

### Task 12: 端到端集成验证

**Files:** 无新文件，纯验证

- [ ] **Step 1: 全项目编译**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./...`
Expected: PASS

- [ ] **Step 2: 全项目测试**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./... -count=1 -timeout 5m`
Expected: ALL PASS（或已知预存失败项不变）

- [ ] **Step 3: golangci-lint**

Run: `cd /Users/kelin/Work/DTWorkflow && golangci-lint run ./...`
Expected: 无新 warning/error

- [ ] **Step 4: 检查向后兼容（代码层面逻辑路径验证）**

1. `--module backend`（单框架）→ 不触发扫描，单任务入队
2. `--module "" --framework junit5`（显式框架）→ 不触发扫描
3. `--module ""`，根有 `pom.xml`（单框架）→ ScanRepoModules 返回单条，单任务入队
4. `--module ""`，根有 `pom.xml` + `package.json`（双框架）→ 拆分为 2 个子任务
5. `--module ""`，根无标记文件，子目录有 → depth=1 扫描 + 拆分
6. `moduleScanner` 未注入 → 走现有单任务逻辑（fail-open）
7. `ScanRepoModules` 返回 error → 回退单任务逻辑（fail-open）
8. `payload.BaseRef` 为空 → 跳过扫描，走单任务逻辑
9. `auto-test/all-junit5` 分支 PR → `shouldSkipAutoTestReview` 正确拦截
10. 同 module 不同 framework 的任务 → Cancel-and-Replace 不互相取消

- [ ] **Step 5: 提交（如有修复）**

如果验证过程发现问题并修复，创建修复提交。
