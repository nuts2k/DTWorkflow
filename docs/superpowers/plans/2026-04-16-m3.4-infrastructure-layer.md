# M3.4 基础设施层实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 Issue 自动修复（M3.5）构建基础设施——两级镜像、Pool 多镜像选择、TaskType 拆分（`analyze_issue` / `fix_issue`）、entrypoint 写模式、Webhook 双标签路由。

**Architecture:** 按层切分（非按阶段），M3.4 纯基础设施改动。新增 `TaskTypeAnalyzeIssue` 承接 M3.1-M3.3 的只读分析流程，`TaskTypeFixIssue` 语义修正为真正的修复。`PoolConfig.ImageFull` 支持按任务类型选择轻量/执行镜像。Webhook 新增 `fix-to-pr` 标签路由到 `fix_issue`，`auto-fix` 路由到 `analyze_issue`。

**Tech Stack:** Go, Docker SDK, asynq, Cobra, Viper (YAML), Bash (entrypoint)

**设计文档:** `docs/superpowers/specs/2026-04-16-m3.4-m3.5-fix-execution-design.md` 第 3 章

---

## 文件改动总览

| 操作 | 文件路径 | 职责 |
|------|---------|------|
| Modify | `internal/model/task.go:8-13,39-46` | 新增 `TaskTypeAnalyzeIssue` 常量 + `IsValid()` |
| Modify | `internal/worker/types.go:39-43,46-66,75-88,91-105` | `PoolConfig.ImageFull` + `TaskTimeoutsConfig.AnalyzeIssue` + `Lookup` + `Validate` |
| Modify | `internal/worker/pool.go:118,155-156,173-180` | `resolveImage` 方法 + `runContainer` 集成 |
| Modify | `internal/worker/container.go:68-88,118-183` | `buildContainerEnv/Cmd` 新增 `analyze_issue` case |
| Modify | `internal/config/config.go:75-86,89-93` | `WorkerConfig.ImageFull` + `TaskTimeouts.AnalyzeIssue` |
| Modify | `internal/config/validate.go:100-122` | `ImageFull` 格式校验 + `analyze_issue` 超时校验 |
| Modify | `internal/cmd/serve_deps.go:47-64` | `buildWorkerPoolConfigFromServeConfig` 传递 `ImageFull` |
| Modify | `internal/queue/client.go:18-23,148-163` | `AsynqTypeAnalyzeIssue` + `taskTypeToAsynq` |
| Modify | `internal/queue/options.go:11-15,18-27,35-47` | queue 层 `TaskTimeoutsConfig.AnalyzeIssue` + `TaskTimeout` + `defaultTaskTimeout` |
| Modify | `internal/cmd/worker_queue_config.go:9-18` | `buildQueueTimeoutConfigFromAppConfig` 传递 `AnalyzeIssue` |
| Modify | `internal/queue/enqueue.go:127-192,394-410` | `HandleIssueLabel` 双标签分流 + `EnqueueManualFix` 支持 TaskType 参数 |
| Modify | `internal/webhook/parser.go:139-161` | `isFixToPRLabel` + `extractLabel` 扩展 |
| Modify | `internal/webhook/event.go:60-71` | `IssueLabelEvent` 新增 `FixToPR*` 字段 |
| Modify | `internal/notify/notifier.go:8-19` | 新增 `EventIssueAnalyzeStarted` / `EventIssueAnalyzeDone` |
| Modify | `internal/queue/processor.go:219-240,255-264,431-482,502-639,718-739` | 路由 + 开始/完成通知分流 + handleSkipRetryFailure 签名 |
| Modify | `internal/api/fix_handler.go:13-16,77-87` | `fixRequest` 新增 `TaskType` + `triggerFix` 传递 |
| Modify | `internal/cmd/serve.go:236-239` | Mux 注册 `AsynqTypeAnalyzeIssue` |
| Modify | `build/docker/entrypoint.sh:81-94` | `analyze_issue` 只读 + `fix_issue` 写模式 |
| Create | `build/docker/Dockerfile.worker-full` | 执行镜像（JDK + Maven） |
| Modify | `Makefile:15,64-68` | `build-worker-full` 构建目标 |
| Modify | `internal/dtw/cmd/fix_issue.go` | `--analyze` / `--fix` flag |
| Modify | `configs/dtworkflow.example.yaml:49-72` | `image_full` + `analyze_issue` 超时 |

---

## Task 1: TaskType 拆分 — model 层

**Files:**
- Modify: `internal/model/task.go:8-13` (常量区) + `:39-46` (IsValid)
- Test: `internal/model/task.go` (已有 IsValid 内联测试逻辑)

- [ ] **Step 1: 写失败测试**

在 `internal/model/task_test.go`（如不存在则创建）中添加：

```go
package model

import "testing"

func TestTaskTypeAnalyzeIssue_IsValid(t *testing.T) {
	if !TaskTypeAnalyzeIssue.IsValid() {
		t.Error("TaskTypeAnalyzeIssue 应为合法 TaskType")
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/model/ -run TestTaskTypeAnalyzeIssue -v`
Expected: 编译错误 `undefined: TaskTypeAnalyzeIssue`

- [ ] **Step 3: 实现 TaskTypeAnalyzeIssue**

`internal/model/task.go` 常量区（第 8-13 行）修改为：

```go
const (
	TaskTypeReviewPR       TaskType = "review_pr"
	TaskTypeAnalyzeIssue   TaskType = "analyze_issue"   // M3.4: 只读分析（从 fix_issue 拆分）
	TaskTypeFixIssue       TaskType = "fix_issue"        // M3.4: 语义修正为真正的修复
	TaskTypeGenTests       TaskType = "gen_tests"
	TaskTypeGenDailyReport TaskType = "gen_daily_report"
)
```

`IsValid()` 方法（第 39-46 行区域）添加 case：

```go
func (t TaskType) IsValid() bool {
	switch t {
	case TaskTypeReviewPR, TaskTypeAnalyzeIssue, TaskTypeFixIssue, TaskTypeGenTests, TaskTypeGenDailyReport:
		return true
	}
	return false
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/model/ -run TestTaskTypeAnalyzeIssue -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/model/task.go internal/model/task_test.go
git commit -m "feat(model): 新增 TaskTypeAnalyzeIssue 常量

M3.4 TaskType 拆分：analyze_issue 承接只读分析，fix_issue 语义修正为修复。"
```

---

## Task 2: Pool 多镜像 — PoolConfig.ImageFull + TaskTimeoutsConfig.AnalyzeIssue

**Files:**
- Modify: `internal/worker/types.go:39-43,75-88,91-105`
- Test: `internal/worker/types_test.go`

- [ ] **Step 1: 写失败测试 — AnalyzeIssue 超时**

`internal/worker/types_test.go` 追加：

```go
func TestTaskTimeoutsConfig_Lookup_AnalyzeIssue(t *testing.T) {
	// 零值回退默认 15 分钟
	cfg := TaskTimeoutsConfig{}
	got := cfg.Lookup(model.TaskTypeAnalyzeIssue)
	if got != 15*time.Minute {
		t.Errorf("AnalyzeIssue 默认超时 = %v, 期望 15m", got)
	}

	// 显式配置
	cfg.AnalyzeIssue = 20 * time.Minute
	got = cfg.Lookup(model.TaskTypeAnalyzeIssue)
	if got != 20*time.Minute {
		t.Errorf("AnalyzeIssue 配置超时 = %v, 期望 20m", got)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/worker/ -run TestTaskTimeoutsConfig_Lookup_AnalyzeIssue -v`
Expected: 编译错误 `cfg.AnalyzeIssue undefined`

- [ ] **Step 3: 写失败测试 — PoolConfig.ImageFull 校验**

`internal/worker/types_test.go` 追加：

```go
func TestPoolConfig_Validate_ImageFull(t *testing.T) {
	// ImageFull 为空时不报错（向后兼容）
	cfg := PoolConfig{
		Image:        "img",
		GiteaURL:     "http://g",
		GiteaToken:   "t",
		ClaudeAPIKey: "k",
	}
	if err := cfg.Validate(); err != nil {
		t.Errorf("ImageFull 为空时不应报错: %v", err)
	}

	// ImageFull 非空合法
	cfg.ImageFull = "img-full:latest"
	if err := cfg.Validate(); err != nil {
		t.Errorf("ImageFull 合法时不应报错: %v", err)
	}

	// ImageFull 含空格（非法）
	cfg.ImageFull = "img full"
	if err := cfg.Validate(); err == nil {
		t.Error("ImageFull 含空格时应报错")
	}
}
```

- [ ] **Step 4: 实现 types.go 改动**

`internal/worker/types.go` — `TaskTimeoutsConfig` 结构体（第 39-43 行）：

```go
type TaskTimeoutsConfig struct {
	ReviewPR     time.Duration
	AnalyzeIssue time.Duration // M3.4: 只读分析超时（默认 15m）
	FixIssue     time.Duration
	GenTests     time.Duration
}
```

`Lookup` 方法（第 46-66 行）新增 case：

```go
func (c TaskTimeoutsConfig) Lookup(taskType model.TaskType) time.Duration {
	switch taskType {
	case model.TaskTypeReviewPR:
		if c.ReviewPR > 0 {
			return c.ReviewPR
		}
		return 10 * time.Minute
	case model.TaskTypeAnalyzeIssue:
		if c.AnalyzeIssue > 0 {
			return c.AnalyzeIssue
		}
		return 15 * time.Minute
	case model.TaskTypeFixIssue:
		if c.FixIssue > 0 {
			return c.FixIssue
		}
		return 30 * time.Minute
	case model.TaskTypeGenTests:
		if c.GenTests > 0 {
			return c.GenTests
		}
		return 20 * time.Minute
	default:
		return 10 * time.Minute
	}
}
```

`PoolConfig` 结构体（第 75-88 行）新增字段：

```go
type PoolConfig struct {
	Image        string       // 轻量镜像（review、analyze）
	ImageFull    string       // 执行镜像（fix、gen_tests），可选
	// ... 其余字段不变
}
```

`Validate` 方法（第 91-105 行）追加 `ImageFull` 校验：

```go
func (c PoolConfig) Validate() error {
	if c.Image == "" {
		return fmt.Errorf("PoolConfig.Image 不可为空")
	}
	if c.ImageFull != "" && strings.Contains(c.ImageFull, " ") {
		return fmt.Errorf("PoolConfig.ImageFull 格式非法: %q", c.ImageFull)
	}
	if c.GiteaURL == "" {
		return fmt.Errorf("PoolConfig.GiteaURL 不可为空")
	}
	if c.GiteaToken == "" {
		return fmt.Errorf("PoolConfig.GiteaToken 不可为空")
	}
	if c.ClaudeAPIKey == "" {
		return fmt.Errorf("PoolConfig.ClaudeAPIKey 不可为空")
	}
	return nil
}
```

注意：需在 `types.go` 的 import 区追加 `"strings"`。

- [ ] **Step 5: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/worker/ -run "TestTaskTimeoutsConfig_Lookup_AnalyzeIssue|TestPoolConfig_Validate_ImageFull" -v`
Expected: PASS

- [ ] **Step 6: 确认既有测试无回归**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/worker/ -v`
Expected: 全部 PASS（`TestPoolConfig_Validate` 等既有测试不受影响）

- [ ] **Step 7: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/worker/types.go internal/worker/types_test.go
git commit -m "feat(worker): PoolConfig.ImageFull + TaskTimeoutsConfig.AnalyzeIssue

M3.4 多镜像支持：ImageFull 可选字段（空时所有任务用 Image）。
AnalyzeIssue 超时默认 15 分钟。"
```

---

## Task 3: Pool 多镜像 — resolveImage + runContainer 集成

**Files:**
- Modify: `internal/worker/pool.go:118,155-156,173-180`
- Test: `internal/worker/pool_test.go`

- [ ] **Step 1: 写失败测试**

`internal/worker/pool_test.go` 追加：

```go
func TestPool_ResolveImage(t *testing.T) {
	tests := []struct {
		name      string
		imageFull string
		taskType  model.TaskType
		want      string
	}{
		{"review 用轻量镜像", "", model.TaskTypeReviewPR, "dtworkflow-worker:test"},
		{"analyze 用轻量镜像", "", model.TaskTypeAnalyzeIssue, "dtworkflow-worker:test"},
		{"fix 无 ImageFull 回退轻量", "", model.TaskTypeFixIssue, "dtworkflow-worker:test"},
		{"fix 有 ImageFull 用执行镜像", "dtworkflow-worker-full:test", model.TaskTypeFixIssue, "dtworkflow-worker-full:test"},
		{"gen_tests 有 ImageFull 用执行镜像", "dtworkflow-worker-full:test", model.TaskTypeGenTests, "dtworkflow-worker-full:test"},
		{"analyze 有 ImageFull 仍用轻量", "dtworkflow-worker-full:test", model.TaskTypeAnalyzeIssue, "dtworkflow-worker:test"},
		{"review 有 ImageFull 仍用轻量", "dtworkflow-worker-full:test", model.TaskTypeReviewPR, "dtworkflow-worker:test"},
	}
	for _, tc := range tests {
		t.Run(tc.name, func(t *testing.T) {
			cfg := defaultPoolConfig()
			cfg.ImageFull = tc.imageFull
			p := mustNewPool(t, cfg, &mockDockerClient{})
			got := p.resolveImage(tc.taskType)
			if got != tc.want {
				t.Errorf("resolveImage(%s) = %q, 期望 %q", tc.taskType, got, tc.want)
			}
		})
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/worker/ -run TestPool_ResolveImage -v`
Expected: 编译错误 `p.resolveImage undefined`

- [ ] **Step 3: 实现 resolveImage**

`internal/worker/pool.go` 在 `ensureNetwork` 方法后（约第 80 行附近）添加：

```go
// resolveImage 根据任务类型选择镜像。
// fix_issue 和 gen_tests 使用执行镜像（需编译环境）；其余使用轻量镜像。
// ImageFull 为空时所有任务回退到 Image（向后兼容）。
func (p *Pool) resolveImage(taskType model.TaskType) string {
	switch taskType {
	case model.TaskTypeFixIssue, model.TaskTypeGenTests:
		if p.config.ImageFull != "" {
			return p.config.ImageFull
		}
	}
	return p.config.Image
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/worker/ -run TestPool_ResolveImage -v`
Expected: PASS

- [ ] **Step 5: 集成 resolveImage 到 runContainer**

`internal/worker/pool.go` 的 `runContainer` 方法中，将硬编码 `p.config.Image` 改为 `resolveImage`。修改 3 处：

**处 1**（第 155-156 行附近）— `containerCfg.Image`：
```go
// 原: Image: p.config.Image,
resolvedImage := p.resolveImage(payload.TaskType)

containerCfg := &ContainerConfig{
	Image:       resolvedImage,
	// ... 其余不变
}
```

**处 2**（第 173-174 行附近）— `ImageExists` 检查：
```go
// 原: exists, err := p.docker.ImageExists(ctx, p.config.Image)
exists, err := p.docker.ImageExists(ctx, resolvedImage)
```

**处 3**（第 176-179 行附近）— 错误消息：
```go
if !exists {
	return nil, fmt.Errorf("镜像 %s 不存在，请先构建或拉取", resolvedImage)
}
```

完整修改区域（runContainer 方法内，第 147 行 `useStdin` 赋值之后）：

```go
useStdin := opts != nil && len(opts.stdinData) > 0

// M3.4: 按任务类型选择镜像
resolvedImage := p.resolveImage(payload.TaskType)

// 根据配置注入 stream-json 标志
if p.config.StreamMonitor.Enabled {
	cmd = injectStreamJsonFlags(cmd)
}

containerCfg := &ContainerConfig{
	Image:       resolvedImage,
	Name:        containerName,
	// ... 其余字段不变
}

// 确保 Docker 网络存在
if err := p.ensureNetwork(ctx); err != nil {
	return nil, fmt.Errorf("确保 Docker 网络失败: %w", err)
}

// 检查镜像是否存在
exists, err := p.docker.ImageExists(ctx, resolvedImage)
if err != nil {
	return nil, fmt.Errorf("检查镜像 %s 失败: %w", resolvedImage, err)
}
if !exists {
	return nil, fmt.Errorf("镜像 %s 不存在，请先构建或拉取", resolvedImage)
}
```

- [ ] **Step 6: 运行全量 worker 测试确认无回归**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/worker/ -v`
Expected: 全部 PASS

- [ ] **Step 7: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/worker/pool.go internal/worker/pool_test.go
git commit -m "feat(worker): resolveImage 按任务类型选择轻量/执行镜像

M3.4: fix_issue 和 gen_tests 使用 ImageFull（如配置），
其余任务使用 Image。runContainer 中 3 处硬编码 p.config.Image 已替换。"
```

---

## Task 4: 容器环境适配 — buildContainerEnv

**Files:**
- Modify: `internal/worker/container.go:68-88`
- Test: `internal/worker/container_test.go`

- [ ] **Step 1: 写失败测试**

`internal/worker/container_test.go` 追加：

```go
func TestBuildContainerEnv_AnalyzeIssue(t *testing.T) {
	config := PoolConfig{
		GiteaURL:     "http://gitea.example.com",
		GiteaToken:   "token",
		ClaudeAPIKey: "key",
	}
	payload := model.TaskPayload{
		TaskType:     model.TaskTypeAnalyzeIssue,
		RepoOwner:    "owner",
		RepoName:     "repo",
		RepoFullName: "owner/repo",
		CloneURL:     "http://gitea.example.com/owner/repo.git",
		IssueNumber:  15,
		IssueTitle:   "Login page crash",
		IssueRef:     "main",
	}

	env := buildContainerEnv(config, payload)
	envMap := envSliceToMap(env)

	if envMap["TASK_TYPE"] != "analyze_issue" {
		t.Errorf("TASK_TYPE = %q, 期望 analyze_issue", envMap["TASK_TYPE"])
	}
	if envMap["ISSUE_NUMBER"] != "15" {
		t.Errorf("ISSUE_NUMBER = %q, 期望 15", envMap["ISSUE_NUMBER"])
	}
	if envMap["ISSUE_TITLE"] != "Login page crash" {
		t.Errorf("ISSUE_TITLE = %q, 期望 Login page crash", envMap["ISSUE_TITLE"])
	}
	if envMap["ISSUE_REF"] != "main" {
		t.Errorf("ISSUE_REF = %q, 期望 main", envMap["ISSUE_REF"])
	}
	// analyze_issue 不应包含 PR_NUMBER
	if _, ok := envMap["PR_NUMBER"]; ok {
		t.Error("analyze_issue 不应包含 PR_NUMBER")
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/worker/ -run TestBuildContainerEnv_AnalyzeIssue -v`
Expected: FAIL — `TASK_TYPE = "analyze_issue"` 但 `ISSUE_NUMBER` 缺失（因为 switch 未匹配 `analyze_issue`，走了 default 无额外 env）

- [ ] **Step 3: 实现 buildContainerEnv analyze_issue case**

`internal/worker/container.go` 第 68-88 行的 switch 块，在 `case model.TaskTypeFixIssue:` 之前添加：

```go
	case model.TaskTypeAnalyzeIssue:
		env = append(env,
			fmt.Sprintf("ISSUE_NUMBER=%d", payload.IssueNumber),
			fmt.Sprintf("ISSUE_TITLE=%s", sanitizeEnvValue(payload.IssueTitle)),
		)
		if payload.IssueRef != "" {
			env = append(env, fmt.Sprintf("ISSUE_REF=%s", sanitizeEnvValue(payload.IssueRef)))
		}
```

注意：`analyze_issue` 的环境变量与 `fix_issue` 完全一致（Issue 编号 + 标题 + Ref），可以合并 case：

```go
	case model.TaskTypeAnalyzeIssue, model.TaskTypeFixIssue:
		env = append(env,
			fmt.Sprintf("ISSUE_NUMBER=%d", payload.IssueNumber),
			fmt.Sprintf("ISSUE_TITLE=%s", sanitizeEnvValue(payload.IssueTitle)),
		)
		if payload.IssueRef != "" {
			env = append(env, fmt.Sprintf("ISSUE_REF=%s", sanitizeEnvValue(payload.IssueRef)))
		}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/worker/ -run "TestBuildContainerEnv_AnalyzeIssue|TestBuildContainerEnv_FixIssue" -v`
Expected: 两个测试均 PASS

- [ ] **Step 5: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/worker/container.go internal/worker/container_test.go
git commit -m "feat(worker): buildContainerEnv 支持 analyze_issue 任务类型

M3.4: analyze_issue 与 fix_issue 共享 Issue 环境变量（合并 case）。"
```

---

## Task 5: 容器命令适配 — buildContainerCmd

**Files:**
- Modify: `internal/worker/container.go:117-183`
- Test: `internal/worker/container_test.go`

- [ ] **Step 1: 写失败测试**

`internal/worker/container_test.go` 追加：

```go
func TestBuildContainerCmd_AnalyzeIssue(t *testing.T) {
	payload := model.TaskPayload{
		TaskType:     model.TaskTypeAnalyzeIssue,
		RepoOwner:    "owner",
		RepoName:     "repo",
		RepoFullName: "owner/repo",
		IssueNumber:  15,
		IssueTitle:   "Login page crash",
		IssueRef:     "release/v1.0",
	}

	cmd := buildContainerCmd(payload)

	// 应包含 claude -p
	if len(cmd) < 2 || cmd[0] != "claude" || cmd[1] != "-p" {
		t.Fatalf("命令前缀应为 [claude -p], 实际: %v", cmd[:min(len(cmd), 2)])
	}

	prompt := cmd[2]
	// analyze_issue 的 prompt 应包含 Issue 编号和分析指令
	if !strings.Contains(prompt, "#15") {
		t.Error("prompt 应包含 Issue 编号 #15")
	}
	if !strings.Contains(prompt, "analyze") || !strings.Contains(prompt, "Analyze") {
		t.Error("prompt 应包含分析指令关键词")
	}
	// analyze_issue 不应包含祈使句修复指令（允许 "automated fix" 等描述性用法）
	if strings.Contains(prompt, "Fix issue") || strings.Contains(prompt, "implement a fix") {
		t.Error("analyze_issue prompt 不应包含祈使句修复指令")
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/worker/ -run TestBuildContainerCmd_AnalyzeIssue -v`
Expected: FAIL — 当前 `analyze_issue` 走 default case，prompt 不包含 Issue 编号

- [ ] **Step 3: 实现 buildContainerCmd analyze_issue case**

`internal/worker/container.go` 的 `buildContainerCmd` 函数（第 118 行 switch 内），在 `case model.TaskTypeReviewPR:` 之后、`case model.TaskTypeFixIssue:` 之前添加：

```go
	case model.TaskTypeAnalyzeIssue:
		repoInfo := "The repository has been cloned to the current directory."
		if payload.IssueRef != "" {
			repoInfo = fmt.Sprintf(
				"The repository has been cloned and ref '%s' is checked out.",
				sanitizePromptInput(payload.IssueRef, 200))
		}
		return []string{
			"claude", "-p",
			fmt.Sprintf(
				"Analyze issue #%d (%s) in repository %s. "+
					"%s "+
					"Read the issue description and comments. "+
					"Explore the codebase to identify the root cause. "+
					"Provide a detailed analysis report with: root cause, affected files, suggested approach, and whether the information is sufficient for an automated fix.",
				payload.IssueNumber,
				sanitizePromptInput(payload.IssueTitle, 500),
				sanitizePromptInput(payload.RepoFullName, 200),
				repoInfo,
			),
		}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/worker/ -run TestBuildContainerCmd_AnalyzeIssue -v`
Expected: PASS

- [ ] **Step 5: 确认既有 buildContainerCmd 测试无回归**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/worker/ -run TestBuildContainerCmd -v`
Expected: 全部 PASS

- [ ] **Step 6: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/worker/container.go internal/worker/container_test.go
git commit -m "feat(worker): buildContainerCmd 支持 analyze_issue 分析 prompt

M3.4: analyze_issue 使用独立的分析导向 prompt（不含修复指令）。"
```

---

## Task 6: 配置层 — WorkerConfig.ImageFull + TaskTimeouts.AnalyzeIssue

**Files:**
- Modify: `internal/config/config.go:75-86,89-93`
- Test: 无独立测试文件（配置测试通过 validate_test.go 覆盖）

- [ ] **Step 1: 修改 config.go**

`internal/config/config.go` 的 `WorkerConfig` 结构体（第 75-86 行）新增 `ImageFull`：

```go
type WorkerConfig struct {
	Concurrency   int           `mapstructure:"concurrency"`
	Timeout       time.Duration `mapstructure:"timeout"`
	Image         string        `mapstructure:"image"`
	ImageFull     string        `mapstructure:"image_full"`     // M3.4: 执行镜像（可选）
	CPULimit      string        `mapstructure:"cpu_limit"`
	MemoryLimit   string        `mapstructure:"memory_limit"`
	NetworkName   string        `mapstructure:"network_name"`
	Timeouts      TaskTimeouts  `mapstructure:"timeouts"`
	StreamMonitor StreamMonitorConf `mapstructure:"stream_monitor"`
}
```

`TaskTimeouts` 结构体（第 89-93 行）新增 `AnalyzeIssue`：

```go
type TaskTimeouts struct {
	ReviewPR     time.Duration `mapstructure:"review_pr"`
	AnalyzeIssue time.Duration `mapstructure:"analyze_issue"`  // M3.4: 默认 15m
	FixIssue     time.Duration `mapstructure:"fix_issue"`
	GenTests     time.Duration `mapstructure:"gen_tests"`
}
```

- [ ] **Step 2: 更新 Clone() 方法**

在 `config.go` 的 `Clone()` 方法中（`cfg.Worker` 复制区域），确保 `ImageFull` 和 `AnalyzeIssue` 被复制。由于结构体是值复制，直接赋值 `out.Worker = cfg.Worker` 已自动包含新字段，无需额外改动。验证 Clone 中是否有逐字段赋值——如果有则需追加。

- [ ] **Step 3: 编译验证**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./...`
Expected: 编译通过

- [ ] **Step 4: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/config/config.go
git commit -m "feat(config): 新增 worker.image_full + timeouts.analyze_issue 配置字段

M3.4: ImageFull 可选执行镜像；AnalyzeIssue 超时独立配置。"
```

---

## Task 7: 配置校验 — validate.go

**Files:**
- Modify: `internal/config/validate.go:100-122`
- Test: `internal/config/validate_test.go`

- [ ] **Step 1: 写失败测试**

`internal/config/validate_test.go` 追加：

```go
func TestValidate_WorkerImageFull(t *testing.T) {
	cfg := validBaseConfig() // 使用已有的基础配置构造函数

	// ImageFull 为空 → 合法
	cfg.Worker.ImageFull = ""
	if err := Validate(cfg); err != nil {
		t.Errorf("ImageFull 为空应合法: %v", err)
	}

	// ImageFull 合法
	cfg.Worker.ImageFull = "dtworkflow-worker-full:latest"
	if err := Validate(cfg); err != nil {
		t.Errorf("ImageFull 合法应通过: %v", err)
	}

	// ImageFull 含空格
	cfg.Worker.ImageFull = "bad image"
	if err := Validate(cfg); err == nil {
		t.Error("ImageFull 含空格应报错")
	}
}

func TestValidate_WorkerTimeoutsAnalyzeIssue(t *testing.T) {
	cfg := validBaseConfig()

	// 零值合法（使用默认值）
	cfg.Worker.Timeouts.AnalyzeIssue = 0
	if err := Validate(cfg); err != nil {
		t.Errorf("AnalyzeIssue 零值应合法: %v", err)
	}

	// 负值非法
	cfg.Worker.Timeouts.AnalyzeIssue = -1 * time.Minute
	if err := Validate(cfg); err == nil {
		t.Error("AnalyzeIssue 负值应报错")
	}
}
```

注意：如果 `validate_test.go` 中没有 `validBaseConfig` 辅助函数，需要参照已有测试中的配置构造方式。

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/config/ -run "TestValidate_WorkerImageFull|TestValidate_WorkerTimeoutsAnalyzeIssue" -v`
Expected: FAIL

- [ ] **Step 3: 实现 validate.go 改动**

`internal/config/validate.go` 两处修改：

**处 1**：第 107-122 行的超时校验循环中追加 `analyze_issue`（融入已有循环，不单独添加）：

```go
	const maxTimeout = 24 * time.Hour
	for _, tc := range []struct {
		name string
		val  time.Duration
	}{
		{"worker.timeouts.review_pr", cfg.Worker.Timeouts.ReviewPR},
		{"worker.timeouts.analyze_issue", cfg.Worker.Timeouts.AnalyzeIssue}, // M3.4
		{"worker.timeouts.fix_issue", cfg.Worker.Timeouts.FixIssue},
		{"worker.timeouts.gen_tests", cfg.Worker.Timeouts.GenTests},
	} {
		if tc.val < 0 {
			errs = append(errs, fmt.Errorf("%s 不能为负数，当前值: %s", tc.name, tc.val))
		} else if tc.val > maxTimeout {
			errs = append(errs, fmt.Errorf("%s 不能超过 %s，当前值: %s", tc.name, maxTimeout, tc.val))
		}
	}
```

**处 2**：在循环之后、`stream_monitor` 校验之前，追加 `ImageFull` 格式校验：

```go
	// M3.4: ImageFull 格式校验（可选字段）
	if cfg.Worker.ImageFull != "" {
		if strings.Contains(cfg.Worker.ImageFull, " ") {
			errs = append(errs, fmt.Errorf("worker.image_full 格式非法: %q", cfg.Worker.ImageFull))
		}
	}
```

确保 `"strings"` 在 import 中。

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/config/ -run "TestValidate_WorkerImageFull|TestValidate_WorkerTimeoutsAnalyzeIssue" -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/config/validate.go internal/config/validate_test.go
git commit -m "feat(config): worker.image_full 格式校验 + analyze_issue 超时校验

M3.4: ImageFull 非空时检查无空格；AnalyzeIssue 超时非负且不超 24h。"
```

---

## Task 8: 装配层 — serve_deps.go + worker_queue_config.go 传递新字段

**Files:**
- Modify: `internal/cmd/serve_deps.go:47-64`
- Modify: `internal/cmd/worker_queue_config.go:9-18,20-29`

- [ ] **Step 1: 修改 buildWorkerPoolConfigFromServeConfig**

`internal/cmd/serve_deps.go` 第 47-64 行的 `buildWorkerPoolConfigFromServeConfig` 函数中，`pcfg` 构造新增 `ImageFull`：

```go
func buildWorkerPoolConfigFromServeConfig(cfg serveConfig) worker.PoolConfig {
	pcfg := worker.PoolConfig{
		Image:         cfg.WorkerImage,
		CPULimit:      cfg.CPULimit,
		MemoryLimit:   cfg.MemoryLimit,
		GiteaURL:      cfg.GiteaURL,
		GiteaToken:    worker.SecretString(cfg.GiteaToken),
		ClaudeAPIKey:  worker.SecretString(cfg.ClaudeAPIKey),
		ClaudeBaseURL: cfg.ClaudeBaseURL,
		NetworkName:   cfg.NetworkName,
		Timeouts:      buildWorkerTimeoutConfigFromAppConfig(cfg.AppCfg),
		StreamMonitor: buildWorkerStreamMonitorConfigFromAppConfig(cfg.AppCfg),
	}
	if cfg.AppCfg != nil {
		pcfg.GiteaInsecureSkipVerify = cfg.AppCfg.Gitea.InsecureSkipVerify
		pcfg.ImageFull = cfg.AppCfg.Worker.ImageFull // M3.4
	}
	return pcfg
}
```

- [ ] **Step 2: 修改 buildWorkerTimeoutConfigFromAppConfig（worker 层）**

`internal/cmd/worker_queue_config.go` 第 20-29 行，新增 `AnalyzeIssue` 映射：

```go
func buildWorkerTimeoutConfigFromAppConfig(cfg *config.Config) worker.TaskTimeoutsConfig {
	if cfg == nil {
		return worker.TaskTimeoutsConfig{}
	}
	return worker.TaskTimeoutsConfig{
		ReviewPR:     cfg.Worker.Timeouts.ReviewPR,
		AnalyzeIssue: cfg.Worker.Timeouts.AnalyzeIssue, // M3.4
		FixIssue:     cfg.Worker.Timeouts.FixIssue,
		GenTests:     cfg.Worker.Timeouts.GenTests,
	}
}
```

- [ ] **Step 3: 修改 buildQueueTimeoutConfigFromAppConfig（queue 层）**

`internal/cmd/worker_queue_config.go` 第 9-18 行，新增 `AnalyzeIssue` 映射：

```go
func buildQueueTimeoutConfigFromAppConfig(cfg *config.Config) queue.TaskTimeoutsConfig {
	if cfg == nil {
		return queue.TaskTimeoutsConfig{}
	}
	return queue.TaskTimeoutsConfig{
		ReviewPR:     cfg.Worker.Timeouts.ReviewPR,
		AnalyzeIssue: cfg.Worker.Timeouts.AnalyzeIssue, // M3.4
		FixIssue:     cfg.Worker.Timeouts.FixIssue,
		GenTests:     cfg.Worker.Timeouts.GenTests,
	}
}
```

- [ ] **Step 4: 编译验证**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./...`
Expected: 编译通过

- [ ] **Step 5: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/cmd/serve_deps.go internal/cmd/worker_queue_config.go
git commit -m "feat(cmd): 装配层传递 ImageFull + AnalyzeIssue 超时到 Pool/Queue 两层

M3.4: buildWorkerPoolConfigFromServeConfig 新增 ImageFull；
buildWorkerTimeoutConfigFromAppConfig 和 buildQueueTimeoutConfigFromAppConfig 均新增 AnalyzeIssue。"
```

---

## Task 9: 队列层 — queue/options.go AnalyzeIssue 超时

**Files:**
- Modify: `internal/queue/options.go:11-15,18-27,35-47`
- Test: `internal/queue/options_test.go`

- [ ] **Step 1: 写失败测试**

`internal/queue/options_test.go` 追加：

```go
func TestTaskTimeout_AnalyzeIssue_Default(t *testing.T) {
	cfg := TaskTimeoutsConfig{} // 零值
	got := TaskTimeout(model.TaskTypeAnalyzeIssue, cfg)
	if got != 15*time.Minute {
		t.Errorf("AnalyzeIssue 默认超时 = %v, 期望 15m", got)
	}
}

func TestTaskTimeout_AnalyzeIssue_Configured(t *testing.T) {
	cfg := TaskTimeoutsConfig{AnalyzeIssue: 20 * time.Minute}
	got := TaskTimeout(model.TaskTypeAnalyzeIssue, cfg)
	if got != 20*time.Minute {
		t.Errorf("AnalyzeIssue 配置超时 = %v, 期望 20m", got)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/queue/ -run "TestTaskTimeout_AnalyzeIssue" -v`
Expected: 编译错误 `cfg.AnalyzeIssue undefined`

- [ ] **Step 3: 实现**

`internal/queue/options.go` 三处修改：

**处 1**（第 11-15 行）— `TaskTimeoutsConfig` 新增字段：

```go
type TaskTimeoutsConfig struct {
	ReviewPR     time.Duration
	AnalyzeIssue time.Duration // M3.4: 只读分析超时（默认 15m）
	FixIssue     time.Duration
	GenTests     time.Duration
}
```

**处 2**（第 18-27 行）— `TaskTimeout` 新增 case：

```go
func TaskTimeout(taskType model.TaskType, cfg TaskTimeoutsConfig) time.Duration {
	var configured time.Duration
	switch taskType {
	case model.TaskTypeReviewPR:
		configured = cfg.ReviewPR
	case model.TaskTypeAnalyzeIssue:
		configured = cfg.AnalyzeIssue
	case model.TaskTypeFixIssue:
		configured = cfg.FixIssue
	case model.TaskTypeGenTests:
		configured = cfg.GenTests
	}
	if configured > 0 {
		return configured
	}
	return defaultTaskTimeout(taskType)
}
```

**处 3**（第 35-47 行）— `defaultTaskTimeout` 新增 case：

```go
func defaultTaskTimeout(taskType model.TaskType) time.Duration {
	switch taskType {
	case model.TaskTypeReviewPR:
		return 10 * time.Minute
	case model.TaskTypeAnalyzeIssue:
		return 15 * time.Minute
	case model.TaskTypeFixIssue:
		return 30 * time.Minute
	case model.TaskTypeGenTests:
		return 20 * time.Minute
	case model.TaskTypeGenDailyReport:
		return 10 * time.Minute
	default:
		return 10 * time.Minute
	}
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/queue/ -run "TestTaskTimeout_AnalyzeIssue" -v`
Expected: PASS

- [ ] **Step 5: 确认既有 options 测试无回归**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/queue/ -run "TestTaskTimeout|TestDefaultTaskTimeout|TestBuildAsynqOptions" -v`
Expected: 全部 PASS

- [ ] **Step 6: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/queue/options.go internal/queue/options_test.go
git commit -m "feat(queue): queue 层 TaskTimeoutsConfig 新增 AnalyzeIssue 超时

M3.4: TaskTimeout + defaultTaskTimeout 新增 analyze_issue case，默认 15 分钟。
与 worker 层 TaskTimeoutsConfig 保持一致（两层独立定义避免循环依赖）。"
```

---

## Task 10: 队列层 — AsynqTypeAnalyzeIssue + 映射

**Files:**
- Modify: `internal/queue/client.go:18-23,148-163`
- Test: `internal/queue/client_test.go`（如存在）

- [ ] **Step 1: 写失败测试**

`internal/queue/client_test.go` 追加（如果文件不存在则创建）：

```go
func TestTaskTypeToAsynq_AnalyzeIssue(t *testing.T) {
	got := taskTypeToAsynq(model.TaskTypeAnalyzeIssue)
	if got != AsynqTypeAnalyzeIssue {
		t.Errorf("taskTypeToAsynq(AnalyzeIssue) = %q, 期望 %q", got, AsynqTypeAnalyzeIssue)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/queue/ -run TestTaskTypeToAsynq_AnalyzeIssue -v`
Expected: 编译错误 `undefined: AsynqTypeAnalyzeIssue`

- [ ] **Step 3: 实现**

`internal/queue/client.go` 第 18-23 行常量区新增：

```go
const (
	AsynqTypeReviewPR       = "dtworkflow:review_pr"
	AsynqTypeAnalyzeIssue   = "dtworkflow:analyze_issue"  // M3.4
	AsynqTypeFixIssue       = "dtworkflow:fix_issue"
	AsynqTypeGenTests       = "dtworkflow:gen_tests"
	AsynqTypeGenDailyReport = "dtworkflow:gen_daily_report"
)
```

`taskTypeToAsynq` 函数（第 148-163 行）新增 case：

```go
func taskTypeToAsynq(t model.TaskType) string {
	switch t {
	case model.TaskTypeReviewPR:
		return AsynqTypeReviewPR
	case model.TaskTypeAnalyzeIssue:
		return AsynqTypeAnalyzeIssue
	case model.TaskTypeFixIssue:
		return AsynqTypeFixIssue
	case model.TaskTypeGenTests:
		return AsynqTypeGenTests
	case model.TaskTypeGenDailyReport:
		return AsynqTypeGenDailyReport
	default:
		slog.Warn("未知的任务类型，将原样使用", slog.String("task_type", string(t)))
		return string(t)
	}
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/queue/ -run TestTaskTypeToAsynq_AnalyzeIssue -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/queue/client.go internal/queue/client_test.go
git commit -m "feat(queue): 新增 AsynqTypeAnalyzeIssue 常量 + taskTypeToAsynq 映射

M3.4: analyze_issue 对应 asynq 类型 dtworkflow:analyze_issue。"
```

---

## ⚠️ 既有测试回归提醒

以下测试可能因 M3.4 改动而失败，需在对应 Task 中同步修复：

| 测试文件 | 预期失败原因 | 在哪个 Task 修复 |
|---------|-------------|----------------|
| `internal/queue/enqueue_test.go` 中 `HandleIssueLabel` 测试 | `auto-fix` 事件原来断言 `TaskTypeFixIssue`，现在应为 `TaskTypeAnalyzeIssue` | Task 12（入队分流） |
| `internal/queue/processor_test.go` 中 `buildStartMessage` 测试 | `TaskTypeFixIssue` 的标题从"Issue 自动分析开始"改为"Issue 自动修复开始" | Task 14（Processor 通知） |
| `internal/queue/processor_test.go` 中 `sendCompletionNotification` 测试 | 函数签名增加 `fixResult` 参数 | Task 14（Processor 通知） |
| `internal/queue/enqueue_test.go` 中 `EnqueueManualFix` 测试 | 原来硬编码 `TaskTypeFixIssue`，现在由调用方指定 | Task 19（dtw + API） |

执行每个 Task 时，**必须检查并修复对应既有测试**，确保 `go test ./internal/queue/ -v` 全量通过后再提交。

---

## Task 11: Webhook 层 — 标签识别扩展

**Files:**
- Modify: `internal/webhook/parser.go:139-161`
- Modify: `internal/webhook/event.go:60-71`
- Test: `internal/webhook/parser_test.go`

- [ ] **Step 1: 写失败测试 — isFixToPRLabel**

`internal/webhook/parser_test.go` 追加：

```go
func TestIsFixToPRLabel(t *testing.T) {
	tests := []struct {
		name string
		want bool
	}{
		{"fix-to-pr", true},
		{"Fix-To-PR", true},
		{"FIX-TO-PR", true},
		{"auto-fix", false},
		{"fix-to-pr2", false},
		{"", false},
	}
	for _, tc := range tests {
		t.Run(tc.name, func(t *testing.T) {
			if got := isFixToPRLabel(tc.name); got != tc.want {
				t.Errorf("isFixToPRLabel(%q) = %v, 期望 %v", tc.name, got, tc.want)
			}
		})
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/webhook/ -run TestIsFixToPRLabel -v`
Expected: 编译错误 `undefined: isFixToPRLabel`

- [ ] **Step 3: 实现 isFixToPRLabel**

`internal/webhook/parser.go` 在 `isAutoFixLabel` 函数（第 159-161 行）之后添加：

```go
// isFixToPRLabel 检查标签是否为 fix-to-pr（大小写不敏感）
func isFixToPRLabel(name string) bool {
	return strings.EqualFold(name, "fix-to-pr")
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/webhook/ -run TestIsFixToPRLabel -v`
Expected: PASS

- [ ] **Step 5: 扩展 IssueLabelEvent**

`internal/webhook/event.go` 第 60-71 行的 `IssueLabelEvent` 新增字段：

```go
type IssueLabelEvent struct {
	DeliveryID     string
	EventType      string
	Action         string
	Repository     RepositoryRef
	Issue          IssueRef
	Label          LabelRef
	Sender         UserRef
	AutoFixChanged bool
	AutoFixAdded   bool
	AutoFixRemoved bool
	FixToPRChanged bool  // M3.4: fix-to-pr 标签变化
	FixToPRAdded   bool  // M3.4: fix-to-pr 标签添加
	FixToPRRemoved bool  // M3.4: fix-to-pr 标签移除
}
```

- [ ] **Step 6: 新增 hasFixToPRLabel 函数（保持 extractLabel 不变）**

**关键设计决策**：保留 `extractLabel` 原有签名和逻辑不变（返回 `LabelRef, bool`），新增独立函数检测 `fix-to-pr`。这样不破坏已有 `auto-fix` 流程（包括 `LabelRef` 填充和 `label_cleared` 边界处理）。

`internal/webhook/parser.go` 在 `isFixToPRLabel` 之后添加：

```go
// hasFixToPRLabel 从 payload 中检测是否包含 fix-to-pr 标签。
// 与 extractLabel 并行调用，互不干扰。
func hasFixToPRLabel(payload giteaIssueEventPayload) bool {
	// 早期版本：顶层 label 字段
	if isFixToPRLabel(payload.Label.Name) {
		return true
	}
	// Gitea 1.21+: label_updated 时标签在 issue.labels 中
	for _, l := range payload.Issue.Labels {
		if isFixToPRLabel(l.Name) {
			return true
		}
	}
	return false
}
```

- [ ] **Step 7: 修改 parseIssue 填充 FixToPR 字段**

`internal/webhook/parser.go` 的 `parseIssue` 方法（第 69-122 行），在 `extractLabel` 调用之后（第 84 行）追加一行：

```go
	labelRef, isAutoFix := extractLabel(payload)
	isFixToPR := hasFixToPRLabel(payload)  // M3.4: 并行检测 fix-to-pr
```

在返回 `IssueLabelEvent` 构造处（第 94-121 行），追加 `FixToPR*` 字段：

```go
	return IssueLabelEvent{
		// ... 所有现有字段保持不变 ...
		AutoFixChanged: isAutoFix,
		AutoFixAdded:   isAutoFix && action == "labeled",
		AutoFixRemoved: isAutoFix && action == "unlabeled",
		FixToPRChanged: isFixToPR,                             // M3.4
		FixToPRAdded:   isFixToPR && action == "labeled",      // M3.4
		FixToPRRemoved: isFixToPR && action == "unlabeled",    // M3.4
	}, nil
```

**注意**：`label_cleared` 边界场景处理。当 action 为 `unlabeled`（来自 `label_cleared`）时，`FixToPRRemoved` 正确为 `isFixToPR && true`。当无 `fix-to-pr` 标签时，`isFixToPR=false`，所有 `FixToPR*` 字段均为 `false`，不影响下游逻辑。

同时需调整第 90-92 行的校验逻辑——当 `fix-to-pr` 标签事件到来但 `labelRef.Name` 恰好为空时（`fix-to-pr` 不在 `extractLabel` 搜索范围内），不应视为无效：

```go
	// 早期版本要求 label.name 非空；但 fix-to-pr 独立检测，不依赖 labelRef
	if action == "labeled" && labelRef.Name == "" && !isFixToPR {
		return nil, ErrInvalidPayload
	}
```

- [ ] **Step 7: 写集成测试**

`internal/webhook/parser_test.go` 追加：

```go
func TestParseIssue_FixToPRLabel(t *testing.T) {
	// 构造 fix-to-pr labeled 事件的 webhook payload
	// 参照已有 TestParseIssue_AutoFixLabel 测试的 payload 结构
	// 修改 label.name 为 "fix-to-pr"
	// 验证事件字段：FixToPRAdded=true, AutoFixAdded=false
}
```

- [ ] **Step 8: 运行全量 webhook 测试**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/webhook/ -v`
Expected: 全部 PASS

- [ ] **Step 9: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/webhook/parser.go internal/webhook/event.go internal/webhook/parser_test.go
git commit -m "feat(webhook): fix-to-pr 标签识别 + IssueLabelEvent 扩展

M3.4: isFixToPRLabel 函数 + extractLabels 双标签检测 + FixToPR* 事件字段。"
```

---

## Task 12: 入队分流 — HandleIssueLabel 路由

**Files:**
- Modify: `internal/queue/enqueue.go:127-192`
- Test: `internal/queue/enqueue_test.go`

- [ ] **Step 1: 写失败测试**

`internal/queue/enqueue_test.go` 追加：

```go
func TestHandleIssueLabel_FixToPRAdded(t *testing.T) {
	// 构造 FixToPRAdded=true 的事件
	// 验证入队的 TaskType 为 TaskTypeFixIssue（非 TaskTypeAnalyzeIssue）
}

func TestHandleIssueLabel_AutoFixAdded_RoutesToAnalyze(t *testing.T) {
	// 构造 AutoFixAdded=true 的事件
	// 验证入队的 TaskType 为 TaskTypeAnalyzeIssue（不再是 TaskTypeFixIssue）
}

func TestHandleIssueLabel_BothLabels_FixToPRPriority(t *testing.T) {
	// 构造 AutoFixAdded=true + FixToPRAdded=true 的事件
	// 验证仅入队 TaskTypeFixIssue（fix-to-pr 优先）
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/queue/ -run "TestHandleIssueLabel_FixToPR|TestHandleIssueLabel_AutoFixAdded_Routes|TestHandleIssueLabel_BothLabels" -v`
Expected: FAIL

- [ ] **Step 3: 实现 HandleIssueLabel 重构**

`internal/queue/enqueue.go` 第 127-192 行的 `HandleIssueLabel` 方法重写：

```go
func (h *EnqueueHandler) HandleIssueLabel(ctx context.Context, event webhook.IssueLabelEvent) error {
	// M3.4: 双标签分流，fix-to-pr 优先于 auto-fix
	var taskType model.TaskType
	switch {
	case event.FixToPRAdded:
		taskType = model.TaskTypeFixIssue
	case event.AutoFixAdded:
		taskType = model.TaskTypeAnalyzeIssue
	default:
		return nil // 非目标标签事件
	}

	// 幂等检查（按各自 TaskType 独立查询）
	existing, err := h.store.FindByDeliveryID(ctx, event.DeliveryID, taskType)
	if err != nil {
		return fmt.Errorf("幂等检查失败: %w", err)
	}
	if existing != nil {
		h.logger.InfoContext(ctx, "任务已存在，跳过",
			"delivery_id", event.DeliveryID,
			"task_type", taskType,
			"task_id", existing.ID,
			"status", existing.Status,
		)
		return nil
	}

	payload := model.TaskPayload{
		TaskType:     taskType,
		DeliveryID:   event.DeliveryID,
		RepoOwner:    event.Repository.Owner,
		RepoName:     event.Repository.Name,
		RepoFullName: event.Repository.FullName,
		CloneURL:     event.Repository.CloneURL,
		IssueNumber:  event.Issue.Number,
		IssueTitle:   event.Issue.Title,
		IssueRef:     event.Issue.Ref,
	}

	if payload.RepoFullName == "" || payload.CloneURL == "" {
		return fmt.Errorf("webhook 数据不完整: RepoFullName 或 CloneURL 为空")
	}

	record := &model.TaskRecord{
		TaskType:     taskType,
		Priority:     model.PriorityNormal,
		RepoFullName: event.Repository.FullName,
		DeliveryID:   event.DeliveryID,
	}

	activeTasks := h.listActiveIssueTasks(ctx, event.Repository.FullName, event.Issue.Number, taskType)

	if err := h.enqueueTask(ctx, payload, record); err != nil {
		return err
	}
	h.cancelTasks(ctx, activeTasks)

	if record.Status == model.TaskStatusQueued {
		h.logger.InfoContext(ctx, "任务已入队",
			"task_id", record.ID,
			"task_type", taskType,
			"asynq_id", record.AsynqID,
			"repo", event.Repository.FullName,
			"issue", event.Issue.Number,
		)
	} else {
		h.logger.InfoContext(ctx, "任务已创建（pending），等待 RecoveryLoop 入队",
			"task_id", record.ID,
			"task_type", taskType,
			"repo", event.Repository.FullName,
			"issue", event.Issue.Number,
		)
	}
	return nil
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/queue/ -run "TestHandleIssueLabel" -v`
Expected: 全部 PASS（含新测试和既有测试）

- [ ] **Step 5: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/queue/enqueue.go internal/queue/enqueue_test.go
git commit -m "feat(queue): HandleIssueLabel 双标签分流

M3.4: fix-to-pr → TaskTypeFixIssue，auto-fix → TaskTypeAnalyzeIssue。
fix-to-pr 优先级高于 auto-fix。幂等检查按各自 TaskType 独立查询。"
```

---

## Task 13: 通知事件类型扩展

**Files:**
- Modify: `internal/notify/notifier.go:8-19`

- [ ] **Step 1: 新增事件常量**

`internal/notify/notifier.go` 第 8-19 行的 EventType 常量区新增：

```go
const (
	EventPRReviewDone        EventType = "pr.review.done"
	EventPRRejected          EventType = "pr.rejected"
	EventIssueAnalysisDone   EventType = "issue.analysis.done"
	EventIssueNeedInfo       EventType = "issue.need_info"
	EventFixIssueDone        EventType = "fix.issue.done"
	EventFixPRCreated        EventType = "fix.pr.created"
	EventE2ETestFailed       EventType = "e2e.test.failed"
	EventPRReviewStarted     EventType = "pr.review.started"
	EventIssueFixStarted     EventType = "issue.fix.started"
	EventIssueAnalyzeStarted EventType = "issue.analyze.started"  // M3.4
	EventIssueAnalyzeDone    EventType = "issue.analyze.done"     // M3.4
	EventSystemError         EventType = "system.error"
)
```

- [ ] **Step 2: 编译验证**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./...`
Expected: 编译通过

- [ ] **Step 3: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/notify/notifier.go
git commit -m "feat(notify): 新增 EventIssueAnalyzeStarted/Done 事件类型

M3.4: 分析任务独立事件，与修复事件语义分离。"
```

---

## Task 14: Processor 路由 + 通知分流

**Files:**
- Modify: `internal/queue/processor.go:219-240,431-482,502-639`
- Test: `internal/queue/processor_test.go`

- [ ] **Step 1: 修改 ProcessTask 路由 switch**

`internal/queue/processor.go` 第 227-240 行的 switch 块：

```go
switch {
case payload.TaskType == model.TaskTypeReviewPR && p.reviewService != nil:
	reviewResult, runErr = p.reviewService.Execute(ctx, payload)
	if reviewResult != nil {
		result = adaptReviewResult(reviewResult)
	}
case (payload.TaskType == model.TaskTypeAnalyzeIssue || payload.TaskType == model.TaskTypeFixIssue) && p.fixService != nil:
	fixResult, runErr = p.fixService.Execute(ctx, payload)
	if fixResult != nil {
		result = adaptFixResult(fixResult)
	}
default:
	result, runErr = p.pool.Run(ctx, payload)
}
```

- [ ] **Step 2: 修改 buildStartMessage**

`internal/queue/processor.go` 第 437-474 行的 switch 块：

```go
switch payload.TaskType {
case model.TaskTypeReviewPR:
	// ... 保持不变
case model.TaskTypeAnalyzeIssue:
	if payload.IssueNumber <= 0 {
		return notify.Message{}, false
	}
	metadata := map[string]string{}
	if p.giteaBaseURL != "" {
		metadata[notify.MetaKeyIssueURL] = fmt.Sprintf("%s/%s/%s/issues/%d",
			p.giteaBaseURL, payload.RepoOwner, payload.RepoName, payload.IssueNumber)
	}
	msg = notify.Message{
		EventType: notify.EventIssueAnalyzeStarted,
		Severity:  notify.SeverityInfo,
		Target: notify.Target{
			Owner:  payload.RepoOwner,
			Repo:   payload.RepoName,
			Number: payload.IssueNumber,
			IsPR:   false,
		},
		Title:    "Issue 自动分析开始",
		Body:     fmt.Sprintf("正在分析 Issue #%d\n\n仓库：%s", payload.IssueNumber, payload.RepoFullName),
		Metadata: metadata,
	}
case model.TaskTypeFixIssue:
	if payload.IssueNumber <= 0 {
		return notify.Message{}, false
	}
	metadata := map[string]string{}
	if p.giteaBaseURL != "" {
		metadata[notify.MetaKeyIssueURL] = fmt.Sprintf("%s/%s/%s/issues/%d",
			p.giteaBaseURL, payload.RepoOwner, payload.RepoName, payload.IssueNumber)
	}
	msg = notify.Message{
		EventType: notify.EventIssueFixStarted,
		Severity:  notify.SeverityInfo,
		Target: notify.Target{
			Owner:  payload.RepoOwner,
			Repo:   payload.RepoName,
			Number: payload.IssueNumber,
			IsPR:   false,
		},
		Title:    "Issue 自动修复开始",
		Body:     fmt.Sprintf("正在修复 Issue #%d\n\n仓库：%s", payload.IssueNumber, payload.RepoFullName),
		Metadata: metadata,
	}
default:
	return notify.Message{}, false
}
```

- [ ] **Step 3: 修改 buildNotificationMessage**

`internal/queue/processor.go` 第 530-623 行的 `case model.TaskTypeFixIssue:` 拆分为两个 case：

```go
case model.TaskTypeAnalyzeIssue:
	if payload.IssueNumber <= 0 {
		return notify.Message{}, false
	}
	issueTarget := notify.Target{
		Owner: payload.RepoOwner, Repo: payload.RepoName,
		Number: payload.IssueNumber, IsPR: false,
	}
	metadata := map[string]string{}
	if p.giteaBaseURL != "" {
		metadata[notify.MetaKeyIssueURL] = fmt.Sprintf("%s/%s/%s/issues/%d",
			p.giteaBaseURL, payload.RepoOwner, payload.RepoName, payload.IssueNumber)
	}
	if record.Status == model.TaskStatusRetrying {
		metadata[notify.MetaKeyRetryCount] = fmt.Sprintf("%d", record.RetryCount+1)
		metadata[notify.MetaKeyMaxRetry] = fmt.Sprintf("%d", record.MaxRetry)
		metadata[notify.MetaKeyTaskStatus] = string(record.Status)
	}
	switch record.Status {
	case model.TaskStatusSucceeded:
		msg = notify.Message{
			EventType: notify.EventIssueAnalyzeDone,
			Severity:  notify.SeverityInfo,
			Target:    issueTarget,
			Title:     "Issue 自动分析完成",
			Body:      body,
			Metadata:  metadata,
		}
	case model.TaskStatusRetrying:
		msg = notify.Message{
			EventType: notify.EventSystemError,
			Severity:  notify.SeverityWarning,
			Target:    issueTarget,
			Title:     "Issue 自动分析重试中",
			Body:      body,
			Metadata:  metadata,
		}
	default: // failed
		msg = notify.Message{
			EventType: notify.EventSystemError,
			Severity:  notify.SeverityWarning,
			Target:    issueTarget,
			Title:     "Issue 自动分析失败",
			Body:      body,
			Metadata:  metadata,
		}
	}
case model.TaskTypeFixIssue:
	// 保持现有代码，但将标题从 "Issue 自动修复任务完成" 修正为：
	// - succeeded: "Issue 修复 PR 已创建"
	// - retrying: "Issue 自动修复重试中"（保持不变）
	// - failed: "Issue 自动修复失败"（保持不变）
```

- [ ] **Step 4: 修改 sendCompletionNotification + handleSkipRetryFailure 签名**

当前两个函数只接收 `reviewResult`，M3.4 需要传递 `fixResult`（为 M3.5 PR 信息注入做准备）。

**4a — `sendCompletionNotification` 签名**（第 485 行）：

```go
func (p *Processor) sendCompletionNotification(ctx context.Context, record *model.TaskRecord, reviewResult *review.ReviewResult, fixResult *fix.FixResult) {
```

**4b — `handleSkipRetryFailure` 签名**（第 720 行）：

```go
func (p *Processor) handleSkipRetryFailure(ctx context.Context, record *model.TaskRecord, runErr error, reviewResult *review.ReviewResult, fixResult *fix.FixResult, logMsg string) error {
```

内部第 736 行调用同步更新：

```go
	p.sendCompletionNotification(ctx, record, reviewResult, fixResult)
```

**4c — 所有调用处更新**（5 处）：

| 行号 | 原调用 | 新调用 |
|------|--------|--------|
| 255 | `handleSkipRetryFailure(ctx, record, runErr, reviewResult, "PR 不处于...")` | `handleSkipRetryFailure(ctx, record, runErr, reviewResult, nil, "PR 不处于...")` |
| 258 | `handleSkipRetryFailure(ctx, record, runErr, reviewResult, "Issue 不处于...")` | `handleSkipRetryFailure(ctx, record, runErr, reviewResult, fixResult, "Issue 不处于...")` |
| 261 | `handleSkipRetryFailure(ctx, record, runErr, nil, "Issue 未设置...")` | `handleSkipRetryFailure(ctx, record, runErr, nil, fixResult, "Issue 未设置...")` |
| 264 | `handleSkipRetryFailure(ctx, record, runErr, nil, "Issue 关联的 ref...")` | `handleSkipRetryFailure(ctx, record, runErr, nil, fixResult, "Issue 关联的 ref...")` |
| 343 | `sendCompletionNotification(ctx, record, reviewResult)` | `sendCompletionNotification(ctx, record, reviewResult, fixResult)` |

注意：第 258/261/264 行的 `fixResult` 变量来自第 224 行 `var fixResult *fix.FixResult`（零值 nil），在出错路径下确实为 nil，传递是安全的。

- [ ] **Step 5: 运行全量 processor 测试**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/queue/ -v`
Expected: 全部 PASS

- [ ] **Step 6: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/queue/processor.go internal/queue/processor_test.go
git commit -m "feat(queue): Processor 路由 + 通知分流 analyze_issue/fix_issue

M3.4: ProcessTask 路由 analyze_issue 到 fixService；
buildStartMessage 拆分分析/修复独立通知；
buildNotificationMessage 拆分成功/失败/重试通知。"
```

---

## Task 15: serve.go — Mux 注册

**Files:**
- Modify: `internal/cmd/serve.go:236-239`

- [ ] **Step 1: 注册 AsynqTypeAnalyzeIssue**

`internal/cmd/serve.go` 第 236-239 行的 mux 注册区域：

```go
mux := asynq.NewServeMux()
mux.Handle(queue.AsynqTypeReviewPR, processor)
mux.Handle(queue.AsynqTypeAnalyzeIssue, processor)  // M3.4
mux.Handle(queue.AsynqTypeFixIssue, processor)
mux.Handle(queue.AsynqTypeGenTests, processor)
```

- [ ] **Step 2: 编译验证**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./...`
Expected: 编译通过

- [ ] **Step 3: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/cmd/serve.go
git commit -m "feat(cmd): asynq Mux 注册 AsynqTypeAnalyzeIssue handler

M3.4: analyze_issue 任务由 processor 统一处理。"
```

---

## Task 16: entrypoint.sh — analyze_issue 只读 + fix_issue 写模式

**Files:**
- Modify: `build/docker/entrypoint.sh:81-94`

- [ ] **Step 1: 修改 entrypoint.sh**

`build/docker/entrypoint.sh` 第 50-94 行的 case 块，将现有 `fix_issue)` 拆分为两个 case：

```bash
case "${TASK_TYPE:-}" in
    review_pr)
        # ... 保持不变（第 51-79 行）
        ;;
    analyze_issue)
        # M3.4: 只读分析模式（从原 fix_issue 搬迁）
        if [ -n "${ISSUE_REF:-}" ]; then
            log "checkout 到关联 ref: ${ISSUE_REF}"
            git fetch origin "${ISSUE_REF}" 2>&1 >&2
            git checkout FETCH_HEAD 2>&1 >&2
        fi
        ;;
    fix_issue)
        # M3.4: 修复模式（写权限）
        if [ -n "${ISSUE_REF:-}" ]; then
            log "checkout 到关联 ref: ${ISSUE_REF}"
            git fetch origin "${ISSUE_REF}" 2>&1 >&2
            git checkout FETCH_HEAD 2>&1 >&2
        fi
        # 配置凭证缓存（内存模式，1 小时超时），允许 push
        git config --global credential.helper 'cache --timeout=3600'
        git config --global user.name "DTWorkflow Bot"
        git config --global user.email "dtworkflow-bot@noreply.local"
        # Maven/Gradle 缓存重定向到 /workspace（避免 /tmp tmpfs 溢出）
        export MAVEN_OPTS="${MAVEN_OPTS:--Dmaven.repo.local=/workspace/.m2/repository}"
        export GRADLE_USER_HOME="${GRADLE_USER_HOME:-/workspace/.gradle}"
        log "修复模式已启用（credential cache + git identity + build cache redirect）"
        ;;
    gen_tests)
        log "测试生成任务，使用默认分支"
        ;;
    *)
        log "任务类型: ${TASK_TYPE:-<empty>}，使用默认分支"
        ;;
esac
```

**关键区别**：
- `analyze_issue`：只读，与原 `fix_issue` 行为一致（仅 checkout ref）
- `fix_issue`：新增 credential helper（内存缓存）、git identity、Maven/Gradle 缓存重定向

**安全注意**：`fix_issue` 模式下不设置 review 模式的安全加固（pre-commit hook、push 禁用），因为修复需要 push。但仍保留后续的 `unset GITEA_TOKEN` 等凭证清理。

- [ ] **Step 2: ShellCheck 验证**

Run: `shellcheck /Users/kelin/Work/DTWorkflow/build/docker/entrypoint.sh`
Expected: 无新增错误（或可接受的既有警告）

- [ ] **Step 3: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add build/docker/entrypoint.sh
git commit -m "feat(entrypoint): analyze_issue 只读模式 + fix_issue 写模式

M3.4: analyze_issue 从原 fix_issue 搬迁（只读）；
fix_issue 新增 credential cache + git identity + build cache redirect。"
```

---

## Task 17: Dockerfile.worker-full

**Files:**
- Create: `build/docker/Dockerfile.worker-full`

- [ ] **Step 1: 创建 Dockerfile**

```dockerfile
# M3.4: 执行镜像（在轻量 worker 镜像基础上叠加 JDK + Maven 编译环境）
# 用于 fix_issue 和 gen_tests 任务（需运行 mvn test / npm test）
FROM dtworkflow-worker:latest AS base

# Java 工具链
RUN apt-get update && apt-get install -y --no-install-recommends \
    openjdk-17-jdk-headless \
    && rm -rf /var/lib/apt/lists/*

# Maven
ARG MAVEN_VERSION=3.9.9
RUN curl -fsSL https://archive.apache.org/dist/maven/maven-3/${MAVEN_VERSION}/binaries/apache-maven-${MAVEN_VERSION}-bin.tar.gz \
    | tar xz -C /opt \
    && ln -s /opt/apache-maven-${MAVEN_VERSION}/bin/mvn /usr/local/bin/mvn

# Maven/Gradle 缓存目录预创建
# 运行时通过环境变量重定向到 /workspace（避免 /tmp tmpfs 溢出）
ENV MAVEN_OPTS="-Dmaven.repo.local=/workspace/.m2/repository"
ENV GRADLE_USER_HOME="/workspace/.gradle"
```

- [ ] **Step 2: 验证 Dockerfile 语法**

Run: `docker build --check -f /Users/kelin/Work/DTWorkflow/build/docker/Dockerfile.worker-full /Users/kelin/Work/DTWorkflow 2>&1 || echo "Docker BuildKit check 不可用，跳过"`

- [ ] **Step 3: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add build/docker/Dockerfile.worker-full
git commit -m "feat(docker): 新增 Dockerfile.worker-full 执行镜像

M3.4: 在 worker 基础上叠加 JDK 17 + Maven 3.9.9，
用于 fix_issue/gen_tests 任务的编译和测试执行。"
```

---

## Task 18: Makefile — build-worker-full 构建目标

**Files:**
- Modify: `Makefile:15,64-68`

- [ ] **Step 1: 添加构建目标**

`Makefile` 第 15 行 `.PHONY` 追加 `build-worker build-worker-full`：

```makefile
.PHONY: build build-linux build-dtw build-dtw-linux build-all install lint test fmt clean dev-up dev-down docker-build build-worker build-worker-full release deploy rollback help
```

在 `docker-build` 目标（第 64-68 行）之后添加：

```makefile
## build-worker: 构建轻量 Worker 镜像
build-worker:
	docker build -f build/docker/Dockerfile.worker \
		-t dtworkflow-worker:latest .

## build-worker-full: 构建执行 Worker 镜像（依赖 build-worker）
build-worker-full: build-worker
	docker build -f build/docker/Dockerfile.worker-full \
		-t dtworkflow-worker-full:latest .
```

- [ ] **Step 2: 验证 Makefile 语法**

Run: `cd /Users/kelin/Work/DTWorkflow && make -n build-worker-full`
Expected: 输出 docker build 命令（dry-run）

- [ ] **Step 3: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add Makefile
git commit -m "feat(make): 新增 build-worker / build-worker-full 构建目标

M3.4: build-worker-full 依赖 build-worker，确保基础镜像先构建。"
```

---

## Task 19: dtw 客户端 + 服务端 API — fix-issue --fix flag

**Files:**
- Modify: `internal/dtw/cmd/fix_issue.go` (客户端)
- Modify: `internal/api/fix_handler.go:13-16,77-87` (服务端 API)
- Modify: `internal/queue/enqueue.go:394-410` (EnqueueManualFix)

### Step 19a: 服务端 — EnqueueManualFix 支持 TaskType 参数

- [ ] **Step 1: 修改 EnqueueManualFix 接受调用方指定的 TaskType**

`internal/queue/enqueue.go` 第 394-410 行。当前代码硬编码 `payload.TaskType = model.TaskTypeFixIssue`（第 396 行）。改为：仅在调用方未指定时设默认值。

```go
func (h *EnqueueHandler) EnqueueManualFix(ctx context.Context, payload model.TaskPayload, triggeredBy string) (string, error) {
	// M3.4: 支持调用方指定 TaskType（analyze_issue 或 fix_issue）
	if payload.TaskType == "" {
		payload.TaskType = model.TaskTypeAnalyzeIssue // 默认分析模式
	}
	// 校验仅允许 Issue 相关类型
	if payload.TaskType != model.TaskTypeAnalyzeIssue && payload.TaskType != model.TaskTypeFixIssue {
		return "", fmt.Errorf("EnqueueManualFix 不支持任务类型: %s", payload.TaskType)
	}
	payload.DeliveryID = generateManualDeliveryID()

	activeTasks := h.listActiveIssueTasks(ctx, payload.RepoFullName, payload.IssueNumber, payload.TaskType)

	record := &model.TaskRecord{
		TaskType:     payload.TaskType,
		Priority:     model.PriorityNormal,
		RepoFullName: payload.RepoFullName,
		DeliveryID:   payload.DeliveryID,
		TriggeredBy:  triggeredBy,
	}

	if err := h.enqueueTask(ctx, payload, record); err != nil {
		return "", err
	}
	h.cancelTasks(ctx, activeTasks)
	// ... 日志输出保持不变，日志中 task_type 通过 payload.TaskType 体现
```

### Step 19b: 服务端 — fix_handler.go 解析 task_type

- [ ] **Step 2: 修改 fixRequest 结构体**

`internal/api/fix_handler.go` 第 13-16 行：

```go
type fixRequest struct {
	IssueNumber int64  `json:"issue_number" binding:"required,min=1"`
	Ref         string `json:"ref,omitempty"`
	TaskType    string `json:"task_type,omitempty"` // M3.4: analyze_issue（默认）或 fix_issue
}
```

- [ ] **Step 3: 修改 triggerFix 传递 TaskType**

`internal/api/fix_handler.go` 第 77-87 行的 payload 构造：

```go
	// M3.4: 解析 task_type，默认 analyze_issue
	taskType := model.TaskTypeAnalyzeIssue
	if req.TaskType == string(model.TaskTypeFixIssue) {
		taskType = model.TaskTypeFixIssue
	}

	payload := model.TaskPayload{
		TaskType:     taskType, // M3.4: 从请求中获取
		RepoOwner:    owner,
		RepoName:     repo,
		RepoFullName: repoInfo.FullName,
		CloneURL:     repoInfo.CloneURL,
		IssueNumber:  issue.Number,
		IssueTitle:   issue.Title,
		IssueRef:     issueRef,
	}
```

### Step 19c: 客户端 — dtw fix-issue --fix flag

- [ ] **Step 4: 添加 --fix flag**

`internal/dtw/cmd/fix_issue.go` 新增变量和 flag：

```go
var (
	fixRepo    string
	fixIssue   int
	fixNoWait  bool
	fixTimeout time.Duration
	fixMode    bool // M3.4: true=修复模式（fix_issue），false=分析模式（analyze_issue，默认）
)
```

`init()` 中追加：

```go
fixIssueCmd.Flags().BoolVar(&fixMode, "fix", false, "触发修复模式（默认为分析模式）")
```

- [ ] **Step 5: 修改 API 请求体**

`RunE` 函数中：

```go
	// M3.4: 按 --fix flag 区分触发分析或修复
	taskType := "analyze_issue"
	if fixMode {
		taskType = "fix_issue"
	}
	body := map[string]any{
		"issue_number": fixIssue,
		"task_type":    taskType,
	}
```

- [ ] **Step 6: 更新命令描述**

```go
var fixIssueCmd = &cobra.Command{
	Use:   "fix-issue",
	Short: "触发 Issue 自动分析或修复",
	Long:  "默认触发只读分析（analyze_issue），使用 --fix 触发修复（fix_issue）。",
}
```

- [ ] **Step 7: 编译验证**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./cmd/dtw/ && go build ./...`
Expected: 编译通过

- [ ] **Step 8: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add internal/dtw/cmd/fix_issue.go internal/api/fix_handler.go internal/queue/enqueue.go
git commit -m "feat(dtw+api): fix-issue --fix flag + 服务端 task_type 分流

M3.4: dtw 客户端新增 --fix flag（默认分析模式）；
API fix_handler 解析 task_type 字段；
EnqueueManualFix 支持调用方指定 TaskType（含校验）。"
```

---

## Task 20: 配置示例更新

**Files:**
- Modify: `configs/dtworkflow.example.yaml`

- [ ] **Step 1: 更新配置示例**

在 `worker` 段的 `image` 之后追加 `image_full`，在 `timeouts` 段追加 `analyze_issue`：

```yaml
worker:
  concurrency: 2
  image: "dtworkflow-worker:latest"
  image_full: "dtworkflow-worker-full:latest"  # M3.4: 执行镜像（可选，不配则所有任务用 image）
  cpu_limit: "2.0"
  memory_limit: "4g"
  timeouts:
    review_pr: "10m"
    analyze_issue: "15m"   # M3.4: 只读分析
    fix_issue: "30m"       # M3.4: 含分析 + 修复 + 测试
    gen_tests: "20m"
```

- [ ] **Step 2: 提交**

```bash
cd /Users/kelin/Work/DTWorkflow
git add configs/dtworkflow.example.yaml
git commit -m "docs(config): 补充 image_full + analyze_issue 超时配置示例

M3.4: 配置示例同步更新。"
```

---

## Task 21: 全量编译 + 测试验证

- [ ] **Step 1: 全量编译**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./...`
Expected: 编译通过

- [ ] **Step 2: 全量测试**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./... -count=1`
Expected: 全部 PASS（允许已知 skip 的测试）

- [ ] **Step 3: Lint 检查**

Run: `cd /Users/kelin/Work/DTWorkflow && make lint`
Expected: 无新增 lint 错误

- [ ] **Step 4: 回归验证清单**

手动检查以下关键路径（编译和单元测试已覆盖的除外）：

1. `auto-fix` 标签 → 入队 `analyze_issue` 任务（而非 `fix_issue`）
2. `fix-to-pr` 标签 → 入队 `fix_issue` 任务
3. `PoolConfig.ImageFull` 配置后，`fix_issue` 使用执行镜像、`review_pr` / `analyze_issue` 使用轻量镜像
4. `entrypoint.sh` analyze_issue 模式行为与原 fix_issue 一致（只读）
5. `entrypoint.sh` fix_issue 模式：credential helper + git identity + build cache redirect
6. 现有 M3.1-M3.3 分析流程（auto-fix 触发）不受影响

- [ ] **Step 5: 最终提交（如有遗漏修复）**

```bash
cd /Users/kelin/Work/DTWorkflow
git add -A
git commit -m "fix: M3.4 全量验证修复"
```
