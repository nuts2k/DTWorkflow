# M3.2 分析 Prompt 工程与容器执行 — 设计文档

> 日期：2026-04-03
> 状态：已确认
> 依赖：M3.1 完成（fix.Service 骨架 + Issue 上下文采集）

## 目标

在 M3.1 的 `internal/fix` 包基础上，实现分析 Prompt 构造、容器只读执行和双层 JSON 解析，并激活 Processor 路由，使 `fix_issue` 任务完整走 `fixService.Execute` 路径。

**M3.2 不包含**：分析结果回写 Issue 评论（M3.3）、分析结果持久化（M3.3）、fix 专属配置段（按需后续添加）。

## 关键设计决策

| 决策点 | 结论 | 理由 |
|--------|------|------|
| 分析策略 | 全面分析模式 | Claude 自主决定搜索策略（关键词搜索、调用链追踪、关联文件检查），Prompt 给方向引导但不限制手段。最大化利用 Claude Code 的代码探索能力 |
| root_cause 结构 | 单个根因 + related_files 列表 | 强制 Claude 精确定位最根本的原因，避免分散焦点。多文件关联通过 related_files 补充 |
| 配置体系 | 最小配置，复用全局 `claude.model/effort` | fix 专属配置需求在实际运行后才明确，避免过早设计 |
| Processor 路由 | M3.2 激活 | fix_issue 任务完整走 fixService.Execute（上下文采集 + prompt + 容器执行 + 解析），不再走 pool.Run() 默认路径 |
| 代码组织 | 方案 A：镜像 review 包 | 新建 `prompt.go`，与 review 包 1:1 对称。CLIResponse / extractJSON 在 fix 包内独立定义（两包独立演化，不共享） |
| 技术栈检测 | 不复用 | fix 分析场景与 review 不同（review 按文件扩展名检测，fix 是整个仓库），MVP 不做技术栈专项 prompt |

## AnalysisOutput JSON Schema

```go
// AnalysisOutput Issue 分析输出 JSON schema
type AnalysisOutput struct {
    InfoSufficient bool       `json:"info_sufficient"`          // 信息是否充分
    MissingInfo    []string   `json:"missing_info,omitempty"`   // 缺失的信息项（信息不足时填写）
    RootCause      *RootCause `json:"root_cause,omitempty"`     // 根因定位（信息充分时填写）
    Analysis       string     `json:"analysis"`                 // 详细分析说明
    FixSuggestion  string     `json:"fix_suggestion,omitempty"` // 修复建议
    Confidence     string     `json:"confidence"`               // high / medium / low
    RelatedFiles   []string   `json:"related_files,omitempty"`  // 相关文件列表
}

// RootCause 根因定位
type RootCause struct {
    File        string `json:"file"`                // 文件路径（相对于仓库根目录）
    Function    string `json:"function,omitempty"`   // 方法/函数名
    StartLine   int    `json:"start_line,omitempty"` // 起始行号
    EndLine     int    `json:"end_line,omitempty"`   // 结束行号
    Description string `json:"description"`          // 根因描述
}
```

**解析路径分流**：
- `info_sufficient=false` → `root_cause` 为 nil，`missing_info` 非空 → M3.3 走"追问"回写路径
- `info_sufficient=true` → `root_cause` 非 nil → M3.3 走"正常报告"回写路径

## 分析 Prompt 结构

### 分层架构（4 段式）

```
1. 只读模式约束（最高优先级，同 review）
2. 任务上下文（Issue 元数据 + 评论内容）
3. 分析指令
   3a. 信息充分性判断指令
   3b. 根因分析指令（搜索策略、调用链追踪、关联文件）
   3c. 修复建议指令
4. 输出格式定义（JSON schema，硬编码）
```

### Prompt 段落常量

#### 只读约束（`readOnlyConstraint`，同 review）

```
IMPORTANT: You are in READ-ONLY code analysis mode.
- Do NOT call any external APIs, HTTP endpoints, or network services
- Do NOT attempt to submit reviews, comments, or status updates to Gitea or any other platform
- Do NOT run curl, wget, python requests, or any other network commands
- Your ONLY task is to analyze the code and output the JSON result to stdout
```

#### 分析指令前言（`analysisPreamble`）

```
## Issue 分析指令

你是一位资深软件工程师，正在对一个项目中报告的 Issue 进行根因分析。
你的目标是：定位问题根因，给出修复建议。
```

#### 信息充分性判断（`infoSufficiencyInstruction`）

```
### 第一步：信息充分性判断

在开始分析前，先评估 Issue 提供的信息是否足够进行根因定位：
- 充分信息：包含错误信息/异常堆栈/复现步骤/影响范围中至少两项
- 不充分信息：仅有模糊描述（如"页面出错了"）、缺少错误日志和复现步骤

如果信息不充分，设置 info_sufficient=false，在 missing_info 中列出具体需要补充的信息项，
然后在 analysis 中简要说明现有信息能推断出什么。不必进行深入代码分析。
```

#### 根因分析指令（`rootCauseInstruction`）

```
### 第二步：根因分析

如果信息充分，按以下策略进行分析：

1. **关键词定位**：从 Issue 中提取类名、方法名、错误信息等关键词，在代码库中搜索
2. **调用链追踪**：找到可疑代码后，追踪其调用者和被调用者，理解完整执行路径
3. **关联文件检查**：检查配置文件、测试文件、数据模型等关联资源
4. **变更历史**：如有帮助，查看相关文件的 git log 了解近期变更
5. **项目结构理解**：先浏览项目结构，理解模块划分和依赖关系

目标是精确定位到具体文件、方法和行号。优先定位最根本的原因，而非表象。
```

#### 修复建议指令（`fixSuggestionInstruction`）

```
### 第三步：修复建议

基于根因分析给出修复方向：
- 需要修改哪些文件、方法
- 推荐的修复策略
- 需要注意的副作用和回归风险
```

#### 输出格式（`analysisJSONSchemaInstruction`，硬编码）

```
## 输出格式

你必须将分析结果输出为如下结构的单个 JSON 对象。
不要用 markdown 代码块包裹，只输出原始 JSON。
**所有文本字段必须使用中文书写。**

{
  "info_sufficient": true,
  "missing_info": [],
  "root_cause": {
    "file": "relative/path/to/file.java",
    "function": "methodName",
    "start_line": 42,
    "end_line": 55,
    "description": "根因描述（中文）"
  },
  "analysis": "详细分析说明（中文）",
  "fix_suggestion": "修复建议（中文）",
  "confidence": "high | medium | low",
  "related_files": ["path/to/related1.java", "path/to/related2.java"]
}

信息不足时的输出示例：

{
  "info_sufficient": false,
  "missing_info": ["缺少错误堆栈信息", "缺少复现步骤"],
  "analysis": "根据现有描述的初步判断...",
  "confidence": "low",
  "related_files": []
}
```

### buildPrompt 函数

```go
func (s *Service) buildPrompt(issueCtx *IssueContext) string {
    var b strings.Builder

    // 1. 只读模式约束
    b.WriteString(readOnlyConstraint)

    // 2. 任务上下文
    b.WriteString(fmt.Sprintf("你正在分析仓库中的 Issue #%d。\n", issueCtx.Issue.Number))
    b.WriteString(fmt.Sprintf("Issue 标题：%s\n", issueCtx.Issue.Title))
    if issueCtx.Issue.Body != "" {
        b.WriteString(fmt.Sprintf("Issue 描述：\n%s\n", truncate(issueCtx.Issue.Body, 5000)))
    }
    if len(issueCtx.Comments) > 0 {
        b.WriteString(fmt.Sprintf("\n评论（共 %d 条）：\n", len(issueCtx.Comments)))
        for i, c := range issueCtx.Comments {
            b.WriteString(fmt.Sprintf("--- 评论 #%d（%s）---\n%s\n",
                i+1, c.Poster.Login, truncate(c.Body, 2000)))
        }
    }

    // 3. 分析指令
    b.WriteString(analysisPreamble)
    b.WriteString(infoSufficiencyInstruction)
    b.WriteString(rootCauseInstruction)
    b.WriteString(fixSuggestionInstruction)

    // 4. 输出格式
    b.WriteString(analysisJSONSchemaInstruction)

    return b.String()
}
```

### buildCommand 函数

```go
func (s *Service) buildCommand() []string {
    cmd := []string{
        "claude", "-p", "-",
        "--output-format", "json",
        "--disallowedTools", "Edit,Write,NotebookEdit",
    }
    if s.cfgProv != nil {
        if model := s.cfgProv.GetClaudeModel(); model != "" {
            cmd = append(cmd, "--model", model)
        }
        if effort := s.cfgProv.GetClaudeEffort(); effort != "" {
            cmd = append(cmd, "--effort", strings.ToLower(strings.TrimSpace(effort)))
        }
    }
    return cmd
}
```

## Service 变更

### 构造函数签名变更

```go
// FixConfigProvider 获取全局 Claude 配置的接口
type FixConfigProvider interface {
    GetClaudeModel() string
    GetClaudeEffort() string
}

type Service struct {
    gitea   IssueClient
    pool    FixPoolRunner
    cfgProv FixConfigProvider // 可选，nil 时使用默认配置
    logger  *slog.Logger
}

// NewService pool 升级为必须参数（M3.2 开始容器执行是核心依赖）
func NewService(gitea IssueClient, pool FixPoolRunner, opts ...ServiceOption) *Service {
    if gitea == nil { panic("NewService: gitea 不能为 nil") }
    if pool == nil  { panic("NewService: pool 不能为 nil") }
    // ...
}

// WithConfigProvider 注入配置提供者（可选）
func WithConfigProvider(p FixConfigProvider) ServiceOption {
    return func(s *Service) { s.cfgProv = p }
}
```

### Execute 方法扩展

```go
func (s *Service) Execute(ctx context.Context, payload model.TaskPayload) (*FixResult, error) {
    // 1. 前置校验（不变）
    // 2. Issue 状态校验（不变）
    // 3. 采集上下文（不变）

    // 4. [M3.2] 构造 prompt + 容器执行
    prompt := s.buildPrompt(issueCtx)
    cmd := s.buildCommand()
    execResult, err := s.pool.RunWithCommandAndStdin(ctx, payload, cmd, []byte(prompt))
    if err != nil {
        return &FixResult{
            IssueContext: issueCtx,
            RawOutput:    safeOutput(execResult),
        }, fmt.Errorf("容器执行失败: %w", err)
    }

    // 5. [M3.2] 解析结果
    result := s.parseResult(execResult.Output)
    result.IssueContext = issueCtx
    result.RawOutput = execResult.Output

    // 6. M3.3 在此处插入：回写 Issue 评论

    return result, nil
}
```

### parseResult（双层 JSON 解析）

```go
func (s *Service) parseResult(output string) *FixResult {
    result := &FixResult{}

    // 外层 CLI JSON 信封
    var cliResp CLIResponse
    if err := json.Unmarshal([]byte(output), &cliResp); err != nil {
        result.ParseError = fmt.Errorf("CLI JSON 解析失败: %w", err)
        return result
    }
    result.CLIMeta = &model.CLIMeta{
        CostUSD:    cliResp.CostUSD,
        DurationMs: cliResp.DurationMs,
        IsError:    cliResp.IsError,
        NumTurns:   cliResp.NumTurns,
        SessionID:  cliResp.SessionID,
    }

    if cliResp.IsError {
        result.ParseError = fmt.Errorf("Claude CLI 报告错误: subtype=%s", cliResp.Subtype)
        return result
    }

    // 内层分析 JSON
    jsonText := extractJSON(cliResp.Result)
    var analysis AnalysisOutput
    if err := json.Unmarshal([]byte(jsonText), &analysis); err != nil {
        result.ParseError = fmt.Errorf("分析 JSON 解析失败: %w", err)
        return result
    }
    result.Analysis = &analysis
    return result
}
```

### FixResult 扩展

```go
type FixResult struct {
    IssueContext *IssueContext      // 采集到的 Issue 上下文
    RawOutput    string             // Claude CLI 原始 stdout
    CLIMeta      *model.CLIMeta     // CLI 执行元数据
    Analysis     *AnalysisOutput    // 解析成功的分析结果（可能为 nil）
    ParseError   error              // JSON 解析失败时非 nil
}
```

## Processor 路由激活

### 任务路由变更（`processor.go`）

```go
switch {
case payload.TaskType == model.TaskTypeReviewPR && p.reviewService != nil:
    reviewResult, runErr = p.reviewService.Execute(ctx, payload)
    if reviewResult != nil { result = adaptReviewResult(reviewResult) }
case payload.TaskType == model.TaskTypeFixIssue && p.fixService != nil:
    fixResult, runErr = p.fixService.Execute(ctx, payload)
    if fixResult != nil { result = adaptFixResult(fixResult) }
default:
    result, runErr = p.pool.Run(ctx, payload)
}
```

需要在 switch 前声明 `var fixResult *fix.FixResult`。

### adaptFixResult 升级

```go
func adaptFixResult(r *fix.FixResult) *worker.ExecutionResult {
    if r == nil {
        return &worker.ExecutionResult{ExitCode: 0}
    }
    res := &worker.ExecutionResult{
        Output:   r.RawOutput,
        ExitCode: 0,
    }
    if r.CLIMeta != nil {
        res.CostUSD = r.CLIMeta.CostUSD
        res.DurationMs = r.CLIMeta.DurationMs
    }
    return res
}
```

### serve.go 装配层

```go
// M3.2: pool 升级为必须参数
fixSvc := fix.NewService(
    deps.GiteaClient,
    deps.Pool,
    fix.WithServiceLogger(slog.Default()),
    fix.WithConfigProvider(deps.ConfigManager),
)
fixOpts = append(fixOpts, queue.WithFixService(fixSvc))
```

## 辅助类型（prompt.go 内定义）

### CLIResponse（独立于 review 包）

```go
type CLIResponse struct {
    Type          string  `json:"type"`
    Subtype       string  `json:"subtype"`
    CostUSD       float64 `json:"cost_usd"`
    DurationMs    int64   `json:"duration_ms"`
    DurationAPIMs int64   `json:"duration_api_ms"`
    IsError       bool    `json:"is_error"`
    NumTurns      int     `json:"num_turns"`
    Result        string  `json:"result"`
    SessionID     string  `json:"session_id"`
}
```

### extractJSON（同 review 包逻辑，独立实现）

处理 code fence 包装和无 fence 的 JSON 提取。

### safeOutput

```go
func safeOutput(r *worker.ExecutionResult) string {
    if r == nil { return "" }
    return r.Output
}
```

### truncate

```go
func truncate(s string, maxLen int) string {
    runes := []rune(s)
    if len(runes) > maxLen {
        return string(runes[:maxLen]) + "..."
    }
    return s
}
```

## 文件变更清单

| 文件 | 操作 | 主要改动 |
|------|------|---------|
| `internal/fix/prompt.go` | **新建** | buildPrompt + buildCommand + Prompt 常量 + CLIResponse + extractJSON + safeOutput + truncate + analysisJSONSchemaInstruction |
| `internal/fix/result.go` | **修改** | 新增 AnalysisOutput / RootCause 类型，FixResult 新增 Analysis / ParseError 字段 |
| `internal/fix/service.go` | **修改** | NewService 签名（pool 升级必须参数）+ Execute 增加 prompt+容器执行+解析 + parseResult + FixConfigProvider / WithConfigProvider |
| `internal/fix/context.go` | **不变** | — |
| `internal/fix/service_test.go` | **修改** | 补充容器执行+解析相关测试，mockPool 实现 |
| `internal/fix/prompt_test.go` | **新建** | buildPrompt + extractJSON + buildCommand 测试 |
| `internal/queue/processor.go` | **修改** | switch 新增 fixService 分支 + adaptFixResult 升级 + fixResult 变量声明 |
| `internal/queue/processor_test.go` | **修改** | 反转 M3.1 测试（fixService.Execute 被调用、pool.Run 不被调用） |
| `cmd/serve.go` | **修改** | fix.NewService 传入 pool + ConfigProvider |

## 测试策略

### fix 包单元测试

| 测试用例 | 验证点 |
|---------|--------|
| `TestBuildPrompt_Normal` | Issue 上下文正确格式化到 prompt |
| `TestBuildPrompt_WithComments` | 评论内容包含在 prompt 中 |
| `TestBuildPrompt_ReadOnlyConstraint` | prompt 包含 READ-ONLY 约束 |
| `TestBuildPrompt_LongBody` | 超长 Issue Body 被截断 |
| `TestBuildCommand_Default` | 默认命令包含 json 输出 + disallowedTools |
| `TestBuildCommand_WithModelEffort` | model/effort 配置正确追加 |
| `TestBuildCommand_NilConfigProvider` | cfgProv 为 nil 时不追加 model/effort |
| `TestParseResult_Success` | 双层 JSON 正确解析，Analysis 非 nil |
| `TestParseResult_CLIError` | CLI 报错时返回 ParseError，Analysis 为 nil |
| `TestParseResult_InnerJSONFail` | 内层 JSON 解析失败时 ParseError 非 nil |
| `TestParseResult_InfoInsufficient` | info_sufficient=false 时 RootCause 为 nil，MissingInfo 非空 |
| `TestExtractJSON_CodeFence` | code fence 包装的 JSON 正确提取 |
| `TestExtractJSON_NakedJSON` | 无 code fence 的 JSON 正确提取 |
| `TestExtractJSON_LeadingText` | JSON 前有自然语言文本 |
| `TestExecute_ContainerSuccess` | 完整流程：上下文采集 -> prompt -> 容器执行 -> 解析 |
| `TestExecute_ContainerError` | 容器执行失败时返回 RawOutput + error |
| `TestNewService_NilPool` | pool 为 nil -> panic |

### Processor 集成测试

| 测试用例 | 验证点 |
|---------|--------|
| `TestProcessTask_FixIssue_WithService` | fixService != nil 时走 fix.Service 路径（反转 M3.1 测试） |
| `TestProcessTask_FixIssue_WithoutService` | fixService == nil 时走默认 pool.Run()（向后兼容） |

### Mock 策略

- `mockFixPoolRunner`：实现 `FixPoolRunner` 接口，可配置返回值
- `mockFixConfigProvider`：实现 `FixConfigProvider` 接口
- `mockIssueClient`：复用 M3.1 已有 mock

### 不在 M3.2 测试范围

- 回写/格式化测试（M3.3）
- 端到端集成测试（M3.3 完成后）
- 通知元数据增强测试（M3.3）

## M3.3 扩展预期

| 变更文件 | 主要改动 |
|---------|---------|
| 新增 `writeback.go` | Issue 评论回写（正常报告 / 信息不足追问 / 降级原始输出） |
| 新增 `formatter.go` | Markdown 格式化（根因报告 / 追问列表 / 统计信息） |
| 修改 `service.go` | IssueClient 扩展 CreateIssueComment，Execute 末尾调用回写 |
| 修改 `result.go` | FixResult 可能新增 WritebackError 字段 |
| 新建 `analysis_results` 表 | 分析结果持久化（可选） |
