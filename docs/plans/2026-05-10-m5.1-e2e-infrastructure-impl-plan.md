# M5.1 E2E 基础设施与执行引擎 — 实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 为 DTWorkflow 新增 E2E 测试执行能力——容器内 Claude 编排 Playwright 浏览器测试，产出结构化报告。

**Architecture:** 复制 gen_tests 的完整链路（model → config → validation → worker → service → processor → API → CLI → dtw → 通知），新增 `internal/e2e/` 服务包和 `run_e2e` 任务类型。镜像基于轻量 worker + Playwright + Chromium（`Dockerfile.worker-e2e`）。配置引入命名环境（`e2e.environments`），凭证只存 config 不经 CLI/API。

**Tech Stack:** Go, Cobra, Gin, asynq, Docker SDK, Playwright, SQLite

**设计文档：** `docs/plans/2026-05-09-m5.1-e2e-infrastructure-design.md`

---

## Task 1: model 层扩展 — TaskType + TaskPayload

**Files:**
- Modify: `internal/model/task.go`
- Test: `internal/model/task.go`（IsValid 已有 switch，改完直接编译即可）

**Step 1: 新增 TaskTypeRunE2E 常量**

在 `internal/model/task.go:8-14` 的常量块末尾追加：

```go
TaskTypeRunE2E TaskType = "run_e2e"
```

**Step 2: 扩展 IsValid()**

在 `internal/model/task.go:42` 的 switch 中追加 `TaskTypeRunE2E`：

```go
case TaskTypeReviewPR, TaskTypeAnalyzeIssue, TaskTypeFixIssue, TaskTypeGenTests, TaskTypeGenDailyReport, TaskTypeRunE2E:
```

**Step 3: 扩展 TaskPayload**

在 `internal/model/task.go:98`（`ChangedFiles` 之后）追加 E2E 专用字段：

```go
// E2E 测试定位
Environment     string `json:"environment,omitempty"`      // 命名环境（staging / dev 等）
CaseName        string `json:"case_name,omitempty"`        // 单用例指定（需配合 Module）
BaseURLOverride string `json:"base_url_override,omitempty"` // 临时覆盖 base_url
```

**Step 4: 编译验证**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go build ./internal/model/...`
Expected: PASS

**Step 5: Commit**

```bash
git add internal/model/task.go
git commit -m "feat(m5.1): 新增 TaskTypeRunE2E 常量及 E2E payload 字段"
```

---

## Task 2: queue 层扩展 — AsynqType + 超时默认值

**Files:**
- Modify: `internal/queue/client.go`
- Modify: `internal/queue/options.go`

**Step 1: 新增 AsynqType 常量**

在 `internal/queue/client.go:24`（`AsynqTypeGenDailyReport` 之后）追加：

```go
AsynqTypeRunE2E = "dtworkflow:run_e2e"
```

**Step 2: 扩展 taskTypeToAsynq()**

在 `internal/queue/client.go:158`（`AsynqTypeAnalyzeIssue` 之后）追加 case：

```go
case model.TaskTypeRunE2E:
    return AsynqTypeRunE2E
```

**Step 3: 扩展 queue.TaskTimeoutsConfig**

在 `internal/queue/options.go:15`（`AnalyzeIssue` 字段之后）追加：

```go
RunE2E time.Duration
```

**Step 4: 扩展 TaskTimeout()**

在 `internal/queue/options.go:29`（`AnalyzeIssue` case 之后）追加：

```go
case model.TaskTypeRunE2E:
    configured = cfg.RunE2E
```

**Step 5: 扩展 defaultTaskTimeout()**

在 `internal/queue/options.go:49`（`AnalyzeIssue` case 之后）追加：

```go
case model.TaskTypeRunE2E:
    return 60 * time.Minute
```

**Step 6: 编译验证**

Run: `go build ./internal/queue/...`
Expected: PASS

**Step 7: Commit**

```bash
git add internal/queue/client.go internal/queue/options.go
git commit -m "feat(m5.1): 新增 run_e2e asynq 类型与 60m 默认超时"
```

---

## Task 3: worker 层扩展 — PoolConfig + resolveImage + buildContainerEnv + buildContainerCmd + 超时

**Files:**
- Modify: `internal/worker/types.go`
- Modify: `internal/worker/container.go`
- Modify: `internal/worker/pool.go`
- Test: `internal/worker/container_test.go`

**Step 1: 扩展 PoolConfig**

在 `internal/worker/types.go:84`（`ImageFull` 之后）追加：

```go
ImageE2E string // E2E 镜像（Playwright + Chromium），可选
```

**Step 2: 扩展 PoolConfig.Validate()**

在 `internal/worker/types.go:125`（`ImageFull` 校验之后）追加：

```go
if c.ImageE2E != "" && strings.Contains(c.ImageE2E, " ") {
    return fmt.Errorf("PoolConfig.ImageE2E 不可含空格: %q", c.ImageE2E)
}
```

**Step 3: 扩展 TaskTimeoutsConfig**

在 `internal/worker/types.go:44`（`AnalyzeIssue` 之后）追加：

```go
RunE2E time.Duration // M5.1: E2E 默认 60m（多用例串行场景）
```

**Step 4: 扩展 TaskTimeoutsConfig.Lookup()**

在 `internal/worker/types.go:68`（`AnalyzeIssue` default case 之前）追加：

```go
case model.TaskTypeRunE2E:
    if c.RunE2E > 0 {
        return c.RunE2E
    }
    return 60 * time.Minute
```

**Step 5: 扩展 selectGiteaToken()**

`run_e2e` 不创建 PR/Issue（M5.1），复用默认 `GiteaToken`（review token），不需要修改 `selectGiteaToken`——已有 default 分支返回 `config.GiteaToken`。无操作。

**Step 6: 扩展 buildContainerEnv()**

在 `internal/worker/container.go:115`（`GenTests` case 结尾之后）追加 `run_e2e` case。

E2E 环境变量由 `e2e.Service.Execute` 通过 `RunWithCommandAndStdin` 的 cmd/stdin 传递 prompt，
但 entrypoint.sh 需要知道 `TASK_TYPE=run_e2e` 来路由到正确的 case。当前代码在 switch 外面已设置 `TASK_TYPE`（line 84），所以 `run_e2e` 不需要额外的 env vars 在 `buildContainerEnv` 中——E2E 环境变量注入由 `e2e.Service.buildContainerEnv` 通过 `pool.RunWithCommandAndStdin` 的 cmd 参数传递。

但 `run_e2e` 需要追加 `BASE_REF`（entrypoint checkout 用）和 E2E 专用变量。在 `container.go:115` 追加：

```go
case model.TaskTypeRunE2E:
    env = append(env, fmt.Sprintf("BASE_REF=%s", sanitizeEnvValue(payload.BaseRef)))
```

注：E2E 环境变量（`E2E_BASE_URL`、`E2E_DB_*`、`E2E_ACCOUNT_*`）由 `e2e.Service` 在调用 `RunWithCommandAndStdin` 前通过自定义 env slice 注入（设计 §4.3），不在此处硬编码——因为需要访问命名环境配置。需要扩展 `Pool.RunWithCommandAndStdin` 支持额外 env，或由 `e2e.Service` 通过新接口 `RunWithEnv` 传递。

**决策**：按现有架构，`RunWithCommandAndStdin` 已通过 `buildContainerEnv(config, payload)` 统一注入 env。E2E 环境变量需要从 config 层获取（命名环境），但 worker 包不应依赖 config 包。因此采用 **payload 扩展法**：`e2e.Service.Execute` 在调用 pool 前，将解析后的环境变量序列化到 `TaskPayload` 的新字段 `ExtraEnvs []string`，`buildContainerEnv` 在末尾追加 `payload.ExtraEnvs`。

在 `internal/model/task.go` 的 TaskPayload 中追加：

```go
ExtraEnvs []string `json:"extra_envs,omitempty"` // 任务特定的额外环境变量（key=value 格式）
```

在 `internal/worker/container.go` 的 `buildContainerEnv` 末尾（return 前）追加：

```go
env = append(env, payload.ExtraEnvs...)
```

**Step 7: 扩展 buildContainerCmd()**

在 `internal/worker/container.go:287`（`GenTests` case 之后、`default` 之前）追加：

```go
case model.TaskTypeRunE2E:
    return []string{"claude", "-p", "--output-format", "json", "-"}
```

注意：E2E 使用 `RunWithCommandAndStdin`，prompt 通过 stdin 传入，cmd 仅需指定 `claude -p --output-format json -`。但 `buildContainerCmd` 当前只在 `Pool.Run` 路径中使用；`RunWithCommandAndStdin` 使用调用方传入的 cmd。因此 `buildContainerCmd` 的 `run_e2e` case 是 fallback，实际不会被调用——但为完备性仍应加上。简单路由到 default 即可，无需特殊处理。

**Step 8: 扩展 resolveImage()**

在 `internal/worker/pool.go:609`（`GenTests` case 之后）追加：

```go
case model.TaskTypeRunE2E:
    if p.config.ImageE2E != "" {
        return p.config.ImageE2E
    }
```

**Step 9: 扩展 buildBinds()**

E2E 不需要 Maven/npm 缓存挂载，不需要修改 `buildBinds`。
但需要挂载 Artifact volume（宿主机 `{data_dir}/e2e-artifacts/{task_id}/` → `/workspace/artifacts/`）。

Artifact volume 的挂载路径依赖 `task_id`，而 `buildBinds` 目前只接收 `taskType`。需要扩展 `runContainer` 中的绑定逻辑。这个改动涉及 `pool.go` 的 `runContainer` 方法，较为侵入。

**决策**：M5.1 暂不在 worker 层实现 artifact volume。容器内截图写入 `/workspace/artifacts/`（tmpfs），结果通过 Claude JSON 输出的 `screenshots` 路径引用返回。M5.2 实装 Issue 创建时再加 volume 挂载。

**Step 10: 编写测试**

在 `internal/worker/container_test.go` 中新增测试：

```go
func TestBuildContainerEnv_RunE2E(t *testing.T) {
    config := PoolConfig{
        Image:    "test-image",
        GiteaURL: "https://gitea.test",
        GiteaToken: "test-token",
        ClaudeAPIKey: "test-key",
    }
    payload := model.TaskPayload{
        TaskType:     model.TaskTypeRunE2E,
        RepoOwner:    "owner",
        RepoName:     "repo",
        RepoFullName: "owner/repo",
        CloneURL:     "https://gitea.test/owner/repo.git",
        BaseRef:      "main",
        ExtraEnvs: []string{
            "E2E_BASE_URL=https://staging.example.com",
            "E2E_DB_HOST=db.internal",
        },
    }
    env := buildContainerEnv(config, payload)
    // 验证 BASE_REF 存在
    assert.Contains(t, env, "BASE_REF=main")
    // 验证 ExtraEnvs 被追加
    assert.Contains(t, env, "E2E_BASE_URL=https://staging.example.com")
    assert.Contains(t, env, "E2E_DB_HOST=db.internal")
}

func TestResolveImage_RunE2E(t *testing.T) {
    p := &Pool{config: PoolConfig{Image: "worker:latest", ImageE2E: "worker-e2e:latest"}}
    assert.Equal(t, "worker-e2e:latest", p.resolveImage(model.TaskTypeRunE2E))

    p2 := &Pool{config: PoolConfig{Image: "worker:latest"}}
    assert.Equal(t, "worker:latest", p2.resolveImage(model.TaskTypeRunE2E))
}
```

**Step 11: 运行测试**

Run: `go test ./internal/worker/... -run "TestBuildContainerEnv_RunE2E|TestResolveImage_RunE2E" -v`
Expected: PASS

**Step 12: Commit**

```bash
git add internal/model/task.go internal/worker/types.go internal/worker/container.go internal/worker/pool.go internal/worker/container_test.go
git commit -m "feat(m5.1): worker 层支持 run_e2e 镜像选择、环境变量和超时配置"
```

---

## Task 4: config 层 — E2E 配置结构 + 命名环境 + 合并逻辑

**Files:**
- Modify: `internal/config/config.go`
- Modify: `internal/config/repo_config.go`
- Test: `internal/config/repo_config_test.go`

**Step 1: 在 config.go 中定义 E2E 类型**

在 `internal/config/config.go`（`TaskTimeouts` 之后）追加类型定义：

```go
// E2EAccountConfig 测试账号配置。
type E2EAccountConfig struct {
    Username string `mapstructure:"username"`
    Password string `mapstructure:"password"`
}

// E2EDBConfig 数据库连接配置。
type E2EDBConfig struct {
    Host     string `mapstructure:"host"`
    Port     int    `mapstructure:"port"`
    User     string `mapstructure:"user"`
    Password string `mapstructure:"password"`
    Database string `mapstructure:"database"`
}

// E2EEnvironment 单个命名环境配置。
type E2EEnvironment struct {
    BaseURL  string                       `mapstructure:"base_url"`
    DB       *E2EDBConfig                 `mapstructure:"db"`
    Accounts map[string]E2EAccountConfig  `mapstructure:"accounts"`
}

// E2EConfig 全局 E2E 配置。
type E2EConfig struct {
    Enabled              *bool                      `mapstructure:"enabled"`
    DefaultEnv           string                     `mapstructure:"default_env"`
    ArtifactRetentionDays int                       `mapstructure:"artifact_retention_days"`
    Environments         map[string]E2EEnvironment  `mapstructure:"environments"`
}
```

**Step 2: 在 Config struct 中追加 E2E 字段**

在 `internal/config/config.go:31`（`Repos` 之前）追加：

```go
E2E E2EConfig `mapstructure:"e2e"`
```

**Step 3: 在 WorkerConfig 中追加 ImageE2E 和 RunE2E 超时**

在 `internal/config/config.go:121`（`ImageFull` 之后）追加：

```go
ImageE2E string `mapstructure:"image_e2e"` // M5.1: E2E Worker 镜像（Playwright + Chromium）
```

在 `TaskTimeouts` struct 中追加：

```go
RunE2E time.Duration `mapstructure:"run_e2e"` // M5.1: E2E 默认 60m
```

**Step 4: 在 repo_config.go 中定义 E2EOverride 和 ResolveE2EConfig**

在 `internal/config/repo_config.go` 的 `RepoConfig` 中追加：

```go
E2E *E2EOverride `mapstructure:"e2e"`
```

在文件末尾追加：

```go
// E2EOverride 仓库级 E2E 配置覆盖。
type E2EOverride struct {
    Enabled    *bool  `mapstructure:"enabled"`
    DefaultEnv string `mapstructure:"default_env"`
}

// ResolveE2EConfig 解析指定仓库的最终 E2E 配置覆盖。
// 与 ResolveReviewConfig / ResolveTestGenConfig 模式一致。
func (c *Config) ResolveE2EConfig(repoFullName string) E2EOverride {
    if c == nil {
        return E2EOverride{}
    }
    merged := E2EOverride{
        Enabled:    c.E2E.Enabled,
        DefaultEnv: c.E2E.DefaultEnv,
    }
    for _, repo := range c.Repos {
        if repo.Name != repoFullName || repo.E2E == nil {
            continue
        }
        if repo.E2E.Enabled != nil {
            merged.Enabled = repo.E2E.Enabled
        }
        if repo.E2E.DefaultEnv != "" {
            merged.DefaultEnv = repo.E2E.DefaultEnv
        }
        break
    }
    return merged
}
```

**Step 5: 编写测试**

在 `internal/config/repo_config_test.go` 追加：

```go
func TestResolveE2EConfig(t *testing.T) {
    enabled := true
    disabled := false
    cfg := &Config{
        E2E: E2EConfig{Enabled: &enabled, DefaultEnv: "staging"},
        Repos: []RepoConfig{
            {Name: "org/repo1", E2E: &E2EOverride{Enabled: &disabled}},
            {Name: "org/repo2", E2E: &E2EOverride{DefaultEnv: "dev"}},
        },
    }

    // repo1: 显式禁用
    r1 := cfg.ResolveE2EConfig("org/repo1")
    assert.NotNil(t, r1.Enabled)
    assert.False(t, *r1.Enabled)
    assert.Equal(t, "staging", r1.DefaultEnv) // 继承全局

    // repo2: 覆盖 default_env
    r2 := cfg.ResolveE2EConfig("org/repo2")
    assert.True(t, *r2.Enabled) // 继承全局
    assert.Equal(t, "dev", r2.DefaultEnv)

    // 无覆盖仓库
    r3 := cfg.ResolveE2EConfig("org/other")
    assert.True(t, *r3.Enabled)
    assert.Equal(t, "staging", r3.DefaultEnv)
}
```

**Step 6: 运行测试**

Run: `go test ./internal/config/... -run TestResolveE2EConfig -v`
Expected: PASS

**Step 7: Commit**

```bash
git add internal/config/config.go internal/config/repo_config.go internal/config/repo_config_test.go
git commit -m "feat(m5.1): 新增 E2E 配置结构、命名环境和仓库级覆盖合并"
```

---

## Task 5: validation 层 — E2E 参数校验

**Files:**
- Create: `internal/validation/e2e.go`
- Create: `internal/validation/e2e_test.go`

**Step 1: 编写测试**

创建 `internal/validation/e2e_test.go`：

```go
package validation

import "testing"

func TestE2EModule(t *testing.T) {
    tests := []struct{ name, module string; wantErr bool }{
        {"empty OK", "", false},
        {"valid module", "order", false},
        {"nested module", "order/sub", false},
        {"absolute path", "/etc/passwd", true},
        {"path traversal", "../secret", true},
    }
    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            err := E2EModule(tt.module)
            if (err != nil) != tt.wantErr {
                t.Errorf("E2EModule(%q) = %v, wantErr %v", tt.module, err, tt.wantErr)
            }
        })
    }
}

func TestE2ECaseName(t *testing.T) {
    tests := []struct{ name, caseName string; wantErr bool }{
        {"empty OK", "", false},
        {"valid case", "create-order", false},
        {"with slash", "order/create", true},
        {"path traversal", "..", true},
    }
    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            err := E2ECaseName(tt.caseName)
            if (err != nil) != tt.wantErr {
                t.Errorf("E2ECaseName(%q) = %v, wantErr %v", tt.caseName, err, tt.wantErr)
            }
        })
    }
}

func TestE2EBaseURL(t *testing.T) {
    tests := []struct{ name, url string; wantErr bool }{
        {"empty OK", "", false},
        {"valid https", "https://staging.example.com", false},
        {"valid http", "http://localhost:3000", false},
        {"no scheme", "staging.example.com", true},
        {"ftp scheme", "ftp://host", true},
    }
    for _, tt := range tests {
        t.Run(tt.name, func(t *testing.T) {
            err := E2EBaseURL(tt.url)
            if (err != nil) != tt.wantErr {
                t.Errorf("E2EBaseURL(%q) = %v, wantErr %v", tt.url, err, tt.wantErr)
            }
        })
    }
}
```

**Step 2: 运行测试确认失败**

Run: `go test ./internal/validation/... -run "TestE2E" -v`
Expected: FAIL（函数未定义）

**Step 3: 实现校验函数**

创建 `internal/validation/e2e.go`：

```go
package validation

import (
    "fmt"
    "net/url"
    "strings"
)

// E2EModule 校验 E2E 任务的 module 字段。
// 规则同 GenTestsModule：空串合法，拒绝绝对路径和 .. 路径遍历。
func E2EModule(module string) error {
    return GenTestsModule(module)
}

// E2ECaseName 校验 E2E 任务的 case 名称。
// 只允许单层目录名（不含 / 和 ..），用于定位 e2e/{module}/cases/{case}/ 下的用例。
func E2ECaseName(caseName string) error {
    if caseName == "" {
        return nil
    }
    if strings.Contains(caseName, "/") || strings.Contains(caseName, `\`) {
        return fmt.Errorf("不能包含路径分隔符: %q", caseName)
    }
    if caseName == ".." || caseName == "." {
        return fmt.Errorf("不能为相对路径引用: %q", caseName)
    }
    return nil
}

// E2EBaseURL 校验 base_url 覆盖值。
// 空串合法（使用配置默认值）；非空时必须是 http/https URL。
func E2EBaseURL(rawURL string) error {
    if rawURL == "" {
        return nil
    }
    u, err := url.Parse(rawURL)
    if err != nil {
        return fmt.Errorf("无效的 URL: %w", err)
    }
    if u.Scheme != "http" && u.Scheme != "https" {
        return fmt.Errorf("仅支持 http/https 协议: %q", rawURL)
    }
    if u.Host == "" {
        return fmt.Errorf("缺少主机名: %q", rawURL)
    }
    return nil
}
```

**Step 4: 运行测试确认通过**

Run: `go test ./internal/validation/... -run "TestE2E" -v`
Expected: PASS

**Step 5: Commit**

```bash
git add internal/validation/e2e.go internal/validation/e2e_test.go
git commit -m "feat(m5.1): 新增 E2E module/case/baseURL 参数校验"
```

---

## Task 6: e2e.Service 核心 — 接口定义 + Service 骨架 + Execute 六步

**Files:**
- Create: `internal/e2e/service.go`
- Create: `internal/e2e/result.go`
- Create: `internal/e2e/prompt.go`
- Create: `internal/e2e/errors.go`
- Test: `internal/e2e/service_test.go`

**Step 1: 创建 errors.go**

```go
package e2e

import "errors"

var (
    ErrE2EDisabled         = errors.New("e2e: 功能已禁用")
    ErrEnvironmentNotFound = errors.New("e2e: 命名环境不存在")
    ErrInvalidRef          = errors.New("e2e: 指定 ref 不存在")
    ErrNoCasesFound        = errors.New("e2e: 指定范围内无用例")
    ErrE2EParseFailure     = errors.New("e2e: 输出解析失败")
)
```

**Step 2: 创建 result.go**

按设计文档 §6.2 定义 E2EOutput + CaseResult + PhaseResult + ScriptResult + E2EResult。
包含 `validateE2EOutput` 和 `sanitizeE2EOutput` 函数。

```go
package e2e

import "fmt"

// E2EOutput Claude 容器内 E2E 执行的结构化输出。
type E2EOutput struct {
    Success      bool         `json:"success"`
    TotalCases   int          `json:"total_cases"`
    PassedCases  int          `json:"passed_cases"`
    FailedCases  int          `json:"failed_cases"`
    ErrorCases   int          `json:"error_cases"`
    SkippedCases int          `json:"skipped_cases"`
    Cases        []CaseResult `json:"cases"`
    Warnings     []string     `json:"warnings,omitempty"`
}

type CaseResult struct {
    Name            string       `json:"name"`
    Module          string       `json:"module"`
    CasePath        string       `json:"case_path"`
    Status          string       `json:"status"`
    DurationMs      int64        `json:"duration_ms"`
    SetupResult     *PhaseResult `json:"setup_result,omitempty"`
    TestResult      *PhaseResult `json:"test_result,omitempty"`
    TeardownResult  *PhaseResult `json:"teardown_result,omitempty"`
    FailureCategory string       `json:"failure_category,omitempty"`
    FailureAnalysis string       `json:"failure_analysis,omitempty"`
    Screenshots     []string     `json:"screenshots,omitempty"`
}

type PhaseResult struct {
    Status     string         `json:"status"`
    DurationMs int64          `json:"duration_ms"`
    Scripts    []ScriptResult `json:"scripts"`
}

type ScriptResult struct {
    Name     string `json:"name"`
    Status   string `json:"status"`
    ExitCode int    `json:"exit_code"`
    Output   string `json:"output,omitempty"`
    ErrorMsg string `json:"error_msg,omitempty"`
}

// E2EResult Service.Execute 返回的业务结果。
type E2EResult struct {
    Output     *E2EOutput
    RawOutput  string
    DurationMs int64
}

const maxOutputLen = 2048

func validateE2EOutput(o *E2EOutput) error {
    if o == nil {
        return fmt.Errorf("output 为 nil")
    }
    if o.Success && (o.FailedCases > 0 || o.ErrorCases > 0) {
        return fmt.Errorf("success=true 但 failed=%d error=%d", o.FailedCases, o.ErrorCases)
    }
    total := o.PassedCases + o.FailedCases + o.ErrorCases + o.SkippedCases
    if o.TotalCases != total {
        return fmt.Errorf("total_cases=%d != passed+failed+error+skipped=%d", o.TotalCases, total)
    }
    validStatus := map[string]bool{"passed": true, "failed": true, "error": true, "skipped": true}
    validCategory := map[string]bool{"bug": true, "script_outdated": true, "environment": true}
    for i, c := range o.Cases {
        if !validStatus[c.Status] {
            return fmt.Errorf("cases[%d].status=%q 非法", i, c.Status)
        }
        if c.FailureCategory != "" && !validCategory[c.FailureCategory] {
            return fmt.Errorf("cases[%d].failure_category=%q 非法", i, c.FailureCategory)
        }
        if c.Status == "failed" && c.FailureCategory == "" {
            return fmt.Errorf("cases[%d] status=failed 但 failure_category 为空", i)
        }
    }
    return nil
}

func sanitizeE2EOutput(o *E2EOutput) {
    if o == nil {
        return
    }
    for i := range o.Warnings {
        o.Warnings[i] = truncate(o.Warnings[i], maxOutputLen)
    }
    for i := range o.Cases {
        o.Cases[i].FailureAnalysis = truncate(o.Cases[i].FailureAnalysis, maxOutputLen)
        sanitizePhaseResult(o.Cases[i].SetupResult)
        sanitizePhaseResult(o.Cases[i].TestResult)
        sanitizePhaseResult(o.Cases[i].TeardownResult)
    }
}

func sanitizePhaseResult(p *PhaseResult) {
    if p == nil {
        return
    }
    for i := range p.Scripts {
        p.Scripts[i].Output = truncate(p.Scripts[i].Output, maxOutputLen)
        p.Scripts[i].ErrorMsg = truncate(p.Scripts[i].ErrorMsg, maxOutputLen)
    }
}

func truncate(s string, maxLen int) string {
    runes := []rune(s)
    if len(runes) > maxLen {
        return string(runes[:maxLen])
    }
    return s
}
```

**Step 3: 创建 prompt.go**

按设计文档 §6.1 实现 `buildE2EPrompt`。接收 `promptContext` 参数（repo、env、module、case、baseURL、accounts），返回完整 prompt 字符串。此处不写完整代码——用占位注释标记四段结构，实际内容较长但纯字符串拼接。

```go
package e2e

import (
    "fmt"
    "strings"

    "otws19.zicp.vip/kelin/dtworkflow/internal/config"
)

type promptContext struct {
    RepoFullName string
    BaseRef      string
    BaseURL      string
    DB           *config.E2EDBConfig
    Accounts     map[string]config.E2EAccountConfig
    Module       string
    CaseName     string
    Model        string
    Effort       string
}

func buildE2EPrompt(ctx promptContext) string {
    var b strings.Builder

    // 第一段：上下文
    fmt.Fprintf(&b, "你正在为仓库 %s 执行 E2E 测试。\n\n", ctx.RepoFullName)
    fmt.Fprintf(&b, "测试环境：\n")
    fmt.Fprintf(&b, "- 应用地址：%s\n", ctx.BaseURL)
    if ctx.DB != nil {
        fmt.Fprintf(&b, "- 数据库：通过 mysql --defaults-extra-file=/tmp/.my.cnf 访问\n")
    }
    if len(ctx.Accounts) > 0 {
        b.WriteString("- 测试账号：\n")
        for name, acc := range ctx.Accounts {
            fmt.Fprintf(&b, "  - %s: 用户名 %s（密码已写入 /tmp/.e2e-accounts.json）\n", name, acc.Username)
        }
    }
    fmt.Fprintf(&b, "\n代码基线：%s\n", ctx.BaseRef)

    // 测试范围
    switch {
    case ctx.CaseName != "" && ctx.Module != "":
        fmt.Fprintf(&b, "测试范围：e2e/%s/cases/%s/\n", ctx.Module, ctx.CaseName)
    case ctx.Module != "":
        fmt.Fprintf(&b, "测试范围：e2e/%s/cases/*/\n", ctx.Module)
    default:
        b.WriteString("测试范围：e2e/*/cases/*/（全部模块）\n")
    }

    // 第二段：执行指令
    b.WriteString("\n执行步骤：\n\n")
    b.WriteString("1. 进入 e2e/ 目录，根据测试范围扫描 case.yaml 文件\n")
    b.WriteString("2. 对每个用例，按以下阶段顺序执行：\n")
    b.WriteString("   a) Setup：按 case.yaml 中 setup 数组声明的顺序执行脚本\n")
    b.WriteString("      - .sql → mysql --defaults-extra-file=/tmp/.my.cnf < script\n")
    b.WriteString("      - .js → node script\n")
    b.WriteString("      - .ts（非 .spec.ts）→ npx tsx script\n")
    b.WriteString("      - setup 失败 → 跳过 test，标记 error，仍执行 teardown\n")
    b.WriteString("   b) Test：.spec.ts → npx playwright test script --reporter=json\n")
    b.WriteString("   c) Teardown：同 setup 执行方式，无论 test 结果都执行\n")
    b.WriteString("3. 汇总所有用例结果\n\n")
    b.WriteString("重要约束：\n")
    b.WriteString("- 用例之间不共享状态\n")
    b.WriteString("- 严格按 case.yaml 声明顺序执行脚本\n")
    b.WriteString("- 不要修改任何仓库文件\n")
    b.WriteString("- 不要读取凭证文件内容到输出中\n\n")

    // 第三段：失败分析
    b.WriteString("当测试失败时分类原因：\n")
    b.WriteString("- \"bug\"：应用行为与 expectations 不符\n")
    b.WriteString("- \"script_outdated\"：页面元素/流程变更导致脚本失效\n")
    b.WriteString("- \"environment\"：环境问题（服务不可达、超时等）\n\n")

    // 第四段：输出格式
    b.WriteString("以 JSON 格式输出结果（不要在 JSON 前后输出其他内容）：\n")
    b.WriteString(e2eOutputSchema)
    b.WriteString("\n")

    return b.String()
}

const e2eOutputSchema = `{
  "success": bool,
  "total_cases": int,
  "passed_cases": int,
  "failed_cases": int,
  "error_cases": int,
  "skipped_cases": int,
  "cases": [{
    "name": "string",
    "module": "string",
    "case_path": "string",
    "status": "passed|failed|error|skipped",
    "duration_ms": int,
    "setup_result": {"status": "string", "duration_ms": int, "scripts": [{"name": "string", "status": "string", "exit_code": int, "output": "string", "error_msg": "string"}]},
    "test_result": {...},
    "teardown_result": {...},
    "failure_category": "bug|script_outdated|environment",
    "failure_analysis": "string",
    "screenshots": ["path"]
  }],
  "warnings": ["string"]
}`
```

**Step 4: 创建 service.go**

```go
package e2e

import (
    "context"
    "encoding/json"
    "fmt"
    "log/slog"
    "strings"
    "time"

    "otws19.zicp.vip/kelin/dtworkflow/internal/config"
    "otws19.zicp.vip/kelin/dtworkflow/internal/model"
    "otws19.zicp.vip/kelin/dtworkflow/internal/worker"
)

// E2EPoolRunner 容器执行接口。
type E2EPoolRunner interface {
    RunWithCommandAndStdin(ctx context.Context, payload model.TaskPayload,
        cmd []string, stdinData []byte) (*worker.ExecutionResult, error)
}

// E2EConfigProvider 配置访问接口。
type E2EConfigProvider interface {
    ResolveE2EConfig(repoFullName string) config.E2EOverride
    GetE2EEnvironments() map[string]config.E2EEnvironment
    GetClaudeModel() string
    GetClaudeEffort() string
}

// RepoClient 仓库信息查询。
type RepoClient interface {
    GetBranch(ctx context.Context, owner, repo, branch string) (bool, error)
}

type Service struct {
    pool    E2EPoolRunner
    cfgProv E2EConfigProvider
    logger  *slog.Logger
}

type ServiceOption func(*Service)

func WithServiceLogger(logger *slog.Logger) ServiceOption {
    return func(s *Service) { s.logger = logger }
}

func NewService(pool E2EPoolRunner, cfgProv E2EConfigProvider, opts ...ServiceOption) *Service {
    if pool == nil {
        panic("e2e.NewService: pool 不能为 nil")
    }
    if cfgProv == nil {
        panic("e2e.NewService: cfgProv 不能为 nil")
    }
    s := &Service{pool: pool, cfgProv: cfgProv, logger: slog.Default()}
    for _, opt := range opts {
        opt(s)
    }
    return s
}

func (s *Service) Execute(ctx context.Context, payload model.TaskPayload) (*E2EResult, error) {
    start := time.Now()

    // 1. 前置校验
    override := s.cfgProv.ResolveE2EConfig(payload.RepoFullName)
    if override.Enabled != nil && !*override.Enabled {
        return nil, ErrE2EDisabled
    }

    env, err := s.resolveEnvironment(payload, override)
    if err != nil {
        return nil, err
    }

    baseURL := env.BaseURL
    if payload.BaseURLOverride != "" {
        baseURL = payload.BaseURLOverride
    }

    // 2. 构建 prompt
    pctx := promptContext{
        RepoFullName: payload.RepoFullName,
        BaseRef:      payload.BaseRef,
        BaseURL:      baseURL,
        DB:           env.DB,
        Accounts:     env.Accounts,
        Module:       payload.Module,
        CaseName:     payload.CaseName,
        Model:        s.cfgProv.GetClaudeModel(),
        Effort:       s.cfgProv.GetClaudeEffort(),
    }
    prompt := buildE2EPrompt(pctx)

    // 3. 注入 E2E 环境变量到 payload.ExtraEnvs
    payload.ExtraEnvs = buildE2EEnvVars(env, baseURL)

    // 4. 容器执行
    cmd := buildClaudeCmd(pctx.Model, pctx.Effort)
    execResult, err := s.pool.RunWithCommandAndStdin(ctx, payload, cmd, []byte(prompt))
    if err != nil {
        return &E2EResult{RawOutput: "", DurationMs: time.Since(start).Milliseconds()}, err
    }

    // 5. 解析结果
    output, parseErr := parseE2EResult(execResult.Output)
    result := &E2EResult{
        Output:     output,
        RawOutput:  execResult.Output,
        DurationMs: time.Since(start).Milliseconds(),
    }

    if parseErr != nil {
        s.logger.ErrorContext(ctx, "E2E 输出解析失败",
            "task_id", payload.TaskID,
            "error", parseErr,
        )
        return result, fmt.Errorf("%w: %v", ErrE2EParseFailure, parseErr)
    }

    // 6. 返回结果
    if output != nil && !output.Success {
        return result, nil
    }
    return result, nil
}

func (s *Service) resolveEnvironment(payload model.TaskPayload, override config.E2EOverride) (config.E2EEnvironment, error) {
    envName := payload.Environment
    if envName == "" {
        envName = override.DefaultEnv
    }
    if envName == "" {
        return config.E2EEnvironment{}, ErrEnvironmentNotFound
    }
    envs := s.cfgProv.GetE2EEnvironments()
    env, ok := envs[envName]
    if !ok {
        return config.E2EEnvironment{}, fmt.Errorf("%w: %q", ErrEnvironmentNotFound, envName)
    }
    return env, nil
}

func buildE2EEnvVars(env config.E2EEnvironment, baseURL string) []string {
    vars := []string{
        fmt.Sprintf("E2E_BASE_URL=%s", baseURL),
    }
    if env.DB != nil {
        vars = append(vars,
            fmt.Sprintf("E2E_DB_HOST=%s", env.DB.Host),
            fmt.Sprintf("E2E_DB_PORT=%d", env.DB.Port),
            fmt.Sprintf("E2E_DB_USER=%s", env.DB.User),
            fmt.Sprintf("E2E_DB_PASSWORD=%s", env.DB.Password),
            fmt.Sprintf("E2E_DB_DATABASE=%s", env.DB.Database),
        )
    }
    for name, acc := range env.Accounts {
        upper := strings.ToUpper(name)
        vars = append(vars,
            fmt.Sprintf("E2E_ACCOUNT_%s_USERNAME=%s", upper, acc.Username),
            fmt.Sprintf("E2E_ACCOUNT_%s_PASSWORD=%s", upper, acc.Password),
        )
    }
    return vars
}

func buildClaudeCmd(model, effort string) []string {
    cmd := []string{"claude", "-p", "--output-format", "json"}
    if model != "" {
        cmd = append(cmd, "--model", model)
    }
    if effort != "" {
        cmd = append(cmd, "--effort", effort)
    }
    cmd = append(cmd, "--disallowedTools", "Edit,Write,MultiEdit")
    cmd = append(cmd, "-")
    return cmd
}

func parseE2EResult(raw string) (*E2EOutput, error) {
    // 双层解析：先尝试 CLI 信封（{"type":"result","result":...}），再直接解析
    type cliEnvelope struct {
        Type   string          `json:"type"`
        Result json.RawMessage `json:"result"`
    }
    raw = strings.TrimSpace(raw)
    var envelope cliEnvelope
    if err := json.Unmarshal([]byte(raw), &envelope); err == nil && envelope.Type == "result" && len(envelope.Result) > 0 {
        raw = string(envelope.Result)
        // result 可能是一个字符串（Claude 输出），需要反引号解包
        var s string
        if json.Unmarshal([]byte(raw), &s) == nil {
            raw = s
        }
    }

    var output E2EOutput
    if err := json.Unmarshal([]byte(raw), &output); err != nil {
        return nil, fmt.Errorf("JSON 解析失败: %w", err)
    }

    sanitizeE2EOutput(&output)

    if err := validateE2EOutput(&output); err != nil {
        return &output, fmt.Errorf("校验失败: %w", err)
    }

    return &output, nil
}
```

**Step 5: 编写测试**

创建 `internal/e2e/service_test.go`，覆盖核心场景：
- `resolveEnvironment` 优先级
- `buildE2EEnvVars` 正确生成环境变量
- `parseE2EResult` 双层 JSON 解析
- `validateE2EOutput` 不变量校验

（测试代码较长，此处列举关键测试函数名和验证点）

```go
package e2e

import "testing"

func TestResolveEnvironment_Priority(t *testing.T)         { /* payload > override > 空=error */ }
func TestBuildE2EEnvVars(t *testing.T)                     { /* 完整环境 + 无 DB + 无 accounts */ }
func TestParseE2EResult_CLIEnvelope(t *testing.T)          { /* 双层解析 */ }
func TestParseE2EResult_DirectJSON(t *testing.T)           { /* 直接 JSON */ }
func TestValidateE2EOutput_SuccessWithFailed(t *testing.T) { /* success=true + failed>0 → error */ }
func TestValidateE2EOutput_TotalMismatch(t *testing.T)     { /* 计数不一致 → error */ }
func TestSanitizeE2EOutput_Truncation(t *testing.T)        { /* 2KB 截断 */ }
```

**Step 6: 运行测试**

Run: `go test ./internal/e2e/... -v`
Expected: PASS

**Step 7: Commit**

```bash
git add internal/e2e/
git commit -m "feat(m5.1): 新增 e2e.Service 核心 — Execute 六步 + prompt + 输出解析"
```

---

## Task 7: Processor 集成 — E2E 服务路由

**Files:**
- Modify: `internal/queue/processor.go`

**Step 1: 新增 E2EExecutor 窄接口**

在 `internal/queue/processor.go`（`TestExecutor` 定义之后）追加：

```go
// E2EExecutor 窄接口，解耦 e2e 包。
type E2EExecutor interface {
    Execute(ctx context.Context, payload model.TaskPayload) (*e2e.E2EResult, error)
}

// WithE2EService 注入 E2E 执行服务。
func WithE2EService(svc E2EExecutor) ProcessorOption {
    return func(p *Processor) {
        p.e2eService = svc
    }
}
```

**Step 2: 扩展 Processor struct**

在 `internal/queue/processor.go:132`（`giteaBaseURL` 之后）追加：

```go
e2eService E2EExecutor
```

**Step 3: 扩展 ProcessTask switch**

在 `internal/queue/processor.go:269`（`testService` case 之后、`default` 之前）追加：

```go
case payload.TaskType == model.TaskTypeRunE2E && p.e2eService != nil:
    e2eResult, runErr = p.e2eService.Execute(ctx, payload)
    if e2eResult != nil {
        result = adaptE2EResult(e2eResult)
    }
```

需要在 switch 前声明 `var e2eResult *e2e.E2EResult`。

**Step 4: 实现 adaptE2EResult**

```go
func adaptE2EResult(r *e2e.E2EResult) *worker.ExecutionResult {
    if r == nil {
        return nil
    }
    exitCode := 0
    if r.Output != nil && !r.Output.Success {
        exitCode = 1
    }
    return &worker.ExecutionResult{
        ExitCode: exitCode,
        Output:   r.RawOutput,
        Duration: r.DurationMs,
    }
}
```

**Step 5: 扩展 sentinel error 处理**

在 `ProcessTask` 的 `runErr != nil` 分支中追加 E2E sentinel 检查：

```go
if errors.Is(runErr, e2e.ErrE2EDisabled) {
    return p.handleSkipRetryFailure(ctx, record, runErr, nil, nil, nil, "E2E 已禁用，跳过任务")
}
if errors.Is(runErr, e2e.ErrEnvironmentNotFound) {
    return p.handleSkipRetryFailure(ctx, record, runErr, nil, nil, nil, "E2E 环境未找到，跳过任务")
}
if errors.Is(runErr, e2e.ErrNoCasesFound) {
    return p.handleSkipRetryFailure(ctx, record, runErr, nil, nil, nil, "未发现 E2E 用例，跳过任务")
}
```

注意：`handleSkipRetryFailure` 的签名需适配。当前签名接收 `reviewResult`/`fixResult`/`testResult` 三个可选参数——E2E 需要适配。可传 nil 或扩展签名。

**Step 6: 扩展 sendStartNotification + sendCompletionNotification**

在通知发送函数中追加 `run_e2e` 事件类型映射。

**Step 7: 编译验证**

Run: `go build ./internal/queue/...`
Expected: PASS

**Step 8: Commit**

```bash
git add internal/queue/processor.go
git commit -m "feat(m5.1): Processor 路由 run_e2e 任务到 e2e.Service"
```

---

## Task 8: 通知适配 — E2E 事件类型

**Files:**
- Modify: `internal/notify/notifier.go`

**Step 1: 新增 EventType 常量**

在 `internal/notify/notifier.go:28`（`EventGenTestsFailed` 之后）追加：

```go
// M5.1：E2E 测试任务通知事件。
EventE2EStarted EventType = "e2e.started"
EventE2EDone    EventType = "e2e.done"
EventE2EFailed  EventType = "e2e.failed"
```

**Step 2: 新增 Metadata key 常量**

在 `internal/notify/notifier.go:53`（`MetaKeyFailureCategory` 之后）追加：

```go
// M5.1 E2E 事件专用 metadata key。
MetaKeyE2EEnv          = "e2e_env"           // 环境名
MetaKeyE2ETotalCases   = "e2e_total_cases"   // 总用例数
MetaKeyE2EPassedCases  = "e2e_passed_cases"  // 通过数
MetaKeyE2EFailedCases  = "e2e_failed_cases"  // 失败数
MetaKeyE2EErrorCases   = "e2e_error_cases"   // 错误数
```

**Step 3: Commit**

```bash
git add internal/notify/notifier.go
git commit -m "feat(m5.1): 新增 E2E 通知事件类型和 metadata key"
```

---

## Task 9: 入队函数 — EnqueueManualE2E

**Files:**
- Modify: `internal/queue/enqueue.go`

**Step 1: 新增 EnqueueManualE2E**

在 `internal/queue/enqueue.go` 末尾追加：

```go
// EnqueueManualE2E 手动入队 E2E 测试任务。
func (h *EnqueueHandler) EnqueueManualE2E(ctx context.Context, payload model.TaskPayload, triggeredBy string) (string, error) {
    payload.TaskType = model.TaskTypeRunE2E
    if payload.DeliveryID == "" {
        payload.DeliveryID = uuid.New().String()
    }

    record := &model.TaskRecord{
        TaskType:     model.TaskTypeRunE2E,
        Status:       model.TaskStatusPending,
        Priority:     model.PriorityNormal,
        Payload:      payload,
        RepoFullName: payload.RepoFullName,
        DeliveryID:   payload.DeliveryID,
        TriggeredBy:  triggeredBy,
    }

    taskID, err := h.enqueueTask(ctx, payload, record)
    if err != nil {
        return "", fmt.Errorf("入队 run_e2e 失败: %w", err)
    }
    return taskID, nil
}
```

**Step 2: Commit**

```bash
git add internal/queue/enqueue.go
git commit -m "feat(m5.1): 新增 EnqueueManualE2E 入队函数"
```

---

## Task 10: REST API — POST /repos/:owner/:repo/e2e

**Files:**
- Create: `internal/api/e2e_handler.go`
- Modify: `internal/api/router.go`

**Step 1: 创建 e2e_handler.go**

模式完全复制 `test_handler.go`，替换字段和校验：

```go
package api

import (
    "bytes"
    "encoding/json"
    "fmt"
    "io"
    "net/http"
    "strings"

    "github.com/gin-gonic/gin"

    "otws19.zicp.vip/kelin/dtworkflow/internal/gitea"
    "otws19.zicp.vip/kelin/dtworkflow/internal/model"
    "otws19.zicp.vip/kelin/dtworkflow/internal/validation"
)

type e2eRequest struct {
    Module  string `json:"module,omitempty"`
    Case    string `json:"case,omitempty"`
    Env     string `json:"env,omitempty"`
    BaseURL string `json:"base_url,omitempty"`
    Ref     string `json:"ref,omitempty"`
}

const e2eMaxBodyBytes = 1 << 14

func (h *handlers) triggerE2E(c *gin.Context) {
    owner := c.Param("owner")
    repo := c.Param("repo")
    if owner == "" || repo == "" {
        Error(c, http.StatusBadRequest, ErrCodeBadRequest, "owner 和 repo 不能为空")
        return
    }

    var req e2eRequest
    if c.Request.Body != nil {
        limited := http.MaxBytesReader(c.Writer, c.Request.Body, e2eMaxBodyBytes)
        body, err := io.ReadAll(limited)
        if err != nil {
            if strings.Contains(err.Error(), "request body too large") {
                Error(c, http.StatusRequestEntityTooLarge, ErrCodeBadRequest,
                    fmt.Sprintf("请求体过大（上限 %d 字节）", e2eMaxBodyBytes))
                return
            }
            Error(c, http.StatusBadRequest, ErrCodeBadRequest, "读取请求体失败: "+err.Error())
            return
        }
        if len(bytes.TrimSpace(body)) > 0 {
            if err := json.Unmarshal(body, &req); err != nil {
                Error(c, http.StatusBadRequest, ErrCodeBadRequest, "无效的请求体: "+err.Error())
                return
            }
        }
    }

    // case 非空时 module 必须非空
    if req.Case != "" && req.Module == "" {
        Error(c, http.StatusBadRequest, ErrCodeBadRequest, "指定 case 时必须同时指定 module")
        return
    }

    if err := validation.E2EModule(req.Module); err != nil {
        Error(c, http.StatusBadRequest, ErrCodeBadRequest, "module "+err.Error())
        return
    }
    if err := validation.E2ECaseName(req.Case); err != nil {
        Error(c, http.StatusBadRequest, ErrCodeBadRequest, "case "+err.Error())
        return
    }
    if err := validation.E2EBaseURL(req.BaseURL); err != nil {
        Error(c, http.StatusBadRequest, ErrCodeBadRequest, "base_url "+err.Error())
        return
    }

    if h.deps.GiteaClient == nil {
        Error(c, http.StatusBadGateway, ErrCodeBadGateway, "Gitea 客户端未配置")
        return
    }
    if h.deps.EnqueueHandler == nil {
        Error(c, http.StatusInternalServerError, ErrCodeInternalError, "入队服务未配置")
        return
    }

    ctx := c.Request.Context()
    repoInfo, _, err := h.deps.GiteaClient.GetRepo(ctx, owner, repo)
    if err != nil {
        if gitea.IsNotFound(err) {
            Error(c, http.StatusNotFound, ErrCodeNotFound,
                fmt.Sprintf("仓库 %s/%s 不存在", owner, repo))
            return
        }
        h.deps.Logger.Error("查询 Gitea 仓库失败", "owner", owner, "repo", repo, "error", err)
        Error(c, http.StatusBadGateway, ErrCodeBadGateway, "查询 Gitea 仓库信息失败")
        return
    }

    identity, _ := c.Get(ContextKeyIdentity)
    triggeredBy := fmt.Sprintf("manual:%v", identity)

    baseRef := req.Ref
    if baseRef == "" {
        baseRef = repoInfo.DefaultBranch
    }

    payload := model.TaskPayload{
        TaskType:        model.TaskTypeRunE2E,
        RepoOwner:       owner,
        RepoName:        repo,
        RepoFullName:    repoInfo.FullName,
        CloneURL:        repoInfo.CloneURL,
        BaseRef:         baseRef,
        Module:          req.Module,
        CaseName:        req.Case,
        Environment:     req.Env,
        BaseURLOverride: req.BaseURL,
    }

    taskID, err := h.deps.EnqueueHandler.EnqueueManualE2E(ctx, payload, triggeredBy)
    if err != nil {
        h.deps.Logger.Error("入队 e2e 失败",
            "owner", owner, "repo", repo, "error", err)
        Error(c, http.StatusInternalServerError, ErrCodeInternalError, "入队 e2e 失败")
        return
    }

    Success(c, http.StatusAccepted, gin.H{
        "task_id": taskID,
        "repo":    repoInfo.FullName,
        "module":  req.Module,
        "case":    req.Case,
        "env":     req.Env,
        "status":  "pending",
    })
}
```

**Step 2: 注册路由**

在 `internal/api/router.go:48`（`gen-tests` 之后）追加：

```go
v1.POST("/repos/:owner/:repo/e2e", h.triggerE2E)
```

**Step 3: 编译验证**

Run: `go build ./internal/api/...`
Expected: PASS

**Step 4: Commit**

```bash
git add internal/api/e2e_handler.go internal/api/router.go
git commit -m "feat(m5.1): 新增 POST /repos/:owner/:repo/e2e API 端点"
```

---

## Task 11: 服务端 CLI — dtworkflow e2e run

**Files:**
- Create: `internal/cmd/e2e.go`

**Step 1: 创建 e2e.go**

模式复制 `internal/cmd/gen_tests.go`，替换字段。关键差异：
- flags: `--owner`, `--repo`, `--module`, `--case`, `--env`, `--base-url`, `--ref`
- 使用 `EnqueueManualE2E` 而非 `EnqueueManualGenTests`
- 无需 BranchCleaner/ModuleScanner
- 输出格式适配 E2E

**Step 2: 编译验证**

Run: `go build ./cmd/dtworkflow/...`
Expected: PASS

**Step 3: Commit**

```bash
git add internal/cmd/e2e.go
git commit -m "feat(m5.1): 新增 dtworkflow e2e run 服务端 CLI 命令"
```

---

## Task 12: dtw 瘦客户端 — dtw e2e run

**Files:**
- Create: `internal/dtw/cmd/e2e.go`

**Step 1: 创建 e2e.go**

模式复制 `internal/dtw/cmd/gen_tests.go`，替换字段。关键差异：
- flags: `--repo`, `--module`, `--case`, `--env`, `--base-url`, `--ref`, `--no-wait`, `--timeout`
- API path: `/api/v1/repos/{owner}/{repo}/e2e`
- 等待完成后的结果展示适配 E2E

**Step 2: 编译验证**

Run: `go build ./cmd/dtw/...`
Expected: PASS

**Step 3: Commit**

```bash
git add internal/dtw/cmd/e2e.go
git commit -m "feat(m5.1): 新增 dtw e2e run 瘦客户端命令"
```

---

## Task 13: configAdapter 扩展 — E2EConfigProvider 实现

**Files:**
- Modify: `internal/cmd/adapter.go`

**Step 1: 实现 E2EConfigProvider 接口方法**

在 `internal/cmd/adapter.go` 的 `configAdapter` 上追加：

```go
func (a *configAdapter) ResolveE2EConfig(repoFullName string) config.E2EOverride {
    cfg := a.mgr.Get()
    if cfg == nil {
        return config.E2EOverride{}
    }
    return cfg.ResolveE2EConfig(repoFullName)
}

func (a *configAdapter) GetE2EEnvironments() map[string]config.E2EEnvironment {
    cfg := a.mgr.Get()
    if cfg == nil {
        return nil
    }
    return cfg.E2E.Environments
}
```

**Step 2: 编译时断言**

在 `internal/cmd/adapter.go` 顶部追加：

```go
var _ e2e.E2EConfigProvider = (*configAdapter)(nil)
```

**Step 3: Commit**

```bash
git add internal/cmd/adapter.go
git commit -m "feat(m5.1): configAdapter 实现 E2EConfigProvider 接口"
```

---

## Task 14: 装配层 — serve.go 连线

**Files:**
- Modify: `internal/cmd/serve.go`
- Modify: `internal/cmd/serve_deps.go`

**Step 1: 在 serve_deps.go 的 PoolConfig 中传递 ImageE2E**

在 `BuildServiceDeps` 中构建 `PoolConfig` 的地方追加：

```go
ImageE2E: cfg.Worker.ImageE2E,
```

同步在 `TaskTimeoutsConfig` 构建处追加：

```go
RunE2E: cfg.Worker.Timeouts.RunE2E,
```

**Step 2: 在 serve.go 中装配 e2e.Service 并注入 Processor**

在 `internal/cmd/serve.go:263`（`testSvc` 装配之后）追加：

```go
e2eSvc := e2e.NewService(deps.Pool, cfgAdapter,
    e2e.WithServiceLogger(slog.Default()),
)
processorOpts = append(processorOpts, queue.WithE2EService(e2eSvc))
```

**Step 3: 在 mux 中注册 run_e2e handler**

在 `serve.go:271`（`AsynqTypeGenTests` 之后）追加：

```go
mux.Handle(queue.AsynqTypeRunE2E, processor)
```

**Step 4: 编译验证**

Run: `go build ./cmd/dtworkflow/...`
Expected: PASS

**Step 5: Commit**

```bash
git add internal/cmd/serve.go internal/cmd/serve_deps.go
git commit -m "feat(m5.1): 装配层连线 e2e.Service → Processor → asynq mux"
```

---

## Task 15: entrypoint.sh — run_e2e 分支

**Files:**
- Modify: `build/docker/entrypoint.sh`

**Step 1: 新增 run_e2e case**

在 `build/docker/entrypoint.sh:265`（`gen_tests` case 的 `;;` 之后、`*) default` 之前）追加：

```bash
    run_e2e)
        # M5.1: E2E 测试模式（只读，不需要 push 能力）
        if [ -n "${BASE_REF:-}" ]; then
            log "E2E: fetch + checkout BASE_REF=${BASE_REF}"
            git fetch origin "${BASE_REF}" >&2 2>&1
            git checkout FETCH_HEAD >&2 2>&1
        fi
        # 生成 MySQL 配置文件（如有数据库环境变量）
        if [ -n "${E2E_DB_HOST:-}" ]; then
            cat > /tmp/.my.cnf <<MYCNF
[client]
host=${E2E_DB_HOST}
port=${E2E_DB_PORT:-3306}
user=${E2E_DB_USER}
password=${E2E_DB_PASSWORD}
database=${E2E_DB_DATABASE}
MYCNF
            chmod 600 /tmp/.my.cnf
            unset E2E_DB_PASSWORD
            log "MySQL 配置已写入 /tmp/.my.cnf，E2E_DB_PASSWORD 已清除"
        fi
        # 写入测试账号文件
        if env | grep -q '^E2E_ACCOUNT_'; then
            python3 -c "
import os, json
accounts = {}
for k, v in os.environ.items():
    if k.startswith('E2E_ACCOUNT_') and k.endswith('_USERNAME'):
        name = k[12:-9].lower()  # E2E_ACCOUNT_ADMIN_USERNAME -> admin
        pwd_key = 'E2E_ACCOUNT_' + name.upper() + '_PASSWORD'
        accounts[name] = {'username': v, 'password': os.environ.get(pwd_key, '')}
with open('/tmp/.e2e-accounts.json', 'w') as f:
    json.dump(accounts, f)
os.chmod('/tmp/.e2e-accounts.json', 0o600)
" 2>&1 || log "警告：生成 .e2e-accounts.json 失败"
            # 清除账号密码环境变量
            for var in $(env | grep '^E2E_ACCOUNT_.*_PASSWORD=' | cut -d= -f1); do
                unset "$var"
            done
            log "测试账号已写入 /tmp/.e2e-accounts.json，密码环境变量已清除"
        fi
        # 设置 Playwright 输出目录
        export PLAYWRIGHT_OUTPUT_DIR=/workspace/artifacts/test-results
        export PLAYWRIGHT_REPORT_DIR=/workspace/artifacts/reports
        mkdir -p "${PLAYWRIGHT_OUTPUT_DIR}" "${PLAYWRIGHT_REPORT_DIR}" 2>/dev/null || true
        setup_build_cache
        log "E2E 测试模式已启用（只读 + DB 配置 + 账号文件 + Playwright 输出目录）"
        ;;
```

**Step 2: 功能验证**

entrypoint.sh 的测试通过 `build/docker/entrypoint_test.sh` 运行（已有测试框架）。可在后续集成测试中覆盖。

**Step 3: Commit**

```bash
git add build/docker/entrypoint.sh
git commit -m "feat(m5.1): entrypoint.sh 新增 run_e2e 分支（DB 配置 + 账号文件 + Playwright）"
```

---

## Task 16: Dockerfile.worker-e2e + Makefile

**Files:**
- Create: `build/docker/Dockerfile.worker-e2e`
- Modify: `Makefile`

**Step 1: 创建 Dockerfile.worker-e2e**

```dockerfile
# M5.1: E2E Worker 镜像 — 基于轻量 worker + Playwright + Chromium
FROM dtworkflow-worker:latest

USER root

# 安装 Playwright + Chromium（含系统依赖）
RUN npx playwright install --with-deps chromium

# 安装 MySQL 客户端
RUN apt-get update && apt-get install -y --no-install-recommends \
    default-mysql-client \
    && rm -rf /var/lib/apt/lists/*

# 安装 tsx（TypeScript 脚本执行器）
RUN npm install -g tsx

USER worker
```

**Step 2: 追加 Makefile 构建目标**

在 `Makefile` 的 `build-worker-full` 之后追加：

```makefile
## build-worker-e2e: 构建 E2E Worker 镜像（依赖 build-worker）
build-worker-e2e: build-worker
	docker build -f build/docker/Dockerfile.worker-e2e \
		-t dtworkflow-worker-e2e:latest .
```

在 `.PHONY` 行追加 `build-worker-e2e`。

**Step 3: Commit**

```bash
git add build/docker/Dockerfile.worker-e2e Makefile
git commit -m "feat(m5.1): 新增 Dockerfile.worker-e2e 和 Makefile 构建目标"
```

---

## Task 17: Processor 通知适配 — E2E 三事件

**Files:**
- Modify: `internal/queue/processor.go`

**Step 1: 扩展 sendStartNotification**

在 `sendStartNotification` 函数中追加 `run_e2e` 分支：

```go
case model.TaskTypeRunE2E:
    msg.EventType = notify.EventE2EStarted
    msg.Title = fmt.Sprintf("E2E 测试开始 -- %s", payload.RepoFullName)
    msg.Severity = notify.SeverityInfo
```

**Step 2: 扩展 sendCompletionNotification**

在完成通知函数中追加 `run_e2e` 分支，根据 `e2eResult` 判断全部通过/部分失败/全部失败，使用对应 EventType 和 Severity。

**Step 3: 编译验证**

Run: `go build ./internal/queue/...`
Expected: PASS

**Step 4: Commit**

```bash
git add internal/queue/processor.go
git commit -m "feat(m5.1): Processor E2E 三事件通知适配"
```

---

## Task 18: 全量编译 + 现有测试回归

**Step 1: 全量编译**

Run: `go build ./...`
Expected: PASS

**Step 2: 运行全量测试**

Run: `go test ./... -count=1 -timeout 5m`
Expected: PASS（无回归）

**Step 3: 运行 lint**

Run: `golangci-lint run ./...`
Expected: PASS

**Step 4: Commit（如有修复）**

```bash
git add -A
git commit -m "fix(m5.1): 全量编译和测试回归修复"
```

---

## 任务依赖关系

```
Task 1 (model)
  ├→ Task 2 (queue)
  ├→ Task 3 (worker) → Task 14 (装配)
  ├→ Task 4 (config) → Task 13 (adapter) → Task 14 (装配)
  ├→ Task 5 (validation) → Task 10 (API)
  ├→ Task 6 (e2e.Service) → Task 7 (Processor) → Task 14 (装配)
  ├→ Task 8 (notify) → Task 17 (Processor 通知)
  ├→ Task 9 (enqueue) → Task 10 (API), Task 11 (server CLI)
  ├→ Task 10 (API) → Task 14 (装配)
  ├→ Task 11 (server CLI)
  ├→ Task 12 (dtw CLI)
  ├→ Task 15 (entrypoint.sh)
  └→ Task 16 (Dockerfile + Makefile)

Task 14 (装配) → Task 17 (通知) → Task 18 (全量验证)
```

**可并行的任务组：**
- 组 A: Task 2 + Task 4 + Task 5 + Task 8（互不依赖）
- 组 B: Task 3 + Task 6 + Task 9（依赖 Task 1 完成）
- 组 C: Task 10 + Task 11 + Task 12 + Task 15 + Task 16（依赖 Task 5/9 完成）
- 组 D: Task 7 + Task 13（依赖 Task 6/4 完成）
- 收尾: Task 14 → Task 17 → Task 18
