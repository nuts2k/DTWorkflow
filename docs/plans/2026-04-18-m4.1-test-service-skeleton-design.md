# M4.1 test.Service 骨架 + AI 原生测试缺口分析 + Prompt 工程 — 设计文档

> 日期：2026-04-18
> 状态：已通过（2026-04-18 修订 1：补入代码审查 P0/P1/P2 共 15 项修复——`Enabled *bool`、`max_retry_rounds` 默认值、SQL `COALESCE`、entrypoint.sh 改造提前到 M4.1、`createTestPR` 签名冻结、`TestRunResults` 重命名、`ModuleScope` 语义界定、通知事件清单补齐等；2026-04-18 修订 2：**将原定 M4.2 的 `ProcessorOption.WithTestService` 装配 + Processor 错误分发 + `adaptTestResult` 适配器提前到 M4.1 落地**（commit `88214e6` "接通 gen_tests 执行链路并加固分支保护"）；同时 `entrypoint.sh` 新增第 5 项 `pre-push` hook 白名单保护。详见决策 D11 与 §3.3 / §4.6 / §5 更新。）
> 依赖：Phase 1（M1.1-M1.8）、Phase 2（review 全链路）、Phase 3（M3.1-M3.5：fix.Service 模式、PR 创建、worker-full 镜像）
> 范围：Phase 4 第一轮第一个里程碑；产出 `internal/test` 包骨架 + Prompt + **Processor 路由接通**；M4.2 仅剩「容器内真实执行 `mvn/npm test` + `createTestPR` 实装 + `test_gen_results` 表 + 通知事件」等运行时能力。

## 1. 概述

### 1.1 目标

为 Phase 4「集中化测试（测试补全）」搭建骨架：

1. **新建 `internal/test` 包**，架构仿照 `internal/review` 与 `internal/fix` 的 Service/Execute 模式
2. **定义 `TestExecutor` 窄接口**（修订 2：M4.1 已激活路由；原计划 M4.2 激活）
3. **AI 原生测试缺口分析**的 Prompt 工程（Java / Vue 两套独立模板 + 公共段）
4. **`TestGenOutput` JSON schema** + 成功不变量校验
5. **手动触发入口**（REST API / 服务端 CLI / 瘦客户端）
6. **`test_gen` 配置段** 及仓库级覆盖

### 1.2 非目标（延后到 M4.2 / M4.3）

- 容器内真实执行 `mvn test` / `npm test`（M4.2）
- `buildStartMessage` / `buildNotificationMessage` 新增 `gen_tests` case（M4.2）——同时需要 `internal/notify` 新增 `EventGenTestsStarted` / `EventGenTestsDone` / `EventGenTestsFailed` 事件类型
- PR 创建的真实 Gitea API 调用（M4.2，`createTestPR` 在 M4.1 仅占位签名）
- 结果持久化 `test_gen_results` 表（M4.2）
- PR merged Webhook 触发变更驱动测试（M4.3）

> **决策变更（2026-04-18 修订 2）**：原列于此的「`ProcessorOption.WithTestService` 实际装配与路由（M4.2）」已**提前到 M4.1 落地**（见决策 D11）。M4.1 期间 `serve.go` 通过 `queue.WithTestService(testSvc)` 注入 `test.Service`，Processor 执行到 `TaskTypeGenTests` 时直接走 `test.Service.Execute`，而不再走 `pool.Run` default 过渡路径。`createTestPR` 在 M4.1 仍是占位签名（返回 `(0, "", nil)`），Execute 末尾调用它后直接拿到零值——即：**路由已通，PR 创建未通**。用户以 `status=succeeded` 结束时 `record.Result` 已是结构化 `TestGenOutput` 的 raw JSON 文本（经解析 + 不变量校验），不再是 Claude 自由文本。

### 1.4 重要产品约束

**单次 gen_tests 任务只处理一种测试框架（Java 或 Vue）**。混合仓用户需要用 `--module` 缩窄到单一栈的子目录，或通过 `test_gen.test_framework` 指定框架，分两次触发。`resolveFramework` 在检测到同一 `module` 根下同时存在 `pom.xml` 与 `package.json` 时会返回 `ErrAmbiguousFramework` 硬拒绝，不做静默猜测（详见 §4.4）。

### 1.3 M4.1 与 M3.1（fix 骨架期）的类比

| 阶段 | 已落地 | 未激活 |
|------|--------|--------|
| M3.1（fix 骨架） | `internal/fix` 包、`FixExecutor` 接口、sentinel、prompt | Processor 未注入 `fixService`，fix_issue 走 `pool.Run` default |
| M4.1（test 骨架）**原计划** | `internal/test` 包、`TestExecutor` 接口、sentinel、prompt、**手动入口四套** | ~~Processor 未注入 `testService`，gen_tests 走 `pool.Run` default~~ |
| M4.1（test 骨架）**实际落地**（修订 2） | 同上 + **`WithTestService` 装配 + Processor 错误分发 + `adaptTestResult`** | `createTestPR` 占位（实际不创建 PR）；容器内 `mvn/npm test` 未真实执行（Claude 按 prompt 行为自动模拟） |

决策差异：相较 M3.1 的"完全不注入"，M4.1 选择"装配但不完成最后一公里（PR 创建 / 真实容器执行）"。这让 `test.Service.Execute` 的 9 步流程在 M4.1 期间已能被真实任务驱动并产出结构化 `TestGenOutput` JSON（即使 `TestGenOutput.verification_passed` 与 `branch_name` 是否可信取决于 Claude 是否按 prompt 真实跑了 mvn/npm）。M4.2 的升级路径变得**更薄**：仅需实装 `createTestPR`（已冻结签名）+ 加通知事件 + 建 `test_gen_results` 表。

---

## 2. 关键设计决策

> 以下 7 决策在 2026-04-18 brainstorming 中确认，另追加 2 项补充。

| # | 决策点 | 选定方案 | 理由 |
|---|--------|---------|------|
| D1 | Service 骨架深度 | **完整 Service**（原计划：Processor 暂不注入 / M3.1 模式；**修订 2：Processor 已注入，见 D11**） | `TestExecutor` 接口签名完整；Service 方法签名齐全；`createTestPR` 占位；M4.2 加一行装配即激活（修订 2 后这一行已在 M4.1 落地） |
| D2 | 容器调用结构 | **单次 `pool.Run`，Claude 内部多步**（read/edit/bash） | 与 `FixOutput` schema 对称；`max_retry_rounds` 指容器内 Claude 自主修正次数；避免跨任务上下文传递 |
| D3 | 手动入口参数 | **`--owner --repo` 必填；`--module --ref --framework` 可选**；复用 `BaseRef` 传递 ref | 与 review-pr 风格对齐；`test_gen.module_scope` / `test_gen.test_framework` 配置已是护栏；小仓库整仓生成合理 |
| D4 | `TestGenOutput` 细节 | **独立 `TestRunResults` 类型含 Framework**（刻意与 `fix.TestResults` 同义不同名，避免跨包同名消歧）；保留 `InfoSufficient/MissingInfo`；保留 `GeneratedFile.TargetFiles` | 混合仓 Java/Vue 区分；对称 FixOutput 的不足判定；PR 描述与 M4.3 变更驱动可直接用 |
| D5 | Cancel-and-Replace | **按 `repo + module` 粒度替换** | 同 review / fix 惯例；防止重复 PR + Claude 预算浪费；不同 module 并发放行 |
| D6 | `WriteDegraded` | **接口保留，M4.1 no-op 实现** | 保持接口形状，M4.2 不需破坏 `TestExecutor` 签名；M4.1 无通知集成，no-op 符合 milestone 边界 |
| D7 | Prompt 模板 | **Java/Vue 独立模板 + `resolveFramework` + 公共段常量化** | 可独立单测；指令密度高，Claude 不在混合仓错位；公共段抽 `promptCommonHeader` 等常量避免双份维护 |
| D8 | **既有测试处理**（补充） | **M4.1 Prompt 工程 + Schema 补丁** | ROADMAP 本身已纳入 M4.1 范围；落到 prompt 指令 + `ExistingTests` / `Operation` 字段 |
| D9 | **工作丢失防护**（补充） | **M4.1 埋 Prompt 指令（增量 commit + 最后一刻 push），M4.2 落实执行与恢复** | 不做半成品 push；容器被 kill 时分支未创建无副作用；M4.2 可选择持久卷抢救策略 |
| D10 | **entrypoint.sh `gen_tests` 分支改造**（补充） | **提前到 M4.1 落地**（纯基础设施改动） | 复用 `fix_issue` 的凭证脱敏 + credential helper + git identity + 构建缓存重定向。**修订 2 更新**：随 D11（`testService` 装配提前）一起，M4.1 期间容器会真实走新 prompt → 真实 commit / push / 构建缓存读写，配置不再是"为 M4.2 做准备"。**修订 2 新增第 5 项 `pre-push` hook**，仅允许推送 `refs/heads/auto-test/*`，作为 Claude 误推默认分支的防御纵深（详见 §4.6）。 |
| **D11** | **Processor 装配提前**（修订 2） | **`WithTestService(testSvc)` + 错误分发 7 条 sentinel + `adaptTestResult` 适配器在 M4.1 落地**（原定 M4.2） | (1) `test.Service` 的 9 步流程 + sentinel 栅格在 M4.1 已经完整单测覆盖；再拖一个里程碑才接通 Processor 意义有限。(2) 把装配提前可使 M4.1 手动入口产出真实 `TestGenOutput` JSON，便于在 M4.1 期间用真实流量回归校验 prompt 工程；M3.1 之所以不装配 `fixService`，是因为当时 `fix.Service` 尚无完整实现，M4.1 情况不同。(3) M4.1 / M4.2 边界变为 **「路由已通 vs 容器真跑 + PR 真建」**，边界更干净。(4) 风险：`createTestPR` 仍占位 → PR 不真实创建，这点在 §5.1 / §7.5 / §10 验收清单显式声明。 |

---

## 3. 架构

### 3.1 新包文件布局

```
internal/test/
├── service.go        # Service 结构、Execute、WriteDegraded、resolveFramework、窄接口
├── prompt.go         # 公共段常量 + buildJavaPrompt + buildVuePrompt
├── result.go         # TestGenOutput / GapAnalysis / GeneratedFile / TestRunResults / RefKind
├── errors.go         # ErrXxx sentinel（若 result.go 简洁则合并到 result.go）
├── service_test.go
├── prompt_test.go
├── result_test.go
└── testdata/
    ├── testgen_output_success.json
    ├── testgen_output_partial_delivery.json
    ├── testgen_output_info_insufficient.json
    ├── testgen_output_failure.json
    └── testgen_output_invalid_operation.json
```

### 3.2 调用链路

```
[触发层]
  Webhook（M4.3 扩展）      ─┐
  POST /api/v1/gen-tests   ─┼─→ EnqueueHandler.EnqueueManualGenTests
  dtworkflow gen-tests     ─┤      │
  dtw gen-tests            ─┘      ↓
                               SQLite + Cancel-and-Replace + asynq enqueue
                                      ↓
[消费层]
  asynq.ProcessTask(gen_tests)
    │
    └── testService 已注入（修订 2：M4.1 已落地，原计划 M4.2） → TestExecutor.Execute
                        │
                        ├── resolveBaseRef(payload)
                        ├── resolveFramework(payload, cfg)
                        ├── buildPrompt(framework, ctx)
                        ├── pool.RunWithCommandAndStdin
                        ├── parseResult → TestGenOutput（含不变量校验）
                        └── createTestPR（M4.1 占位返回零值；M4.2 完整实装）
```

> 原计划 M4.1 阶段 Processor 未注入 `testService`，gen_tests 走 `pool.Run` default（即 `worker/container.go` 的基础 gen_tests prompt）。**修订 2：该过渡死路径已被移除**，M4.1 不再存在"gen_tests 走 pool.Run default"这条分支。

### 3.3 M4.1 vs M4.2 边界（精要）

> 修订 2 变更摘要：原「❌」改为「✅」的条目共 3 项（均为决策 D11 带来）——`WithTestService` 注入、Processor 错误分发（7 条 test sentinel + parse 降级）、`adaptTestResult` 适配器。

| 能力 | M4.1 | M4.2 |
|------|------|------|
| Service 代码 + 单测 | ✅ | — |
| Prompt（Java + Vue + 公共段） | ✅ | — |
| `TestGenOutput` schema + 不变量 | ✅ | — |
| 手动入口（CLI / API / dtw） | ✅ | — |
| `EnqueueManualGenTests` + Cancel-and-Replace | ✅ | — |
| `test_gen` 配置段 + 校验 | ✅ | — |
| Prompt 里的"既有测试 / 增量 commit / 最后一刻 push"指令 | ✅ | — |
| `entrypoint.sh` `gen_tests` 分支：origin 脱敏 + credential helper + git identity + 构建缓存重定向 + **pre-push hook 限 auto-test/\***（决策 D10 + 修订 2 第 5 项） | ✅ | — |
| **`WithTestService` Processor 注入 + 7 条 sentinel 错误分发 + `adaptTestResult`**（决策 D11，修订 2：原计划 M4.2，**提前到 M4.1**） | ✅ | — |
| `buildStartMessage` / `buildNotificationMessage` case + `internal/notify` 新增 `EventGenTestsStarted/Done/Failed` 事件 | ❌ | ✅ |
| 容器内测试验证 + 自修正循环（真实跑 mvn/npm） | ❌（prompt 已写，Claude 可按指令模拟） | ✅ |
| `createTestPR` 实际调用（M4.1 签名已冻结，M4.2 不得修改签名） | ❌（函数签名占位，Execute 末尾调用拿到零值） | ✅ |
| `test_gen_results` 表 | ❌ | ✅ |

---

## 4. 组件

### 4.1 `TestExecutor` 接口（`internal/queue/processor.go`）

```go
type TestExecutor interface {
    Execute(ctx context.Context, payload model.TaskPayload) (*test.TestGenResult, error)
    WriteDegraded(ctx context.Context, payload model.TaskPayload, result *test.TestGenResult) error
}

func WithTestService(svc TestExecutor) ProcessorOption { // 修订 2：M4.1 已激活
    return func(p *Processor) { p.testService = svc }
}
```

### 4.2 `test.Service` 结构

**窄接口**（仅所需 Gitea API / 执行器 / 配置）：

```go
// 仓库信息/ref 校验
type RepoClient interface {
    GetRepo(ctx context.Context, owner, repo string) (*gitea.Repository, *gitea.Response, error)
    GetBranch(ctx context.Context, owner, repo, branch string) (*gitea.Branch, *gitea.Response, error)
}

// 复用 M3.5 PR 创建能力；*gitea.Client 已满足
type PRClient interface {
    CreatePullRequest(ctx context.Context, owner, repo string,
        opts gitea.CreatePullRequestOption) (*gitea.PullRequest, *gitea.Response, error)
    ListRepoPullRequests(ctx context.Context, owner, repo string,
        opts gitea.ListPullRequestsOptions) ([]*gitea.PullRequest, *gitea.Response, error)
}

type TestPoolRunner interface {
    RunWithCommandAndStdin(ctx context.Context, payload model.TaskPayload,
        cmd []string, stdinData []byte) (*worker.ExecutionResult, error)
}

type TestConfigProvider interface {
    // ResolveTestGenConfig 返回合并后的配置：全局 Config.TestGen + RepoConfig.TestGen 覆盖。
    // 返回的 TestGenOverride.Enabled 是 *bool：nil 表示未覆盖（调用方按默认启用处理），
    // *false 表示显式禁用，*true 表示显式启用。
    ResolveTestGenConfig(repoFullName string) config.TestGenOverride
    GetClaudeModel() string
    GetClaudeEffort() string
}

type Service struct {
    gitea    RepoClient
    prClient PRClient        // M3.5 复用；M4.1 签名占位、M4.2 使用
    pool     TestPoolRunner
    cfgProv  TestConfigProvider
    logger   *slog.Logger
}

// ServiceOption: WithServiceLogger / WithPRClient
// NewService(gitea, pool, cfgProv, opts...) 对 gitea/pool/cfgProv panic 校验（同 review/fix 惯例）
```

**`Execute` 骨架**：

```
1. 前置校验（validateModule，含 module_scope 白名单校验，详见 §4.5.a）
2. resolveBaseRef(payload)：若 payload.BaseRef 空 → Gitea GetRepo → default_branch
3. resolveFramework(payload, cfg)：探测 module/pom.xml 或 package.json → Framework 枚举
4. buildPrompt(framework, ctx)：Java 或 Vue + 公共段
5. pool.RunWithCommandAndStdin
6. parseResult(output)：外层 CLI 信封 → TestGenOutput → validateSuccessfulTestGenOutput
7. if !InfoSufficient / !Success → 对应 sentinel
8. createTestPR（M4.1 占位签名已冻结，见下方；M4.2 实装）
```

**`createTestPR` 占位签名（M4.1 冻结，M4.2 不得修改）**：

```go
// createTestPR 在 Claude 完成 push 后调用 Gitea API 创建 PR。
// M4.1：占位实现，直接返回零值，签名冻结。
// M4.2：实装调用 PRClient.CreatePullRequest + 幂等查询 ListRepoPullRequests。
// 签名冻结的原因：M4.1 单测会以此签名 mock；M4.2 实装若改签名会破坏既有测试与调用点。
func (s *Service) createTestPR(ctx context.Context, payload model.TaskPayload,
    result *TestGenResult) (prNumber int64, prURL string, err error) {
    // M4.1: 占位
    return 0, "", nil
}
```

**`WriteDegraded`（M4.1 实现，no-op 降级回写）**：

```go
// WriteDegraded M4.1 实现。
// 修订 2：由于 M4.1 已装配 testService 并分发 test.ErrTestGenParseFailure 到 WriteDegraded，
// 本方法在 M4.1 运行时 **会被调用**（当外层 JSON 解析失败且重试耗尽时）。
// 但 M4.1 无通知集成，故实际行为仍是"仅记录结构化日志（repo / module / raw_output_len /
// has_parse_error）"；M4.2 引入通知事件后把 RawOutput[:2048] 写到飞书 metadata。
// 设计原则：接口形状稳定，M4.2 扩展日志为真实写回时无需改签名。
func (s *Service) WriteDegraded(ctx context.Context, payload model.TaskPayload,
    result *TestGenResult) error {
    if result == nil { return nil }
    s.logger.ErrorContext(ctx, "gen_tests 降级回写（M4.1 no-op，仅记录原始输出）",
        "repo", payload.RepoFullName,
        "module", payload.Module,
        "raw_output_len", len(result.RawOutput),
        "has_parse_error", result.ParseError != nil,
    )
    return nil
}
```

### 4.3 `TestGenOutput` 完整 schema

```go
type TestGenOutput struct {
    Success            bool            `json:"success"`
    InfoSufficient     bool            `json:"info_sufficient"`
    MissingInfo        []string        `json:"missing_info,omitempty"`
    Analysis           *GapAnalysis    `json:"analysis,omitempty"`
    TestStrategy       string          `json:"test_strategy,omitempty"`
    GeneratedFiles     []GeneratedFile `json:"generated_files,omitempty"`
    CommittedFiles     []string        `json:"committed_files,omitempty"` // 已 commit + 验证通过
    SkippedTargets     []SkippedTarget `json:"skipped_targets,omitempty"`
    TestResults        *TestRunResults `json:"test_results,omitempty"`
    VerificationPassed bool            `json:"verification_passed"`
    BranchName         string          `json:"branch_name,omitempty"`
    CommitSHA          string          `json:"commit_sha,omitempty"` // 分支 head（最后 push 的）
    FailureReason      string          `json:"failure_reason,omitempty"`
    RetryRounds        int             `json:"retry_rounds,omitempty"`
}

type GapAnalysis struct {
    UntestedModules []UntestedModule      `json:"untested_modules"`
    ExistingTests   []ExistingTestSummary `json:"existing_tests,omitempty"`
    ExistingStyle   string                `json:"existing_style"`
    PriorityNotes   string                `json:"priority_notes"`
}

type UntestedModule struct {
    Path     string `json:"path"`
    Kind     string `json:"kind"`     // service/controller/util/component/composable/store
    Priority string `json:"priority"` // high/medium/low
    Reason   string `json:"reason"`
}

type ExistingTestSummary struct {
    TestFile    string   `json:"test_file"`
    TargetFiles []string `json:"target_files"`
    Framework   string   `json:"framework"`
}

type GeneratedFile struct {
    Path        string   `json:"path"`
    Operation   string   `json:"operation"`   // "create" | "append"
    Description string   `json:"description"`
    Framework   string   `json:"framework"`   // junit5 / vitest
    TargetFiles []string `json:"target_files"`
    TestCount   int      `json:"test_count"`
}

type SkippedTarget struct {
    Path   string `json:"path"`
    Reason string `json:"reason"` // time_budget_exhausted / token_budget_exhausted / environment_issue / ...
}

// TestRunResults 与 fix.TestResults 语义类似但多 Framework 字段；命名刻意区别，
// 避免跨包 import 时 fix.TestResults / test.TestResults 同名消歧混乱。
type TestRunResults struct {
    Framework  string `json:"framework,omitempty"`
    Passed     int    `json:"passed"`
    Failed     int    `json:"failed"`
    Skipped    int    `json:"skipped"`
    AllPassed  bool   `json:"all_passed"`
    DurationMs int64  `json:"duration_ms,omitempty"`
}
```

**`validateSuccessfulTestGenOutput` 不变量**（`Success=true` 必须全部成立）：

- `InfoSufficient=true`
- `VerificationPassed=true` ∧ `TestResults.AllPassed=true` ∧ `TestResults.Failed=0`
- `BranchName != ""` ∧ `CommitSHA != ""`
- `len(CommittedFiles) > 0`，且 `CommittedFiles ⊆ GeneratedFiles.Path`
- `GeneratedFile.Operation ∈ {"create", "append"}`；`Operation="append" ⇒ len(TargetFiles) > 0`

`Success=false` 且 `len(CommittedFiles) > 0` → 视为"部分交付"，`FailureReason` 必填。

### 4.4 Prompt 结构

**公共段常量**（顺序即 prompt 顺序）：

| 常量 | 内容要点 |
|------|----------|
| `writePermissionNotice` | gen_tests 需要写权限；`allowedTools` 含 bash/edit/write |
| `testGenPreamble` | "你是资深测试工程师，目标是补全测试缺口" |
| `existingTestsInstruction` | **扫描 `**/*Test.java`、`**/*.{spec,test}.{ts,js}` 清单 → 吸收风格 → 列出 `existing_tests`。若无法确信某测试文件对应的源文件（特别是 Vue 命名不规范情况），`TargetFiles` 留空而非猜测，并在 `ExistingStyle` 注记原因。** |
| `gapAnalysisInstruction` | 识别无测试覆盖的模块，按"公共 API > 复杂业务逻辑 > 工具类"排序 |
| `noOverwriteInstruction` | **`CRITICAL: NEVER overwrite an existing test file. Either skip that target, or append (operation="append") preserving all existing content. Each GeneratedFile must have operation="create" (new file) or "append" (added to existing).`** |
| `incrementalCommitInstruction` | **每完成一个测试文件的生成 + 本地验证，立即 `git add <file> && git commit -m 'test: <target>'`。单次 commit 粒度 = 单个测试文件。所有目标处理完、全部通过后，才 `git push origin HEAD`。容器被 kill 时未 push 的工作将丢失。若 push 失败：立即终止任务，`Success=false`，`FailureReason="push failed: <错误信息>"`，不要重试 push、不要尝试 `-f` 强推、不要重写历史，任务重试由外层 asynq 驱动。** |
| `budgetAwareInstruction` | **token / 时间预算接近上限时，主动在 `skipped_targets` 中记录剩余目标并提前退出，保证已完成的 commit 能 push。** |
| `verificationInstruction` | 每 commit 前在本地运行对应测试；失败时最多修正 `{max_retry_rounds}` 轮；仍不过则回滚该文件并加入 `skipped_targets` |
| `outputJSONSchemaInstruction` | 完整 JSON schema 硬编码描述（同 `fix.analysisJSONSchemaInstruction` 风格） |

**Java 特有段**（`buildJavaPrompt`）：

- `javaTestingInstruction`：JUnit 5 + Mockito 约定；Service / Controller / 工具类的 Mock 策略；断言风格
- `javaVerificationCmd`：`mvn -pl <module> test -Dtest=<ClassName>` 单文件验证命令

**Vue 特有段**（`buildVuePrompt`）：

- `vueTestingInstruction`：Vitest + Vue Test Utils；组件 / Composable / Store / 工具函数差异
- `vueVerificationCmd`：`npx vitest run <file>` 或 `pnpm vitest run <file>` 单文件验证命令

**`resolveFramework(payload, cfg)` 规则**：

1. `cfg.TestFramework` 显式设定 → 直接用
2. 扫 `module/pom.xml` 存在 → Java
3. 扫 `module/package.json` 存在且含 `vitest` / `vue` 依赖 → Vue
4. 两者都在 `module` 根下 → 返回 `ErrAmbiguousFramework`，要求用户缩窄 `--module` 或设 `test_framework`
5. 都不在 → 回退仓库根目录同样探测；仍无 → `ErrNoFrameworkDetected`

### 4.5 手动触发入口

**a) 配置扩展**（`internal/config/config.go`）：

```go
type TestGenOverride struct {
    // Enabled 使用指针语义，与 ReviewOverride.Enabled 一致：
    // nil = 未覆盖（回退到全局）；*false = 显式关闭；*true = 显式启用。
    // 普通 bool 类型会让 RepoConfig 的零值 false 错误覆盖全局 true，
    // 踩的是与 review 相同的坑，此处沿用相同修法。
    Enabled        *bool  `mapstructure:"enabled"`

    // ModuleScope：仓库级"允许做 gen_tests 的模块前缀白名单"（护栏）。
    // 语义：CLI `--module` 或 API `module` 参数必须以此字符串为前缀，
    // 否则 validateModule 返回 ErrModuleOutOfScope（见 §6.1）。
    // 留空 = 无白名单限制（允许任意合法 module 路径）。
    // 不作默认模块：CLI 未传 `--module` 时仍按"整仓生成"处理（module=""），
    // 而非自动套用 ModuleScope 值，避免 CLI 与配置语义混淆。
    ModuleScope    string `mapstructure:"module_scope"`

    MaxRetryRounds int    `mapstructure:"max_retry_rounds"` // 默认 3，合法范围 [1, 10]
    TestFramework  string `mapstructure:"test_framework"`   // "" / "junit5" / "vitest"
}
// 加入 Config.TestGen + RepoConfig.TestGen（同 review / notify 模式）
```

**默认值注册**（`config.WithDefaults()` 追加）：

```go
m.v.SetDefault("test_gen.max_retry_rounds", 3)
// 其余字段（Enabled/ModuleScope/TestFramework）零值即合法默认，无需显式注册
```

**配置校验新规则**（`internal/config/validate.go`）：

- `max_retry_rounds` 必须在 [1, 10]（默认值 3 由 `WithDefaults` 注册；用户若显式写 `0` 或 `11+` 启动时拒绝）
- `test_framework` 合法值：空串、`"junit5"`、`"vitest"`
- `module_scope` 不能以 `/` 开头，不能含 `..`
- 上述规则对 `Config.TestGen` 与 `RepoConfig[*].TestGen` 同等生效

**b) `EnqueueManualGenTests`**（`internal/queue/enqueue.go`）：

```go
func (h *EnqueueHandler) EnqueueManualGenTests(ctx context.Context,
    payload model.TaskPayload, triggeredBy string) (string, error) {
    payload.TaskType = model.TaskTypeGenTests
    payload.DeliveryID = generateManualDeliveryID()

    // listActiveGenTestsTasks 是 EnqueueHandler 的私有方法（同 listActivePRTasks），
    // 内部调用 store.FindActiveGenTestsTasks（§4.5.c）完成底层查询。
    activeTasks := h.listActiveGenTestsTasks(ctx, payload.RepoFullName, payload.Module)

    record := &model.TaskRecord{
        TaskType:     model.TaskTypeGenTests,
        Priority:     model.PriorityLow, // 对齐已有 PriorityLow=测试生成
        RepoFullName: payload.RepoFullName,
        DeliveryID:   payload.DeliveryID,
        TriggeredBy:  triggeredBy,
    }
    if err := h.enqueueTask(ctx, payload, record); err != nil { return "", err }
    h.cancelTasks(ctx, activeTasks)
    return record.ID, nil
}

// listActiveGenTestsTasks EnqueueHandler 私有 helper，封装错误日志，保持与
// listActivePRTasks / listActiveIssueTasks 一致的代码风格。
func (h *EnqueueHandler) listActiveGenTestsTasks(ctx context.Context,
    repoFullName, module string) []*model.TaskRecord {
    tasks, err := h.store.FindActiveGenTestsTasks(ctx, repoFullName, module)
    if err != nil {
        h.logger.WarnContext(ctx, "查找活跃 gen_tests 任务失败，跳过取消",
            "repo", repoFullName, "module", module, "error", err)
        return nil
    }
    return tasks
}
```

**c) `store` 新方法**（`internal/store/store.go` 接口 + `sqlite.go` 实现）：

```go
FindActiveGenTestsTasks(ctx context.Context, repoFullName, module string) ([]*model.TaskRecord, error)
```

SQL 查询：

```sql
SELECT ... FROM tasks
WHERE task_type = 'gen_tests'
  AND repo_full_name = ?
  AND COALESCE(json_extract(payload, '$.module'), '') = ?
  AND status IN ('pending', 'queued', 'running')
ORDER BY created_at ASC
```

**关键实现点：`COALESCE(..., '')` 不可省略**。`TaskPayload.Module` 使用 `omitempty` 标签（`internal/model/task.go:96`），整仓生成（`--module` 未传）时 JSON 中**根本没有 `module` 字段**，`json_extract(payload, '$.module')` 返回 `NULL`；若直接用 `= ?` 比较空串，`NULL = ''` 在 SQLite 恒假，会导致两次无 `--module` 的 `dtw gen-tests` 并发放行、Cancel-and-Replace 失效。`COALESCE(..., '')` 将 NULL 归一化为空串后再比较，确保"整仓粒度"与"具体 module 粒度"的 Cancel-and-Replace 都正确生效。

**d) REST API**（`internal/api/test_handler.go`）：

```
POST /api/v1/gen-tests
Body: {owner, repo, module?, ref?, framework?}
Response: 202 {task_id, status_url}
```

中间件认证沿用 M3.3.1；错误格式沿用 `response.go` 统一 envelope。

**e) 服务端 CLI**（`internal/cmd/gen_tests.go`）：

```
dtworkflow gen-tests --owner o --repo r [--module .] [--ref main] [--framework junit5|vitest]
```

**f) 瘦客户端**（`internal/dtw/cmd/gen_tests.go`）：

```
dtw gen-tests --repo o/r [--module .] [--ref main] [--framework ...] [--wait / --no-wait] [--json]
```

支持 `--wait`（默认）轮询任务最终状态、`--json` 输出结构化结果（沿用 M3.3.1 模式）。

### 4.6 `entrypoint.sh` `gen_tests` 分支改造（决策 D10，M4.1 落地）

M4.1 prompt 指令要求 Claude 在容器内执行 `git commit` 与 `git push origin HEAD:auto-test/<module>-<ts>`。当前 `build/docker/entrypoint.sh:116-118` 的 `gen_tests` 分支仅打印一行日志，缺失以下必需配置，若不补齐则真实执行瞬间失败：

1. **git identity 缺失** → `git commit` 报 `Please tell me who you are`；
2. **credential helper 缺失** → `git push` 401；
3. **origin URL 带 token** → `.git/config` 持久化凭证到运行期（虽在容器销毁后丢失，但期间 Claude 可通过 Bash 读取）；
4. **Maven/Gradle 缓存落在 `/tmp` tmpfs** → 首次全量拉依赖触发 OOM；
5. **Claude 误推非 `auto-test/*` 分支** → 可能污染仓库默认分支（修订 2 新增的第 5 项防御纵深）。

**改造方案**：将 `fix_issue` 分支（`entrypoint.sh:89-115`）的完整凭证/身份/缓存配置复制到 `gen_tests` 分支，**额外追加 pre-push hook**，结果如下：

```bash
gen_tests)
    log "测试生成任务，使用默认分支（将在容器内由 Claude 创建 auto-test/<module>-<ts> 分支）"
    # 安全加固：origin URL 脱敏（避免 token 持久化到 .git/config）
    git remote set-url origin "${REPO_CLONE_URL}"
    # credential helper 按需注入 token，容器销毁即失效
    CRED_HELPER_SCRIPT="/tmp/.git-credential-helper"
    cat > "${CRED_HELPER_SCRIPT}" <<HELPER
#!/bin/sh
echo "username=token"
echo "password=${GITEA_TOKEN}"
HELPER
    chmod 700 "${CRED_HELPER_SCRIPT}"
    git config --global credential.helper "${CRED_HELPER_SCRIPT}"
    git config --global user.name "DTWorkflow Bot"
    git config --global user.email "dtworkflow-bot@noreply.local"
    # 修订 2：安全加固 —— 仅允许推送到 auto-test/*，防止误推默认分支
    mkdir -p .git/hooks
    cat > .git/hooks/pre-push <<'HOOK'
#!/bin/sh
while read -r local_ref local_sha remote_ref remote_sha
do
    case "${remote_ref}" in
        refs/heads/auto-test/*)
            ;;
        *)
            echo "ERROR: gen_tests may only push to refs/heads/auto-test/*" >&2
            exit 1
            ;;
    esac
done
HOOK
    chmod +x .git/hooks/pre-push
    # 构建缓存重定向（避免 tmpfs 溢出）
    export MAVEN_OPTS="${MAVEN_OPTS:--Dmaven.repo.local=/workspace/.m2/repository}"
    export GRADLE_USER_HOME="${GRADLE_USER_HOME:-/workspace/.gradle}"
    log "测试生成模式已启用（origin URL 已脱敏 + credential helper + git identity + auto-test push guard + build cache redirect）"
    ;;
```

**安全等价性**：前 4 项改造与 `fix_issue` 分支完全对称，安全面不扩大：`GITEA_TOKEN` 仍在脚本末尾被 `unset`（`entrypoint.sh:182`），credential helper 脚本在容器销毁后连同 `/tmp` tmpfs 一并蒸发，不写入镜像层。**第 5 项 pre-push hook 是 `gen_tests` 独有**（`fix_issue` 推送到 `auto-fix/issue-{id}` 是 `fix.Service.createPR` 外层控制的，容器内 Claude 不直接 push；但 `gen_tests` 的 push 由容器内 Claude 主动执行，必须加 hook 防御）。

**M4.1 期间的运行时行为**（修订 2 更新）：由于 M4.1 已装配 `testService`（决策 D11），gen_tests 走 `test.Service.Execute` → `pool.RunWithCommandAndStdin` → 真实执行 Java/Vue prompt。该 prompt 指示 Claude 增量 commit + 最后一刻 push 到 `auto-test/*`，上述 5 项配置在 M4.1 期间**会被真实触发**：
- origin URL 脱敏：若 Claude 尝试读 `.git/config` 看到的是无 token 版本（配合 prompt "禁止读凭证文件"）
- credential helper：Claude `git push` 时自动注入 token
- git identity：Claude `git commit` 不再报错
- 构建缓存：`mvn` / `vitest` 依赖下载落到 `/workspace` 而非 tmpfs
- pre-push hook：即使 Claude 生成了错误的 push 命令（例如被 prompt injection 诱导），也无法推送非 `auto-test/*` 分支

**注意**：`createTestPR` 在 M4.1 仍占位，故即使 Claude 成功 push 了 `auto-test/<module>-<ts>` 分支，Gitea 侧也不会自动创建 PR。用户需手动从分支创建 PR，或等 M4.2 激活 `createTestPR`。这是 M4.1 路由接通 / PR 未接通的自然产物。

**测试覆盖**：`build/docker/entrypoint_test.sh` 追加 `gen_tests` 分支的 5 项断言（修订 2 增加后两项）：
- `git config credential.helper` 非空且指向 `/tmp/.git-credential-helper`
- `git config user.email` == `dtworkflow-bot@noreply.local`
- `git remote get-url origin` 不包含 `${GITEA_TOKEN}`
- **`.git/hooks/pre-push` 存在且可执行**
- **pre-push hook 对 `refs/heads/main` / `refs/heads/master` / `refs/heads/auto-test/foo` 的 pattern match 行为符合预期**（前两者 exit 1，后者 exit 0）

---

## 5. 数据流

### 5.1 手动触发 happy path（修订 2：M4.1 已激活路由；仅 createTestPR 占位 + 通知未接）

```
[Client]                [Server API]            [Queue]                [asynq Worker]          [Container]
dtw gen-tests
  --repo o/r             │                       │                       │                       │
  --module srv          ─┼→ POST /api/v1/        │                       │                       │
                         │  gen-tests            │                       │                       │
                         ├─ test_handler         │                       │                       │
                         │  构造 TaskPayload    ─┼→ EnqueueManual        │                       │
                         │                       │  GenTests             │                       │
                         │                       ├─ listActiveGenTests   │                       │
                         │                       │  Tasks → SQLite       │                       │
                         │                       ├─ enqueueTask          │                       │
                         │                       │   ├─ CreateTask(pending)                     │
                         │                       │   ├─ asynq.Enqueue   → Redis                 │
                         │                       │   └─ UpdateTask(queued)                      │
                         │                       ├─ cancelTasks(旧)       │                       │
                         ←─ 202 {task_id}       ─┤                       │                       │
  --wait 轮询           ─┼→ GET /api/v1/         │                       │                       │
                         │  tasks/:id            │                       │                       │
                         │                       │   ProcessTask ────────→                       │
                         │                       │   resolveBaseRef                              │
                         │                       │   resolveFramework                            │
                         │                       │   TestService.Execute                         │
                         │                       │    └─ pool.RunWithCmdStdin ─────────────────→ [launched]
                         │                       │                                                │ 容器内 Claude：
                         │                       │                                                │   1. 扫既有测试
                         │                       │                                                │   2. GapAnalysis
                         │                       │                                                │   3. FOR target:
                         │                       │                                                │      - 生成测试
                         │                       │                                                │      - 本地 mvn/vitest
                         │                       │                                                │      - 通过 → git commit
                         │                       │                                                │   4. 全部完成
                         │                       │                                                │   5. git push（最后一刻）
                         │                       │                                                │   6. stdout TestGenOutput
                         │                       │   parseResult ←──────────────────────────────[stdout]
                         │                       │   validateSuccessfulTestGenOutput              │
                         │                       │   createTestPR（M4.2）────────────────────→ Gitea API
                         │                       │   UpdateTask(succeeded)                       │
                         ←─ 200 {..result}      ─┤                                                │
```

**M4.1 实际行为**（修订 2 更新）：上图中 "TestService.Execute" **已接通**（`serve.go` 通过 `queue.WithTestService(testSvc)` 注入）；Processor 执行到 `TaskTypeGenTests` 时走 `testService.Execute` → 真实 Java/Vue prompt → parseResult → validateSuccessfulTestGenOutput。用户看到 `status=succeeded` 时，`record.Result` 是结构化 `TestGenOutput` 的 raw JSON 文本（已通过不变量校验）。

**M4.1 与 M4.2 的真实差异**（修订 2 后）：
- `createTestPR` 仍占位 → Claude 成功 push 了 `auto-test/*` 分支，但 Gitea 侧无 PR 实体；
- 容器内 `mvn test` / `vitest run` 是否真实执行取决于 Claude 按 prompt 指令的行为（prompt 已明确指示必须本地跑 + 提交前验证，但未做强制校验）；
- 无通知事件 → 任务完成 / 失败不会发飞书（`buildNotificationMessage` 仍不处理 `gen_tests`）。
- M4.2 补齐以上三点后即为完整能力。

### 5.2 Processor 路由决策表（M4.1 实际落地，修订 2 更新）

| payload.TaskType | reviewService 已注入 | fixService 已注入 | testService 已注入 | 实际分支 |
|------------------|---------------------|-------------------|--------------------|----------|
| review_pr | ✅ | — | — | `reviewService.Execute` |
| analyze_issue | — | ✅ | — | `fixService.Execute` |
| fix_issue | — | ✅ | — | `fixService.Execute` |
| **gen_tests（M4.1，修订 2 后）** | — | — | **✅** | **`testService.Execute`**（`createTestPR` 占位；PR 不真实创建，其他链路全通） |
| gen_tests（M4.2+） | — | — | ✅ | `testService.Execute`（完整，含 PR 创建 + 通知 + test_gen_results 表） |
| gen_daily_report | — | — | — | `pool.Run` default |

> 原计划 M4.1 的 gen_tests 走 `pool.Run` default（过渡死路径），修订 2 后该分支已消失。

### 5.3 Cancel-and-Replace 时序

```
T0  dtw gen-tests ... --module srv    → task1 created, status=queued
T1  asynq pulls task1 → Processor     → task1 status=running
T2  容器 launched，Claude 开始扫既有测试
T3  dtw gen-tests ... --module srv（第二次!）
     ↓
    EnqueueManualGenTests
    ├─ listActiveGenTestsTasks(repo, "srv") → [task1: running]
    ├─ enqueueTask(task2) → status=queued
    └─ cancelTasks([task1])
         ├─ canceller.CancelProcessing(task1.AsynqID)
         │    └─ asynq 触发 ctx cancel → pool 停止容器
         │       容器被 kill ← prompt "最后一刻才 push" 保证分支未创建
         │       task1 无副作用
         └─ SQLite UpdateTask(task1, cancelled, "被同一 gen_tests 的新任务取代")
T4  task2 被 asynq pull，正常执行
```

**关键性质**：即使 task1 在 T2-T3 期间已在容器里 commit 了多个文件，被 kill 时这些 commit 仍在本地临时分支里——容器销毁后**全部丢弃，无远程副作用**。task2 启动时是干净状态。

### 5.4 容器内 Claude 内部循环（由 M4.1 prompt 塑形，M4.2 真实运行）

```
[Container init] entrypoint.sh：clone repo + checkout <ref> + 创建工作分支 auto-test/<module>-<ts>

[Claude Agent loop]
  Step 1: 扫描既有测试
    ├─ find **/*Test.java **/*.{spec,test}.{ts,js}
    ├─ 读取各文件首部 → 识别 framework / 断言风格 / Mock 约定
    └─ 填充 GapAnalysis.ExistingTests + ExistingStyle

  Step 2: 缺口分析
    ├─ 列源代码 → 减去 ExistingTests.TargetFiles → untested set
    ├─ 按优先级（公共 API > 复杂业务 > 工具类）排序
    └─ 填充 GapAnalysis.UntestedModules → target_queue

  Step 3: 按目标循环生成
    FOR target IN target_queue:
      ├─ 判断 Operation：已有测试文件 & 可扩展 → "append"；否则 → "create"
      ├─ 生成测试代码（write / edit 文件）
      ├─ 本地单文件验证循环（最多 max_retry_rounds 轮）：
      │    ├─ 执行：mvn -pl <mod> test -Dtest=<cls> | npx vitest run <file>
      │    ├─ 通过 → git add <file> && git commit → append 到 committed_files，break
      │    └─ 失败 → 读错误 → 修正测试 → continue
      ├─ retry 耗尽 → git checkout -- <file>（回滚）→ 加入 skipped_targets
      └─ 预算检测（time / token 接近上限）→ 剩余 target 全部入 skipped_targets，跳出

  Step 4: 最后一刻 push
    IF len(committed_files) > 0:
      git push origin HEAD  ← 唯一的网络写操作
      VerificationPassed = true
    ELSE:
      Success = false, FailureReason = "无可交付测试"

  Step 5: 输出 TestGenOutput JSON 到 stdout
```

**"最后一刻 push" 的正确性**：

1. 容器被 kill（超时 / Cancel / 网络中断）→ 分支未创建 → 无残留 PR、无半成品
2. `len(committed_files) > 0` 但 push 失败（网络瞬断）→ 整任务失败进入 asynq 重试 → 下次容器启动从头开始
3. M4.2 若要加"抢救未 push 工作"能力，只需给容器挂载持久卷；prompt 无需改动

---

## 6. 错误处理

### 6.1 Sentinel 错误（`internal/test/errors.go`）

```go
var (
    // 参数层：用户可修正 → 上层 SkipRetry
    ErrInvalidModule       = errors.New("module 路径不合法")
    ErrModuleOutOfScope    = errors.New("module 路径不在 test_gen.module_scope 白名单前缀下")
    ErrModuleNotFound      = errors.New("module 子路径不存在于仓库")
    ErrInvalidRef          = errors.New("--ref 指定但 Gitea 无法解析")
    ErrAmbiguousFramework  = errors.New("module 下同时存在 pom.xml 与 package.json，请缩窄 --module 或指定 --framework")
    ErrNoFrameworkDetected = errors.New("未检测到 JUnit / Vitest 配置，请设置 test_gen.test_framework")

    // 业务层：Claude 判定或结果失败 → SkipRetry
    ErrInfoInsufficient    = errors.New("Claude 判定信息不足，无法生成测试")
    ErrTestGenFailed       = errors.New("测试生成执行失败（Success=false）")

    // 结果解析层：结构 / 不变量失败 → 允许重试，耗尽后降级
    ErrTestGenParseFailure = errors.New("TestGenOutput 解析失败")
)
```

### 6.2 `parseResult` + 不变量校验

外层 CLI 信封 → 内层 `TestGenOutput` → `validateSuccessfulTestGenOutput`，与 fix 包完全对称。

**不变量校验完整清单**（`Success=true` 必须全部成立）：

| 校验项 | 失败消息 |
|--------|----------|
| `InfoSufficient=true` | `"Success=true 但 InfoSufficient=false"` |
| `VerificationPassed=true` | `"Success=true 但 VerificationPassed=false"` |
| `TestResults != nil && AllPassed && Failed == 0` | `"测试未全部通过 all_passed=%v failed=%d"` |
| `BranchName != ""` | `"branch_name 为空"` |
| `CommitSHA != ""` | `"commit_sha 为空"` |
| `len(CommittedFiles) > 0` | `"committed_files 为空"` |
| `CommittedFiles ⊆ GeneratedFiles.Path` | `"committed_files 包含未在 generated_files 声明的路径 %q"` |
| `GeneratedFile.Operation ∈ {"create","append"}` | `"GeneratedFile.Operation 非法: %q"` |
| `Operation="append" ⇒ len(TargetFiles) > 0` | `"append 操作必须声明 target_files"` |

### 6.3 防 prompt-injection

**约束**（照搬 M3.5 fix 的决定）：`ParseError` 详情**仅走日志**，不进返回 `error` 的消息，避免被后续写入 `record.Error`，再进入通知 / PR 描述。

### 6.4 重试决策表

| 触发点 | sentinel / 情况 | 重试? | M4.1 降级路径 | M4.2 预期路径 |
|--------|----------------|-------|--------------|---------------|
| 参数非法 | `ErrInvalidModule` / `ErrModuleOutOfScope` / `ErrModuleNotFound` / `ErrInvalidRef` / `ErrAmbiguousFramework` / `ErrNoFrameworkDetected` | SkipRetry | n/a | n/a — 直接 failed |
| 容器启动 / 基础设施 | 非 sentinel 的普通 error | ✅ 继续重试 | n/a | n/a |
| 容器非零退出码 | `execResult.ExitCode != 0` | `shouldRetry(ctx)` | n/a | n/a |
| Claude 判信息不足 | `ErrInfoInsufficient` | SkipRetry | **no-op**（仅日志） | 飞书通知 + CLI job 状态附 `MissingInfo` |
| Claude 返回 Success=false | `ErrTestGenFailed` | SkipRetry | **no-op** | 同上，附 `FailureReason` + `SkippedTargets` |
| JSON / 不变量解析失败 | `ErrTestGenParseFailure` | ✅ 重试，耗尽后降级 | `WriteDegraded` **no-op** | 飞书通知 metadata 含 `RawOutput[:2048]` snippet |
| PR 创建失败（push 已成功） | 普通 error | ✅ 重试（`findExistingTestPR` 幂等保护） | n/a | n/a |

**M4.1 与重试表的交互**（修订 2 更新）：M4.1 的 Processor **已装配** `testService` 并实装所有 sentinel 分发（见 §6.5），**此表在 M4.1 是真实运行时行为**。`service_test.go` + `processor_test.go` 共同验证 sentinel 链路完整性。

### 6.5 Processor 路由改动（修订 2：M4.1 已落地）

> 原计划归属 M4.2。决策 D11 提前到 M4.1 后，以下代码块已全部落地在 `internal/queue/processor.go`；`internal/queue/processor_test.go` 对每条 sentinel 分发都有对应测试。

```go
// M4.1 落地（processor.go）
switch {
case payload.TaskType == model.TaskTypeGenTests && p.testService != nil:
    testResult, runErr = p.testService.Execute(ctx, payload)
    if testResult != nil { result = adaptTestResult(testResult) }
// ...
}

// 错误分发（M4.1 落地，共 7 条 SkipRetry + 1 条 parse 降级）
if errors.Is(runErr, test.ErrInvalidModule)        { return p.handleSkipRetryFailure(..., "module 路径不合法") }
if errors.Is(runErr, test.ErrModuleOutOfScope)     { return p.handleSkipRetryFailure(..., "module 超出 module_scope 白名单") }
// ErrModuleNotFound 已定义但 service.validateModule 在 M4.1 尚未真正调用文件存在性校验，
// 故 M4.1 的 Processor 暂未分发该 sentinel —— 这是**已知缺口**，由 code review 标记，
// 跟进工单在 M4.2 前补齐（validateModule 接入 RepoFileChecker + Processor 补一条 errors.Is）。
if errors.Is(runErr, test.ErrInvalidRef)           { return p.handleSkipRetryFailure(..., "ref 不存在") }
if errors.Is(runErr, test.ErrAmbiguousFramework)   { return p.handleSkipRetryFailure(..., "框架歧义") }
if errors.Is(runErr, test.ErrNoFrameworkDetected)  { return p.handleSkipRetryFailure(..., "未检测到框架") }
if errors.Is(runErr, test.ErrInfoInsufficient)     { return p.handleSkipRetryFailure(..., "信息不足") }
if errors.Is(runErr, test.ErrTestGenFailed)        { return p.handleSkipRetryFailure(..., "执行失败") }
if errors.Is(runErr, test.ErrTestGenParseFailure) && !shouldRetry(ctx) && testResult != nil {
    if wbErr := p.testService.WriteDegraded(ctx, payload, testResult); wbErr != nil {
        p.logger.ErrorContext(ctx, "test 解析失败降级回写失败", "error", wbErr)
    }
}
```

---

## 7. 测试覆盖

### 7.1 M4.1 新增测试文件清单

| 测试文件 | 覆盖目标 | 用例数（粗估） |
|---------|----------|----------------|
| `internal/test/service_test.go` | `Execute` 各分支 + `resolveFramework` + `resolveBaseRef` + `validateModule` + `WriteDegraded` | 18-25 |
| `internal/test/prompt_test.go` | `buildJavaPrompt` / `buildVuePrompt` 含指令关键片段 + 参数替换 | 12-15 |
| `internal/test/result_test.go` | `TestGenOutput` JSON 解析 + `validateSuccessfulTestGenOutput` 不变量各分支 | 20-25 |
| `internal/queue/enqueue_test.go`（追加） | `EnqueueManualGenTests` 幂等 + Cancel-and-Replace | 6-8 |
| `internal/store/sqlite_test.go`（追加） | `FindActiveGenTestsTasks` 查询正确性 | 4-5 |
| `internal/api/test_handler_test.go`（新） | `POST /api/v1/gen-tests` 成功 / 参数错误 / 认证失败 / 重复触发 | 10-12 |
| `internal/cmd/gen_tests_test.go`（新） | Cobra 标志解析、Payload 构造、`--json` 输出 | 8-10 |
| `internal/dtw/cmd/gen_tests_test.go`（新） | HTTP 客户端 + `--wait` 轮询 + `--no-wait` + `--json` | 8-10 |
| `internal/config/validate_test.go`（追加） | `test_gen` 配置校验 | 6-8 |

**合计：9 个测试文件 / 追加块，约 90-120 个测试用例。**

### 7.2 `service_test.go` 必覆盖分支

| 测试场景 | 预期返回 | 预期 sentinel |
|---------|---------|---------------|
| module 含 `..` / 绝对路径 | `error` | `ErrInvalidModule` |
| module 未以 `test_gen.module_scope` 前缀开头 | `error` | `ErrModuleOutOfScope` |
| module 子路径不存在（Gitea 404） | `error` | `ErrModuleNotFound` |
| `--ref` 指定但 GetBranch 404 | `error` | `ErrInvalidRef` |
| module 下同时 pom.xml + package.json | `error` | `ErrAmbiguousFramework` |
| 无 pom/package + 无 `test_framework` 配置 | `error` | `ErrNoFrameworkDetected` |
| pool.Run 容器启动错误 | `error`（普通） | 无 sentinel，允许重试 |
| 容器 ExitCode ≠ 0 | `*TestGenResult`, `err=nil` | `result.ExitCode` 被设置 |
| JSON 外层信封解析失败 | `error` | `ErrTestGenParseFailure` |
| JSON 内层 `TestGenOutput` 解析失败 | `error` | `ErrTestGenParseFailure` |
| 不变量：`Success=true` 但 `committed_files=[]` | `error` | `ErrTestGenParseFailure` |
| 不变量：`Operation="append"` 但 `target_files=[]` | `error` | `ErrTestGenParseFailure` |
| `InfoSufficient=false` | `error` | `ErrInfoInsufficient` |
| `Success=false` | `error` | `ErrTestGenFailed` |
| `Success=true` + createTestPR 占位 | `*TestGenResult, nil` | `PRNumber=0, PRURL=""` |
| 防 prompt-injection：ParseError 详情不进 error | — | `assert.NotContains(err.Error(), result.RawOutput)` |
| `NewService` 对 nil 依赖 panic | panic | — |
| `WriteDegraded(result=nil)` 不 panic | `nil` | — |
| `WriteDegraded` 记录 `raw_output_len` 日志 | — | slog handler 捕获断言 |

### 7.3 `prompt_test.go` 必覆盖

| 测试场景 | 断言 |
|---------|------|
| `buildJavaPrompt` 含公共段关键词 | `"git commit"` / `"最后一刻"` / `"**/*Test.java"` / `"NEVER overwrite"` |
| `buildVuePrompt` 含 Vitest 指令 | `"vitest run"` / `"*.spec."` |
| `max_retry_rounds` 占位符替换为配置值（默认 3） | prompt 含 `"最多修正 3 轮"` |
| `module` 参数正确转义注入 | `sanitizePromptInput` 调用 |
| `existing_tests` 指令存在 | `"existing_tests"` / `"吸收风格"` |
| `skipped_targets` 预算指令存在 | `"time_budget_exhausted"` / `"token_budget_exhausted"` |
| `operation` 枚举指令明确 | `"operation=\"create\""` / `"operation=\"append\""` |
| Java prompt 不含 Vitest 关键词（反之亦然） | `"vitest" NOT IN javaPrompt` |
| push 失败即终止指令存在 | `"push failed"` / `"不要重试 push"` / `"-f"` 的禁止说明 |
| `ExistingTests.TargetFiles` 不确信即留空的指令 | `"TargetFiles 留空"` / `"而非猜测"` |

### 7.4 跨包测试要点

**`queue/enqueue_test.go`**：

- `EnqueueManualGenTests` 无旧任务 → 创建 1 条 record，status=queued
- 两次触发同 `repo+module` → 第二次取消第一条（Cancel-and-Replace）
- 两次触发**不同** module → 并发放行
- `listActiveGenTestsTasks` 过滤 `task_type=gen_tests`，不误伤 review_pr / fix_issue
- `TriggeredBy` 注入 `"manual:<identity>"` 正确

**`api/test_handler_test.go`**：

- 合法 body → 202 + `{task_id, status_url}`
- 缺 `owner/repo` → 400 + `"code": "invalid_argument"`
- 错 Bearer token → 401
- body 含 `module: "../etc/passwd"` → 400
- 并发两次相同 body → 第二次仍 202（Cancel-and-Replace 在入队层做）

**`cmd/gen_tests_test.go` / `dtw/cmd/gen_tests_test.go`**：沿用 M3.3.1 的 review_pr / fix_issue 测试模式（`httptest.NewServer` mock API）。

### 7.5 M4.1 **刻意不测**（修订 2 更新）

| 不测项 | 原因 | 归属 |
|-------|------|------|
| Docker 容器真实启动（M4.1 期间是否真的启动了容器）| M4.1 的 `processor_test.go` 用 mock `TestPoolRunner` 返回预设 CLI JSON；真实 Docker 启停由 worker 包既有 E2E 负责 | M4.2 回归 |
| Claude CLI 真实调用（真实拉大模型） | 同上，mock CLI JSON 足够 | M4.2 回归 |
| `createTestPR` 实际调 Gitea CreatePullRequest | M4.1 函数占位返回 nil,"",nil；单测断言 `PRNumber=0, PRURL=""` | M4.2 |
| `findExistingTestPR` 幂等 | 同上 | M4.2 |
| 通知发送（gen_tests started / done / failed 飞书消息） | M4.1 `buildStartMessage` / `buildNotificationMessage` 未处理 `gen_tests`，相应 EventType 未定义 | M4.2 |
| `buildStartMessage` / `buildNotificationMessage` case 对 gen_tests 的分支 | 修订 2：仅 notify 事件未落地；Processor 路由已落地 | M4.2 |
| 容器超时后分支残留回收 | 持久卷策略 | M4.2 / 未来 |
| M4.3 的 PR-merged webhook 触发 | 不同 milestone | M4.3 |
| E2E（手动 CLI → 真实仓库 → 真实 PR） | 依赖 M4.2 容器执行能力 | M4.2 验收 |

### 7.6 验证标准（M4.1 完成态）

1. **`go test -race -cover ./internal/test/...` 通过**，覆盖率 ≥ 80%
2. **`go test -race ./...` 全套通过**（baseline 不被破坏）
3. **`make lint` 无新警告**
4. `golangci-lint` 对 `internal/test` 包无 `govet` / `errcheck` / `staticcheck` 问题
5. **手动回归**：
   - `dtworkflow gen-tests --owner x --repo y --module z` 能成功入队（SQLite 有 record，asynq 有 task）
   - `dtw gen-tests --repo x/y --module z --no-wait` 能通过 REST API 成功入队并返回 task_id
   - `dtw task get <task_id>` 能查询状态（修订 2：M4.1 实际行为 `status=succeeded` 时 `Result` 是结构化 `TestGenOutput` JSON；若 Claude 未产出合规 JSON 则 `status=failed` + error 含 `ErrTestGenParseFailure`）
   - 两次不加 `--module` 的 `dtw gen-tests --repo x/y` 连续触发：第一次 `status=cancelled`，第二次 `status=queued`（验证 `COALESCE` 对空 module 的 Cancel-and-Replace）
   - `dtworkflow.yaml` 里 `test_gen.max_retry_rounds: 0` 或 `11` 被 Config Validate 拒绝启动；留空时使用默认值 3
   - `test_gen.module_scope: "backend"` 配置下，`--module frontend/api` 的请求被 `ErrModuleOutOfScope` 拒绝
   - `build/docker/entrypoint_test.sh` 的 `gen_tests` 分支断言全部通过（credential helper / git identity / 脱敏 origin URL / **pre-push hook** / **pre-push 白名单行为**）

### 7.7 测试固件（`internal/test/testdata/`）

| 文件 | 内容 |
|------|------|
| `testgen_output_success.json` | 完整成功，3 generated files，全部 committed，测试全通过 |
| `testgen_output_partial_delivery.json` | Success=false，`committed_files` 2 个，`skipped_targets` 3 个（time_budget_exhausted） |
| `testgen_output_info_insufficient.json` | InfoSufficient=false，MissingInfo 列表 |
| `testgen_output_failure.json` | Success=false，FailureReason="某测试 3 轮修正未过" |
| `testgen_output_invalid_operation.json` | Operation="replace"（非法值），用于不变量测试 |

---

## 8. 规模预估

- **新增 Go 代码**：`internal/test/` ≈ 800-1100 行（含测试）；跨包追加 ≈ 300-500 行（修订 2 后 Processor 追加 `testService` 字段 + 7 条 sentinel 分发 + `adaptTestResult` 适配器 ≈ 50-80 行净增）
- **新增测试**：≈ 90-120 用例（修订 2 新增 `processor_test.go` 对 gen_tests sentinel 分支的用例，约 5-8 条）
- **触达文件**：约 14-16 个（新建 9，追加 4-6，基础设施 1：`build/docker/entrypoint.sh` 的 `gen_tests` 分支改造）
- **对既有模块的影响**（修订 2 更新）：
  - review / fix / worker / container 的**业务逻辑**未触；
  - `processor.go` 新增 `testService` 字段、`case payload.TaskType == model.TaskTypeGenTests && p.testService != nil` 分支、7 条 `errors.Is(runErr, test.ErrXxx)` 分发，原 review / fix 流水线不受影响（新增 switch case 在旧 case 之后，且 TaskType 互斥）；
  - `entrypoint.sh` 改动仅扩展 `gen_tests` 分支，不影响 `review_pr` / `analyze_issue` / `fix_issue` 分支；`entrypoint_test.sh` 追加 `gen_tests` 断言段，既有 review / fix 断言保持；
  - `serve.go` 装配层新增 8 行（`testgen.NewService(...)` + `queue.WithTestService(testSvc)`），与 review / fix 装配完全对称。

## 9. 风险与缓解

| 风险 | 缓解 |
|------|------|
| prompt 公共段过长导致 Claude token 消耗大 | 公共段常量化，按需拼接；Java/Vue 特有段分离，不相互污染 |
| Cancel-and-Replace 在容器运行中触发，`git push` 已开始 → 半成品分支 | prompt 约束"最后一刻 push"；push 是单次原子操作，时间窗极小；若仍发生，M4.2 的幂等 `findExistingTestPR` 兜底 |
| 既有测试风格混乱（多种 Mock 框架） | prompt 指令"吸收主流风格"；模棱两可时在 `ExistingStyle` 字段透明报告 |
| 框架探测误判（混合仓中 `module` 边界不清） | `ErrAmbiguousFramework` 明确要求用户缩窄 `--module` 或设 `test_framework`；不静默猜测 |
| ~~M4.1 的过渡期内 gen_tests 走旧 prompt 产出无用结果~~（**修订 2 已消除**：Processor 已注入 testService，M4.1 直接走新 prompt。保留此行作为决策轨迹） | 文档原说"过渡行为仅用于验证入队链路"；修订 2 后手动触发能产出真实 `TestGenOutput`，验收价值提升 |
| `test_gen_results` 表在 M4.1 未建，任务结果仅留 `tasks.result` 字段 | 接受；M4.2 建表时一次性迁移既有 `tasks.result` 中 gen_tests 数据 |
| `ExistingTests.TargetFiles` 反向推导不准（特别是 Vue `foo.spec.ts` 与 `foo.ts` / `useFoo.ts` 对应关系模糊） | prompt 追加指令"无法确信源文件对应关系时 `TargetFiles` 留空而非猜测，并在 `ExistingStyle` 字段注记原因"；单测固件 `testgen_output_success.json` 覆盖 `TargetFiles=[]` 的场景 |
| Claude push 失败后自作主张重试或 `-f` 强推，污染分支历史 | `incrementalCommitInstruction` 明示"push 失败 → 立即终止任务 + `Success=false`，不重试、不强推"；任务重试由外层 asynq 驱动 |
| `entrypoint.sh` `gen_tests` 分支改造使 Maven/Gradle 缓存挂到 `/workspace`（修订 2：M4.1 路由已接通，Claude 按 prompt 本地跑 mvn / vitest 时会真实读写该目录） | 接受；首次任务会触发依赖下载耗时较长（Java 仓库首轮可能触发 ~5 分钟），跨任务复用缓存后显著加速；容器销毁时缓存丢失属已知限制，M4.2 可考虑持久卷 |
| （修订 2 新增）Claude 按 prompt 指令**是否真实执行** `mvn test` / `vitest run` 无强制校验 | prompt 侧已明确"每 commit 前必须在本地运行对应测试并通过"；验证信任 Claude 的能力；M4.2 可在 `TestRunResults` 解析时加与容器 exec log 交叉验证的机制 |
| （修订 2 新增）`createTestPR` 占位导致 Claude 成功 push `auto-test/*` 分支但无 PR 实体 | 文档 §1.2 / §4.6 / §5.1 / §10 显式声明；M4.1 期间用户需手动从分支创建 PR，或等 M4.2 激活 `createTestPR` |

---

## 10. 验收清单

M4.1 结束前完成以下清单即可进入 M4.2（修订 2 更新：新增 Processor 装配相关 3 项）：

- [x] `internal/test/` 包建立，7 个文件全部存在且编译通过
- [x] `TestExecutor` 接口在 `internal/queue/processor.go` 定义，**`WithTestService` Option 已在 `serve.go` 实际装配并注入**（修订 2：原计划"预留"，实际已激活）
- [x] `TestGenOutput` 全套结构 + `validateSuccessfulTestGenOutput` 不变量实现（`TestRunResults` 而非 `TestResults`，避免与 `fix.TestResults` 同名冲突）
- [x] `createTestPR` 占位签名冻结（`(prNumber int64, prURL string, err error)`，M4.2 不得修改）
- [x] `buildJavaPrompt` / `buildVuePrompt` 覆盖 9 项指令关键片段（含 push 失败即终止的指令）
- [x] `resolveFramework` 覆盖 5 条规则
- [x] `EnqueueManualGenTests` + `listActiveGenTestsTasks`（EnqueueHandler 私有）+ `FindActiveGenTestsTasks`（Store 接口，SQL 使用 `COALESCE` 处理空 module）实现
- [x] `test_gen` 配置段 + 校验 + 仓库级覆盖（`Enabled *bool`；`max_retry_rounds` 默认 3 注册在 `WithDefaults`；`ModuleScope` 语义为白名单前缀）
- [x] `ErrModuleOutOfScope` sentinel + Service `validateModule` 中对 `ModuleScope` 的前缀校验
- [x] `POST /api/v1/gen-tests` handler + 路由注册 + 认证中间件兼容
- [x] `dtworkflow gen-tests` + `dtw gen-tests` 两套 CLI
- [x] `build/docker/entrypoint.sh` `gen_tests` 分支补齐凭证 helper + git identity + 构建缓存重定向（决策 D10）
- [x] **（修订 2 新增）`build/docker/entrypoint.sh` `gen_tests` 分支追加 `pre-push` hook，仅允许推送 `refs/heads/auto-test/*`**
- [x] `build/docker/entrypoint_test.sh` 追加 `gen_tests` 分支断言 **≥ 5 项**（credential helper / git identity / 脱敏 origin URL / **pre-push hook 存在 / pre-push hook 白名单行为**）
- [x] **（修订 2 新增）`processor.go` 装配 `testService` 字段 + `case TaskTypeGenTests && p.testService != nil` 分支 + 7 条 `errors.Is(..., test.ErrXxx)` SkipRetry 分发 + `test.ErrTestGenParseFailure` 降级回写**
- [x] **（修订 2 新增）`processor_test.go` 对 gen_tests 路由与所有 sentinel 分发分支的用例覆盖**
- [x] 全套单测通过，覆盖率 ≥ 80%
- [x] `configs/dtworkflow.example.yaml` 追加两处 `test_gen` 示例：全局 `test_gen:` 块 + `repos[*].test_gen:` 仓库级覆盖示例
- [x] `make lint` + `go test -race ./...` 全套通过

**跟进工单（不阻塞 M4.1 合入，在 M4.2 前解决）**：

- [ ] `service.validateModule` 接入 `RepoFileChecker.HasFile` 做 module 子路径存在性校验 + Processor 补 `errors.Is(runErr, test.ErrModuleNotFound)` 分发（code review 发现）
- [ ] `internal/test/service.go` 中 `safeOutput` + `internal/test/result.go` 中 `truncate` 两处死代码清理或接入 `WriteDegraded` 日志限长
- [ ] `internal/api/test_handler.go` 的 `io.ReadAll` 补 `http.MaxBytesReader` 限制
- [ ] `internal/validation/module.go` 抽取三处重复的 `validateGenTestsModule*`（API / 服务端 CLI / dtw CLI）
- [ ] `internal/test/prompt.go` `branchSuffix` 字符白名单扩展（目前仅替换 `/` → `-`，未处理空格、`:`、`~`、`^`、`?` 等 git 非法字符）
- [ ] `internal/queue/enqueue.go` `EnqueueManualGenTests` 补 `payload.RepoFullName == "" || CloneURL == ""` 完整性校验（与 `HandlePullRequest` / `EnqueueManualReview` 对齐）
