# M5.4 E2E 回归自动化 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** PR 合并后自动触发两阶段 E2E 回归——阶段 1 Claude 分析变更与用例关联性选择模块，阶段 2 按模块并行执行 E2E 测试。

**Architecture:** 新增 `TaskTypeTriageE2E` 任务类型。Webhook PR merged 事件经宿主侧初筛后入队 triage 分析任务，Processor 执行只读容器让 Claude 判断需要回归的模块，完成后链式调用 `EnqueueManualE2E` 按模块入队 `run_e2e`。复用 M5.1/M5.2/M5.3 全部执行能力。

**Tech Stack:** Go, asynq, SQLite, Gitea REST API, Claude Code CLI, Docker SDK

**设计文档:** `docs/plans/2026-05-12-m5.4-e2e-regression-automation-design.md`

---

## 文件结构

### 新建文件

| 文件 | 职责 |
|------|------|
| `internal/e2e/triage.go` | `TriageE2EOutput` / `TriageModule` 类型定义 + `ParseTriageResult` 双层 JSON 解析 + `sanitizeTriageOutput` 脱敏 + `BuildTriagePrompt` prompt 构建 |
| `internal/e2e/triage_test.go` | triage 解析、校验、脱敏、prompt 构建测试 |

### 修改文件

| 文件 | 改动 |
|------|------|
| `internal/model/task.go` | `TaskTypeTriageE2E` 常量 + `IsValid()` 追加 case |
| `internal/queue/options.go` | `TaskTimeoutsConfig.TriageE2E` 字段 + `TaskTimeout` / `defaultTaskTimeout` 追加 case |
| `internal/queue/client.go` | `AsynqTypeTriageE2E` 常量 + `taskTypeToAsynq` 追加 case |
| `internal/config/repo_config.go` | `RegressionConfig` 类型 + `E2EOverride.Regression` 字段 + `ResolveE2EConfig` 合并逻辑 |
| `internal/config/config.go` | `TaskTimeouts.TriageE2E` 字段 + `Clone()` 深拷贝 `RegressionConfig` |
| `internal/config/validate.go` | `validateE2EConfig` 追加 regression 校验 |
| `internal/store/migrations.go` | 迁移 v23：tasks 表 CHECK 约束追加 `triage_e2e` |
| `internal/e2e/errors.go` | `ErrE2ETriageParseFailure` sentinel |
| `internal/notify/notifier.go` | 3 个 triage EventType + metadata key 常量 |
| `internal/notify/feishu_card.go` | triage 三事件飞书卡片渲染 |
| `internal/queue/enqueue.go` | `handleMergedPullRequest` 重构 + `handleMergedE2ERegression` + `WithE2ERegressionConfigProvider` + 幂等/Cancel-and-Replace |
| `internal/queue/processor.go` | `TaskTypeTriageE2E` 路由 + `handleTriageE2EResult` 链式入队 + `WithEnqueueHandler` option + 通知构建 |
| `internal/worker/container.go` | `resolveImage` / `buildContainerCmd` / `buildContainerEnv` / `selectGiteaToken` 追加 `triage_e2e` case |
| `build/docker/entrypoint.sh` | `triage_e2e)` case |
| `internal/cmd/serve.go` | ServeMux 注册 `AsynqTypeTriageE2E` |
| `internal/cmd/serve_deps.go` | 注入 `WithE2ERegressionConfigProvider` + `WithEnqueueHandler` |
| `configs/dtworkflow.example.yaml` | regression 配置示例 |

---

## Task 1: TaskType + 超时 + asynq 路由基础设施

**Files:**
- Modify: `internal/model/task.go:8-47`
- Modify: `internal/queue/options.go:9-58`
- Modify: `internal/queue/client.go:17-169`
- Test: `internal/queue/options_test.go`

- [ ] **Step 1: 新增 `TaskTypeTriageE2E` 常量**

在 `internal/model/task.go` 的常量组末尾追加：

```go
TaskTypeTriageE2E  TaskType = "triage_e2e"
```

在 `IsValid()` switch 中追加 `TaskTypeTriageE2E`：

```go
case TaskTypeReviewPR, TaskTypeAnalyzeIssue, TaskTypeFixIssue, TaskTypeGenTests, TaskTypeGenDailyReport, TaskTypeRunE2E, TaskTypeTriageE2E:
```

- [ ] **Step 2: 新增超时配置**

在 `internal/queue/options.go` 的 `TaskTimeoutsConfig` 追加字段：

```go
TriageE2E    time.Duration
```

在 `TaskTimeout` switch 追加：

```go
case model.TaskTypeTriageE2E:
    configured = cfg.TriageE2E
```

在 `defaultTaskTimeout` switch 追加：

```go
case model.TaskTypeTriageE2E:
    return 10 * time.Minute
```

- [ ] **Step 3: 新增 asynq 类型常量和路由**

在 `internal/queue/client.go` 常量组追加：

```go
AsynqTypeTriageE2E = "dtworkflow:triage_e2e"
```

在 `taskTypeToAsynq` switch 追加：

```go
case model.TaskTypeTriageE2E:
    return AsynqTypeTriageE2E
```

- [ ] **Step 4: 在 `internal/config/config.go` 的 `TaskTimeouts` 追加字段**

```go
TriageE2E    time.Duration `mapstructure:"triage_e2e"` // M5.4: 默认 10m
```

- [ ] **Step 5: 写超时测试**

在 `internal/queue/options_test.go` 追加测试：

```go
func TestTaskTimeout_TriageE2E_Default(t *testing.T) {
    cfg := TaskTimeoutsConfig{}
    got := TaskTimeout(model.TaskTypeTriageE2E, cfg)
    if got != 10*time.Minute {
        t.Errorf("want 10m, got %v", got)
    }
}

func TestTaskTimeout_TriageE2E_Configured(t *testing.T) {
    cfg := TaskTimeoutsConfig{TriageE2E: 5 * time.Minute}
    got := TaskTimeout(model.TaskTypeTriageE2E, cfg)
    if got != 5*time.Minute {
        t.Errorf("want 5m, got %v", got)
    }
}
```

- [ ] **Step 6: 运行测试**

Run: `go test ./internal/model/... ./internal/queue/... -count=1 -run "TaskTimeout|IsValid"`
Expected: PASS

- [ ] **Step 7: 提交**

```bash
git add internal/model/task.go internal/queue/options.go internal/queue/client.go internal/config/config.go internal/queue/options_test.go
git commit -m "feat(m5.4): 新增 TaskTypeTriageE2E 常量 + 超时配置 + asynq 路由"
```

---

## Task 2: 数据库迁移 v23

**Files:**
- Modify: `internal/store/migrations.go`
- Test: `internal/store/sqlite_test.go`

- [ ] **Step 1: 新增迁移 v23**

在 `internal/store/migrations.go` 的 `migrations` 切片末尾追加（仿 v21 模式）：

```go
// M5.4: tasks 表 task_type CHECK 约束追加 triage_e2e。
{
    Version: 23,
    SQL: `
        CREATE TABLE tasks_new (
            id              TEXT PRIMARY KEY,
            asynq_id        TEXT NOT NULL DEFAULT '',
            task_type       TEXT NOT NULL CHECK(task_type IN ('review_pr', 'analyze_issue', 'fix_issue', 'gen_tests', 'gen_daily_report', 'run_e2e', 'triage_e2e')),
            status          TEXT NOT NULL DEFAULT 'pending'
                            CHECK(status IN ('pending','queued','running','succeeded','failed','retrying','cancelled')),
            priority        INTEGER NOT NULL DEFAULT 5,
            payload         TEXT NOT NULL,
            repo_full_name  TEXT NOT NULL DEFAULT '',
            result          TEXT NOT NULL DEFAULT '',
            error           TEXT NOT NULL DEFAULT '',
            retry_count     INTEGER NOT NULL DEFAULT 0,
            max_retry       INTEGER NOT NULL DEFAULT 3,
            worker_id       TEXT NOT NULL DEFAULT '',
            delivery_id     TEXT NOT NULL DEFAULT '',
            created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
            updated_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
            started_at      DATETIME,
            completed_at    DATETIME,
            pr_number       INTEGER,
            triggered_by    TEXT NOT NULL DEFAULT 'webhook'
        );
        INSERT INTO tasks_new SELECT * FROM tasks;
        DROP TABLE tasks;
        ALTER TABLE tasks_new RENAME TO tasks;
        CREATE INDEX IF NOT EXISTS idx_tasks_status ON tasks(status);
        CREATE INDEX IF NOT EXISTS idx_tasks_repo ON tasks(repo_full_name);
        CREATE INDEX IF NOT EXISTS idx_tasks_type_status ON tasks(task_type, status);
        CREATE UNIQUE INDEX IF NOT EXISTS idx_tasks_delivery_dedup ON tasks(delivery_id, task_type) WHERE delivery_id != '';
        CREATE INDEX IF NOT EXISTS idx_tasks_pending_created ON tasks(status, created_at) WHERE status = 'pending';
        CREATE INDEX IF NOT EXISTS idx_tasks_repo_pr ON tasks(repo_full_name, pr_number, task_type, status);
    `,
},
```

- [ ] **Step 2: 写迁移测试**

验证新建 store 后能成功插入 `triage_e2e` 类型的任务：

```go
func TestMigration_TriageE2ETaskType(t *testing.T) {
    store := newTestStore(t)
    ctx := context.Background()
    rec := &model.TaskRecord{
        ID:           "test-triage-001",
        TaskType:     model.TaskTypeTriageE2E,
        Status:       model.TaskStatusPending,
        Priority:     model.PriorityNormal,
        Payload:      model.TaskPayload{TaskType: model.TaskTypeTriageE2E},
        RepoFullName: "org/repo",
    }
    err := store.CreateTask(ctx, rec)
    if err != nil {
        t.Fatalf("CreateTask with triage_e2e should succeed: %v", err)
    }
}
```

- [ ] **Step 3: 运行测试**

Run: `go test ./internal/store/... -count=1 -run "Migration_TriageE2E"`
Expected: PASS

- [ ] **Step 4: 提交**

```bash
git add internal/store/migrations.go internal/store/sqlite_test.go
git commit -m "feat(m5.4): 迁移 v23 — tasks 表 CHECK 约束追加 triage_e2e"
```

---

## Task 3: 配置结构 — RegressionConfig + 校验

**Files:**
- Modify: `internal/config/repo_config.go:209-237`
- Modify: `internal/config/config.go` (Clone)
- Modify: `internal/config/validate.go:373-412`
- Test: `internal/config/config_test.go`

- [ ] **Step 1: 写配置测试**

在 `internal/config/config_test.go` 追加：

```go
func TestRegressionConfig_IsEnabled_NilDefault(t *testing.T) {
    var rc RegressionConfig
    if rc.IsEnabled() {
        t.Error("nil Enabled should be disabled")
    }
}

func TestRegressionConfig_IsEnabled_True(t *testing.T) {
    v := true
    rc := RegressionConfig{Enabled: &v}
    if !rc.IsEnabled() {
        t.Error("explicit true should be enabled")
    }
}

func TestResolveE2EConfig_RegressionOverride(t *testing.T) {
    v := true
    cfg := &Config{
        E2E: E2EConfig{DefaultEnv: "staging"},
        Repos: []RepoConfig{{
            Name: "org/repo",
            E2E: &E2EOverride{
                Regression: &RegressionConfig{Enabled: &v},
            },
        }},
    }
    resolved := cfg.ResolveE2EConfig("org/repo")
    if resolved.Regression == nil || !resolved.Regression.IsEnabled() {
        t.Error("repo-level regression override should apply")
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `go test ./internal/config/... -count=1 -run "RegressionConfig|ResolveE2EConfig_Regression"`
Expected: FAIL（类型不存在）

- [ ] **Step 3: 实现 RegressionConfig 类型**

在 `internal/config/repo_config.go` 的 `E2EOverride` 结构体后追加：

```go
// RegressionConfig E2E 回归自动化配置。
type RegressionConfig struct {
    Enabled     *bool    `mapstructure:"enabled"`
    IgnorePaths []string `mapstructure:"ignore_paths"`
}

func (c RegressionConfig) IsEnabled() bool {
    return c.Enabled != nil && *c.Enabled
}
```

在 **两个** 结构体中追加 `Regression` 字段：

1. `E2EConfig`（`internal/config/config.go`，全局配置）：

```go
Regression *RegressionConfig `mapstructure:"regression"` // M5.4
```

2. `E2EOverride`（`internal/config/repo_config.go`，仓库级覆盖）：

```go
Regression *RegressionConfig `mapstructure:"regression"` // M5.4
```

在 `ResolveE2EConfig` 中：
- 初始化 `merged` 时从全局读取：`Regression: c.E2E.Regression`
- 仓库覆盖循环中，`break` 之前追加：

```go
if repo.E2E.Regression != nil {
    merged.Regression = repo.E2E.Regression
}
```

这样全局 `e2e.regression:` 和仓库级 `repos[].e2e.regression:` 都能正确生效。

- [ ] **Step 4: Clone 深拷贝**

在 `internal/config/config.go` 的 `Clone()` 方法中，找到 E2E 相关的深拷贝位置，追加：

```go
// 全局 E2E.Regression 深拷贝
if c.E2E.Regression != nil {
    regCopy := *c.E2E.Regression
    if regCopy.Enabled != nil {
        v := *regCopy.Enabled
        regCopy.Enabled = &v
    }
    if regCopy.IgnorePaths != nil {
        regCopy.IgnorePaths = append([]string(nil), regCopy.IgnorePaths...)
    }
    clone.E2E.Regression = &regCopy
}
```

仓库级 `repos[*].E2E.Regression` 同样需要深拷贝（在仓库级 E2E 深拷贝循环中追加同样逻辑）。

- [ ] **Step 5: 配置校验**

在 `internal/config/validate.go` 的 `validateE2EConfig` 函数末尾（`return errs` 前）追加：

```go
// M5.4: regression 子结构校验
if cfg.E2E.Regression != nil {
    reg := cfg.E2E.Regression
    for i, pattern := range reg.IgnorePaths {
        if !doublestar.ValidatePattern(pattern) {
            errs = append(errs, fmt.Errorf("e2e.regression.ignore_paths[%d] 语法不合法: %q", i, pattern))
        }
    }
    if reg.IsEnabled() {
        if cfg.E2E.Enabled != nil && !*cfg.E2E.Enabled {
            errs = append(errs, fmt.Errorf("e2e.regression.enabled=true 但 e2e.enabled=false，矛盾"))
        }
        if cfg.E2E.DefaultEnv == "" {
            errs = append(errs, fmt.Errorf("e2e.regression.enabled=true 时 e2e.default_env 不能为空"))
        }
    }
}
```

- [ ] **Step 6: 写校验测试**

在 `internal/config/config_test.go` 追加校验测试：

```go
func TestValidateE2E_RegressionEnabledNoDefaultEnv(t *testing.T) {
    // regression.enabled=true 但 default_env 为空 → 校验失败
}

func TestValidateE2E_RegressionEnabledE2EDisabled(t *testing.T) {
    // regression.enabled=true 但 e2e.enabled=false → 矛盾报错
}

func TestValidateE2E_RegressionInvalidIgnorePath(t *testing.T) {
    // ignore_paths 含非法 glob → 校验失败
}
```

- [ ] **Step 7: 运行测试**

Run: `go test ./internal/config/... -count=1 -run "Regression"`
Expected: PASS

- [ ] **Step 8: 提交**

```bash
git add internal/config/repo_config.go internal/config/config.go internal/config/validate.go internal/config/config_test.go
git commit -m "feat(m5.4): RegressionConfig 类型 + E2EOverride 扩展 + 配置校验"
```

---

## Task 4: Triage 输出解析 + Prompt 构建

**Files:**
- Create: `internal/e2e/triage.go`
- Create: `internal/e2e/triage_test.go`
- Modify: `internal/e2e/errors.go`

- [ ] **Step 1: 新增 sentinel error**

在 `internal/e2e/errors.go` 追加：

```go
ErrE2ETriageParseFailure = errors.New("e2e: triage 输出解析失败")
```

- [ ] **Step 2: 写解析测试**

新建 `internal/e2e/triage_test.go`：

```go
func TestParseTriageResult_Normal(t *testing.T) {
    // 正常 JSON，modules + skipped_modules + analysis 正确提取
}

func TestParseTriageResult_EmptyModules(t *testing.T) {
    // modules 为空数组 → 不报错，返回空切片
}

func TestParseTriageResult_MissingModulesField(t *testing.T) {
    // JSON 中无 modules 字段 → 返回解析错误
}

func TestParseTriageResult_EmptyModuleName(t *testing.T) {
    // module name 为空 → 校验失败
}

func TestParseTriageResult_CLIEnvelope(t *testing.T) {
    // 双层 JSON（CLI 信封包裹）正确剥离
}

func TestSanitizeTriageOutput_Truncation(t *testing.T) {
    // reason / analysis 超 2KB 截断
}
```

- [ ] **Step 3: 运行测试确认失败**

Run: `go test ./internal/e2e/... -count=1 -run "Triage"`
Expected: FAIL

- [ ] **Step 4: 实现 triage.go**

新建 `internal/e2e/triage.go`：

```go
package e2e

import (
    "encoding/json"
    "fmt"
    "strings"
)

type TriageE2EOutput struct {
    Modules        []TriageModule `json:"modules"`
    SkippedModules []TriageModule `json:"skipped_modules"`
    Analysis       string         `json:"analysis"`
}

type TriageModule struct {
    Name   string `json:"name"`
    Reason string `json:"reason"`
}

// ParseTriageResult 双层 JSON 解析（CLI 信封 → TriageE2EOutput）。
func ParseTriageResult(raw string) (*TriageE2EOutput, error) {
    // 1. 尝试剥离 CLI 信封（extractJSON）
    // 2. json.Unmarshal → TriageE2EOutput
    // 3. 校验 modules 字段存在（非 nil）
    // 4. 校验每个 module.Name 非空
    // 5. sanitizeTriageOutput
}

func sanitizeTriageOutput(out *TriageE2EOutput) {
    // 截断 analysis、每个 module/skippedModule 的 reason（2KB）
    // 过滤控制字符
}

const maxTriageChangedFiles = 50

// BuildTriagePrompt 构建 triage 分析 prompt。
func BuildTriagePrompt(repo, baseRef string, changedFiles []string) string {
    // 四段式：上下文 + 分析指令 + 约束 + 输出格式
    // changedFiles 超 maxTriageChangedFiles 截断
}
```

实现时参考 `internal/e2e/result.go` 的 `ParseE2EResult` 双层解析模式和 `internal/test/result.go` 的 `extractJSON`。

- [ ] **Step 5: 运行测试**

Run: `go test ./internal/e2e/... -count=1 -run "Triage"`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add internal/e2e/triage.go internal/e2e/triage_test.go internal/e2e/errors.go
git commit -m "feat(m5.4): TriageE2EOutput 解析 + BuildTriagePrompt + 脱敏"
```

---

## Task 5: 通知事件 + 飞书卡片

**Files:**
- Modify: `internal/notify/notifier.go`
- Modify: `internal/notify/feishu_card.go`
- Test: `internal/notify/feishu_card_test.go`

- [ ] **Step 1: 新增 EventType 和 metadata key**

在 `internal/notify/notifier.go` 的常量组追加：

```go
// M5.4: E2E triage 通知事件。
EventE2ETriageStarted EventType = "e2e.triage.started"
EventE2ETriageDone    EventType = "e2e.triage.done"
EventE2ETriageFailed  EventType = "e2e.triage.failed"
```

metadata key 常量追加：

```go
MetaKeyTriageModules        = "triage_modules"         // JSON: 选中的模块列表
MetaKeyTriageSkippedModules = "triage_skipped_modules"  // JSON: 跳过的模块列表
MetaKeyTriageAnalysis       = "triage_analysis"         // 整体分析摘要
```

- [ ] **Step 2: 写飞书卡片测试**

在 `internal/notify/feishu_card_test.go` 追加：

```go
func TestBuildCard_E2ETriageStarted(t *testing.T) {
    // 蓝色卡片，显示"E2E 回归分析开始"+ PR 信息
}

func TestBuildCard_E2ETriageDone_WithModules(t *testing.T) {
    // 绿色卡片，显示选中模块列表 + 跳过模块列表
}

func TestBuildCard_E2ETriageDone_EmptyModules(t *testing.T) {
    // 绿色卡片，显示"本次变更无需 E2E 回归"
}

func TestBuildCard_E2ETriageFailed(t *testing.T) {
    // 红色卡片，显示失败原因
}
```

- [ ] **Step 3: 运行测试确认失败**

Run: `go test ./internal/notify/... -count=1 -run "E2ETriage"`
Expected: FAIL

- [ ] **Step 4: 实现飞书卡片渲染**

在 `internal/notify/feishu_card.go` 的 `buildCard` switch 中追加三个 case（参照现有 `EventE2EStarted/Done/Failed` 的实现模式）：

- `EventE2ETriageStarted`：蓝色，标题"E2E 回归分析开始"，显示仓库和触发 PR
- `EventE2ETriageDone`：绿色，标题"E2E 回归分析完成"；metadata 中 `triage_modules` 非空时列出选中模块；为空时文案改为"本次变更无需 E2E 回归"
- `EventE2ETriageFailed`：红色，标题"E2E 回归分析失败"，提示可手动触发

- [ ] **Step 5: 运行测试**

Run: `go test ./internal/notify/... -count=1 -run "E2ETriage"`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add internal/notify/notifier.go internal/notify/feishu_card.go internal/notify/feishu_card_test.go
git commit -m "feat(m5.4): E2E triage 三事件通知 + 飞书卡片渲染"
```

---

## Task 6: Worker 容器配置 + entrypoint

**Files:**
- Modify: `internal/worker/container.go`
- Modify: `build/docker/entrypoint.sh`
- Test: `internal/worker/container_test.go`

- [ ] **Step 1: 写 container 测试**

在 `internal/worker/container_test.go` 追加：

```go
func TestResolveImage_TriageE2E(t *testing.T) {
    // triage_e2e → 轻量镜像（cfg.Image，非 ImageFull / ImageE2E）
}

func TestSelectGiteaToken_TriageE2E(t *testing.T) {
    // triage_e2e → review token
}

func TestBuildContainerEnv_TriageE2E(t *testing.T) {
    // 注入 BASE_REF，使用 review token
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `go test ./internal/worker/... -count=1 -run "TriageE2E"`
Expected: FAIL

- [ ] **Step 3: 实现 container.go 扩展**

在 `internal/worker/container.go` 中：

`resolveImage` switch 追加：

```go
case model.TaskTypeTriageE2E:
    return cfg.Image // 轻量镜像
```

`selectGiteaToken` switch 追加：

```go
case model.TaskTypeTriageE2E:
    return cfg.GiteaTokenReview
```

`buildContainerEnv` switch 追加 `triage_e2e` case：注入 `BASE_REF`、`TASK_TYPE`，token 使用 review。

`buildContainerCmd` switch 追加 `triage_e2e` case：调用 `e2e.BuildTriagePrompt(repo, baseRef, changedFiles)` 构建 prompt，通过 stdin 传入，`--output-format json` + `--disallowedTools`（与 `analyze_issue` 模式一致）。Prompt 在 `buildContainerCmd` 中构建而非 Processor 层，因为 Processor 通过 `PoolRunner` 接口调用 `pool.Run(ctx, payload)`，该接口不暴露 `RunWithCommandAndStdin`。

- [ ] **Step 4: 实现 entrypoint.sh 扩展**

在 `build/docker/entrypoint.sh` 的 case 语句中追加：

```bash
triage_e2e)
    log "=== triage_e2e 模式：只读克隆 + checkout ==="
    clone_repo
    if [ -n "$BASE_REF" ]; then
        log "Checkout BASE_REF: $BASE_REF"
        git fetch origin "$BASE_REF" && git checkout "FETCH_HEAD" -B "$BASE_REF"
    fi
    cleanup_env
    ;;
```

参照 `analyze_issue` case 的模式：只读克隆，无 credential helper，无 git identity。

- [ ] **Step 5: 运行测试**

Run: `go test ./internal/worker/... -count=1 -run "TriageE2E"`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add internal/worker/container.go internal/worker/container_test.go build/docker/entrypoint.sh
git commit -m "feat(m5.4): worker 容器 triage_e2e 支持（轻量镜像 + 只读 + entrypoint）"
```

---

## Task 7: 入队层 — handleMergedPullRequest 重构 + E2E 回归入队

**Files:**
- Modify: `internal/queue/enqueue.go`
- Test: `internal/queue/enqueue_test.go`

- [ ] **Step 1: 写入队测试**

在 `internal/queue/enqueue_test.go` 追加 E2E 回归相关测试：

```go
func TestHandleMergedE2ERegression_BotPRSkipped(t *testing.T) {
    // HeadRef=auto-test/xxx → 不入队 triage_e2e
}

func TestHandleMergedE2ERegression_Disabled(t *testing.T) {
    // regression.enabled=false → 不入队
}

func TestHandleMergedE2ERegression_ConfigProviderNil(t *testing.T) {
    // e2eConfigProvider 未注入 → 静默跳过
}

func TestHandleMergedE2ERegression_NoSourceFiles(t *testing.T) {
    // 纯文档 PR → 过滤后为空 → 不入队
}

func TestHandleMergedE2ERegression_Normal(t *testing.T) {
    // 正常入队 triage_e2e，payload 包含变更文件列表和 environment
}

func TestHandleMergedE2ERegression_Idempotent(t *testing.T) {
    // 同一 webhook 重发不重复入队
}

func TestHandleMergedE2ERegression_CancelAndReplace(t *testing.T) {
    // 新 triage 取消旧 triage
}

func TestHandleMergedPullRequest_GenTestsAndE2EParallel(t *testing.T) {
    // 同一 PR 同时触发 gen_tests 和 E2E 回归
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `go test ./internal/queue/... -count=1 -run "MergedE2E|GenTestsAndE2E"`
Expected: FAIL

- [ ] **Step 3: 实现依赖注入**

在 `internal/queue/enqueue.go` 中追加：

```go
// E2ERegressionConfigProvider E2E 回归配置读取窄接口。
type E2ERegressionConfigProvider interface {
    ResolveE2EConfig(repoFullName string) config.E2EOverride
}

func WithE2ERegressionConfigProvider(p E2ERegressionConfigProvider) EnqueueOption {
    return func(h *EnqueueHandler) { h.e2eConfigProvider = p }
}
```

`EnqueueHandler` 新增字段 `e2eConfigProvider E2ERegressionConfigProvider`。

- [ ] **Step 4: 重构 handleMergedPullRequest**

将现有函数体（去掉 Bot PR 过滤）提取为 `handleMergedGenTests`。重写 `handleMergedPullRequest` 为调度层：

```go
func (h *EnqueueHandler) handleMergedPullRequest(ctx context.Context, event webhook.PullRequestEvent) error {
    pr := event.PullRequest
    if strings.HasPrefix(pr.HeadRef, "auto-test/") || strings.HasPrefix(pr.HeadRef, "auto-fix/") {
        h.logger.DebugContext(ctx, "change-driven: skipping bot PR",
            "repo", event.Repository.FullName, "head_ref", pr.HeadRef)
        return nil
    }

    genErr := h.handleMergedGenTests(ctx, event)
    h.handleMergedE2ERegression(ctx, event)
    return genErr
}
```

验证 `handleMergedGenTests` 内不再有 Bot PR 过滤（已移至调度层）。

- [ ] **Step 5: 实现 handleMergedE2ERegression**

```go
func (h *EnqueueHandler) handleMergedE2ERegression(ctx context.Context, event webhook.PullRequestEvent) {
    repo := event.Repository.FullName
    pr := event.PullRequest

    // 1. 配置检查
    if h.e2eConfigProvider == nil {
        return
    }
    e2eCfg := h.e2eConfigProvider.ResolveE2EConfig(repo)
    if e2eCfg.Regression == nil || !e2eCfg.Regression.IsEnabled() {
        return
    }

    // 2. 宿主侧初筛
    if h.prFilesLister == nil {
        return
    }
    allFiles, err := h.listAllPullRequestFiles(ctx, event.Repository.Owner, event.Repository.Name, pr.Number)
    if err != nil {
        h.logger.WarnContext(ctx, "e2e-regression: 获取 PR 文件失败", "repo", repo, "error", err)
        return
    }
    filenames := extractFilenames(allFiles)
    sourceFiles := filterSourceFiles(filenames, e2eCfg.Regression.IgnorePaths)
    if len(sourceFiles) == 0 {
        h.logger.DebugContext(ctx, "e2e-regression: 无源码变更", "repo", repo, "pr", pr.Number)
        return
    }

    // 3. 构建 payload 入队 triage_e2e
    deliveryID := buildE2ERegressionDeliveryID(event.DeliveryID)
    payload := model.TaskPayload{
        TaskType:     model.TaskTypeTriageE2E,
        RepoOwner:    event.Repository.Owner,
        RepoName:     event.Repository.Name,
        RepoFullName: repo,
        CloneURL:     event.Repository.CloneURL,
        BaseRef:      pr.BaseRef,
        Environment:  e2eCfg.DefaultEnv,
        ChangedFiles: sourceFiles,
    }

    // Cancel-and-Replace：取消同仓库旧 triage
    oldTasks, _ := h.store.FindActiveTasksByModule(ctx, repo, "", model.TaskTypeTriageE2E)

    _, err = h.enqueueTask(ctx, payload, "webhook:pr_merged:"+fmt.Sprint(pr.Number),
        deliveryID, model.PriorityNormal)
    if err != nil {
        h.logger.WarnContext(ctx, "e2e-regression: 入队失败", "repo", repo, "error", err)
        return
    }

    h.cancelTasks(ctx, oldTasks)
    h.logger.InfoContext(ctx, "e2e-regression: triage_e2e 已入队",
        "repo", repo, "pr", pr.Number, "source_files", len(sourceFiles))
}

func buildE2ERegressionDeliveryID(webhookDeliveryID string) string {
    if webhookDeliveryID == "" {
        return ""
    }
    return webhookDeliveryID + ":triage_e2e"
}
```

- [ ] **Step 6: 运行测试**

Run: `go test ./internal/queue/... -count=1 -run "Merged"`
Expected: PASS（含旧 gen_tests merged 测试回归）

- [ ] **Step 7: 提交**

```bash
git add internal/queue/enqueue.go internal/queue/enqueue_test.go
git commit -m "feat(m5.4): handleMergedPullRequest 重构 + handleMergedE2ERegression 入队"
```

---

## Task 8: Processor 集成 — triage 路由 + 链式入队

**Files:**
- Modify: `internal/queue/processor.go`
- Test: `internal/queue/processor_test.go`

- [ ] **Step 1: 写 Processor 测试**

在 `internal/queue/processor_test.go` 追加：

```go
func TestProcessor_TriageE2E_SuccessWithModules(t *testing.T) {
    // triage 输出 2 个模块 → 链式入队 2 个 run_e2e
}

func TestProcessor_TriageE2E_SuccessEmptyModules(t *testing.T) {
    // triage 输出空模块列表 → 通知无需回归 + succeeded
}

func TestProcessor_TriageE2E_ParseFailure(t *testing.T) {
    // 解析失败 → 返回 error（允许重试，非 SkipRetry）
}

func TestProcessor_TriageE2E_ChainEnqueuePartialFailure(t *testing.T) {
    // 一个模块入队成功，另一个失败 → warn 不阻断
}

func TestProcessor_TriageE2E_NoEnqueueHandler(t *testing.T) {
    // enqueueHandler 为 nil → 标记失败
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `go test ./internal/queue/... -count=1 -run "Processor_TriageE2E"`
Expected: FAIL

- [ ] **Step 3: 实现 WithEnqueueHandler option**

在 `internal/queue/processor.go` 追加：

```go
func WithEnqueueHandler(h *EnqueueHandler) ProcessorOption {
    return func(p *Processor) { p.enqueueHandler = h }
}
```

Processor 结构体新增 `enqueueHandler *EnqueueHandler` 字段。

- [ ] **Step 4: 实现 triage 路由和通知**

在 Processor 的 `processTask` switch 中追加 `TaskTypeTriageE2E` case：

1. 走 `pool.Run(ctx, payload)` 路径（prompt 已在 `buildContainerCmd` 中构建，通过 `PoolRunner` 接口调用）
2. 从容器输出中调用 `e2e.ParseTriageResult` 解析 `TriageE2EOutput`
3. 解析失败 → 返回 error（允许重试）
4. 解析成功 → 将 `triageResult` 存入局部变量，传递给通知和链式入队

在 `buildStartMessage` 追加 `TaskTypeTriageE2E` case（发送 `EventE2ETriageStarted`）。

在 `sendCompletionNotification` 和 `buildNotificationMessage` 中新增 `triageResult *e2e.TriageE2EOutput` 参数（与已有的 `reviewResult`、`fixResult`、`testResult`、`e2eResult` 同级），用于传递选中/跳过模块列表到飞书卡片。在 `buildNotificationMessage` 追加 `TaskTypeTriageE2E` case：
- 成功时发送 `EventE2ETriageDone`，metadata 携带 `triage_modules` JSON + `triage_skipped_modules` JSON + `triage_analysis`
- 失败时发送 `EventE2ETriageFailed`

其他任务类型的调用处传 `nil` 给 `triageResult` 参数。

对 `record.Error` 脱敏检查追加 `TaskTypeTriageE2E`（与 `gen_tests` / `run_e2e` 一致）。

- [ ] **Step 5: 实现 handleTriageE2EResult**

```go
func (p *Processor) handleTriageE2EResult(ctx context.Context, record *model.TaskRecord, output *e2e.TriageE2EOutput) {
    if len(output.Modules) == 0 {
        return // succeeded，通知已在 buildNotificationMessage 中处理
    }
    if p.enqueueHandler == nil {
        p.logger.ErrorContext(ctx, "triage_e2e: enqueueHandler 未注入，无法链式入队")
        return
    }
    triggeredBy := fmt.Sprintf("triage_e2e:%s", record.ID)
    for _, mod := range output.Modules {
        payload := model.TaskPayload{
            TaskType:     model.TaskTypeRunE2E,
            RepoOwner:    record.Payload.RepoOwner,
            RepoName:     record.Payload.RepoName,
            RepoFullName: record.Payload.RepoFullName,
            CloneURL:     record.Payload.CloneURL,
            Module:       mod.Name,
            BaseRef:      record.Payload.BaseRef,
            Environment:  record.Payload.Environment,
        }
        if _, err := p.enqueueHandler.EnqueueManualE2E(ctx, payload, triggeredBy); err != nil {
            p.logger.WarnContext(ctx, "triage_e2e: 链式入队失败",
                "module", mod.Name, "error", err)
        }
    }
}
```

- [ ] **Step 6: 运行测试**

Run: `go test ./internal/queue/... -count=1 -run "Processor_TriageE2E"`
Expected: PASS

- [ ] **Step 7: 提交**

```bash
git add internal/queue/processor.go internal/queue/processor_test.go
git commit -m "feat(m5.4): Processor triage_e2e 路由 + 链式入队 + 通知"
```

---

## Task 9: 装配层 + 示例配置

**Files:**
- Modify: `internal/cmd/serve.go`
- Modify: `internal/cmd/serve_deps.go`
- Modify: `configs/dtworkflow.example.yaml`

- [ ] **Step 1: serve.go — ServeMux 注册**

在 `internal/cmd/serve.go` 的 asynq `ServeMux` 注册处追加：

```go
mux.HandleFunc(queue.AsynqTypeTriageE2E, processor.ProcessTask)
```

- [ ] **Step 2: serve_deps.go — 依赖注入**

在 `enqueueOpts` 构建处追加：

```go
queue.WithE2ERegressionConfigProvider(cfgAdapter),
```

在 Processor 构建处追加：

```go
queue.WithEnqueueHandler(enqueueHandler),
```

确保 `enqueueHandler` 在 Processor 构建之前已创建（依赖顺序）。

- [ ] **Step 3: serve_deps.go — buildWorkerPoolConfigFromServeConfig 追加超时**

在 `buildWorkerPoolConfigFromServeConfig` 中，将 `cfg.Worker.Timeouts.TriageE2E` 传递到 `TaskTimeoutsConfig.TriageE2E`。

- [ ] **Step 4: 示例配置**

在 `configs/dtworkflow.example.yaml` 的 `e2e:` 段落追加：

```yaml
  # M5.4: E2E 回归自动化（PR 合并后自动触发）
  regression:
    enabled: false          # 默认关闭，显式启用
    ignore_paths:           # 宿主侧初筛忽略路径（doublestar glob）
      # - "docs/**"
      # - "scripts/**"
```

在 `worker.timeouts:` 段落追加：

```yaml
    # triage_e2e: 10m  # E2E 回归分析超时（默认 10 分钟）
```

- [ ] **Step 5: 编译验证**

Run: `make build`
Expected: 编译成功

- [ ] **Step 6: 提交**

```bash
git add internal/cmd/serve.go internal/cmd/serve_deps.go configs/dtworkflow.example.yaml
git commit -m "feat(m5.4): 装配层注入 + ServeMux 注册 + 示例配置"
```

---

## Task 10: 全量回归测试 + 文档同步

**Files:**
- All test files

- [ ] **Step 1: 全量测试**

Run: `go test ./... -count=1 -race`
Expected: 全部 PASS，无 race condition

- [ ] **Step 2: Linux 交叉编译**

Run: `GOOS=linux GOARCH=amd64 go build ./cmd/dtworkflow && GOOS=linux GOARCH=amd64 go build ./cmd/dtw`
Expected: 编译成功

- [ ] **Step 3: 同步 CLAUDE.md 和 ROADMAP（如有遗漏）**

确认 `CLAUDE.md` 和 `docs/ROADMAP.md` 中 M5.4 描述与实际实现一致。

- [ ] **Step 4: 最终提交**

```bash
git add -A
git commit -m "docs(m5.4): 同步 M5.4 实现到 CLAUDE.md 和 ROADMAP"
```

---

## 实施顺序依赖

```
Task 1 (TaskType + 超时 + asynq)
  ↓
Task 2 (迁移 v23)
  ↓
Task 3 (配置结构)    Task 4 (triage 解析 + prompt)    Task 5 (通知事件)    Task 6 (worker 容器)
  ↓                  ↓                                 ↓                    ↓
  └──────────────────┴─────────────────────────────────┴────────────────────┘
                                    ↓
                              Task 7 (入队层)
                                    ↓
                              Task 8 (Processor)
                                    ↓
                              Task 9 (装配层)
                                    ↓
                              Task 10 (回归测试)
```

Tasks 3/4/5/6 可并行执行（无相互依赖），Tasks 7/8/9/10 必须按序执行。
