# M2.1 PR 仓库环境准备与变更上下文提取 - 设计文档

> Phase 2 首个里程碑，建立 PR 自动评审的核心编排层

## 1. 背景与范围

### 1.1 现状

Phase 1 已完成的基础：
- `entrypoint.sh` 实现仓库 clone + PR 分支 checkout + base 分支 fetch
- `buildContainerCmd` 构造基础评审 prompt（Claude 用 `git diff` 查看变更）
- Gitea 客户端已实现完整 PR API（GetPullRequest、ListPullRequestFiles、CreatePullReview 含行级评论等）
- 通知框架仅回写"任务成功/失败"状态摘要

### 1.2 核心变化

Phase 2 采用"完整仓库 + Claude Code CLI"评审模式。容器内拥有完整代码仓库，Claude Code CLI 可自主探索调用链、关联文件和项目结构。因此：
- **不再需要**宿主机侧提取完整 diff 作为评审输入
- **仍然需要**宿主机侧轻量预取 PR 元数据，用于决策和 prompt 引导
- Claude CLI 自主探索仓库为评审核心方式

### 1.3 M2.1 范围

**做**：
- 创建 `internal/review` 包（评审编排层）
- PR 元数据预取（描述、文件列表、状态校验）
- Prompt 增强（元数据注入 + 评审指令 + JSON 输出格式约束）
- 定义评审输出 JSON schema（M2.3 的解析合约）
- 评审指令可配置化（YAML 配置 + 仓库级追加覆盖）
- Processor 集成（分发 review_pr 任务到 review.Service）

**不做**：
- Prompt 模板精细化、Java/Vue 专项 prompt（M2.2）
- 评审结果解析回写 Gitea（M2.3）
- 大 PR 分批策略（延后，当前 PR 规模不超过万行）
- 项目编码规范文件集成（M2.2）

### 1.4 实施前置条件

> **重要**：实施前必须在容器内执行一次 `claude -p "回复OK" --output-format json`，
> 记录 v2.1.76 的实际 JSON 输出字段和结构。本文档中 CLIResponse 的字段定义
> 基于已知的 Claude CLI JSON 输出格式，但应以实际验证为准。若字段名有差异，
> 需同步更新 `CLIResponse` 结构体的 JSON tag。

---

## 2. 架构设计

### 2.1 数据流

```
Webhook (PR opened/synchronized)
  -> Enqueue (已有)
    -> Processor.ProcessTask
      -> 检测 task_type == review_pr
        -> review.Service.Execute(ctx, payload)
            1. GetPullRequest -> 校验 state=="open"，获取 PR body
            2. ListPullRequestFiles -> 获取变更文件摘要
            3. 获取评审配置（全局 + 仓库级合并）
            4. 构造增强 prompt（元数据 + 评审指令 + JSON schema）
            5. Pool.RunWithCommand(ctx, payload, cmd)
            6. 解析外层 CLI JSON 信封，提取 result 字段
            7. 返回 ReviewResult
        <- Processor 更新 store + 发通知
```

### 2.2 包职责

| 包 | 职责 | 变化 |
|----|------|------|
| `internal/review` | PR 评审编排：元数据获取、prompt 构造、结果解析 | **新建** |
| `internal/queue` | 任务分发、store 状态更新、通知 | 中改：新增 ReviewExecutor 分发 + ProcessorOption |
| `internal/worker` | 容器生命周期管理 | 小改：新增 RunWithCommand 方法 |
| `internal/config` | 配置管理 | 小改：扩展 ReviewOverride 结构体 |
| `internal/gitea` | Gitea API 客户端 | 不变 |
| `internal/notify` | 通知框架 | 不变（M2.3 再增强） |

### 2.3 Pool 接口扩展

**设计原则**：不改变 `Pool.Run` 现有签名，避免破坏 `queue.PoolRunner` 接口及所有下游测试。

新增独立方法 `RunWithCommand`：

```go
// internal/worker/pool.go 新增方法

// RunWithCommand 与 Run 相同的容器生命周期管理，但使用调用方提供的命令替代 buildContainerCmd。
// 用于 review.Service 等需要自定义 prompt 的场景。
func (p *Pool) RunWithCommand(ctx context.Context, payload model.TaskPayload, cmd []string) (*ExecutionResult, error) {
    // 内部实现复用 Run 的核心逻辑，唯一区别是 containerCfg.Cmd = cmd
}
```

**影响范围**：
- `queue.PoolRunner` 接口 **不变**，`processor_test.go` 中 29 处 mock 调用 **不需要修改**
- `review.Service` 使用自己的窄接口 `ReviewPoolRunner`（见 3.1）
- 原有 `Pool.Run` 保持不变，`fix_issue` / `gen_tests` 任务走原有路径

---

## 3. 核心类型定义

### 3.1 review.Service

```go
// internal/review/service.go

// PRClient 窄接口，仅暴露评审所需的 Gitea API
type PRClient interface {
    GetPullRequest(ctx context.Context, owner, repo string, index int64) (*gitea.PullRequest, *gitea.Response, error)
    ListPullRequestFiles(ctx context.Context, owner, repo string, index int64, opts gitea.ListOptions) ([]*gitea.ChangedFile, *gitea.Response, error)
}

// ReviewPoolRunner 评审专用的容器执行接口，与 queue.PoolRunner 独立
// *worker.Pool 同时满足 queue.PoolRunner（Run）和 ReviewPoolRunner（RunWithCommand）
type ReviewPoolRunner interface {
    RunWithCommand(ctx context.Context, payload model.TaskPayload, cmd []string) (*worker.ExecutionResult, error)
}

// ConfigProvider 获取评审配置的接口
type ConfigProvider interface {
    ResolveReviewConfig(repoFullName string) config.ReviewOverride
}

// ServiceOption Service 配置选项
type ServiceOption func(*Service)

// WithLogger 设置自定义日志记录器
func WithServiceLogger(logger *slog.Logger) ServiceOption {
    return func(s *Service) {
        if logger != nil {
            s.logger = logger
        }
    }
}

type Service struct {
    gitea    PRClient
    pool     ReviewPoolRunner
    cfgProv  ConfigProvider      // 访问评审配置（支持热加载）
    logger   *slog.Logger
}

func NewService(gitea PRClient, pool ReviewPoolRunner, cfgProv ConfigProvider, opts ...ServiceOption) *Service {
    s := &Service{
        gitea:   gitea,
        pool:    pool,
        cfgProv: cfgProv,
        logger:  slog.Default(),
    }
    for _, opt := range opts {
        opt(s)
    }
    return s
}
```

**ConfigProvider 说明**：`Service` 通过 `ConfigProvider` 接口获取配置而非直接持有 `Config` 值。
这样 `config.Manager`（其 `Get()` 方法返回热加载后的最新快照）可直接满足此接口，
新评审任务自动使用最新配置，无需额外的热加载订阅机制。

### 3.2 Claude CLI JSON 输出格式

Claude Code CLI `--output-format json` 输出的顶层结构：

```go
// internal/review/result.go

// CLIResponse 表示 Claude Code CLI --output-format json 的完整输出。
// 字段基于 Claude CLI v2.x 的已知格式，实施前需在容器内验证实际输出并更新。
type CLIResponse struct {
    Type           string  `json:"type"`             // "result"
    Subtype        string  `json:"subtype"`          // "success" / "error"
    CostUSD        float64 `json:"cost_usd"`
    DurationMs     int64   `json:"duration_ms"`
    DurationAPIMs  int64   `json:"duration_api_ms"`
    IsError        bool    `json:"is_error"`
    NumTurns       int     `json:"num_turns"`
    Result         string  `json:"result"`           // Claude 的文本回答（字符串）
    SessionID      string  `json:"session_id"`
}
```

**关键点**：`Result` 字段是 **string 类型**，包含 Claude 的文本回答。
当 prompt 要求 Claude 输出 JSON 时，`Result` 是一个"内含 JSON 文本的字符串"。
解析流程为双层 Unmarshal：
1. 第一层：`json.Unmarshal(output) -> CLIResponse`
2. 第二层：`json.Unmarshal(cliResp.Result) -> ReviewOutput`

如果 Claude 的回答包含 markdown code fence（如 ` ```json ... ``` `），
需在第二层解析前做文本清理（剥离 code fence 标记）。

### 3.3 评审结果

```go
// internal/review/result.go

// ReviewResult 是 Service.Execute 的返回值
type ReviewResult struct {
    RawOutput  string        // Claude CLI 原始 stdout
    CLIMeta    *CLIMeta      // 外层 JSON 信封摘要
    Review     *ReviewOutput // 解析成功的内层评审结果（可能为 nil）
    ParseError error         // JSON 解析失败时非 nil（外层或内层）
}

// CLIMeta 从 CLIResponse 提取的执行元数据
type CLIMeta struct {
    CostUSD    float64
    DurationMs int64
    IsError    bool
    NumTurns   int
    SessionID  string
}
```

### 3.4 评审输出 JSON Schema（M2.3 解析合约）

```go
// internal/review/result.go

type ReviewOutput struct {
    Summary string        `json:"summary"`
    Verdict VerdictType   `json:"verdict"`
    Issues  []ReviewIssue `json:"issues,omitempty"`
}

type VerdictType string

const (
    VerdictApprove        VerdictType = "approve"
    VerdictRequestChanges VerdictType = "request_changes"
    VerdictComment        VerdictType = "comment"
)

type ReviewIssue struct {
    File       string `json:"file"`
    Line       int    `json:"line"`
    EndLine    int    `json:"end_line,omitempty"`
    Severity   string `json:"severity"`             // CRITICAL / ERROR / WARNING / INFO
    Category   string `json:"category"`             // security / logic / style / architecture
    Message    string `json:"message"`
    Suggestion string `json:"suggestion,omitempty"`
}
```

### 3.5 Verdict 判定规则

在 prompt 中指导 Claude 遵循，M2.3 做二次校验：
- 存在任何 CRITICAL 或 ERROR -> `request_changes`
- 仅有 WARNING / INFO -> `comment`
- 无任何 issue -> `approve`

---

## 4. Prompt 构造

### 4.1 Prompt 结构（四段式）

```
+-------------------------------------------+
| 1. 任务上下文（PR 元数据）                  |
|    PR #号、仓库、作者、base 分支             |
|    PR 描述（作者写的 body）                 |
|    变更文件摘要（文件名 + 增删行数）          |
+-------------------------------------------+
| 2. 评审指令（可配置）                       |
|    评审原则、严重程度定义、各维度评审要点      |
+-------------------------------------------+
| 3. 输出格式约束（硬编码）                   |
|    JSON schema 定义 + verdict 判定规则      |
+-------------------------------------------+
| 4. 大 PR 警示（条件性）                     |
|    变更超过阈值时提示聚焦关键变更             |
+-------------------------------------------+
```

### 4.2 辅助函数定义

```go
// internal/review/prompt.go

// truncate 按 rune 截断字符串，避免截断 UTF-8 多字节字符
func truncate(s string, maxLen int) string {
    runes := []rune(s)
    if len(runes) > maxLen {
        return string(runes[:maxLen]) + "..."
    }
    return s
}

// sumChanges 计算变更文件的总增删行数
func sumChanges(files []*gitea.ChangedFile) int {
    total := 0
    for _, f := range files {
        total += f.Additions + f.Deletions
    }
    return total
}

// formatFilesSummary 将变更文件列表格式化为 prompt 摘要
// 超过 maxFiles 个文件时只列前 maxFiles 个并注明总数
// 注意：此列表可能不完整（单页 API 获取），prompt 中会提示 Claude 用 git diff 查看完整变更
func formatFilesSummary(files []*gitea.ChangedFile) string {
    const maxFiles = 50
    var b strings.Builder
    total := sumChanges(files)
    b.WriteString(fmt.Sprintf("\nChanged files (%d files, +%d/-%d lines):\n",
        len(files), totalAdditions(files), totalDeletions(files)))
    limit := len(files)
    if limit > maxFiles {
        limit = maxFiles
    }
    for _, f := range files[:limit] {
        b.WriteString(fmt.Sprintf("  - %s (+%d/-%d) [%s]\n",
            f.Filename, f.Additions, f.Deletions, f.Status))
    }
    if len(files) > maxFiles {
        b.WriteString(fmt.Sprintf("  ... and %d more files (use 'git diff' to see all)\n",
            len(files)-maxFiles))
    }
    b.WriteString("\nNote: this file list may be partial. Use 'git diff' to see the complete changeset.\n")
    return b.String()
}

// safeOutput 安全提取 ExecutionResult 的 Output，execResult 为 nil 时返回空字符串
func safeOutput(r *worker.ExecutionResult) string {
    if r == nil {
        return ""
    }
    return r.Output
}

// extractJSON 从 Claude 回答文本中提取 JSON 内容
// 处理可能的 markdown code fence 包装（```json ... ```）
func extractJSON(text string) string {
    text = strings.TrimSpace(text)
    // 尝试剥离 markdown code fence
    if strings.HasPrefix(text, "```") {
        lines := strings.SplitN(text, "\n", 2)
        if len(lines) == 2 {
            text = lines[1]
        }
        if idx := strings.LastIndex(text, "```"); idx >= 0 {
            text = text[:idx]
        }
        text = strings.TrimSpace(text)
    }
    return text
}
```

### 4.3 Prompt 构造逻辑

```go
// internal/review/prompt.go

// ReviewConfig 评审编排层的内部配置（从 config.ReviewOverride 转换而来）
type ReviewConfig struct {
    Instructions     string   // 全局评审指令
    RepoInstructions string   // 仓库级追加指令
    Dimensions       []string // 启用的评审维度
    LargePRThreshold int      // 大 PR 警告阈值
}

func (s *Service) buildPrompt(pr *gitea.PullRequest, files []*gitea.ChangedFile, cfg ReviewConfig) string {
    var b strings.Builder

    // 1. 任务上下文
    b.WriteString(fmt.Sprintf("You are reviewing PR #%d in repository %s.\n", pr.Number, pr.Base.Repo.FullName))
    b.WriteString(fmt.Sprintf("Author: %s\n", pr.User.Login))
    b.WriteString(fmt.Sprintf("Base branch: %s\n", pr.Base.Ref))
    b.WriteString(fmt.Sprintf("PR title: %s\n", pr.Title))
    if pr.Body != "" {
        b.WriteString(fmt.Sprintf("PR description:\n%s\n", truncate(pr.Body, 2000)))
    }
    b.WriteString(formatFilesSummary(files))

    // 2. 评审指令（全局 + 仓库级追加）
    b.WriteString("\n")
    b.WriteString(cfg.Instructions)
    if cfg.RepoInstructions != "" {
        b.WriteString("\n\nAdditional repository-specific instructions:\n")
        b.WriteString(cfg.RepoInstructions)
    }

    // 3. 输出格式（硬编码，不可配置）
    b.WriteString(jsonSchemaInstruction)

    // 4. 大 PR 警示（条件性）
    totalChanges := sumChanges(files)
    if totalChanges > cfg.LargePRThreshold || len(files) > 30 {
        b.WriteString(largePRGuidance)
    }

    return b.String()
}

// buildCommand 构造容器执行命令
func (s *Service) buildCommand(prompt string) []string {
    return []string{
        "claude", "-p", prompt, "--output-format", "json",
    }
}
```

### 4.4 Prompt 常量

```go
// internal/review/prompt.go

const jsonSchemaInstruction = `

## Output Format

You MUST output your review as a single JSON object with the following schema.
Do NOT wrap it in markdown code fences. Output ONLY the raw JSON.

{
  "summary": "Overall review summary in 2-3 paragraphs",
  "verdict": "approve | request_changes | comment",
  "issues": [
    {
      "file": "relative/path/to/file.java",
      "line": 42,
      "end_line": 45,
      "severity": "CRITICAL | ERROR | WARNING | INFO",
      "category": "security | logic | style | architecture",
      "message": "Description of the issue",
      "suggestion": "How to fix it (optional)"
    }
  ]
}

Verdict rules:
- If any issue has severity CRITICAL or ERROR -> "request_changes"
- If issues exist but all are WARNING or INFO -> "comment"
- If no issues found -> "approve"
`

const largePRGuidance = `

## Large PR Notice

This is a large PR. Focus your review on:
1. Security-critical changes first
2. Core logic changes (new features, bug fixes)
3. Skip trivial formatting or auto-generated files
Do not try to comment on every file. Prioritize high-impact issues.
`
```

### 4.5 传递机制与安全约束

prompt 通过 CLI 参数传入 `claude -p "..." --output-format json`。

安全约束：
- PR body 截断到 2000 字符（`truncate`）
- 文件列表超过 50 个文件时只列前 50 个并注明总数
- 总 prompt 长度远低于 Linux MAX_ARG_STRLEN（128KB）
- 后续优化考虑：通过 stdin 传入 prompt 提升可调试性（`ps aux` 不暴露完整 prompt）

---

## 5. 评审指令配置

### 5.1 配置结构

扩展现有 `config.ReviewOverride`（`internal/config/repo_config.go`），新增三个字段：

```go
// internal/config/repo_config.go

type ReviewOverride struct {
    // --- 现有字段（M1.8 预留）---
    Enabled        *bool    `mapstructure:"enabled"`
    IgnorePatterns []string `mapstructure:"ignore_patterns"`
    Severity       string   `mapstructure:"severity"`

    // --- M2.1 新增字段 ---
    Instructions     string   `mapstructure:"instructions"`       // 评审指令文本
    Dimensions       []string `mapstructure:"dimensions"`         // 启用的评审维度
    LargePRThreshold int      `mapstructure:"large_pr_threshold"` // 大 PR 警告阈值（变更行数）
}
```

`ResolveReviewConfig` 方法扩展合并逻辑：

```go
// internal/config/repo_config.go 扩展

func (c *Config) ResolveReviewConfig(repoFullName string) ReviewOverride {
    // ... 现有合并逻辑 ...

    // M2.1 新增字段的合并（仓库级字段非空时覆盖全局值）
    // 注意：Instructions 在 review.Service 中以追加模式使用，
    // 此处按字段独立合并，追加逻辑在 review.Service.resolveConfig 中处理
    if repo.Review.Instructions != "" {
        // 不覆盖 merged.Instructions，由 review.Service 做追加
        // 此处存入独立字段或通过 Service 层处理
    }
    if repo.Review.Dimensions != nil {
        merged.Dimensions = repo.Review.Dimensions
    }
    if repo.Review.LargePRThreshold > 0 {
        merged.LargePRThreshold = repo.Review.LargePRThreshold
    }
}
```

**review.Service 中的配置转换**：

```go
// internal/review/service.go

// resolveConfig 从 ConfigProvider 获取配置并转换为内部 ReviewConfig
func (s *Service) resolveConfig(repoFullName string) ReviewConfig {
    override := s.cfgProv.ResolveReviewConfig(repoFullName)

    cfg := ReviewConfig{
        Instructions:     override.Instructions,
        Dimensions:       override.Dimensions,
        LargePRThreshold: override.LargePRThreshold,
    }

    // 应用默认值
    if cfg.Instructions == "" {
        cfg.Instructions = defaultReviewInstructions // 见 5.2
    }
    if len(cfg.Dimensions) == 0 {
        cfg.Dimensions = []string{"security", "logic", "architecture", "style"}
    }
    if cfg.LargePRThreshold <= 0 {
        cfg.LargePRThreshold = 5000
    }

    // 仓库级追加指令：从仓库配置中获取 Instructions（如果全局和仓库级都有）
    // 追加逻辑：全局 Instructions 作为 cfg.Instructions，仓库级作为 cfg.RepoInstructions
    // ConfigProvider 返回的是合并后的值，需要分别获取
    // 实现方式：Service 持有 ConfigProvider，分别读取全局配置和仓库配置
    // 简化实现：直接从 override 中区分（详见实施阶段）

    return cfg
}
```

### 5.2 默认评审指令

```go
// internal/review/prompt.go

const defaultReviewInstructions = `
## Review Instructions

你是一位资深代码评审专家，正在对一个团队项目的 Pull Request 进行评审。

### 评审原则
- 先理解 PR 的目的和作者意图，再评判实现是否合理
- 使用 git diff 查看变更，同时主动探索相关文件、追溯调用链、检查项目结构
- 聚焦真正重要的问题，不要吹毛求疵
- 安全问题和逻辑错误零容忍，必须明确指出
- 风格问题仅在严重影响可读性或违反项目既有惯例时提出
- 评估变更对现有功能的回归影响

### 严重程度定义
- CRITICAL: 安全漏洞（注入、越权、信息泄露）、数据损坏风险、生产事故隐患。必须修复。
- ERROR: 逻辑错误、边界条件遗漏、异常处理缺失、资源泄漏、并发安全问题。强烈建议修复。
- WARNING: 代码异味、潜在性能问题、错误传播不当、过度耦合、缺少必要校验。建议改进。
- INFO: 命名优化、注释补充、可读性提升、更优的设计模式建议。仅供参考。

### 安全 (security)
- SQL 注入、XSS、CSRF、路径遍历、不安全的反序列化
- 硬编码密钥/凭证、敏感信息写入日志
- 认证/授权绕过、权限校验遗漏

### 逻辑 (logic)
- 空指针/nil 引用、数组越界、类型转换失败
- 边界条件（空集合、零值、最大值、并发场景）
- 错误处理：吞没错误、错误类型不匹配、缺少回滚
- 资源泄漏（数据库连接、文件句柄、goroutine）

### 架构 (architecture)
- 职责划分是否清晰，是否违反分层约定
- API 设计的一致性和向后兼容性
- 变更是否影响其他模块（评估回归半径）

### 风格 (style)
- 命名是否清晰表达意图
- 函数/方法是否过长（超过 80 行需关注）
- 与项目既有风格的一致性
`
```

### 5.3 YAML 配置示例

```yaml
review:
  instructions: |
    你是一位资深代码评审专家...（见 5.2 默认值）
  dimensions:
    - security
    - logic
    - architecture
    - style
  large_pr_threshold: 5000

# 仓库级覆盖（追加模式）
repos:
  - name: "changyu/backend"
    review:
      instructions: |
        在全局指令基础上额外关注：
        - Spring Boot 事务注解使用是否正确
        - MyBatis SQL 是否存在注入风险
        - 接口是否遵循 RESTful 规范
  - name: "changyu/frontend"
    review:
      instructions: |
        在全局指令基础上额外关注：
        - Vue 组件是否遵循单一职责
        - 响应式数据是否正确使用 ref/reactive
        - 是否存在 XSS 风险（v-html 使用）
```

---

## 6. Service.Execute 流程

### 6.1 主流程

```go
func (s *Service) Execute(ctx context.Context, payload model.TaskPayload) (*ReviewResult, error) {
    owner, repo, prNum := payload.RepoOwner, payload.RepoName, payload.PRNumber

    // 0. 前置校验
    if prNum <= 0 {
        return nil, fmt.Errorf("无效的 PR 编号: %d", prNum)
    }

    // 1. PR 状态校验
    pr, _, err := s.gitea.GetPullRequest(ctx, owner, repo, prNum)
    if err != nil {
        return nil, fmt.Errorf("获取 PR #%d 信息失败: %w", prNum, err)
    }
    if pr.State != "open" {
        return nil, fmt.Errorf("PR #%d 状态为 %s: %w", prNum, pr.State, ErrPRNotOpen)
    }

    // 2. 获取变更文件列表（单页，最多 100 个文件）
    // 此列表仅用于 prompt 摘要和大 PR 检测，非评审的完整输入。
    // Claude CLI 在容器内通过 git diff 获取完整变更。
    files, _, err := s.gitea.ListPullRequestFiles(ctx, owner, repo, prNum, gitea.ListOptions{PageSize: 100})
    if err != nil {
        return nil, fmt.Errorf("获取 PR #%d 文件列表失败: %w", prNum, err)
    }

    // 3. 大 PR 警告（不阻断，仅日志）
    totalChanges := sumChanges(files)
    if totalChanges > 10000 {
        s.logger.WarnContext(ctx, "超大 PR，评审质量可能受影响",
            "pr", prNum, "files", len(files), "changes", totalChanges)
    }

    // 4. 获取评审配置（全局 + 仓库级合并）
    cfg := s.resolveConfig(payload.RepoFullName)

    // 5. 构造 prompt + 命令
    prompt := s.buildPrompt(pr, files, cfg)
    cmd := s.buildCommand(prompt)

    // 6. 执行容器
    execResult, err := s.pool.RunWithCommand(ctx, payload, cmd)
    if err != nil {
        return &ReviewResult{RawOutput: safeOutput(execResult)}, fmt.Errorf("容器执行失败: %w", err)
    }

    // 7. 解析结果
    result := s.parseResult(execResult.Output)
    result.RawOutput = execResult.Output
    return result, nil
}
```

### 6.2 结果解析

```go
func (s *Service) parseResult(output string) *ReviewResult {
    result := &ReviewResult{}

    // 解析外层 CLI JSON 信封
    var cliResp CLIResponse
    if err := json.Unmarshal([]byte(output), &cliResp); err != nil {
        result.ParseError = fmt.Errorf("CLI JSON 解析失败: %w", err)
        return result
    }
    result.CLIMeta = &CLIMeta{
        CostUSD:    cliResp.CostUSD,
        DurationMs: cliResp.DurationMs,
        IsError:    cliResp.IsError,
        NumTurns:   cliResp.NumTurns,
        SessionID:  cliResp.SessionID,
    }

    // CLI 报告执行错误
    if cliResp.IsError {
        result.ParseError = fmt.Errorf("Claude CLI 报告错误: subtype=%s", cliResp.Subtype)
        return result
    }

    // 解析内层评审 JSON（result 字段是字符串，可能包含 code fence）
    jsonText := extractJSON(cliResp.Result)
    var review ReviewOutput
    if err := json.Unmarshal([]byte(jsonText), &review); err != nil {
        // 优雅降级：内层 JSON 解析失败，保留 RawOutput 供 M2.3 作为普通评论
        result.ParseError = fmt.Errorf("评审 JSON 解析失败: %w", err)
        return result
    }
    result.Review = &review
    return result
}
```

### 6.3 错误处理策略

| 场景 | 行为 |
|------|------|
| PR 编号无效 (<=0) | 返回 error，确定性失败 |
| Gitea API 不可达 | 返回 error，Processor 标记 failed，asynq 自动重试 |
| PR 已关闭/合并 | 返回 `ErrPRNotOpen`，Processor 标记 failed，`asynq.SkipRetry` 不重试 |
| 容器执行超时 | Pool 返回 error + 部分日志，Processor 按现有逻辑处理 |
| CLI 报告 is_error | `ParseError != nil`，Review 为 nil，M2.3 降级为普通评论 |
| CLI 输出非 JSON | `ParseError != nil`，同上 |
| 内层 JSON 格式不符 | `ParseError != nil`，同上 |
| 超大 PR (>10000 行) | 日志 warn，不阻断，正常执行 |

哨兵错误定义：

```go
// internal/review/errors.go
var ErrPRNotOpen = errors.New("PR 不处于 open 状态")
```

---

## 7. Processor 集成

### 7.1 ProcessorOption 模式

```go
// internal/queue/processor.go

// ReviewExecutor 窄接口，解耦 review 包
type ReviewExecutor interface {
    Execute(ctx context.Context, payload model.TaskPayload) (*review.ReviewResult, error)
}

// ProcessorOption Processor 配置选项
type ProcessorOption func(*Processor)

// WithReviewService 注入评审服务
func WithReviewService(svc ReviewExecutor) ProcessorOption {
    return func(p *Processor) {
        p.reviewService = svc
    }
}

type Processor struct {
    pool          PoolRunner
    store         store.Store
    notifier      TaskNotifier
    reviewService ReviewExecutor  // 新增，可选（nil 时回退到 pool.Run）
    logger        *slog.Logger
}

// NewProcessor 创建 Processor 实例。
// 签名变更：末尾新增 ...ProcessorOption，现有 4 参数调用无需修改。
func NewProcessor(pool PoolRunner, store store.Store, notifier TaskNotifier, logger *slog.Logger, opts ...ProcessorOption) *Processor {
    // ... 现有校验逻辑 ...
    p := &Processor{pool: pool, store: store, notifier: notifier, logger: logger}
    for _, opt := range opts {
        opt(p)
    }
    return p
}
```

**向后兼容性**：`NewProcessor` 签名在末尾追加 `...ProcessorOption`（variadic），
现有所有 4 参数调用（serve.go + 全部测试）无需修改即可编译通过。

### 7.2 ProcessTask 分发逻辑

```go
// 在 ProcessTask 方法中，替换原有的 p.pool.Run 调用

var execResult *worker.ExecutionResult
var runErr error

switch payload.TaskType {
case model.TaskTypeReviewPR:
    if p.reviewService != nil {
        reviewResult, err := p.reviewService.Execute(ctx, payload)
        execResult = adaptReviewResult(reviewResult)
        runErr = err
    } else {
        execResult, runErr = p.pool.Run(ctx, payload)
    }
default:
    execResult, runErr = p.pool.Run(ctx, payload)
}
```

### 7.3 adaptReviewResult 转换函数

```go
// internal/queue/processor.go

// adaptReviewResult 将 review.ReviewResult 适配为 worker.ExecutionResult，
// 复用 Processor 现有的 store 状态更新和通知逻辑。
func adaptReviewResult(r *review.ReviewResult) *worker.ExecutionResult {
    if r == nil {
        return nil
    }
    result := &worker.ExecutionResult{
        Output: r.RawOutput,
    }
    if r.CLIMeta != nil {
        result.Duration = r.CLIMeta.DurationMs
    }
    if r.ParseError != nil {
        result.Error = r.ParseError.Error()
    }
    return result
}
```

### 7.4 ErrPRNotOpen 处理

在 ProcessTask 的错误处理分支中增加判定：

```go
if runErr != nil {
    // PR 已关闭/合并：确定性失败，跳过重试
    if errors.Is(runErr, review.ErrPRNotOpen) {
        record.Status = model.TaskStatusFailed
        record.Error = runErr.Error()
        // ... 更新 store ...
        return fmt.Errorf("评审跳过: %w", asynq.SkipRetry)
    }
    // ... 现有错误处理逻辑 ...
}
```

### 7.5 依赖注入链

```go
// internal/cmd/serve.go 启动时

giteaClient := gitea.NewClient(...)
pool := worker.NewPool(...)
cfgManager := config.NewManager(...)

// review.Service 需要的 ConfigProvider 适配
// config.Manager.Get() 返回 *Config，需要一个适配器满足 review.ConfigProvider
reviewSvc := review.NewService(giteaClient, pool, &configAdapter{mgr: cfgManager})

// 末尾追加 option，现有参数不变
processor := queue.NewProcessor(pool, store, notifier, logger,
    queue.WithReviewService(reviewSvc))
```

---

## 8. 改动文件清单

### 新增文件

| 文件 | 说明 |
|------|------|
| `internal/review/service.go` | Service 定义、NewService、Execute、resolveConfig |
| `internal/review/result.go` | CLIResponse、CLIMeta、ReviewResult、ReviewOutput 等类型 |
| `internal/review/prompt.go` | ReviewConfig（内部类型）、prompt 常量、buildPrompt、辅助函数 |
| `internal/review/errors.go` | ErrPRNotOpen 哨兵错误 |
| `internal/review/service_test.go` | 核心单元测试（mock PRClient + ReviewPoolRunner） |

### 改动文件

| 文件 | 改动幅度 | 说明 |
|------|---------|------|
| `internal/worker/pool.go` | 小 | 新增 `RunWithCommand` 方法（复用 Run 核心逻辑，CMD 参数不同） |
| `internal/config/repo_config.go` | 小 | `ReviewOverride` 新增 3 个字段 + `ResolveReviewConfig` 扩展合并逻辑 |
| `internal/queue/processor.go` | 中 | 新增 ReviewExecutor 接口、ProcessorOption、WithReviewService、adaptReviewResult、ErrPRNotOpen 分发 |
| `internal/cmd/serve.go` | 中 | 初始化 review.Service（含 ConfigProvider 适配）、注入 Processor |

### 不改动的文件

| 文件 | 原因 |
|------|------|
| `internal/worker/container.go` | `buildContainerCmd` 中 review_pr 分支保留作为回退 |
| `internal/worker/docker.go` | 容器操作无变化 |
| `internal/queue/processor_test.go` | queue.PoolRunner 接口不变，现有 mock 和测试不受影响 |
| `internal/notify/` | M2.1 不改通知内容，M2.3 再增强 |
| `build/docker/entrypoint.sh` | clone + checkout 逻辑完全复用 |
| `internal/gitea/` | API 已齐全，无需新增 |

---

## 9. 与后续里程碑的接口

| 里程碑 | M2.1 提供的接口 |
|--------|----------------|
| M2.2 评审 Prompt 工程 | 替换 `prompt.go` 中的 `defaultReviewInstructions`，增加 Java/Vue 专项模板、项目规范文件读取 |
| M2.3 评审结果解析与回写 | 消费 `ReviewResult.Review`（`ReviewOutput` 结构体），解析 issues 调用 Gitea `CreatePullReview` API 生成行级评论 |
| M2.4 重新评审机制 | 复用 `Service.Execute`，增加增量评审判断和防抖逻辑 |
| M2.5 评审配置 | 扩展 `ReviewOverride` / `ReviewConfig`，增加文件过滤 (`IgnorePatterns` 已预留)、bot 身份等配置 |

---

## 10. 设计决策记录

| # | 决策 | 理由 |
|---|------|------|
| D1 | PR 元数据宿主机轻量预取，Claude CLI 自主探索仓库为核心 | 容器已有完整仓库，Claude CLI 能力足够 |
| D2 | 不做大 PR 分批，超阈值仅 warn | 当前团队 PR 规模不超万行，YAGNI |
| D3 | 新建 `internal/review` 包 | 关注点分离，Phase 2 后续里程碑均归入此包 |
| D4 | 新增 `RunWithCommand` 方法而非修改 `Run` 签名 | 避免破坏 queue.PoolRunner 接口和全部下游测试 |
| D5 | 双层 JSON（CLI 信封 + 评审内容），含 code fence 清理 | CLI result 字段为字符串，需 extractJSON 预处理 |
| D6 | 内层 JSON 解析失败时优雅降级 | 不丢失评审信息，M2.3 降级为普通评论 |
| D7 | PR 已关闭为确定性失败，不重试 | 避免浪费资源 |
| D8 | 扩展现有 ReviewOverride 而非新建配置类型 | 复用 M1.8 的 YAML 映射和合并机制，review.ReviewConfig 为包内部类型 |
| D9 | 仓库级指令追加模式 | 简单，满足"全局通用 + 仓库补充"的绝大多数场景 |
| D10 | ConfigProvider 接口注入而非直接持有 Config 值 | 支持 config.Manager 热加载，新任务自动使用最新配置 |
| D11 | NewProcessor 末尾追加 variadic ProcessorOption | 向后兼容，现有全部调用和测试无需修改 |
| D12 | 文件列表单页获取（最多 100 个），prompt 中标注可能不完整 | 仅用于 prompt 引导，Claude CLI 通过 git diff 获取完整变更 |
