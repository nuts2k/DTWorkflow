# M4.2 容器执行 + 测试验证 + PR 创建 + 通知 — 设计文档

> 日期：2026-04-19
> 状态：设计已通过（brainstorming 完成，等待写实施计划）
> 依赖：M4.1（`internal/test` 包骨架 + Processor 已装配 + entrypoint.sh `gen_tests` 分支已加固）
> 范围：Phase 4 第一轮第二个里程碑——把 M4.1 的"路由已通 + PR 占位"补齐为「容器真跑 + 进度可恢复 + PR 真建 + 通知齐全 + 结果持久化」

## 1. 概述

### 1.1 目标

承接 M4.1 已落地的 `test.Service` 骨架与 Processor 路由，本里程碑解决 **运行时能力 + 长任务韧性 + 与既有 review 流水线的协作**：

1. **容器内真实执行 `mvn test` / `vitest run`** —— prompt 已写好，本期保证它真的跑、且区分基础设施故障与测试质量问题
2. **`createTestPR` 实装** —— 复用 M3.5 fix.createFixPR 的幂等模板，签名按 M4.1 冻结
3. **「分批 push + 断点续传」** —— 长任务中途失败不丢已完成工作（用户最关心的痛点）
4. **review enqueue 拦截** —— 防止分批 push 引发 N 次 review 任务被反复 Cancel-and-Replace
5. **稳定分支命名 + Cancel-and-Replace 主动清理** —— 同 module 永远只有一个 auto-test 分支与一个 PR
6. **通知三件套**（开始 / 完成 / 失败）—— 对齐 review / fix
7. **`test_gen_results` 表** —— 结果持久化，便于审计与 M4.3 变更驱动复用
8. **完成后主动 enqueue review** —— 任务结束时由 test.Service 显式触发 1 次 review

### 1.2 非目标（延后到 M4.3 / 后续）

- PR-merged Webhook 触发变更驱动测试生成（M4.3）
- 持久卷跨任务复用 Maven/Gradle 缓存（待评估）
- 多模块并行 gen_tests 调度优化（用户当前可手动并发）
- 远程孤儿分支定时清理工具（本设计已确保不产生孤儿，无需）

### 1.3 与 M4.1 的边界（增量视图）

| 能力 | M4.1 | M4.2 |
|------|------|------|
| `test.Service` 骨架 + Processor 装配 + sentinel 分发 | ✅ | — |
| Java / Vue prompt + 公共段 + 不变量校验 | ✅ | — |
| entrypoint.sh `gen_tests` 分支：credential / identity / 缓存 / pre-push hook | ✅ | — |
| `createTestPR` 真实创建 PR（M4.1 占位返回零值） | ❌ | ✅ |
| 容器内 Claude 按 prompt 真实跑 `mvn test` / `vitest run` 并验证 | ⚠️ 行为依赖 Claude 自觉 | ✅ 加交叉验证 |
| **稳定分支 `auto-test/{module}` + 断点续传** | ❌（M4.1 是 timestamp 分支） | ✅ |
| **每文件 commit + push** | ❌（M4.1 是"最后一刻 push"） | ✅ |
| **review enqueue 对 auto-test/* 的状态拦截** | ❌ | ✅ |
| **完成后主动 EnqueueManualReview** | ❌ | ✅ |
| **Cancel-and-Replace 主动删旧分支删旧 PR** | ❌ | ✅ |
| 通知事件 `EventGenTestsStarted/Done/Failed` + Processor case | ❌ | ✅ |
| `test_gen_results` 表 + Store 接口 | ❌ | ✅ |
| 基础设施故障 vs 测试质量问题区分 | ❌ | ✅ |

---

## 2. 关键设计决策

> 本节决策来自 2026-04-19 brainstorming，按 D1..D8 编号。每条决策列「选定方案 + 否决方案 + 理由」。

### D1：长任务失败处理 — 「分批 push + 断点续传」

**选定**：每个测试文件 commit 后立即 push 到稳定分支 `auto-test/{module}`；失败时已 push 的工作保留在远程，重试时从中断点继续。

**否决**：
- A. 「全或无 + 最后一刻 push」（M4.1 默认） — 失败成本太高，大仓 30 分钟工作可能全废
- C. 服务端分片调度（拆多个子任务） — 架构改动太大，多 PR 用户体验差
- D. 时间窗 push（每 5 分钟攒一次） — 实现复杂，prompt 难表达

**理由**：直接解决用户提出的"长任务中途失败"痛点；实现最简单（prompt 改 1 句话）；失败爆炸半径最小。

---

### D2：Review 拦截 — 方案 N（分支前缀 + 服务端任务状态查询）

**选定**：review enqueue 在收到 PR webhook 时，若 head 匹配 `auto-test/*`，查 SQLite 是否有该 repo 的活跃 gen_tests 任务（status IN queued/running），有则跳过入队。

**否决**：
- α. 不拦截（现状） — 首次任务因 D4 延迟建 PR 天然无 review 入队；**但**断点续传（asynq retry）与失败保留 PR（D5）后的续跑会在 PR 已存在的前提下继续 push，触发 `pull_request.synchronize` → review 反复入队 + Cancel-and-Replace，浪费 Claude 预算与飞书噪音
- γ. PR draft 状态拦截 —— **Gitea 不支持真正的 draft PR**（仅靠标题前缀 `WIP:`，依赖实例 `app.ini` 配置 `WORK_IN_PROGRESS_PREFIXES`，且去掉前缀不会发 `ready_for_review` webhook，不可靠）
- δ. 拦截一切 auto-test/* PR —— auto-fix/* 也算"机器人产出"但用户期望被审，区分对待

**理由**：
- 不依赖 Gitea 任何 draft 概念，跨 Gitea 实例完全可移植
- 状态由 DTWorkflow 自己掌控，无需 PR 编辑动作
- 与 auto-fix（M3.5 现状）零干扰
- 实现成本低：~20 行白名单逻辑 + 1 个 store 查询方法

**关键 finding**（已查证）：
- Gitea `CreatePullRequestOption` **无 `draft` 字段**
- Gitea `PullRequest` 响应**无 `draft` 布尔字段**
- Gitea webhook **无 `ready_for_review` 动作**
- 唯一接近的机制是 `WORK_IN_PROGRESS_PREFIXES`（实例级配置，默认 `WIP:,[WIP]`），但去掉前缀仅触发 `pull_request.edited`

---

### D3：稳定分支命名 — `auto-test/{module}`（无 timestamp）

**选定**：分支命名固定为 `auto-test/{sanitize(module)}`（整仓时 module 用 `_root_`），同 module 永远只有一个分支。

**否决**：
- M4.1 的 `auto-test/{module}-{timestamp}` —— 每次任务新分支，失败留孤儿，重试无法续传
- `auto-test/{module}-{task_id_short}` —— 同样产生孤儿

**理由**：
- 重试时容器内 `git fetch origin auto-test/{module}` → checkout → Claude 看到已有测试加入 ExistingTests → 跳过已生成的，只补缺失的 → **真正的断点续传**
- PR 同 head 分支幂等复用 → 远程**永远不堆积孤儿**
- Cancel-and-Replace 时由 test.Service 主动删除分支删除 PR（用户语义就是"重做"）

**`module` 路径合法化**：M4.1 已有的 `internal/test/prompt.go: sanitizeBranchRef` 把 `/` 等非法字符统一替换为 `-`，已满足 git ref 命名要求——本期**直接复用**，不另起名。

Worker 侧（`internal/worker/container.go`）在注入 `MODULE_SANITIZED` 环境变量前，导入 `internal/test` 并调用 `test.SanitizeBranchRef(module)`（需将 `sanitizeBranchRef` 首字母大写导出，或在 `internal/test` 新建 `SanitizeModuleBranchSegment` 薄 wrapper）；若 module 为空，回落 `"_root_"`。容器内 entrypoint.sh 直接读 env，不再自行 sanitize。

---

### D4：PR 创建时机 — 第一次 push 后立即创建（普通 ready 状态）

**选定**：容器内 Claude 完成第一个测试文件的 commit + push 后，由后续 push 触发 webhook → enqueue 已被 D2 拦截 → 不入队 review；test.Service 在 `Execute` 末尾调 `createTestPR` 检查并创建/复用 PR。

**否决**：
- 延迟到任务结束才创建 — 失败时无 PR，用户失去观察"Claude 跑了多少"的能力
- 创建时打 WIP 前缀 — Gitea draft 不可用，且污染 PR 标题

**理由**：用户能实时通过 PR 看到分批 push 的进度；失败时 PR 保留，便于接管；不依赖 Gitea draft。

**实施细节**：
- `createTestPR` 在 `Execute` 末尾调用（即使 `Success=false` 但 `len(CommittedFiles)>0` 也调，保证半成品也有 PR）
- 幂等：调 `findExistingTestPR`（仿 fix.findExistingFixPR），按 head 分支查 open PR；存在则复用 number/url

---

### D5：失败时 PR 处理 — 保留 + 默认不审

**选定**：任务 `failed` / `cancelled` 时 PR 保留，且**不主动 enqueue review**。

**否决**：
- 失败时删分支删 PR — 用户失去现场，无法接管
- 失败时也自动审 — 浪费预算，Claude 评审不完整工作意义有限

**理由**：用户失败现场可见、可接管；审与不审是用户决策。

**配置开关**：`test_gen.review_on_failure: false`（默认 false），置 true 则 `failed` 时也 enqueue review。

---

### D6：完成后主动 enqueue review — `EnqueueManualReview`

**选定**：`test.Service.Execute` 在 `Success=true && PRNumber>0` 时调 `EnqueueHandler.EnqueueManualReview(repo, pr_number, triggered_by="gen_tests:<task_id>")`。

**否决**：
- 不主动入队 — 由用户手动触发，体验差
- 通过 Gitea API 触发空 commit / 重新 open PR 让 webhook 自然触发 — 绕路且不可靠

**理由**：
- review 触发时机精准在 1 次：任务真正结束后
- 触发源标记 `triggered_by="gen_tests:<task_id>"` → 便于审计与日志关联
- review 自身已有 Cancel-and-Replace 机制 → 即使有边缘并发也安全

---

### D7：基础设施故障 vs 测试质量问题区分

**选定**：在 `TestGenOutput` 增加 `failure_category` 字段，Claude 在 prompt 指令下分类填写，service 层根据该字段决定通知严重级与是否计入"测试质量问题"。

**`failure_category` 枚举**：
- `infrastructure` — 依赖下载失败、Maven/Gradle 工具异常、网络问题、容器资源不足
- `test_quality` — 编译失败 / 断言失败 / mock 配置错误（已耗尽 max_retry_rounds）
- `info_insufficient` — 信息不足，需用户补充
- `none` — 成功路径

**通知差异化**：
- `infrastructure` → severity=`Warning`，通知文案"基础设施故障，建议重试或检查环境"
- `test_quality` → severity=`Info`，通知文案"测试质量未达标，已生成的 N 个测试可参考"
- `info_insufficient` → severity=`Info`，通知文案附 `MissingInfo`

**否决**：
- 不区分，统一报"失败" — 运维无法定位问题源
- 在服务端用启发式（看 RawOutput 关键词）推断 — 不可靠，Claude 自报最准确

**理由**：基础设施问题应触发 oncall 重试，测试质量问题不应；分类由 Claude 在容器内现场判断最准。

---

### D8：通知策略 — 三事件 + 飞书 + Gitea PR 评论

**选定**：新增 3 个事件类型，`buildStartMessage` / `buildNotificationMessage` 增加 `gen_tests` case：

```go
EventGenTestsStarted EventType = "gen_tests.started"
EventGenTestsDone    EventType = "gen_tests.done"
EventGenTestsFailed  EventType = "gen_tests.failed"
```

**触发时机**：
- `Started` → 任务从 queued 进入 running 时（Processor 调 `sendStartNotification` 时）
- `Done` → `Success=true` 且 PR 创建成功
- `Failed` → 各类 sentinel 失败 / 解析失败 / 容器异常退出

**通知载体**：
- **飞书**（与 review/fix 一致）：开始 + 结束都发，metadata 含 `repo / module / pr_url / generated_count / failure_category`
- **Gitea PR 评论**：仅 `Done` 时在 PR 上评论一次，附 GapAnalysis 摘要 + 测试运行结果（用户在 PR 上能看到）

**否决**：
- 中间发进度通知（"已生成 5/30"） — 噪音，用户看 PR commits 即可
- 仅发飞书不发 PR 评论 — PR 评论对评审者更直接

**理由**：对齐 review/fix 模式；PR 评论补足上下文。

---

## 3. 架构

### 3.1 调用链路（M4.2 完整版）

```
[触发层]
  POST /api/v1/gen-tests / dtworkflow gen-tests / dtw gen-tests
                                  ↓
                          EnqueueManualGenTests
                                  ↓
                  (M4.2 新增) deleteStaleAutoTestBranch?
                                  ↓                 ← Cancel-and-Replace 时
                  Cancel-and-Replace（M4.1 已实现）
                                  ↓
                          asynq enqueue
                                  ↓
[消费层]
  asynq.ProcessTask(gen_tests)
    ├─ sendStartNotification（M4.2 新增 gen_tests case）
    └─ testService.Execute
         ├─ resolveBaseRef / validateModule / resolveFramework（M4.1）
         ├─ buildPrompt（M4.1，M4.2 prompt 微调：扫描 ExistingTests 含分支已有）
         ├─ pool.RunWithCommandAndStdin
         │     │
         │     └─ [容器内 Claude 循环]
         │          ├─ entrypoint.sh：fetch + checkout auto-test/{module}（若存在）
         │          ├─ 扫源 + 扫已有测试（含分支上的） → ExistingTests
         │          ├─ GapAnalysis → target_queue（已生成的跳过）
         │          ├─ FOR target:
         │          │    ├─ 生成测试文件
         │          │    ├─ 本地验证 + 修正 ≤ N 轮
         │          │    ├─ 通过 → git commit + git push  ← D1：每文件 push
         │          │    └─ 失败 → 回滚 + 加入 skipped_targets
         │          └─ stdout TestGenOutput JSON（含 failure_category）
         │
         ├─ parseResult + 不变量校验（M4.1）
         ├─ createTestPR（M4.2 实装：幂等 + 复用 fix 模板）
         ├─ persistTestGenResult → test_gen_results 表（M4.2 新建）
         └─ 末尾：if Success → EnqueueManualReview（D6）

[Webhook 拦截]（D2）
  PR push webhook → HandlePullRequest
    ├─ if head 匹配 auto-test/* AND store.HasActiveGenTests(repo) → SKIP
    └─ else → 正常入队 review_pr

[完成后通知]（D8）
  Processor.sendCompletionNotification → buildNotificationMessage
    └─ 新 case：TaskTypeGenTests → 飞书 + PR 评论
```

### 3.2 涉及文件改动（增量）

| 文件 | 改动 | 估行 |
|------|------|------|
| `internal/test/service.go` | `createTestPR` 实装 + `findExistingTestPR` + `EnqueueReview` 调用 + `persistTestGenResult` | +180 |
| `internal/test/result.go` | `TestGenOutput.FailureCategory` 字段 + 不变量补充 | +30 |
| `internal/test/prompt.go` | prompt 改 timestamp → 稳定分支；扫描分支已有测试；failure_category 指令；每文件 push 指令 | +40 / -20 |
| `internal/test/pr_body.go` (新) | `FormatTestGenPRBody` —— PR 描述模板 | +120 |
| `internal/queue/enqueue.go` | `HandlePullRequest` 加 auto-test/* 拦截 + `cleanupStaleAutoTestBranch`（Cancel-and-Replace 路径） | +60 |
| `internal/queue/processor.go` | `buildStartMessage` / `buildNotificationMessage` 加 `TaskTypeGenTests` case；adaptTestResult 扩展 | +120 |
| `internal/notify/notifier.go` | 3 个 EventType 常量 | +6 |
| `internal/notify/feishu_card.go` | 3 个事件的卡片渲染 | +60 |
| `internal/notify/gitea_notifier.go` | gen_tests Done 时的 PR 评论格式 | +30 |
| `internal/store/store.go` | `HasActiveGenTests(repo) (bool, error)` + `SaveTestGenResult` + `GetTestGenResultByTaskID` | +40 |
| `internal/store/sqlite.go` | 上述方法实现 | +120 |
| `internal/store/migrations.go` | 迁移 v19：`test_gen_results` 表 + 索引（tasks 表 CHECK 约束已在 v1 含 `gen_tests`，本期**不需**新增 v20） | +30 |
| `internal/gitea/pulls.go` | 新增 `ClosePullRequest`（Cancel-and-Replace 清理用；`EditPullRequest` 作为其通用底座同时落地以便复用） | +70 |
| `internal/gitea/branches.go`（新文件） | `DeleteBranch` 实现（现有 repos.go 仅含 GetBranch / CreateBranch，新增独立文件） | +40 |
| `internal/config/*.go` | 新增 `test_gen.review_on_failure: bool`（D5）；validate 对应校验 | +30 |
| `build/docker/entrypoint.sh` | `gen_tests` 分支增加 fetch + checkout 已有 auto-test/{module}；落后 base 时重置；fetch 后校验分支作者仅含 bot，非 bot commit 存在则重置并告警 | +40 |
| `build/docker/entrypoint_test.sh` | 追加断言：fetch 复用 / 落后重置 / 作者污染重置 | +30 |
| **测试**（service_test / processor_test / enqueue_test / store_test / gitea_test / validate_test） | 全套覆盖 | +520 |
| **总计** | | **约 1,550 行净增（含测试）** |

---

## 4. 组件细节

### 4.1 `TestGenOutput` schema 增量（result.go）

```go
type TestGenOutput struct {
    // ... M4.1 已有字段 ...

    // M4.2 新增：失败分类，由 Claude 在 prompt 指令下分类填写
    FailureCategory FailureCategory `json:"failure_category"` // 枚举见下
}

type FailureCategory string

const (
    FailureCategoryNone           FailureCategory = "none"
    FailureCategoryInfrastructure FailureCategory = "infrastructure"
    FailureCategoryTestQuality    FailureCategory = "test_quality"
    FailureCategoryInfoInsufficient FailureCategory = "info_insufficient"
)
```

**不变量补充**：
- `Success=true ⇒ FailureCategory == "none"`
- `Success=false ⇒ FailureCategory ∈ {infrastructure, test_quality, info_insufficient}`
- `InfoSufficient=false ⇒ FailureCategory == "info_insufficient"`

### 4.2 `createTestPR` 实装（service.go）

```go
// createTestPR 在 Claude 完成 push 后创建/复用 PR。
// 幂等：先 ListRepoPullRequests 查询同 head 分支的 open PR，存在则复用。
// 即使 Success=false 但 len(CommittedFiles) > 0 也创建 PR（保留半成品供用户接管）。
func (s *Service) createTestPR(ctx context.Context, payload model.TaskPayload,
    result *TestGenResult) (prNumber int64, prURL string, err error) {

    if s.prClient == nil {
        return 0, "", fmt.Errorf("PRClient 未注入")
    }
    if result.Output == nil || result.Output.BranchName == "" {
        return 0, "", nil  // 容器未产出分支，无 PR 可建
    }
    if len(result.Output.CommittedFiles) == 0 {
        return 0, "", nil  // 无 commit，无 PR 可建
    }

    // 1. 幂等：查同 head 分支 open PR
    if num, url, ok := s.findExistingTestPR(ctx, payload.RepoOwner, payload.RepoName,
        result.Output.BranchName); ok {
        return num, url, nil
    }

    // 2. 构造 title / body
    moduleLabel := payload.Module
    if moduleLabel == "" {
        moduleLabel = "整仓"
    }
    title := fmt.Sprintf("test: 补充 %s 测试用例", moduleLabel)
    if len(title) > 250 { title = title[:250] }

    body := FormatTestGenPRBody(result.Output, payload, result.Framework)

    pr, _, err := s.prClient.CreatePullRequest(ctx, payload.RepoOwner, payload.RepoName,
        gitea.CreatePullRequestOption{
            Head:  result.Output.BranchName,
            Base:  result.BaseRef,
            Title: title,
            Body:  body,
        })
    if err != nil { return 0, "", err }
    if pr == nil { return 0, "", fmt.Errorf("Gitea API 返回空 PR") }
    return pr.Number, pr.HTMLURL, nil
}

func (s *Service) findExistingTestPR(ctx context.Context, owner, repo, headBranch string) (int64, string, bool) {
    // 与 fix.findExistingFixPR 完全对称
    ...
}
```

### 4.3 完成后主动 enqueue review（service.go 新增 hook）

`test.Service` 定义窄接口（**定义在 `internal/test` 包内，由 `internal/queue.EnqueueHandler` 天然满足**，从而保持 `queue → test` 的既有依赖方向——禁止 `internal/test` 反向 import `internal/queue`）：

```go
// ReviewEnqueuer 完成后触发 review 入队的窄接口，由 EnqueueHandler 实现。
// 接口定义在 internal/test，wiring（注入具体实现）在 cmd/dtworkflow 主程序完成。
type ReviewEnqueuer interface {
    EnqueueManualReview(ctx context.Context, payload model.TaskPayload, triggeredBy string) (string, error)
}

// WithReviewEnqueuer 注入 review 入队器（M4.2 新增 ServiceOption）
func WithReviewEnqueuer(e ReviewEnqueuer) ServiceOption { ... }

// Execute 末尾（record 由 Service 通过新增的 record ID 参数透传，或由 Processor 调用侧注入）：
if result.Output.Success && result.PRNumber > 0 && s.reviewEnqueuer != nil {
    reviewPayload := model.TaskPayload{
        TaskType:     model.TaskTypeReviewPR,
        RepoOwner:    payload.RepoOwner,
        RepoName:     payload.RepoName,
        RepoFullName: payload.RepoFullName,
        CloneURL:     payload.CloneURL,
        PRNumber:     result.PRNumber,
        // BaseRef / HeadRef / HeadSHA 从 result.Output 取
        BaseRef:      result.BaseRef,
        HeadRef:      result.Output.BranchName,
        HeadSHA:      result.Output.CommitSHA,
    }
    // triggered_by 使用当前 gen_tests 任务的 record.ID（永远非空，便于审计关联）。
    // record.ID 由 Processor 在调 Execute 时通过 payload/context 透传，或在
    // Service 方法签名中新增 taskID string 参数（推荐后者，避免 payload 膨胀）。
    if _, err := s.reviewEnqueuer.EnqueueManualReview(ctx, reviewPayload,
        "gen_tests:"+taskID); err != nil {
        // 不阻断主流程：review 入队失败仅记日志
        s.logger.WarnContext(ctx, "完成后入队 review 失败", "task_id", taskID, "error", err)
    }
}
```

**依赖方向图**：

```
cmd/dtworkflow ──wires──▶ queue.EnqueueHandler
                     ┬──▶ test.Service ──(test.ReviewEnqueuer)──▶ queue.EnqueueHandler
                     └──▶ queue.Processor ──(test.Service)──▶ test.Service
```

`queue.EnqueueHandler` 同时扮演 `test.ReviewEnqueuer` 角色，`test.Service` 只依赖接口，编译时 `internal/test` 不 import `internal/queue`，循环依赖被切断。

### 4.4 review enqueue 拦截（enqueue.go）

```go
// HandlePullRequest 头部追加：
func (h *EnqueueHandler) HandlePullRequest(ctx context.Context, event webhook.PullRequestEvent) error {
    // M4.2 新增：auto-test/* 分支拦截
    if strings.HasPrefix(event.PullRequest.HeadRef, "auto-test/") {
        active, err := h.store.HasActiveGenTests(ctx, event.Repository.FullName)
        if err != nil {
            // 查询失败按 fail-open 处理（继续走原逻辑），仅记日志
            h.logger.WarnContext(ctx, "HasActiveGenTests 查询失败，跳过拦截",
                "repo", event.Repository.FullName, "error", err)
        } else if active {
            h.logger.InfoContext(ctx, "auto-test/* PR 触发的 review 被拦截（gen_tests 进行中）",
                "repo", event.Repository.FullName,
                "pr", event.PullRequest.Number,
                "head", event.PullRequest.HeadRef,
                "delivery_id", event.DeliveryID,
            )
            return nil  // 直接放弃，不入队
        }
    }
    // ... 原 M4.1 逻辑 ...
}
```

`store.HasActiveGenTests` SQL（纳入 `retrying` 以覆盖 asynq backoff 窗口——此时 tasks 表 status 已改写为 retrying，但容器尚未重新 checkout 分支，仍应视作"活跃"）：

```sql
SELECT EXISTS(
    SELECT 1 FROM tasks
    WHERE task_type = 'gen_tests'
      AND repo_full_name = ?
      AND status IN ('queued', 'running', 'retrying')
)
```

### 4.5 Cancel-and-Replace 主动清理（enqueue.go）

`EnqueueManualGenTests` 在 `cancelTasks(activeTasks)` 之后追加：

```go
// M4.2：稳定分支策略下，用户手动重触发意味着"重做"，需主动清理远程旧分支与旧 PR
if len(activeTasks) > 0 && h.branchCleaner != nil {
    branchName := buildAutoTestBranchName(payload.Module)  // auto-test/{sanitize(module)}
    if err := h.branchCleaner.CleanupAutoTestBranch(ctx, payload.RepoOwner, payload.RepoName,
        branchName); err != nil {
        // 清理失败不阻断新任务（容器内 fetch 时若分支异常会自行重置）
        h.logger.WarnContext(ctx, "清理旧 auto-test 分支失败，继续入队",
            "branch", branchName, "error", err)
    }
}
```

`BranchCleaner` 接口（**定义与实现均在 `internal/queue`**，便于 EnqueueHandler 直接 new 并调用 gitea client；不放 `internal/test` 是为了避免增加 `queue → test` 之外的耦合，且清理动作在"入队"阶段触发，语义归属 queue）：

```go
// 定义在 internal/queue/branch_cleaner.go
type BranchCleaner interface {
    // CleanupAutoTestBranch 关闭分支上的 open PR + 删除分支。
    // 任一步失败仅记日志，不阻断（容器内 entrypoint 兜底重置）。
    CleanupAutoTestBranch(ctx context.Context, owner, repo, branch string) error
}
```

实现步骤：
1. `gitea.ListRepoPullRequests(state=open)` → 找到 head=branch 的 PR → `gitea.ClosePullRequest`
2. `gitea.DeleteBranch(owner, repo, branch)`

`EnqueueHandler` 通过构造函数或 Option 注入 `BranchCleaner`，wiring 在 cmd 层完成。

### 4.6 entrypoint.sh `gen_tests` 分支增量

M4.1 已有 5 项配置（credential / identity / cache / pre-push hook）。M4.2 追加：

```bash
gen_tests)
    # ... M4.1 已有的 5 项配置 ...

    # M4.2 新增：稳定分支断点续传
    AUTO_TEST_BRANCH="auto-test/${MODULE_SANITIZED:-_root_}"
    BOT_EMAIL="dtworkflow-bot@noreply.local"
    log "尝试 fetch 已存在的 ${AUTO_TEST_BRANCH} 分支以续传"
    if git fetch origin "${AUTO_TEST_BRANCH}" 2>/dev/null; then
        BASE_SHA=$(git rev-parse "origin/${BASE_REF}")
        BRANCH_SHA=$(git rev-parse "origin/${AUTO_TEST_BRANCH}")
        RESET_REASON=""
        # 1. 孤儿检测：分支不含 base SHA（被 squash/rebase merge）
        if ! git merge-base --is-ancestor "${BASE_SHA}" "${BRANCH_SHA}" 2>/dev/null; then
            RESET_REASON="落后 base"
        else
            # 2. 作者污染检测：分支上仅允许 bot commit（防止人工直推）
            NON_BOT=$(git log "${BASE_SHA}..${BRANCH_SHA}" --pretty=format:'%ae' 2>/dev/null \
                | grep -v "^${BOT_EMAIL}$" | head -n 1)
            if [ -n "${NON_BOT}" ]; then
                RESET_REASON="检测到非 bot 提交者 ${NON_BOT}"
            fi
        fi
        if [ -z "${RESET_REASON}" ]; then
            log "复用现有 ${AUTO_TEST_BRANCH}（断点续传）"
            git checkout -B "${AUTO_TEST_BRANCH}" "origin/${AUTO_TEST_BRANCH}"
        else
            log "${AUTO_TEST_BRANCH} 重置回 ${BASE_REF}（原因：${RESET_REASON}）"
            git checkout -B "${AUTO_TEST_BRANCH}" "${BASE_SHA}"
        fi
    else
        log "未发现已有 ${AUTO_TEST_BRANCH}，从 ${BASE_REF} 创建"
        git checkout -B "${AUTO_TEST_BRANCH}" "origin/${BASE_REF}"
    fi
    ;;
```

`MODULE_SANITIZED` 由 `worker/container.go` 在 env 注入前调用 **`test.SanitizeBranchRef(module)`**（复用 M4.1 `internal/test/prompt.go` 现有 `sanitizeBranchRef`，首字母大写导出即可）计算；module 为空时回落 `_root_`。容器内 entrypoint.sh 直接读取 env，不自行处理。

### 4.7 Prompt 指令增量（prompt.go）

公共段追加 / 修改：

| 指令 | 内容要点 |
|------|----------|
| `branchPersistenceInstruction` | 分支固定为 `auto-test/{module}`，**禁止创建带 timestamp 的新分支**；entrypoint 已 checkout 该分支 |
| `existingBranchScanInstruction` | 启动时除扫源代码 + 项目内既有测试外，**必须扫描当前 auto-test/{module} 分支已有的测试文件**（即可能从上次任务延续过来），加入 ExistingTests，跳过其对应的 target |
| `pushPerFileInstruction` | **取代 M4.1 的 `incrementalCommitInstruction`**：每完成一个测试文件 → `git add <file> && git commit && git push origin HEAD`；push 失败终止任务（不重试不强推） |
| `failureCategoryInstruction` | `Success=false` 时必须填 `failure_category`；分类标准：infrastructure / test_quality / info_insufficient |

### 4.8 通知（notify + processor）

#### 4.8.1 事件类型（notifier.go）

```go
const (
    // ... 既有 ...
    EventGenTestsStarted EventType = "gen_tests.started"
    EventGenTestsDone    EventType = "gen_tests.done"
    EventGenTestsFailed  EventType = "gen_tests.failed"
)
```

#### 4.8.2 飞书卡片（feishu_card.go）

`titleByEvent` / `severityByEvent` 增加三个 case；卡片字段：
- repo / module / framework / pr_url（若有）/ generated_count / committed_count / skipped_count / failure_category（失败时）

#### 4.8.3 Gitea PR 评论（仅 Done 时）

新增 `gitea_notifier.go::CommentOnGenTestsPR`：在 PR 上评论一条 markdown，含：
- GapAnalysis 摘要（前 5 个 UntestedModules）
- TestRunResults（passed/failed/skipped/duration）
- GeneratedFiles 列表（path / operation / test_count）
- SkippedTargets 列表（如有）

#### 4.8.4 Processor 改动

`buildStartMessage` 加 `case TaskTypeGenTests`，构造 `EventGenTestsStarted`。

`buildNotificationMessage` 加 `case TaskTypeGenTests`：
- `Succeeded` → `EventGenTestsDone` + 飞书 + PR 评论
- `Failed` → `EventGenTestsFailed` + 飞书（按 `failure_category` 调 severity）
- `Retrying` → `EventSystemError` + 飞书（同 review/fix）

### 4.9 数据持久化（test_gen_results 表）

迁移 v19：

```sql
CREATE TABLE IF NOT EXISTS test_gen_results (
    id                 TEXT PRIMARY KEY,
    task_id            TEXT REFERENCES tasks(id) ON DELETE SET NULL,
    repo_full_name     TEXT NOT NULL,
    module             TEXT NOT NULL DEFAULT '',
    framework          TEXT NOT NULL,           -- junit5 / vitest
    base_ref           TEXT NOT NULL,
    branch_name        TEXT NOT NULL DEFAULT '',
    commit_sha         TEXT NOT NULL DEFAULT '',
    pr_number          INTEGER NOT NULL DEFAULT 0,
    pr_url             TEXT NOT NULL DEFAULT '',
    success            INTEGER NOT NULL DEFAULT 0,
    info_sufficient    INTEGER NOT NULL DEFAULT 0,
    verification_passed INTEGER NOT NULL DEFAULT 0,
    failure_category   TEXT NOT NULL DEFAULT 'none', -- M4.2 新增枚举，见 §4.1
    failure_reason     TEXT NOT NULL DEFAULT '',     -- 取自 TestGenOutput.FailureReason（M4.1 已有字段）
    generated_count    INTEGER NOT NULL DEFAULT 0,   -- = len(TestGenOutput.GeneratedFiles)
    committed_count    INTEGER NOT NULL DEFAULT 0,   -- = len(TestGenOutput.CommittedFiles)
    skipped_count      INTEGER NOT NULL DEFAULT 0,   -- = len(TestGenOutput.SkippedTargets)
    test_passed        INTEGER NOT NULL DEFAULT 0,
    test_failed        INTEGER NOT NULL DEFAULT 0,
    test_duration_ms   INTEGER NOT NULL DEFAULT 0,
    cost_usd           REAL NOT NULL DEFAULT 0,
    duration_ms        INTEGER NOT NULL DEFAULT 0,
    output_json        TEXT NOT NULL DEFAULT '{}',  -- TestGenOutput 完整序列化
    created_at         DATETIME NOT NULL DEFAULT (datetime('now'))
);
CREATE INDEX IF NOT EXISTS idx_test_gen_results_repo ON test_gen_results(repo_full_name);
CREATE INDEX IF NOT EXISTS idx_test_gen_results_task ON test_gen_results(task_id);
CREATE INDEX IF NOT EXISTS idx_test_gen_results_repo_module ON test_gen_results(repo_full_name, module);
CREATE INDEX IF NOT EXISTS idx_test_gen_results_created ON test_gen_results(created_at);
```

`store.SaveTestGenResult(ctx, *TestGenResultRecord) error` —— 由 `test.Service` 在 Execute 末尾调用（无论成功失败，只要有 result 就持久化）。

---

## 5. 数据流

### 5.1 长任务 happy path（断点续传 + 拦截）

```
T0  dtw gen-tests --repo o/r --module srv
    └─ EnqueueManualGenTests → task1 status=queued

T0+5s  asynq pull task1 → Processor → status=running
       ├─ sendStartNotification → 飞书"测试生成开始"
       └─ testService.Execute
            ├─ entrypoint.sh: fetch auto-test/srv → 不存在 → 从 main 创建
            └─ Claude 开始

T0+30s  Claude 生成第 1 个测试文件 → mvn test 通过 → commit + push
        ※ 此时 PR 还未创建（D4：Execute 末尾才调 createTestPR）
        ※ Gitea 对"尚未关联 PR 的分支 push"只发 push event，DTWorkflow 的
          HandlePullRequest 仅订阅 pull_request 事件 → 天然不会触发 review 入队
        ※ 用户能从 Gitea UI 看到 auto-test/srv 分支已有 commit（分支视图可见）

T1+25min  Claude 完成全部 50 个目标 → 最后 push → 输出 TestGenOutput JSON
          └─ 容器退出，pool.Run 返回

T1+25min  parseResult → validateSuccessfulTestGenOutput → 通过
          ├─ createTestPR
          │  ├─ findExistingTestPR(head=auto-test/srv) → 未找到
          │  └─ CreatePullRequest → PR #42 创建
          ├─ persistTestGenResult → test_gen_results 表 insert
          └─ EnqueueManualReview(o/r, #42, "gen_tests:<delivery_id>")
              └─ task2 (review_pr) created status=queued

T1+25min+1s  Processor → task1 status=succeeded
             └─ sendCompletionNotification → 飞书 "Done" + PR #42 评论 GapAnalysis

T1+25min+2s  asynq pull task2（review_pr）→ 正常审 PR #42
```

**关键性质**：
- 首次任务期间 PR 尚未创建，Gitea 不会对"无 PR 分支 push"发 `pull_request.synchronize` → 天然 0 次 review 入队，D2 拦截**本次不起作用**
- 任务结束后**精准触发** 1 次 review（由 test.Service 主动 `EnqueueManualReview`），时机可控
- 失败时（容器超时 / Claude error）：已 push 的工作保留在远程 → 重试时容器内 fetch 复用 → Claude 跳过已生成的 → 真断点续传
- **D2 拦截真正生效的场景**（在 §5.2 / §5.4 展开）：
  ① asynq 断点续传重跑——前一轮末尾已创建 PR，重跑期间继续 push 会触发 `synchronize`，此时必须拦截；
  ② 失败保留 PR（D5）后用户再次 dtw gen-tests——新任务会先 cleanup 再新建分支，短暂窗口若恰逢旧 PR 上有外部 push 事件进来，也靠拦截兜底

### 5.2 容器超时失败 → asynq 重试（断点续传）

```
T0      task1 启动，Claude 生成 30 / 50 个文件，已 push 到 auto-test/srv
T0+30m  worker.timeouts.gen_tests 触发，容器被 SIGKILL
T0+30m  testService.Execute 返回 ctx.Err()
        └─ Processor → shouldRetry=true → task1 status=retrying
            （tasks 表同一行 status 字段被就地更新，task_id 不变；
             asynq 自身的 retry 与 DTWorkflow tasks 行一一对应）
        └─ sendCompletionNotification → 飞书 "Retrying"
        ※ 此期间 HasActiveGenTests 查 status IN (queued/running/retrying) → true

T1      asynq 调度重试 task1（同 asynq_id 同 task_id）→ Processor → status=running
        ├─ entrypoint.sh: fetch auto-test/srv → 已存在 → checkout 复用
        ├─ git merge-base --is-ancestor: 仍可达 main → 不重置
        └─ Claude 启动
            ├─ 扫现有测试（含分支上已有的 30 个） → ExistingTests=30
            ├─ GapAnalysis 排除已生成 → target_queue=20（剩余 20 个）
            ├─ FOR 20 个 target: 生成 + 验证 + commit + push
            └─ 输出 TestGenOutput

T1+15m  Success → createTestPR → 已有 open PR（findExistingTestPR 命中）→ 复用
        └─ EnqueueManualReview → task3
        └─ status=succeeded
```

**收益**：第一次 30 分钟 + 重试 15 分钟 = **节省了第一次的 30 分钟**重做成本。

### 5.3 Cancel-and-Replace（用户主动重触发）

```
T0   task1 跑了 5 分钟，已 push 10 个文件
T0   dtw gen-tests --repo o/r --module srv （第二次手动触发）
     ├─ EnqueueManualGenTests
     │   ├─ listActiveGenTestsTasks → [task1]
     │   ├─ enqueueTask(task2) → status=queued
     │   ├─ cancelTasks([task1]) → asynq cancel → 容器被 kill
     │   │  └─ task1 status=cancelled
     │   └─ branchCleaner.CleanupAutoTestBranch(o, r, "auto-test/srv")  ← D3 + D5
     │      ├─ findOpenPRByHead(auto-test/srv) → PR 不存在（task1 还没到 createTestPR）
     │      ├─ DeleteBranch(o, r, auto-test/srv) → 远程分支被删
     │      └─ 完成
     └─ 返回 task2 ID

T1   asynq pull task2 → entrypoint fetch auto-test/srv → 不存在 → 从 main 重建
     └─ Claude 从干净状态开始（用户预期"重做"语义达成）
```

**关键性质**：用户重触发 = 干净重做；asynq 自动重试 = 断点续传。两种语义清晰区分。

### 5.4 失败保留 PR（D5）

```
T0      task1 跑了 20 分钟，Claude 生成 25 / 50，遇到 5 次 mvn 编译失败
        Claude 决定 Success=false, FailureCategory="test_quality"
        └─ committed_files=20 (其余 5 个 retry 耗尽 + skipped 25 因预算)

T0      parseResult → validateSuccessfulTestGenOutput → 失败（Success=false）
        ├─ createTestPR：len(CommittedFiles)=20 > 0 → 仍然创建 PR
        │  └─ PR #50 created（用户能看到 20 个测试文件）
        ├─ persistTestGenResult → 写表（含 failure_category）
        ├─ ※ 不调 EnqueueManualReview（D5：失败不主动审）
        └─ Execute 返回 ErrTestGenFailed

T0      Processor → SkipRetry → status=failed
        └─ sendCompletionNotification → 飞书 "Failed"
            ├─ severity=Info（test_quality 不严重）
            └─ body 含 PR #50 链接 + "已生成 20 个测试可参考"

T0+     用户在 Gitea 看到 PR #50（20 个测试 + 失败说明）
        ├─ 可手动接管修复剩余 5 个
        ├─ 可在 PR 上 /review（如有命令）触发评审
        └─ 可关闭 PR，重新 dtw gen-tests 触发新一轮
```

---

## 6. 错误处理增量

### 6.1 sentinel 增量

```go
// 沿用 M4.1 已有 sentinel；M4.2 不新增 sentinel，仅扩展 FailureCategory 取值
// 但增加：
var ErrTestGenInfraFailure = errors.New("基础设施故障，建议重试")  // 严重，触发 oncall
```

仅用于 Processor 在判定 `Output.FailureCategory == FailureCategoryInfrastructure` 时**额外**包装一层 error，便于通知层用 `errors.Is` 切换 severity；不替换既有 `ErrTestGenFailed`。

### 6.2 重试决策表（M4.2 增量）

| 触发点 | 情况 | 重试? | M4.2 行为 |
|--------|------|-------|----------|
| `failure_category=infrastructure` | Claude 自报基础设施故障 | ✅ 重试（asynq backoff） | 飞书 severity=Warning，body 含"建议人工检查环境" |
| `failure_category=test_quality` | Claude 自报测试质量问题 | ❌ SkipRetry | 飞书 severity=Info；PR 保留半成品 |
| `failure_category=info_insufficient` | Claude 自报信息不足 | ❌ SkipRetry | 飞书附 MissingInfo |
| 容器超时（worker.timeouts.gen_tests） | ctx.Err() | ✅ 重试（断点续传） | sendCompletionNotification → "Retrying" |
| createTestPR 失败 | Gitea 5xx / 网络 | ✅ 重试（findExistingTestPR 幂等） | 已 push 的 commit 保留 |
| persistTestGenResult 失败 | SQLite 错误 | ❌ 不阻断主流程 | 仅日志，主流程继续返回 |
| EnqueueManualReview 失败 | Redis / SQLite 错误 | ❌ 不阻断主流程 | 仅日志；用户可手动触发 review |

### 6.3 Processor 完成路径增量

`Processor.handleSuccess` 在调用 `sendCompletionNotification` 之后，对 `TaskTypeGenTests` 增加分支：
- 已由 `test.Service.Execute` 内部完成 `createTestPR` + `persistTestGenResult` + `EnqueueManualReview`
- Processor 仅负责把 `TestGenResult` 序列化到 `record.Result` 字段（与 review/fix 一致）

---

## 7. 测试覆盖

### 7.1 新增/扩展测试文件

| 文件 | 覆盖 | 新增用例（粗估） |
|------|------|----------------|
| `internal/test/service_test.go` | createTestPR happy / 幂等 / Output=nil / CommittedFiles=空 / PRClient 未注入；EnqueueReview 调用条件；persistTestGenResult 失败不阻断 | +20 |
| `internal/test/pr_body_test.go`（新） | FormatTestGenPRBody 分支：成功 / 部分交付 / 各 failure_category | +12 |
| `internal/test/result_test.go` | FailureCategory 不变量 | +8 |
| `internal/queue/enqueue_test.go` | HandlePullRequest auto-test/* 拦截：active=true 跳过 / active=false 入队 / store err fail-open（断言日志含 repo 与 error 字段） / 非 auto-test/* 前缀不受影响；EnqueueManualGenTests 触发 BranchCleaner；cleanup 失败不阻断 | +14 |
| `internal/queue/processor_test.go` | gen_tests 启动 / 完成 / 失败通知；不同 failure_category 的 severity；Retrying 路径 | +15 |
| `internal/store/sqlite_test.go` | HasActiveGenTests（覆盖 queued/running/retrying 三态命中 + 非 gen_tests 不误命中） / SaveTestGenResult / GetTestGenResultByTaskID；迁移 v19 应用后表结构正确 | +14 |
| `internal/config/validate_test.go` | `test_gen.review_on_failure` 默认 false / 显式 true / 非法值拒绝 | +6 |
| `internal/notify/feishu_card_test.go` | 3 个新事件的卡片渲染 | +6 |
| `internal/notify/gitea_notifier_test.go` | CommentOnGenTestsPR 模板 | +4 |
| `internal/gitea/pulls_test.go` | EditPullRequest / DeleteBranch / ClosePullRequest | +8 |
| `build/docker/entrypoint_test.sh` | fetch 复用 / 落后 base 重置 / 不存在则新建 | +6 断言 |
| **合计** | | **+111 用例 + 8 shell 断言**（含污染重置 2 断言） |

### 7.2 集成测试（手动 / 半自动）

- 在测试服务器（companytest）上准备一个**预先 push 了部分 auto-test 测试**的 Java 仓库
- 触发 `dtw gen-tests --repo testorg/javasample --module backend`
- 验证：
  - 容器日志显示"复用现有 auto-test/backend 分支"
  - Claude 输出 `existing_tests` 含已 push 的文件
  - 任务结束后 PR 已存在 + 评论已发 + review 任务已入队

### 7.3 显式不测（继承 M4.1 + 本期）

- 真实 Claude API 调用（用 mock CLI JSON）
- 跨 Gitea 实例的 webhook 兼容性（默认 Gitea 1.20+）
- 测试服务器的具体 mvn 版本兼容（依赖 `worker-full` 镜像，独立维护）
- `test_gen.review_on_failure=true` 触发失败 PR 被 review 的端到端链路——单测层仅校验分支逻辑（`Success=false && review_on_failure=true → EnqueueManualReview 被调用`），E2E 手工回归覆盖

---

## 8. 风险与缓解

| 风险 | 缓解 |
|------|------|
| **稳定分支被人工提交污染** | entrypoint.sh 在 fetch 后检查 `git log --author` 是否仅含 `dtworkflow-bot`；非 bot commit 存在则重置回 base 并日志告警 |
| **断点续传时 Claude 误把已有测试当做"既有"误判风格** | prompt 明确"当前分支上的测试也是本服务历史产出，作为续传基线，不要把它们当成用户既有风格"；ExistingTests 字段加 `Source` 区分 |
| **review 拦截被绕过**（用户手动从 auto-test 创建 PR 时正好有 gen_tests 在跑） | 接受——极小概率；用户可在 PR 上手动评论触发；后续可加 `triggered_by` 维度区分 |
| **Cancel-and-Replace 清理失败导致状态不一致**（旧 PR 没关 + 新任务又跑） | branchCleaner 失败仅 warn；entrypoint 启动时若分支已存在但任务期望"干净"可由 prompt 决定（M4.2 简化为：信任 cleanup 已尽力，cleanup 失败时容器内会复用旧分支 → 退化为断点续传，无副作用） |
| **失败时 PR 保留 + 不审导致仓库 PR 列表堆积** | 失败 PR 数量受限于 module 数量（每 module 最多 1 个）；用户可定期手动清理；后续可加 `dtw gc-test-prs` 命令 |
| **failure_category 由 Claude 自报，可能不准** | 服务端在持久化时做交叉校验（如 RawOutput 含 `mvn-resolver` 错误关键词但 Claude 报 test_quality → 日志告警，但仍信任 Claude 的分类） |
| **EnqueueManualReview 与已自然触发的 review 重复**（用户在 task 结束前已手动从 PR 触发 /review） | review 自身的 Cancel-and-Replace 兜底（同 PR 第二个 review 任务取消第一个） |
| **gen_tests succeeded 瞬间的竞态双触发**：Execute 已 createTestPR 但尚未 EnqueueManualReview 时，外部 push 进来导致 webhook 查 HasActiveGenTests → 可能漏判 active（依任务 status 更新与 Execute 返回顺序而定），正常入队 review；随后 Execute 又 EnqueueManualReview → 两次 review | 接受——review 侧 Cancel-and-Replace 兜底，最多浪费一次任务创建/取消开销；不引入分布式锁 |
| **大仓首次 fetch + git checkout 慢** | 容器 timeout 配额已留余量；后续可考虑浅克隆 + 持久卷加速 |
| **prompt 中 max_retry_rounds 与 worker.timeouts.gen_tests 配合不当** | 在 config validate 增加交叉校验：`max_retry_rounds * 单文件预估 60s * 文件数估算` 若超过 timeout 则 warn |

---

## 9. 验收清单

M4.2 结束前完成以下清单（按 D1..D8 决策分组）：

**D1 + D3：分批 push + 稳定分支**
- [ ] 导出 `internal/test.SanitizeBranchRef`（或新增薄 wrapper），`internal/worker/container.go` 注入 `MODULE_SANITIZED` 环境变量（空 module → `_root_`）
- [ ] entrypoint.sh `gen_tests` 分支增加 fetch + checkout `auto-test/${MODULE_SANITIZED}` 逻辑
- [ ] entrypoint.sh 增加"落后 base 重置"+"非 bot author 重置"双重守护
- [ ] prompt 改为「每文件 commit + push」并取消 timestamp 分支命名
- [ ] prompt 加「扫描分支已有测试加入 ExistingTests」指令
- [ ] `entrypoint_test.sh` 追加 8 项断言（含污染重置 2 项）

**D2：review 拦截**
- [ ] `enqueue.HandlePullRequest` 增加 auto-test/* + HasActiveGenTests 拦截
- [ ] `store.HasActiveGenTests` 实现（status IN queued/running/**retrying**）+ 单测
- [ ] `enqueue_test.go` 覆盖 active=true / active=false / err fail-open（断言日志 repo+error 字段） / 非 auto-test/* 不受影响

**D4 + D5：PR 创建 + 失败保留**
- [ ] `createTestPR` 实装（幂等 + 复用 fix 模板）
- [ ] `findExistingTestPR` 实现
- [ ] `FormatTestGenPRBody` PR 描述模板
- [ ] `Success=false && CommittedFiles>0` 仍创建 PR 的单测
- [ ] `test_gen.review_on_failure` 配置项 + validate

**D6：完成后入队 review**
- [ ] `ReviewEnqueuer` 接口**定义在 `internal/test`**（由 queue.EnqueueHandler 天然满足），cmd 层 wiring；禁止 `internal/test` import `internal/queue`
- [ ] `WithReviewEnqueuer` ServiceOption
- [ ] `Service.Execute` 方法签名新增 `taskID string` 参数，`triggered_by = "gen_tests:"+taskID`
- [ ] `Execute` 末尾 `Success && PRNumber>0` 时调 `EnqueueManualReview`
- [ ] `EnqueueManualReview` 失败仅 warn 不阻断主流程

**D7：failure_category**
- [ ] `TestGenOutput.FailureCategory` 字段 + 4 枚举值
- [ ] 不变量校验：Success=true ⇔ category=none
- [ ] prompt 加 failure_category 分类指令
- [ ] testdata fixture 覆盖 4 个枚举

**D8：通知**
- [ ] 3 个 `EventGenTests*` 常量
- [ ] feishu_card 渲染 3 个事件
- [ ] gitea_notifier `CommentOnGenTestsPR`（PR 评论模板）
- [ ] processor `buildStartMessage` / `buildNotificationMessage` 加 gen_tests case
- [ ] failure_category → severity 映射

**Cancel-and-Replace 清理**
- [ ] `BranchCleaner` 接口 + 实现（关 PR + 删分支），**定义与实现均在 `internal/queue/branch_cleaner.go`**
- [ ] `EnqueueManualGenTests` 在 cancelTasks 后调用 cleanup
- [ ] cleanup 失败仅 warn 不阻断
- [ ] `gitea.ClosePullRequest` + 其通用底座 `EditPullRequest` 实装（新建 `internal/gitea/pulls.go` 方法）
- [ ] `gitea.DeleteBranch` 实装（新建 `internal/gitea/branches.go`）
- [ ] 单测：ClosePullRequest / EditPullRequest / DeleteBranch（成功 + 404 + 权限失败）

**持久化**
- [ ] 迁移 v19：`test_gen_results` 表 + 4 个索引
- [ ] `store.SaveTestGenResult` / `GetTestGenResultByTaskID` 实现
- [ ] `test.Service.Execute` 末尾调 SaveTestGenResult（成功失败均存）

**E2E 回归**
- [ ] 测试服务器手动跑 1 次 Java 仓 gen_tests，验证 PR 创建 + review 触发
- [ ] 模拟容器超时验证断点续传（人工 kill 容器 → 重启 → 看分支复用）
- [ ] 模拟 Cancel-and-Replace 验证清理生效

**质量门**
- [ ] `go test -race ./...` 全套通过
- [ ] `make lint` 无新警告
- [ ] `internal/test` 包覆盖率维持 ≥ 80%
- [ ] CHANGELOG / ROADMAP 更新 M4.2 状态

---

## 10. 决策溯源（brainstorming 关键节点）

| 时间 | 决策点 | 推动者反馈 |
|------|--------|----------|
| 2026-04-19 | 长任务失败如何处理 | 用户提出"一次生成很多测试时间长，中途出错怎么办" → 引出 D1 |
| 2026-04-19 | 推荐"每 commit 即 push" | 用户反问"会触发无数次 review 被 cancel-and-replace" → 引出 D2 |
| 2026-04-19 | 推荐 draft + 切 ready 方案 | 用户质疑"auto-fix 为什么需要 draft" → 缩窄白名单 |
| 2026-04-19 | 用户追问"draft 是否完整支持" | **查证 Gitea 不支持真 draft PR** → 否决 draft 方案，确立方案 N（D2 最终形态） |
| 2026-04-19 | 重试时分支策略 | 讨论中出现过"延续原分支"与"按 task_id 新建分支"两种方案，合并为 D3 的稳定分支 `auto-test/{module}` + entrypoint 智能续传（fetch 存在则复用，落后 base 或含非 bot commit 则重置） |

---

## 11. 待补充（实施阶段决定）

> 本设计已锁定方向，以下细节在实施时按"合理默认 + 可后续调整"处理，不阻塞起步：

- `worker.timeouts.gen_tests` 默认值（建议 30 分钟，配合 `max_retry_rounds=3`）
- Maven/Gradle 缓存目录是否升级为持久卷（评估收益后再定）
- `dtw gc-test-prs` 命令是否本期附带（建议放 M4.3 或后续）
- PR 评论的 markdown 格式细节（按 fix.PR body 风格，实施时定）

## 12. 本文档修订历史

| 时间 | 修订内容 |
|------|---------|
| 2026-04-19 v1 | brainstorming 定稿，D1..D8 决策确立，首次提交 |
| 2026-04-19 v2 | 审核反馈修订：①修正 §5.1 timeline（首次任务天然无 PR 故不触发 synchronize，D2 真正生效于 retry/D5 场景）；②删除虚构的迁移 v20；③统一复用 `sanitizeBranchRef`（导出首字母），取消 `sanitizeBranchSegment` 命名歧义；④明确 `ReviewEnqueuer` 与 `BranchCleaner` 的包归属与 import 方向（禁止 test→queue 反向依赖）；⑤`triggered_by` 改用 `record.ID` 避免 DeliveryID 无关联；⑥补 test_gen_results 字段与 TestGenOutput 的映射注释；⑦entrypoint.sh 增加"非 bot author 重置"守护并给出 bash 实现；⑧`HasActiveGenTests` SQL 纳入 retrying 状态；⑨风险表补竞态双触发条目；⑩ §10 决策溯源删除未定义的 γ/ε 代号；⑪补 `test_gen.review_on_failure` 配置改动与 validate 测试 |
