# M3.3.1 远程瘦客户端与 REST API 层 — 实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 为 DTWorkflow 新增服务端 REST API 层（`/api/v1/`）和独立瘦客户端二进制 `dtw`，支持远程任务管理、评审/修复触发

**Architecture:** 服务端在现有 Gin Engine 上新增 `/api/v1/` 路由组，Bearer Token 认证中间件保护。客户端 `dtw` 是独立二进制，依赖最小化（cobra + yaml + 标准库），通过 HTTP 调用服务端 API。任务始终通过 asynq 队列分发，API handler 只做校验+入队。

**Tech Stack:** Go, Gin, Cobra, asynq, SQLite (modernc.org/sqlite), gopkg.in/yaml.v3

**Design Doc:** `docs/plans/2026-04-07-m3.3.1-remote-client-rest-api-design.md`

---

## Part A: 服务端 REST API 层

### Task 1: 配置扩展 — `api.tokens` 结构与校验

**Files:**
- Modify: `internal/config/config.go` — 新增 `APIConfig` 和 `TokenConfig`
- Modify: `internal/config/validate.go` — 新增 token 格式校验
- Test: `internal/config/validate_test.go`

**Step 1: 写失败测试**

在 `internal/config/validate_test.go` 中新增测试：

```go
func TestValidate_APITokens(t *testing.T) {
	tests := []struct {
		name    string
		tokens  []config.TokenConfig
		wantErr bool
		errMsg  string
	}{
		{
			name:    "空 tokens 列表合法（API 不注册）",
			tokens:  nil,
			wantErr: false,
		},
		{
			name:    "合法 token",
			tokens:  []config.TokenConfig{{Token: "dtw_a1b2c3d4e5f67890", Identity: "admin"}},
			wantErr: false,
		},
		{
			name:    "token 缺少 dtw_ 前缀",
			tokens:  []config.TokenConfig{{Token: "a1b2c3d4e5f67890xx", Identity: "admin"}},
			wantErr: true,
			errMsg:  "dtw_",
		},
		{
			name:    "token 太短（<16 字符）",
			tokens:  []config.TokenConfig{{Token: "dtw_short", Identity: "admin"}},
			wantErr: true,
			errMsg:  "16",
		},
		{
			name:    "identity 为空",
			tokens:  []config.TokenConfig{{Token: "dtw_a1b2c3d4e5f67890", Identity: ""}},
			wantErr: true,
			errMsg:  "identity",
		},
		{
			name: "identity 重复",
			tokens: []config.TokenConfig{
				{Token: "dtw_a1b2c3d4e5f67890", Identity: "admin"},
				{Token: "dtw_x9y8z7w6v5u43210", Identity: "admin"},
			},
			wantErr: true,
			errMsg:  "重复",
		},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			cfg := validBaseConfig()
			cfg.API.Tokens = tt.tokens
			err := config.Validate(cfg)
			if tt.wantErr {
				require.Error(t, err)
				assert.Contains(t, err.Error(), tt.errMsg)
			} else {
				// 确保无 API 相关错误（其他字段错误忽略）
				if err != nil {
					assert.NotContains(t, err.Error(), "api.tokens")
				}
			}
		})
	}
}
```

**Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/config/ -run TestValidate_APITokens -v`
Expected: 编译失败 — `config.TokenConfig` 未定义

**Step 3: 实现配置结构**

在 `internal/config/config.go` 的 `Config` 结构体中新增 `API` 字段：

```go
// Config 结构体新增字段：
API APIConfig `mapstructure:"api" yaml:"api"`

// 新增类型定义：
type APIConfig struct {
	Tokens []TokenConfig `mapstructure:"tokens" yaml:"tokens"`
}

type TokenConfig struct {
	Token    string `mapstructure:"token" yaml:"token"`
	Identity string `mapstructure:"identity" yaml:"identity"`
}
```

在 `internal/config/validate.go` 的 `Validate` 函数末尾新增校验逻辑：

```go
// API Token 校验
identities := make(map[string]bool)
for i, tc := range cfg.API.Tokens {
	if !strings.HasPrefix(tc.Token, "dtw_") {
		errs = append(errs, fmt.Errorf("api.tokens[%d].token 必须以 dtw_ 开头", i))
	} else if len(tc.Token) < 16 {
		errs = append(errs, fmt.Errorf("api.tokens[%d].token 长度不能少于 16 字符", i))
	}
	if strings.TrimSpace(tc.Identity) == "" {
		errs = append(errs, fmt.Errorf("api.tokens[%d].identity 不能为空", i))
	} else if identities[tc.Identity] {
		errs = append(errs, fmt.Errorf("api.tokens[%d].identity \"%s\" 重复", i, tc.Identity))
	} else {
		identities[tc.Identity] = true
	}
}
```

**Step 4: 运行测试确认通过**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/config/ -run TestValidate_APITokens -v`
Expected: PASS

**Step 5: 提交**

```
feat(config): 新增 api.tokens 配置结构与校验
```

---

### Task 2: 数据库迁移 — `triggered_by` 列

**Files:**
- Modify: `internal/model/task.go` — TaskRecord 新增 `TriggeredBy` 字段
- Modify: `internal/store/migrations.go` — 新增迁移 V17
- Modify: `internal/store/sqlite.go` — `taskColumns` 和 `scanTaskRecord` 更新
- Test: `internal/store/sqlite_test.go`

**Step 1: 写失败测试**

在 `internal/store/sqlite_test.go` 中新增测试：

```go
func TestTaskTriggeredBy(t *testing.T) {
	s := newTestStore(t)
	record := &model.TaskRecord{
		ID:           "triggered-by-test",
		TaskType:     model.TaskTypeReviewPR,
		Status:       model.TaskStatusPending,
		Priority:     model.PriorityHigh,
		RepoFullName: "owner/repo",
		TriggeredBy:  "manual:admin",
	}
	require.NoError(t, s.CreateTask(context.Background(), record))

	got, err := s.GetTask(context.Background(), "triggered-by-test")
	require.NoError(t, err)
	assert.Equal(t, "manual:admin", got.TriggeredBy)
}
```

**Step 2: 运行测试确认失败**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/store/ -run TestTaskTriggeredBy -v`
Expected: 编译失败 — `TaskRecord.TriggeredBy` 未定义

**Step 3: 实现**

1. `internal/model/task.go` — TaskRecord 新增字段：
```go
TriggeredBy string `json:"triggered_by,omitempty"` // "webhook" / "manual:{identity}"
```

2. `internal/store/migrations.go` — 新增迁移（在 `migrations` slice 末尾追加）：
```go
{
    version: 17,
    sql:     `ALTER TABLE tasks ADD COLUMN triggered_by TEXT NOT NULL DEFAULT 'webhook';`,
},
```

3. `internal/store/sqlite.go` — 更新 `taskColumns` 常量和 `scanTaskRecord` 函数，在最后一个现有列后添加 `triggered_by`。同时更新 `CreateTask` 的 INSERT 语句和 `UpdateTask` 的 UPDATE 语句。

**Step 4: 运行测试确认通过**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/store/ -run TestTaskTriggeredBy -v`
Expected: PASS

**Step 5: 运行全量 store 测试确保无回归**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/store/ -v`
Expected: 全部 PASS

**Step 6: 提交**

```
feat(store): 新增 triggered_by 列（迁移 V17）
```

---

### Task 3: API 统一响应格式 + 错误码

**Files:**
- Create: `internal/api/response.go`
- Test: `internal/api/response_test.go`

**Step 1: 写失败测试**

创建 `internal/api/response_test.go`：

```go
package api

import (
	"net/http"
	"net/http/httptest"
	"testing"

	"github.com/gin-gonic/gin"
	"github.com/stretchr/testify/assert"
)

func init() { gin.SetMode(gin.TestMode) }

func TestSuccess(t *testing.T) {
	w := httptest.NewRecorder()
	c, _ := gin.CreateTestContext(w)
	Success(c, http.StatusOK, gin.H{"version": "0.3.3"})
	assert.Equal(t, http.StatusOK, w.Code)
	assert.JSONEq(t, `{"data":{"version":"0.3.3"}}`, w.Body.String())
}

func TestError(t *testing.T) {
	w := httptest.NewRecorder()
	c, _ := gin.CreateTestContext(w)
	Error(c, http.StatusNotFound, ErrCodeNotFound, "任务不存在: abc123")
	assert.Equal(t, http.StatusNotFound, w.Code)
	assert.JSONEq(t, `{"error":{"code":"not_found","message":"任务不存在: abc123"}}`, w.Body.String())
}
```

**Step 2: 实现**

创建 `internal/api/response.go`：

```go
package api

import "github.com/gin-gonic/gin"

// 错误码枚举
const (
	ErrCodeUnauthorized  = "unauthorized"
	ErrCodeNotFound      = "not_found"
	ErrCodeBadRequest    = "bad_request"
	ErrCodeConflict      = "conflict"
	ErrCodeBadGateway    = "bad_gateway"
	ErrCodeInternalError = "internal_error"
)

// Success 统一成功响应
func Success(c *gin.Context, httpStatus int, data any) {
	c.JSON(httpStatus, gin.H{"data": data})
}

// Error 统一错误响应
func Error(c *gin.Context, httpStatus int, code, message string) {
	c.JSON(httpStatus, gin.H{
		"error": gin.H{
			"code":    code,
			"message": message,
		},
	})
}
```

**Step 3: 运行测试**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/api/ -v`
Expected: PASS

**Step 4: 提交**

```
feat(api): 统一响应格式 Success/Error 和错误码枚举
```

---

### Task 4: Bearer Token 认证中间件

**Files:**
- Create: `internal/api/middleware.go`
- Test: `internal/api/middleware_test.go`

**Step 1: 写失败测试**

创建 `internal/api/middleware_test.go`：

```go
package api

import (
	"net/http"
	"net/http/httptest"
	"testing"

	"github.com/gin-gonic/gin"
	"github.com/stretchr/testify/assert"

	"otws19.zicp.vip/kelin/dtworkflow/internal/config"
)

func TestTokenAuth(t *testing.T) {
	tokens := []config.TokenConfig{
		{Token: "dtw_a1b2c3d4e5f67890", Identity: "admin"},
		{Token: "dtw_x9y8z7w6v5u43210", Identity: "ci-bot"},
	}

	tests := []struct {
		name       string
		authHeader string
		wantStatus int
		wantIdent  string // 空表示不检查 identity
	}{
		{"无 header", "", http.StatusUnauthorized, ""},
		{"错误前缀", "Basic dtw_a1b2c3d4e5f67890", http.StatusUnauthorized, ""},
		{"错误 token", "Bearer dtw_wrong_token_here", http.StatusUnauthorized, ""},
		{"正确 token admin", "Bearer dtw_a1b2c3d4e5f67890", http.StatusOK, "admin"},
		{"正确 token ci-bot", "Bearer dtw_x9y8z7w6v5u43210", http.StatusOK, "ci-bot"},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			router := gin.New()
			router.Use(TokenAuth(tokens))
			router.GET("/test", func(c *gin.Context) {
				c.JSON(http.StatusOK, gin.H{"identity": c.GetString(ContextKeyIdentity)})
			})

			req := httptest.NewRequest("GET", "/test", nil)
			if tt.authHeader != "" {
				req.Header.Set("Authorization", tt.authHeader)
			}
			w := httptest.NewRecorder()
			router.ServeHTTP(w, req)
			assert.Equal(t, tt.wantStatus, w.Code)
			if tt.wantIdent != "" {
				assert.Contains(t, w.Body.String(), tt.wantIdent)
			}
		})
	}
}
```

**Step 2: 实现**

创建 `internal/api/middleware.go`：

```go
package api

import (
	"crypto/subtle"
	"net/http"
	"strings"

	"github.com/gin-gonic/gin"

	"otws19.zicp.vip/kelin/dtworkflow/internal/config"
)

const ContextKeyIdentity = "api_identity"

// TokenAuth Bearer Token 认证中间件
func TokenAuth(tokens []config.TokenConfig) gin.HandlerFunc {
	return func(c *gin.Context) {
		header := c.GetHeader("Authorization")
		if !strings.HasPrefix(header, "Bearer ") {
			Error(c, http.StatusUnauthorized, ErrCodeUnauthorized, "缺少有效的认证凭证")
			c.Abort()
			return
		}
		provided := strings.TrimPrefix(header, "Bearer ")

		for _, tc := range tokens {
			if subtle.ConstantTimeCompare([]byte(provided), []byte(tc.Token)) == 1 {
				c.Set(ContextKeyIdentity, tc.Identity)
				c.Next()
				return
			}
		}

		Error(c, http.StatusUnauthorized, ErrCodeUnauthorized, "缺少有效的认证凭证")
		c.Abort()
	}
}
```

**Step 3: 运行测试**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/api/ -run TestTokenAuth -v`
Expected: PASS

**Step 4: 提交**

```
feat(api): Bearer Token 认证中间件（constant-time 比较）
```

---

### Task 5: API Router 骨架 + Dependencies

**Files:**
- Create: `internal/api/router.go`
- Test: `internal/api/router_test.go`

**Step 1: 写失败测试**

```go
// internal/api/router_test.go
package api

import (
	"log/slog"
	"testing"
	"time"

	"github.com/gin-gonic/gin"
	"github.com/stretchr/testify/assert"

	"otws19.zicp.vip/kelin/dtworkflow/internal/config"
)

func TestRegisterRoutes_NoTokens(t *testing.T) {
	router := gin.New()
	deps := Dependencies{
		Logger: slog.Default(),
	}
	RegisterRoutes(router, deps)
	// 无 token 时不注册 API 路由
	routes := router.Routes()
	for _, r := range routes {
		assert.NotContains(t, r.Path, "/api/v1/")
	}
}

func TestRegisterRoutes_WithTokens(t *testing.T) {
	router := gin.New()
	deps := Dependencies{
		Tokens:    []config.TokenConfig{{Token: "dtw_test1234567890", Identity: "test"}},
		Version:   "0.3.3",
		StartTime: time.Now(),
		Logger:    slog.Default(),
	}
	RegisterRoutes(router, deps)
	paths := make(map[string]bool)
	for _, r := range router.Routes() {
		paths[r.Method+" "+r.Path] = true
	}
	assert.True(t, paths["GET /api/v1/status"])
	assert.True(t, paths["GET /api/v1/tasks"])
	assert.True(t, paths["GET /api/v1/tasks/:id"])
	assert.True(t, paths["POST /api/v1/tasks/:id/retry"])
	assert.True(t, paths["POST /api/v1/repos/:owner/:repo/review-pr"])
	assert.True(t, paths["POST /api/v1/repos/:owner/:repo/fix-issue"])
}
```

**Step 2: 实现**

创建 `internal/api/router.go`：

```go
package api

import (
	"log/slog"
	"time"

	"github.com/gin-gonic/gin"

	"otws19.zicp.vip/kelin/dtworkflow/internal/config"
	"otws19.zicp.vip/kelin/dtworkflow/internal/gitea"
	"otws19.zicp.vip/kelin/dtworkflow/internal/queue"
	"otws19.zicp.vip/kelin/dtworkflow/internal/store"
	"otws19.zicp.vip/kelin/dtworkflow/internal/worker"
)

// Dependencies API 层的全部依赖
type Dependencies struct {
	Store       store.Store
	QueueClient *queue.Client
	GiteaClient *gitea.Client
	Pool        *worker.Pool
	Tokens      []config.TokenConfig
	Version     string
	StartTime   time.Time
	Logger      *slog.Logger
}

// RegisterRoutes 注册 /api/v1/ 路由组。tokens 为空时跳过注册。
func RegisterRoutes(r *gin.Engine, deps Dependencies) {
	if len(deps.Tokens) == 0 {
		if deps.Logger != nil {
			deps.Logger.Info("api.tokens 未配置，跳过 REST API 注册")
		}
		return
	}

	v1 := r.Group("/api/v1", TokenAuth(deps.Tokens))

	h := &handlers{deps: deps}
	v1.GET("/status", h.getStatus)
	v1.GET("/tasks", h.listTasks)
	v1.GET("/tasks/:id", h.getTask)
	v1.POST("/tasks/:id/retry", h.retryTask)
	v1.POST("/repos/:owner/:repo/review-pr", h.triggerReview)
	v1.POST("/repos/:owner/:repo/fix-issue", h.triggerFix)
}

// handlers 聚合所有 API handler
type handlers struct {
	deps Dependencies
}
```

注意：handler 方法（`getStatus`、`listTasks` 等）在后续 Task 中实现，此处先定义占位：

```go
func (h *handlers) getStatus(c *gin.Context)    { Success(c, 200, gin.H{}) }
func (h *handlers) listTasks(c *gin.Context)     { Success(c, 200, gin.H{}) }
func (h *handlers) getTask(c *gin.Context)       { Success(c, 200, gin.H{}) }
func (h *handlers) retryTask(c *gin.Context)     { Success(c, 200, gin.H{}) }
func (h *handlers) triggerReview(c *gin.Context)  { Success(c, 200, gin.H{}) }
func (h *handlers) triggerFix(c *gin.Context)     { Success(c, 200, gin.H{}) }
```

**Step 3: 运行测试**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/api/ -run TestRegisterRoutes -v`
Expected: PASS

**Step 4: 提交**

```
feat(api): Router 骨架 + Dependencies 结构 + 占位 handler
```

---

### Task 6: Status Handler

**Files:**
- Create: `internal/api/status_handler.go`
- Test: `internal/api/status_handler_test.go`

**Step 1: 写测试**

```go
// internal/api/status_handler_test.go
func TestGetStatus(t *testing.T) {
	router, deps := setupTestRouter(t)  // 辅助函数，构造带 mock 依赖的 router
	_ = deps

	req := authedRequest("GET", "/api/v1/status", nil, "dtw_test1234567890")
	w := httptest.NewRecorder()
	router.ServeHTTP(w, req)

	assert.Equal(t, http.StatusOK, w.Code)
	var resp map[string]any
	require.NoError(t, json.Unmarshal(w.Body.Bytes(), &resp))
	data := resp["data"].(map[string]any)
	assert.Equal(t, "0.3.3", data["version"])
	assert.Contains(t, data, "uptime_seconds")
	assert.Contains(t, data, "redis_connected")
}
```

**Step 2: 实现**

将 `getStatus` 从 router.go 的占位方法移到 `internal/api/status_handler.go`：

```go
package api

import (
	"net/http"
	"time"

	"github.com/gin-gonic/gin"
)

func (h *handlers) getStatus(c *gin.Context) {
	redisOK := false
	if h.deps.QueueClient != nil {
		redisOK = h.deps.QueueClient.Ping(c.Request.Context()) == nil
	}

	activeWorkers := 0
	if h.deps.Pool != nil {
		activeWorkers = h.deps.Pool.Stats().Active
	}

	Success(c, http.StatusOK, gin.H{
		"version":         h.deps.Version,
		"uptime_seconds":  int(time.Since(h.deps.StartTime).Seconds()),
		"redis_connected": redisOK,
		"active_workers":  activeWorkers,
	})
}
```

注意：`queue_size` 需要 `asynq.Inspector.GetQueueInfo()`，但当前 `queue.Client` 未暴露此方法。此处先省略 `queue_size`（或传 0），后续可在 `queue.Client` 上追加一个 `QueueSize(ctx) int` 方法。也可以在 Dependencies 中直接注入 `*asynq.Inspector`。根据设计文档选择后者更直接。

**Step 3: 测试 + 提交**

```
feat(api): GET /api/v1/status handler
```

---

### Task 7: Task Handlers（list/detail/retry）

**Files:**
- Create: `internal/api/task_handler.go`
- Test: `internal/api/task_handler_test.go`

**Step 1: 写测试**

覆盖场景：
- `GET /api/v1/tasks` — 无过滤、repo 过滤、status 过滤、type 过滤、limit/offset
- `GET /api/v1/tasks/:id` — 存在 / 不存在
- `POST /api/v1/tasks/:id/retry` — 成功 / 任务不存在 / 非终态任务 409

**Step 2: 实现**

```go
// internal/api/task_handler.go
package api

import (
	"net/http"
	"strconv"

	"github.com/gin-gonic/gin"

	"otws19.zicp.vip/kelin/dtworkflow/internal/model"
	"otws19.zicp.vip/kelin/dtworkflow/internal/store"
)

func (h *handlers) listTasks(c *gin.Context) {
	limit, _ := strconv.Atoi(c.DefaultQuery("limit", "20"))
	if limit <= 0 || limit > 100 {
		limit = 20
	}
	offset, _ := strconv.Atoi(c.DefaultQuery("offset", "0"))
	if offset < 0 {
		offset = 0
	}

	opts := store.ListOptions{
		RepoFullName: c.Query("repo"),
		Limit:        limit,
		Offset:       offset,
	}
	if s := c.Query("status"); s != "" {
		status := model.TaskStatus(s)
		if !status.IsValid() {
			Error(c, http.StatusBadRequest, ErrCodeBadRequest, "无效的状态值: "+s)
			return
		}
		opts.Status = status
	}
	if t := c.Query("type"); t != "" {
		taskType := model.TaskType(t)
		if !taskType.IsValid() {
			Error(c, http.StatusBadRequest, ErrCodeBadRequest, "无效的任务类型: "+t)
			return
		}
		opts.TaskType = taskType
	}

	tasks, err := h.deps.Store.ListTasks(c.Request.Context(), opts)
	if err != nil {
		h.deps.Logger.Error("列出任务失败", "error", err)
		Error(c, http.StatusInternalServerError, ErrCodeInternalError, "查询任务失败")
		return
	}

	items := make([]gin.H, 0, len(tasks))
	for _, t := range tasks {
		items = append(items, taskToJSON(t))
	}
	Success(c, http.StatusOK, gin.H{
		"tasks":  items,
		"limit":  limit,
		"offset": offset,
	})
}

func (h *handlers) getTask(c *gin.Context) {
	id := c.Param("id")
	record, err := h.deps.Store.GetTask(c.Request.Context(), id)
	if err != nil {
		h.deps.Logger.Error("查询任务详情失败", "id", id, "error", err)
		Error(c, http.StatusInternalServerError, ErrCodeInternalError, "查询任务失败")
		return
	}
	if record == nil {
		Error(c, http.StatusNotFound, ErrCodeNotFound, "任务不存在: "+id)
		return
	}
	Success(c, http.StatusOK, taskDetailToJSON(record))
}

func (h *handlers) retryTask(c *gin.Context) {
	// 复用 internal/cmd/task.go 中的 retryTask 逻辑（已提取为公共函数）
	// 或直接在 handler 中内联实现相同逻辑
	id := c.Param("id")
	ctx := c.Request.Context()

	record, err := h.deps.Store.GetTask(ctx, id)
	if err != nil {
		h.deps.Logger.Error("查询任务失败", "id", id, "error", err)
		Error(c, http.StatusInternalServerError, ErrCodeInternalError, "查询任务失败")
		return
	}
	if record == nil {
		Error(c, http.StatusNotFound, ErrCodeNotFound, "任务不存在: "+id)
		return
	}
	if record.Status != model.TaskStatusFailed && record.Status != model.TaskStatusCancelled {
		Error(c, http.StatusConflict, ErrCodeConflict,
			"任务状态为 "+string(record.Status)+"，只有 failed 或 cancelled 状态的任务可以重试")
		return
	}

	// 重置并重新入队（逻辑与 cmd/task.go retryTask 一致）
	record, message, err := retryTaskViaQueue(ctx, h.deps.Store, h.deps.QueueClient, id)
	if err != nil {
		h.deps.Logger.Error("重试任务失败", "id", id, "error", err)
		Error(c, http.StatusInternalServerError, ErrCodeInternalError, "重试任务失败")
		return
	}

	Success(c, http.StatusOK, gin.H{
		"task_id": id,
		"message": message,
	})
}

// taskToJSON 列表简要格式
func taskToJSON(r *model.TaskRecord) gin.H {
	return gin.H{
		"id":           r.ID,
		"type":         r.TaskType,
		"status":       r.Status,
		"repo":         r.RepoFullName,
		"pr_number":    r.PRNumber,
		"issue_number": r.Payload.IssueNumber,
		"triggered_by": r.TriggeredBy,
		"created_at":   r.CreatedAt,
		"updated_at":   r.UpdatedAt,
	}
}

// taskDetailToJSON 详情格式（比列表多 result/error/started_at/completed_at）
func taskDetailToJSON(r *model.TaskRecord) gin.H {
	j := taskToJSON(r)
	j["result"] = r.Result
	j["error_message"] = r.Error
	j["started_at"] = r.StartedAt
	j["completed_at"] = r.CompletedAt
	return j
}
```

**注意：** `retryTaskViaQueue` 需要把 `internal/cmd/task.go` 中的 `retryTask` 函数提取为可复用的公共函数。有两种方案：
- **方案 A**：把 `retryTask` 移到 `internal/queue/` 包，暴露为公共函数
- **方案 B**：在 `internal/api/task_handler.go` 中内联实现相同逻辑（代码重复但解耦更清晰）

推荐**方案 A**：在 `internal/queue/retry.go` 中提取 `RetryTask(ctx, store, enqueuer, id) (*model.TaskRecord, string, error)`，`cmd/task.go` 和 `api/task_handler.go` 都调用它。

**Step 3: 测试 + 提交**

```
feat(api): Task handlers — list/detail/retry
```

---

### Task 8: 手动入队函数 — EnqueueManualReview / EnqueueManualFix

**Files:**
- Modify: `internal/queue/enqueue.go` — 新增两个公共方法
- Test: `internal/queue/enqueue_test.go`

**Step 1: 写测试**

```go
func TestEnqueueManualReview(t *testing.T) {
	mockClient := &mockEnqueuer{asynqID: "asynq-manual-1"}
	mockStore := newMockStore()
	h := queue.NewEnqueueHandler(mockClient, nil, mockStore, slog.Default())

	payload := model.TaskPayload{
		TaskType:     model.TaskTypeReviewPR,
		RepoOwner:    "owner",
		RepoName:     "repo",
		RepoFullName: "owner/repo",
		CloneURL:     "https://gitea.example.com/owner/repo.git",
		PRNumber:     42,
		PRTitle:      "test PR",
	}

	taskID, err := h.EnqueueManualReview(context.Background(), payload, "manual:admin")
	require.NoError(t, err)
	assert.NotEmpty(t, taskID)

	// 验证 store 中的记录
	record, _ := mockStore.GetTask(context.Background(), taskID)
	assert.Equal(t, "manual:admin", record.TriggeredBy)
	assert.Equal(t, model.TaskStatusQueued, record.Status)
}

func TestEnqueueManualFix(t *testing.T) {
	// 类似测试，验证 fix_issue 入队 + triggered_by 设置
}
```

**Step 2: 实现**

在 `internal/queue/enqueue.go` 新增：

```go
// EnqueueManualReview 手动触发 PR 评审入队。
// payload 由 API handler 组装（含完整 PR 信息），triggeredBy 格式为 "manual:{identity}"。
// 跳过 delivery_id 幂等检查，执行 Cancel-and-Replace。
func (h *EnqueueHandler) EnqueueManualReview(ctx context.Context, payload model.TaskPayload, triggeredBy string) (string, error) {
	payload.TaskType = model.TaskTypeReviewPR
	payload.DeliveryID = generateManualDeliveryID()

	// Cancel-and-Replace
	activeTasks, superseded := h.listActivePRTasks(ctx, payload.RepoFullName, payload.PRNumber)
	payload.SupersededCount = superseded.Count
	payload.PreviousHeadSHA = superseded.LastHeadSHA

	record := &model.TaskRecord{
		TaskType:     model.TaskTypeReviewPR,
		Priority:     model.PriorityHigh,
		RepoFullName: payload.RepoFullName,
		PRNumber:     payload.PRNumber,
		DeliveryID:   payload.DeliveryID,
		TriggeredBy:  triggeredBy,
	}

	if err := h.enqueueTask(ctx, payload, record); err != nil {
		return "", err
	}
	h.cancelTasks(ctx, activeTasks)

	h.logger.InfoContext(ctx, "手动 PR 评审任务已入队",
		"task_id", record.ID, "repo", payload.RepoFullName,
		"pr", payload.PRNumber, "triggered_by", triggeredBy)
	return record.ID, nil
}

// EnqueueManualFix 手动触发 Issue 修复入队。
func (h *EnqueueHandler) EnqueueManualFix(ctx context.Context, payload model.TaskPayload, triggeredBy string) (string, error) {
	payload.TaskType = model.TaskTypeFixIssue
	payload.DeliveryID = generateManualDeliveryID()

	record := &model.TaskRecord{
		TaskType:     model.TaskTypeFixIssue,
		Priority:     model.PriorityNormal,
		RepoFullName: payload.RepoFullName,
		DeliveryID:   payload.DeliveryID,
		TriggeredBy:  triggeredBy,
	}

	if err := h.enqueueTask(ctx, payload, record); err != nil {
		return "", err
	}

	h.logger.InfoContext(ctx, "手动 Issue 修复任务已入队",
		"task_id", record.ID, "repo", payload.RepoFullName,
		"issue", payload.IssueNumber, "triggered_by", triggeredBy)
	return record.ID, nil
}

// generateManualDeliveryID 生成手动触发的合成 delivery ID
func generateManualDeliveryID() string {
	return fmt.Sprintf("manual-%d-%s", time.Now().UnixMilli(), uuid.New().String()[:8])
}
```

**Step 3: 测试 + 提交**

```
feat(queue): 新增 EnqueueManualReview/EnqueueManualFix 手动入队
```

---

### Task 9: Review Handler + Fix Handler（API 触发）

**Files:**
- Create: `internal/api/review_handler.go`
- Create: `internal/api/fix_handler.go`
- Test: `internal/api/review_handler_test.go`
- Test: `internal/api/fix_handler_test.go`

**Step 1: 写测试**

Review handler 测试覆盖：
- pr_number 无效 → 400
- PR 不存在/已关闭 → 400
- Gitea API 不可达 → 502
- 正常入队 → 202 + task_id
- owner/repo 路径参数校验

Fix handler 测试覆盖类似，替换为 issue_number。

**Step 2: 实现 review handler**

```go
// internal/api/review_handler.go
package api

import (
	"fmt"
	"net/http"
	"regexp"

	"github.com/gin-gonic/gin"

	"otws19.zicp.vip/kelin/dtworkflow/internal/model"
)

var repoSegmentRE = regexp.MustCompile(`^[a-zA-Z0-9._-]+$`)

func (h *handlers) triggerReview(c *gin.Context) {
	owner := c.Param("owner")
	repo := c.Param("repo")
	if !validateRepoParams(c, owner, repo) {
		return
	}

	var req struct {
		PRNumber int64 `json:"pr_number"`
	}
	if err := c.ShouldBindJSON(&req); err != nil || req.PRNumber <= 0 {
		Error(c, http.StatusBadRequest, ErrCodeBadRequest, "pr_number 必须为正整数")
		return
	}

	ctx := c.Request.Context()
	identity := c.GetString(ContextKeyIdentity)

	// 调用 Gitea API 校验 PR 存在且 open
	pr, _, err := h.deps.GiteaClient.GetPullRequest(ctx, owner, repo, req.PRNumber)
	if err != nil {
		h.deps.Logger.Error("Gitea API 查询 PR 失败", "error", err)
		Error(c, http.StatusBadGateway, ErrCodeBadGateway, "Gitea API 不可达")
		return
	}
	if pr.State != "open" {
		Error(c, http.StatusBadRequest, ErrCodeBadRequest,
			fmt.Sprintf("PR #%d 状态为 %s，需要 open", req.PRNumber, pr.State))
		return
	}

	// 从 Gitea 响应组装完整 TaskPayload
	payload := model.TaskPayload{
		RepoOwner:    owner,
		RepoName:     repo,
		RepoFullName: owner + "/" + repo,
		CloneURL:     pr.Base.Repo.CloneURL,
		PRNumber:     pr.Number,
		PRTitle:      pr.Title,
		BaseRef:      pr.Base.Ref,
		HeadRef:      pr.Head.Ref,
		HeadSHA:      pr.Head.SHA,
	}

	triggeredBy := "manual:" + identity
	taskID, err := h.deps.EnqueueHandler.EnqueueManualReview(ctx, payload, triggeredBy)
	if err != nil {
		h.deps.Logger.Error("入队评审任务失败", "error", err)
		Error(c, http.StatusInternalServerError, ErrCodeInternalError, "创建评审任务失败")
		return
	}

	Success(c, http.StatusAccepted, gin.H{
		"task_id": taskID,
		"message": "评审任务已创建",
	})
}

func validateRepoParams(c *gin.Context, owner, repo string) bool {
	if !repoSegmentRE.MatchString(owner) || len(owner) > 100 {
		Error(c, http.StatusBadRequest, ErrCodeBadRequest, "无效的 owner 参数")
		return false
	}
	if !repoSegmentRE.MatchString(repo) || len(repo) > 100 {
		Error(c, http.StatusBadRequest, ErrCodeBadRequest, "无效的 repo 参数")
		return false
	}
	return true
}
```

**Step 3: 实现 fix handler**

```go
// internal/api/fix_handler.go — 结构同 review_handler.go
// 差异：调用 GetIssue 替代 GetPullRequest，调用 EnqueueManualFix
```

**注意：** `Dependencies` 结构需要新增 `EnqueueHandler *queue.EnqueueHandler` 字段。

**Step 4: 测试 + 提交**

```
feat(api): Review/Fix trigger handlers（校验 + Gitea API + 入队）
```

---

### Task 10: serve.go 接线 — 注册 API 路由

**Files:**
- Modify: `internal/cmd/serve.go` — 在 webhook 注册后调用 `api.RegisterRoutes`

**Step 1: 修改 serve.go**

在 `serve.go` 的 webhook 注册之后（约 638 行后）新增：

```go
// 注册 REST API 路由（需认证）
if cfg.AppCfg != nil {
	apiDeps := api.Dependencies{
		Store:          deps.Store,
		QueueClient:    deps.QueueClient,
		GiteaClient:    deps.GiteaClient,
		Pool:           deps.Pool,
		EnqueueHandler: deps.Handler.(*queue.EnqueueHandler), // 类型断言
		Tokens:         cfg.AppCfg.API.Tokens,
		Version:        version,
		StartTime:      time.Now(),
		Logger:         slog.Default(),
	}
	api.RegisterRoutes(router, apiDeps)
}
```

**注意：** `deps.Handler` 是 `webhook.Handler` 接口，需要类型断言为 `*queue.EnqueueHandler` 才能调用 `EnqueueManualReview`/`EnqueueManualFix`。更好的做法是让 `ServiceDeps` 直接暴露 `*queue.EnqueueHandler` 类型（或将手动入队能力抽成独立接口）。

**Step 2: 更新示例配置**

在 `configs/dtworkflow.example.yaml` 新增 `api` 配置段：

```yaml
# === REST API 配置 ===
# 用于远程客户端 (dtw) 认证。为空时 API 不注册。
api:
  tokens:
    - token: "dtw_替换为你的安全token"
      identity: "admin"
    # - token: "dtw_ci_bot_token_here"
    #   identity: "ci-bot"
```

**Step 3: 运行集成测试**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go build ./... && go test ./... -count=1`
Expected: 编译通过，全量测试 PASS

**Step 4: 提交**

```
feat(serve): 接线 REST API 路由到 Gin Engine
```

---

### Task 11: 删除空壳命令

**Files:**
- Delete: `internal/cmd/fix_issue.go`
- Delete: `internal/cmd/review_pr.go`
- Delete: `internal/cmd/gen_tests.go`
- Modify: `internal/cmd/commands_test.go` — 移除相关测试

**Step 1: 检查引用**

确认这三个文件只在 `init()` 中通过 `rootCmd.AddCommand()` 注册，无其他引用。

**Step 2: 删除文件**

```bash
rm internal/cmd/fix_issue.go internal/cmd/review_pr.go internal/cmd/gen_tests.go
```

**Step 3: 更新测试**

在 `internal/cmd/commands_test.go` 中移除对 `fix-issue`、`review-pr`、`gen-tests` 子命令的存在性断言。

**Step 4: 运行测试确保无编译错误**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go build ./... && go test ./internal/cmd/ -v`
Expected: PASS

**Step 5: 提交**

```
refactor(cmd): 删除 review-pr/fix-issue/gen-tests 空壳命令（D6）
```

---

## Part B: 瘦客户端 `dtw`

### Task 12: 客户端配置管理 — hosts.yml 读写

**Files:**
- Create: `internal/dtw/config.go`
- Test: `internal/dtw/config_test.go`

**Step 1: 写测试**

```go
// internal/dtw/config_test.go
package dtw

import (
	"os"
	"path/filepath"
	"testing"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)

func TestConfigSaveLoad(t *testing.T) {
	dir := t.TempDir()
	path := filepath.Join(dir, "hosts.yml")

	cfg := &HostsConfig{
		Active: "prod",
		Servers: map[string]ServerConfig{
			"prod": {URL: "https://prod:8080", Token: "dtw_prod_token_12345"},
		},
	}
	require.NoError(t, SaveHostsConfig(path, cfg))

	// 校验文件权限
	info, _ := os.Stat(path)
	assert.Equal(t, os.FileMode(0600), info.Mode().Perm())

	loaded, err := LoadHostsConfig(path)
	require.NoError(t, err)
	assert.Equal(t, "prod", loaded.Active)
	assert.Equal(t, "https://prod:8080", loaded.Servers["prod"].URL)
}

func TestConfigNotFound(t *testing.T) {
	_, err := LoadHostsConfig("/nonexistent/hosts.yml")
	assert.Error(t, err)
}

func TestResolveServer_FlagOverride(t *testing.T) {
	cfg := &HostsConfig{
		Active: "prod",
		Servers: map[string]ServerConfig{
			"prod":    {URL: "https://prod:8080", Token: "dtw_prod_token_12345"},
			"staging": {URL: "https://staging:8080", Token: "dtw_stag_token_12345"},
		},
	}
	srv, err := cfg.ResolveServer("staging")
	require.NoError(t, err)
	assert.Equal(t, "https://staging:8080", srv.URL)
}

func TestResolveServer_EnvOverride(t *testing.T) {
	t.Setenv("DTW_SERVER_URL", "https://env:9090")
	t.Setenv("DTW_TOKEN", "dtw_env_token_1234567")

	cfg := &HostsConfig{Active: "prod", Servers: map[string]ServerConfig{
		"prod": {URL: "https://prod:8080", Token: "dtw_prod_token_12345"},
	}}

	srv, err := cfg.ResolveServer("")
	require.NoError(t, err)
	assert.Equal(t, "https://env:9090", srv.URL)
	assert.Equal(t, "dtw_env_token_1234567", srv.Token)
}
```

**Step 2: 实现**

```go
// internal/dtw/config.go
package dtw

import (
	"fmt"
	"os"
	"path/filepath"

	"gopkg.in/yaml.v3"
)

type HostsConfig struct {
	Active  string                  `yaml:"active"`
	Servers map[string]ServerConfig `yaml:"servers"`
}

type ServerConfig struct {
	URL   string `yaml:"url"`
	Token string `yaml:"token"`
}

func DefaultConfigPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".config", "dtw", "hosts.yml")
}

func LoadHostsConfig(path string) (*HostsConfig, error) {
	data, err := os.ReadFile(path)
	if err != nil {
		return nil, fmt.Errorf("读取配置文件失败: %w", err)
	}
	// 校验文件权限
	info, err := os.Stat(path)
	if err == nil && info.Mode().Perm() != 0600 {
		fmt.Fprintf(os.Stderr, "警告: %s 权限不安全 (%04o)，建议设置为 0600\n", path, info.Mode().Perm())
	}
	var cfg HostsConfig
	if err := yaml.Unmarshal(data, &cfg); err != nil {
		return nil, fmt.Errorf("解析配置文件失败: %w", err)
	}
	return &cfg, nil
}

func SaveHostsConfig(path string, cfg *HostsConfig) error {
	if err := os.MkdirAll(filepath.Dir(path), 0700); err != nil {
		return fmt.Errorf("创建配置目录失败: %w", err)
	}
	data, err := yaml.Marshal(cfg)
	if err != nil {
		return fmt.Errorf("序列化配置失败: %w", err)
	}
	return os.WriteFile(path, data, 0600)
}

// ResolveServer 解析最终目标服务器，优先级：flag > env > active
func (c *HostsConfig) ResolveServer(flagServer string) (ServerConfig, error) {
	// 环境变量覆盖
	envURL := os.Getenv("DTW_SERVER_URL")
	envToken := os.Getenv("DTW_TOKEN")
	if envURL != "" && envToken != "" {
		return ServerConfig{URL: envURL, Token: envToken}, nil
	}

	name := flagServer
	if name == "" {
		name = c.Active
	}
	if name == "" {
		return ServerConfig{}, fmt.Errorf("未指定目标服务器，请运行 dtw auth login 或使用 --server")
	}
	srv, ok := c.Servers[name]
	if !ok {
		return ServerConfig{}, fmt.Errorf("服务器 \"%s\" 未配置，请运行 dtw auth login", name)
	}
	return srv, nil
}
```

**Step 3: 测试 + 提交**

```
feat(dtw): hosts.yml 配置管理（读写/权限/优先级链）
```

---

### Task 13: HTTP 客户端封装

**Files:**
- Create: `internal/dtw/client.go`
- Test: `internal/dtw/client_test.go`

**Step 1: 写测试（使用 httptest.Server）**

覆盖：请求构造、Authorization header、JSON 序列化/反序列化、错误解析

**Step 2: 实现**

```go
// internal/dtw/client.go
package dtw

import (
	"bytes"
	"context"
	"encoding/json"
	"fmt"
	"io"
	"net/http"
)

type Client struct {
	baseURL    string
	token      string
	httpClient *http.Client
}

func NewClient(baseURL, token string) *Client {
	return &Client{
		baseURL:    baseURL,
		token:      token,
		httpClient: &http.Client{},
	}
}

type APIError struct {
	Code    string `json:"code"`
	Message string `json:"message"`
}

func (e *APIError) Error() string {
	return fmt.Sprintf("%s: %s", e.Code, e.Message)
}

func (c *Client) Do(ctx context.Context, method, path string, body, result any) error {
	var bodyReader io.Reader
	if body != nil {
		data, err := json.Marshal(body)
		if err != nil {
			return fmt.Errorf("请求序列化失败: %w", err)
		}
		bodyReader = bytes.NewReader(data)
	}

	req, err := http.NewRequestWithContext(ctx, method, c.baseURL+path, bodyReader)
	if err != nil {
		return fmt.Errorf("创建请求失败: %w", err)
	}
	req.Header.Set("Authorization", "Bearer "+c.token)
	if body != nil {
		req.Header.Set("Content-Type", "application/json")
	}

	resp, err := c.httpClient.Do(req)
	if err != nil {
		return fmt.Errorf("请求失败: %w", err)
	}
	defer resp.Body.Close()

	respBody, err := io.ReadAll(io.LimitReader(resp.Body, 10<<20))
	if err != nil {
		return fmt.Errorf("读取响应失败: %w", err)
	}

	if resp.StatusCode >= 400 {
		var errResp struct {
			Error APIError `json:"error"`
		}
		if json.Unmarshal(respBody, &errResp) == nil && errResp.Error.Code != "" {
			return &APIError{Code: errResp.Error.Code, Message: errResp.Error.Message}
		}
		return fmt.Errorf("HTTP %d: %s", resp.StatusCode, string(respBody))
	}

	if result != nil {
		var dataResp struct {
			Data json.RawMessage `json:"data"`
		}
		if err := json.Unmarshal(respBody, &dataResp); err != nil {
			return fmt.Errorf("响应解析失败: %w", err)
		}
		if err := json.Unmarshal(dataResp.Data, result); err != nil {
			return fmt.Errorf("数据解析失败: %w", err)
		}
	}
	return nil
}
```

**Step 3: 测试 + 提交**

```
feat(dtw): HTTP 客户端封装（自动认证/JSON 序列化/错误处理）
```

---

### Task 14: `--wait` 轮询逻辑

**Files:**
- Create: `internal/dtw/wait.go`
- Test: `internal/dtw/wait_test.go`

**Step 1: 写测试**

覆盖：轮询间隔策略（前 30s 2s 间隔，之后 5s）、终态检测、超时退出

**Step 2: 实现**

```go
// internal/dtw/wait.go
package dtw

import (
	"context"
	"fmt"
	"time"
)

type WaitOptions struct {
	Timeout       time.Duration
	CheckInterval func(elapsed time.Duration) time.Duration // 动态间隔
}

func DefaultWaitOptions() WaitOptions {
	return WaitOptions{
		Timeout: 30 * time.Minute,
		CheckInterval: func(elapsed time.Duration) time.Duration {
			if elapsed < 30*time.Second {
				return 2 * time.Second
			}
			return 5 * time.Second
		},
	}
}

// TaskStatus 从 API 返回的任务状态
type TaskStatus struct {
	ID     string `json:"id"`
	Status string `json:"status"`
	Result string `json:"result,omitempty"`
	Error  string `json:"error_message,omitempty"`
}

func isTerminal(status string) bool {
	switch status {
	case "succeeded", "failed", "cancelled":
		return true
	}
	return false
}

// WaitForTask 轮询任务直到终态或超时
func WaitForTask(ctx context.Context, client *Client, taskID string, opts WaitOptions) (*TaskStatus, error) {
	start := time.Now()
	deadline := start.Add(opts.Timeout)

	for {
		var status TaskStatus
		if err := client.Do(ctx, "GET", "/api/v1/tasks/"+taskID, nil, &status); err != nil {
			return nil, fmt.Errorf("查询任务状态失败: %w", err)
		}

		if isTerminal(status.Status) {
			return &status, nil
		}

		if time.Now().After(deadline) {
			return &status, fmt.Errorf("等待超时（%s），当前状态: %s", opts.Timeout, status.Status)
		}

		interval := opts.CheckInterval(time.Since(start))
		select {
		case <-ctx.Done():
			return &status, ctx.Err()
		case <-time.After(interval):
		}
	}
}
```

**Step 3: 测试 + 提交**

```
feat(dtw): --wait 轮询逻辑（渐进间隔 + 超时）
```

---

### Task 15: 输出格式化

**Files:**
- Create: `internal/dtw/output.go`
- Test: `internal/dtw/output_test.go`

简短实现：`PrintJSON(data any)` 和 `PrintHuman(text string)` 两个函数，由全局 `--json` flag 控制。

**提交:**

```
feat(dtw): 双模式输出（JSON / 人类可读）
```

---

### Task 16: dtw Cobra 命令 — root + auth + 全部业务命令

**Files:**
- Create: `internal/dtw/cmd/root.go` — 全局 flags（`--server`、`--json`、`--verbose`）
- Create: `internal/dtw/cmd/auth.go` — login/logout/status/switch
- Create: `internal/dtw/cmd/status.go` — `dtw status`
- Create: `internal/dtw/cmd/task.go` — `dtw task list/status/retry`
- Create: `internal/dtw/cmd/review_pr.go` — `dtw review-pr`
- Create: `internal/dtw/cmd/fix_issue.go` — `dtw fix-issue`
- Create: `internal/dtw/cmd/version.go` — `dtw version`

**实现顺序建议:** root → version → auth → status → task → review-pr → fix-issue

每个命令文件对应一个 commit。代码结构参考 `internal/cmd/` 的模式，但依赖 `internal/dtw/` 包的 Client 和 Config。

**关键实现点:**

1. **root.go** — `PersistentPreRunE` 中加载 `hosts.yml`、解析 `--server` flag、创建 `Client` 实例
2. **auth login** — 交互式输入（`bufio.Scanner`）+ 非交互式 flags（`--name/--url/--token`）+ 连通性验证
3. **review-pr** — `--repo`、`--pr`（必填）、`--no-wait`、`--timeout`
4. **fix-issue** — `--repo`、`--issue`（必填）、`--no-wait`、`--timeout`
5. 所有业务命令调用 `Client.Do()` 后根据 `--json` flag 决定输出格式

**提交:** 每个命令文件单独提交，例如：

```
feat(dtw): root 命令 + 全局 flags
feat(dtw): auth login/logout/status/switch 命令
feat(dtw): status 命令
feat(dtw): task list/status/retry 命令
feat(dtw): review-pr 命令（--wait 默认启用）
feat(dtw): fix-issue 命令（--wait 默认启用）
feat(dtw): version 命令
```

---

### Task 17: dtw 入口 + Makefile 构建目标

**Files:**
- Create: `cmd/dtw/main.go`
- Modify: `Makefile`

**Step 1: 创建入口**

```go
// cmd/dtw/main.go
package main

import (
	"os"

	dtwcmd "otws19.zicp.vip/kelin/dtworkflow/internal/dtw/cmd"
)

func main() {
	if err := dtwcmd.Execute(); err != nil {
		os.Exit(1)
	}
}
```

**Step 2: 更新 Makefile**

在现有 build 目标后新增：

```makefile
## build-dtw: 构建 dtw 瘦客户端到 bin/
build-dtw:
	CGO_ENABLED=0 go build -ldflags "$(LDFLAGS)" -o bin/dtw ./cmd/dtw

## build-dtw-linux: 交叉编译 dtw Linux amd64
build-dtw-linux:
	CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -ldflags "$(LDFLAGS)" -o bin/dtw-linux-amd64 ./cmd/dtw

## build-all: 构建所有二进制
build-all: build build-dtw
```

更新 `.PHONY` 行加入新目标。

**Step 3: 验证构建**

Run: `cd /Users/kelin/Workspace/DTWorkflow && make build-all`
Expected: `bin/dtworkflow` 和 `bin/dtw` 均生成成功

**Step 4: 提交**

```
feat: dtw 入口 + Makefile 构建目标
```

---

## Part C: 收尾

### Task 18: 全量测试 + 编译验证

**Step 1: 编译检查**

```bash
cd /Users/kelin/Workspace/DTWorkflow
go build ./...
CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build ./cmd/dtworkflow ./cmd/dtw
```

**Step 2: 全量测试**

```bash
go test -race -cover ./...
```

**Step 3: Lint**

```bash
golangci-lint run ./...
```

修复任何问题后提交。

---

### Task 19: 更新示例配置与文档

**Files:**
- Modify: `configs/dtworkflow.example.yaml` — 新增 `api` 配置段（如 Task 10 未完成）
- Verify: `dtw --help` 输出正确

**提交:**

```
docs: 更新示例配置，新增 api.tokens 段
```

---

## 实施顺序总结

| 序号 | Task | 核心变更 | 依赖 |
|------|------|---------|------|
| 1 | 配置扩展 | `APIConfig` + `TokenConfig` + 校验 | 无 |
| 2 | DB 迁移 | `triggered_by` 列 V17 | 无 |
| 3 | API 响应格式 | `response.go` | 无 |
| 4 | 认证中间件 | `middleware.go` | Task 1, 3 |
| 5 | Router 骨架 | `router.go` + Dependencies | Task 3, 4 |
| 6 | Status Handler | `status_handler.go` | Task 5 |
| 7 | Task Handlers | `task_handler.go` | Task 5 |
| 8 | 手动入队 | `EnqueueManualReview/Fix` | Task 2 |
| 9 | Review/Fix Handlers | `review_handler.go` / `fix_handler.go` | Task 5, 8 |
| 10 | serve.go 接线 | 注册 API 路由 | Task 5, 9 |
| 11 | 删除空壳命令 | 删除 3 个文件 | 无 |
| 12 | 客户端配置 | `internal/dtw/config.go` | 无 |
| 13 | HTTP 客户端 | `internal/dtw/client.go` | 无 |
| 14 | Wait 轮询 | `internal/dtw/wait.go` | Task 13 |
| 15 | 输出格式 | `internal/dtw/output.go` | 无 |
| 16 | Cobra 命令 | `internal/dtw/cmd/*.go` | Task 12-15 |
| 17 | dtw 入口 + Makefile | `cmd/dtw/main.go` | Task 16 |
| 18 | 全量验证 | 编译 + 测试 + lint | 全部 |
| 19 | 示例配置 | `dtworkflow.example.yaml` | Task 10 |

**并行可能性：** Task 1-3 可并行，Task 11-15 可并行，Part B（Task 12-17）在 Part A 的 Task 5 完成后即可开始客户端开发（不依赖 handler 实现完成）。
