# M1.5-M1.7 文档同步 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 基于 2026-03-23 12:00 之后已落地代码，同步更新 M1.5 到 M1.7 的主文档与实施记录，使路线图、设计文档与当前实现状态一致。

**Architecture:** 先以 `docs/ROADMAP.md` 作为对外状态来源完成主口径修正，再分别补齐 M1.5-6 与 M1.7 设计文档中的“已落地结果”和“当前边界”说明。最后对实施记录文档做最小措辞同步，确保不会把未实现能力写成已实现，也不会让已落地能力继续停留在“待做”状态。

**Tech Stack:** Markdown 文档、现有设计文档、Git 提交记录、Go 代码实现（`internal/store` / `internal/queue` / `internal/worker` / `internal/notify` / `internal/cmd`）

---

### Task 1: 更新路线图中的 M1.5-M1.7 状态

**Files:**
- Modify: `docs/ROADMAP.md`
- Reference: `docs/plans/2026-03-23-m1.5-6-task-execution-engine-design.md`
- Reference: `docs/plans/2026-03-23-notify-integration-design.md`
- Reference: `internal/cmd/serve.go`
- Reference: `internal/cmd/task.go`
- Reference: `internal/queue/processor.go`

**Step 1: 明确要改的三处核心口径**

在 `docs/ROADMAP.md` 中定位并记录以下内容需要调整：

1. `M1.5`、`M1.6` 的标题或说明需明确“本轮按 M1.5-6 合并交付”
2. 已落地事项要从未完成改为已完成
3. `M1.7` 中属于 M1.8 的配置化能力要保留边界说明

**Step 2: 修改 M1.5 段落**

将以下能力改为已完成或在说明中明确已落地：

- Redis + asynq 集成
- 任务类型定义
- 优先级 / 重试 / 超时机制
- 任务状态持久化到 SQLite
- CLI 命令：`dtworkflow task status/list/retry`

同时在段首或段尾补一句：

```md
> 说明：本轮 M1.5 与 M1.6 按“任务执行引擎”合并实施，以下条目按实际交付状态同步。
```

**Step 3: 修改 M1.6 段落**

将以下能力改为已完成或按当前实现修正文案：

- Docker SDK for Go 集成
- Worker 池管理
- Claude Code CLI 非交互执行链路
- `serve` 中的执行引擎组装

对“容器内 Git 仓库 clone + worktree 验证”保持谨慎表达；如果当前代码尚未形成稳定链路，不要直接勾成完成，可改为描述“执行容器链路已具备，真实仓库工作树链路待后续业务阶段继续接入”。

**Step 4: 修改 M1.7 段落**

将以下能力改为已完成：

- 通知接口定义（策略模式）
- Gitea 评论通知实现
- 主流程通知接线（通过说明补充）

对“按仓库/事件类型的通知路由配置”不要夸大为完整配置化实现；建议改成类似：

```md
- [x] 默认路由与最小可用通知分发
- [ ] 按仓库 / 事件类型的配置化通知路由（待 M1.8）
```

**Step 5: 修正 Phase 1 交付物与验证标准措辞**

至少补齐以下事实：

- Phase 1 交付物中应体现任务执行引擎与通知框架骨架已落地
- 验证标准要反映当前已打通的链路，不要继续停留在纯目标态表述
- 若“结果回写 Gitea 评论”当前只覆盖最终任务通知，应据实表述，不要写成完整业务评审回写

**Step 6: 人工复核文案边界**

逐项检查是否出现以下问题：

- 把 M1.8 配置化能力写成已完成
- 把 M2/M3 业务能力写成已完成
- 把容器执行基础设施误写成完整业务闭环

---

### Task 2: 补充 M1.5-6 设计文档中的实施落地结果

**Files:**
- Modify: `docs/plans/2026-03-23-m1.5-6-task-execution-engine-design.md`
- Reference: `internal/store/sqlite.go`
- Reference: `internal/queue/enqueue.go`
- Reference: `internal/queue/processor.go`
- Reference: `internal/queue/recovery.go`
- Reference: `internal/worker/pool.go`
- Reference: `internal/cmd/serve.go`
- Reference: `internal/cmd/task.go`

**Step 1: 在设计文档中选择插入位置**

优先在文档前部摘要区域之后，新增一个二级标题，例如：

```md
## 0. 实施落地结果（2026-03-23 同步）
```

保持该小节简洁，不重写整篇设计。

**Step 2: 写“代码落点映射”**

用列表说明当前设计已落地到以下模块：

- `internal/model/`：共享任务模型
- `internal/store/`：SQLite 持久化与迁移
- `internal/queue/`：入队、处理器、恢复机制
- `internal/worker/`：Docker Client、Pool、GC
- `internal/cmd/serve.go`：依赖组装与服务启动
- `internal/cmd/task.go`：任务查询与重试 CLI

**Step 3: 写“当前已达成能力”**

至少覆盖：

- Webhook 处理器已可替换为入队 handler
- SQLite + Redis 双写与恢复链路已存在
- asynq 消费端与 Worker 容器执行链路已接通
- `task status/list/retry` 已可使用
- `serve /readyz` 已增强为反映关键执行前提

**Step 4: 写“当前边界”**

明确指出：

- 该里程碑解决的是“基础设施执行引擎”而非完整 PR/Issue 业务逻辑
- 配置管理仍待 M1.8
- 更完整的业务取数 / 仓库 clone / worktree / 评审修复语义仍由后续阶段补齐

**Step 5: 检查是否与正文已有结论冲突**

若发现正文有“未来做 / 尚未做”的原表述与当前状态冲突，仅做最小修正，不大面积重写其他章节。

---

### Task 3: 同步 M1.7 通知接线设计文档

**Files:**
- Modify: `docs/plans/2026-03-23-notify-integration-design.md`
- Reference: `internal/cmd/serve.go`
- Reference: `internal/queue/processor.go`
- Reference: `internal/notify/router.go`
- Reference: `internal/notify/gitea_notifier.go`

**Step 1: 在背景或摘要处补“已接线”说明**

把原来“本轮目标”语气补充为“当前已完成结果”，例如新增一段：

```md
截至 2026-03-23 晚间，本设计对应的最小通知闭环已在代码中实现，并接入服务主链路。
```

**Step 2: 补充实际主流程链路**

明确写出当前已落地链路：

```text
serve -> BuildServiceDeps -> notify.Router -> queue.Processor -> GiteaNotifier
```

并说明最终状态落库后才发通知。

**Step 3: 校正文案与代码一致**

与当前代码保持一致地写明：

- `review_pr` 成功/失败会通知
- `fix_issue` 成功/失败会通知
- `gen_tests` 暂不通知
- 非最终状态（如 `retrying`）不通知
- 通知失败不改变任务状态与返回语义

**Step 4: 写清 M1.8 边界**

补充一句或一小段说明：

- 当前路由为最小默认规则
- 仓库级 / 事件级配置化路由仍待 M1.8 配置管理接入

**Step 5: 复核事件命名与正文表述**

如果文中仍把 `fix_issue` 成功描述成“已创建修复 PR”之类强语义，需要改回“任务执行完成”，避免超出当前实现。

---

### Task 4: 同步实施记录文档措辞

**Files:**
- Modify: `docs/plans/2026-03-23-notify-integration.md`
- Modify: `docs/plans/2026-03-23-serve-readiness.md`
- Reference: `internal/cmd/serve.go`
- Reference: `internal/queue/processor.go`

**Step 1: 检查两份实施记录中的时态**

重点查找以下措辞：

- “将会”
- “计划中”
- “待实现”
- “尚未接入”

**Step 2: 仅修正与现状冲突的句子**

如果某段已经被代码实现，就改成完成态；如果仍然未实现，则保持原状。

示例：

- 把“将在 `Processor` 中发送通知”改为“已在 `Processor` 中于最终状态落库后发送通知”
- 把“`/readyz` 将反映 notifier 状态”改为“`/readyz` 已反映 notifier 状态”

**Step 3: 避免过度扩写**

这一步只做措辞同步，不新增大量新设计内容，避免与 Task 2、Task 3 重复。

---

### Task 5: 做最终一致性复核

**Files:**
- Review: `docs/ROADMAP.md`
- Review: `docs/plans/2026-03-23-m1.5-6-task-execution-engine-design.md`
- Review: `docs/plans/2026-03-23-notify-integration-design.md`
- Review: `docs/plans/2026-03-23-notify-integration.md`
- Review: `docs/plans/2026-03-23-serve-readiness.md`

**Step 1: 逐文件检查三类一致性**

检查：

1. 与代码一致
2. 各文档之间不互相打架
3. 不提前宣称后续阶段能力已完成

**Step 2: 重点核对这些事实**

必须确认文档中准确反映：

- M1.5 与 M1.6 本轮为合并交付
- `task status/list/retry` 已存在
- 通知已经主流程接线
- 通知配置化仍待 M1.8
- `gen_tests` 暂不通知

**Step 3: 检查语言风格**

确保：

- 全部使用中文
- 语气准确，不夸张
- 路线图面向状态汇报，设计文档面向实现说明，各自职责清晰

**Step 4: 审查最终 diff**

确认修改是“同步现状”而不是“重写规划”。如果某处改动超出本轮目标，回退到最小必要修改。

**Step 5: 记录结果并准备交付**

整理最终说明时，按以下结构反馈：

1. 修改了哪些文档
2. 每份文档同步了什么事实
3. 仍然保留了哪些边界未宣称完成

**Step 6: Commit（仅在用户明确要求时执行）**

若用户明确要求提交，再执行：

```bash
git add docs/ROADMAP.md docs/plans/2026-03-23-m1.5-6-task-execution-engine-design.md docs/plans/2026-03-23-notify-integration-design.md docs/plans/2026-03-23-notify-integration.md docs/plans/2026-03-23-serve-readiness.md docs/plans/2026-03-23-m1.5-m1.7-doc-sync-design.md docs/plans/2026-03-23-m1.5-m1.7-doc-sync.md
git commit -m "docs: 同步 M1.5-M1.7 实施状态与设计记录"
```
