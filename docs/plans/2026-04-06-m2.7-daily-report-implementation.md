# M2.7 每日评审统计报告 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 实现每日定时通过飞书发送 PR 评审统计摘要，包含趋势对比、仓库分组和关键 PR 高亮。

**Architecture:** asynq Scheduler 按 cron 表达式定时入队 `gen_daily_report` 任务，Processor 直接在进程内执行（不需要 Docker 容器），ReportGenerator 编排数据收集、格式化和飞书发送。

**Tech Stack:** Go, asynq Scheduler, SQLite, 飞书 Webhook API

**Design doc:** `docs/plans/2026-04-06-m2.7-daily-report-design.md`

---

### Task 1: 统计数据模型

**Files:**
- Create: `internal/report/model.go`
- Test: `internal/report/model_test.go`

**Step 1: Write the failing test**

```go
// internal/report/model_test.go
package report

import "testing"

func TestAggregatedStats_IsEmpty(t *testing.T) {
	tests := []struct {
		name  string
		stats AggregatedStats
		want  bool
	}{
		{"zero value", AggregatedStats{}, true},
		{"has reviews", AggregatedStats{ReviewCount: 1}, false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			if got := tt.stats.IsEmpty(); got != tt.want {
				t.Errorf("IsEmpty() = %v, want %v", got, tt.want)
			}
		})
	}
}

func TestDailyStats_IsEmpty(t *testing.T) {
	tests := []struct {
		name  string
		stats DailyStats
		want  bool
	}{
		{"empty", DailyStats{Date: "2026-04-05"}, true},
		{"has total", DailyStats{Date: "2026-04-05", Total: AggregatedStats{ReviewCount: 1}}, false},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			if got := tt.stats.IsEmpty(); got != tt.want {
				t.Errorf("IsEmpty() = %v, want %v", got, tt.want)
			}
		})
	}
}
```

**Step 2: Run test to verify it fails**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/report/ -run TestAggregatedStats_IsEmpty -v`
Expected: FAIL — package/types not defined

**Step 3: Write minimal implementation**

```go
// internal/report/model.go
package report

import "time"

// TimeRange 查询时间窗口
type TimeRange struct {
	Start time.Time // inclusive
	End   time.Time // exclusive
}

// DailyStats 每日统计汇总
type DailyStats struct {
	Date      string          // "2026-04-05"
	RepoStats []RepoStats     // 按仓库分组
	Total     AggregatedStats // 跨仓库汇总
}

// IsEmpty 判断是否无评审数据
func (s *DailyStats) IsEmpty() bool {
	return s.Total.IsEmpty()
}

// RepoStats 单仓库统计
type RepoStats struct {
	RepoFullName string
	Stats        AggregatedStats
	TopPRs       []TopPR
}

// AggregatedStats 聚合统计
type AggregatedStats struct {
	ReviewCount    int     // 评审总数
	ApproveCount   int     // approve 次数
	RequestChanges int     // request_changes 次数
	CommentCount   int     // comment 次数（降级场景）
	FailedCount    int     // 解析失败次数
	CriticalCount  int     // CRITICAL issue 总数
	ErrorCount     int     // ERROR issue 总数
	WarningCount   int     // WARNING issue 总数
	InfoCount      int     // INFO issue 总数
	AvgDurationMs  int64   // 平均评审耗时
	TotalCostUSD   float64 // 总费用
}

// IsEmpty 判断是否无评审数据
func (s *AggregatedStats) IsEmpty() bool {
	return s.ReviewCount == 0
}

// TopPR 关键 PR
type TopPR struct {
	PRNumber    int64
	RepoName    string
	ReviewCount int    // 该 PR 被评审次数
	IssueCount  int    // 该 PR 的 issue 总数
	Reason      string // "review_count" 或 "issue_count"
}
```

**Step 4: Run test to verify it passes**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/report/ -v`
Expected: PASS

**Step 5: Commit**

```bash
git add internal/report/model.go internal/report/model_test.go
git commit -m "feat(m2.7): 新增每日报告统计数据模型"
```

---

### Task 2: Store 扩展 — ListReviewResultsByTimeRange

**Files:**
- Modify: `internal/store/store.go:51-58` — 新增接口方法
- Modify: `internal/store/sqlite.go:436-467` — 新增实现
- Test: `internal/store/sqlite_test.go` — 新增测试

**Step 1: Write the failing test**

在 `internal/store/sqlite_test.go` 末尾追加：

```go
func TestSQLiteStore_ListReviewResultsByTimeRange(t *testing.T) {
	s := newTestStore(t)
	defer s.Close()

	// 准备测试数据：3 条记录，跨越两天
	now := time.Now().UTC().Truncate(24 * time.Hour)
	yesterday := now.Add(-24 * time.Hour)

	records := []*model.ReviewRecord{
		{ID: "rr-1", RepoFullName: "acme/backend", PRNumber: 1, Verdict: "approve", CreatedAt: yesterday.Add(2 * time.Hour)},
		{ID: "rr-2", RepoFullName: "acme/frontend", PRNumber: 2, Verdict: "request_changes", CriticalCount: 1, CreatedAt: yesterday.Add(10 * time.Hour)},
		{ID: "rr-3", RepoFullName: "acme/backend", PRNumber: 3, Verdict: "approve", CreatedAt: now.Add(1 * time.Hour)}, // 今天的，应被排除
	}
	for _, r := range records {
		if err := s.SaveReviewResult(context.Background(), r); err != nil {
			t.Fatalf("SaveReviewResult: %v", err)
		}
	}

	// 查询昨天的记录
	results, err := s.ListReviewResultsByTimeRange(context.Background(), yesterday, now)
	if err != nil {
		t.Fatalf("ListReviewResultsByTimeRange: %v", err)
	}
	if len(results) != 2 {
		t.Errorf("want 2 results, got %d", len(results))
	}

	// 查询今天的记录
	tomorrow := now.Add(24 * time.Hour)
	results, err = s.ListReviewResultsByTimeRange(context.Background(), now, tomorrow)
	if err != nil {
		t.Fatalf("ListReviewResultsByTimeRange: %v", err)
	}
	if len(results) != 1 {
		t.Errorf("want 1 result, got %d", len(results))
	}

	// 空时间窗口
	results, err = s.ListReviewResultsByTimeRange(context.Background(), now.Add(-48*time.Hour), now.Add(-47*time.Hour))
	if err != nil {
		t.Fatalf("ListReviewResultsByTimeRange: %v", err)
	}
	if len(results) != 0 {
		t.Errorf("want 0 results, got %d", len(results))
	}
}
```

**Step 2: Run test to verify it fails**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/store/ -run TestSQLiteStore_ListReviewResultsByTimeRange -v`
Expected: FAIL — method not found

**Step 3: Add interface method**

在 `internal/store/store.go` 的 `ListReviewResults` 方法后追加：

```go
	// ListReviewResultsByTimeRange 按时间范围查询所有仓库的评审结果。
	// start inclusive, end exclusive。按 created_at DESC 排序。
	ListReviewResultsByTimeRange(ctx context.Context, start, end time.Time) ([]*model.ReviewRecord, error)
```

**Step 4: Implement in SQLiteStore**

在 `internal/store/sqlite.go` 的 `ListReviewResults` 方法后追加：

```go
// ListReviewResultsByTimeRange 按时间范围查询所有仓库的评审结果
func (s *SQLiteStore) ListReviewResultsByTimeRange(ctx context.Context, start, end time.Time) ([]*model.ReviewRecord, error) {
	query := `SELECT ` + reviewResultColumns + `
	FROM review_results
	WHERE created_at >= ? AND created_at < ?
	ORDER BY created_at DESC`

	rows, err := s.db.QueryContext(ctx, query, start.UTC().Format(time.DateTime), end.UTC().Format(time.DateTime))
	if err != nil {
		return nil, fmt.Errorf("按时间范围查询评审结果失败: %w", err)
	}
	defer rows.Close()

	var results []*model.ReviewRecord
	for rows.Next() {
		r, err := scanReviewRecord(rows)
		if err != nil {
			return nil, fmt.Errorf("扫描评审结果失败: %w", err)
		}
		results = append(results, r)
	}
	if err := rows.Err(); err != nil {
		return nil, fmt.Errorf("遍历评审结果失败: %w", err)
	}
	return results, nil
}
```

**Step 5: Run test to verify it passes**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/store/ -run TestSQLiteStore_ListReviewResultsByTimeRange -v`
Expected: PASS

**Step 6: Run all store tests to confirm no regression**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/store/ -v`
Expected: ALL PASS

**Step 7: Commit**

```bash
git add internal/store/store.go internal/store/sqlite.go internal/store/sqlite_test.go
git commit -m "feat(m2.7): Store 新增 ListReviewResultsByTimeRange 方法"
```

---

### Task 3: StatCollector 接口与 ReviewStatCollector 实现

**Files:**
- Create: `internal/report/collector.go`
- Test: `internal/report/collector_test.go`

**Step 1: Write the failing test**

```go
// internal/report/collector_test.go
package report

import (
	"context"
	"testing"
	"time"

	"otws19.zicp.vip/kelin/dtworkflow/internal/model"
)

// mockReviewStore 仅实现 ListReviewResultsByTimeRange
type mockReviewStore struct {
	results []*model.ReviewRecord
	err     error
}

func (m *mockReviewStore) ListReviewResultsByTimeRange(_ context.Context, _, _ time.Time) ([]*model.ReviewRecord, error) {
	return m.results, m.err
}

func TestReviewStatCollector_Collect(t *testing.T) {
	records := []*model.ReviewRecord{
		{RepoFullName: "acme/backend", PRNumber: 1, Verdict: "approve", IssueCount: 2, CriticalCount: 0, ErrorCount: 1, WarningCount: 1, InfoCount: 0, DurationMs: 10000, CostUSD: 0.5},
		{RepoFullName: "acme/backend", PRNumber: 2, Verdict: "request_changes", IssueCount: 5, CriticalCount: 2, ErrorCount: 2, WarningCount: 1, InfoCount: 0, DurationMs: 20000, CostUSD: 0.8},
		{RepoFullName: "acme/frontend", PRNumber: 3, Verdict: "approve", IssueCount: 1, CriticalCount: 0, ErrorCount: 0, WarningCount: 0, InfoCount: 1, DurationMs: 5000, CostUSD: 0.2},
	}

	store := &mockReviewStore{results: records}
	collector := NewReviewStatCollector(store)

	now := time.Now()
	tr := TimeRange{Start: now.Add(-24 * time.Hour), End: now}

	stats, err := collector.Collect(context.Background(), tr)
	if err != nil {
		t.Fatalf("Collect: %v", err)
	}

	// 总计
	if stats.Total.ReviewCount != 3 {
		t.Errorf("Total.ReviewCount = %d, want 3", stats.Total.ReviewCount)
	}
	if stats.Total.ApproveCount != 2 {
		t.Errorf("Total.ApproveCount = %d, want 2", stats.Total.ApproveCount)
	}
	if stats.Total.RequestChanges != 1 {
		t.Errorf("Total.RequestChanges = %d, want 1", stats.Total.RequestChanges)
	}
	if stats.Total.CriticalCount != 2 {
		t.Errorf("Total.CriticalCount = %d, want 2", stats.Total.CriticalCount)
	}
	if stats.Total.TotalCostUSD != 1.5 {
		t.Errorf("Total.TotalCostUSD = %f, want 1.5", stats.Total.TotalCostUSD)
	}

	// 仓库分组
	if len(stats.RepoStats) != 2 {
		t.Fatalf("RepoStats count = %d, want 2", len(stats.RepoStats))
	}
}

func TestReviewStatCollector_Collect_Empty(t *testing.T) {
	store := &mockReviewStore{results: nil}
	collector := NewReviewStatCollector(store)

	stats, err := collector.Collect(context.Background(), TimeRange{})
	if err != nil {
		t.Fatalf("Collect: %v", err)
	}
	if !stats.IsEmpty() {
		t.Error("expected empty stats")
	}
}

func TestReviewStatCollector_TopPRs(t *testing.T) {
	// 同一 PR 多次评审
	records := []*model.ReviewRecord{
		{RepoFullName: "acme/backend", PRNumber: 42, Verdict: "request_changes", IssueCount: 3},
		{RepoFullName: "acme/backend", PRNumber: 42, Verdict: "request_changes", IssueCount: 2},
		{RepoFullName: "acme/backend", PRNumber: 42, Verdict: "approve", IssueCount: 1},
		{RepoFullName: "acme/backend", PRNumber: 10, Verdict: "approve", IssueCount: 12},
	}

	store := &mockReviewStore{results: records}
	collector := NewReviewStatCollector(store)
	stats, err := collector.Collect(context.Background(), TimeRange{})
	if err != nil {
		t.Fatalf("Collect: %v", err)
	}

	// acme/backend 应有 TopPRs
	if len(stats.RepoStats) != 1 {
		t.Fatalf("RepoStats count = %d, want 1", len(stats.RepoStats))
	}
	repo := stats.RepoStats[0]
	if len(repo.TopPRs) == 0 {
		t.Fatal("expected TopPRs, got none")
	}

	// PR #42 评审次数最多（3 次）
	foundReviewTop := false
	for _, tp := range repo.TopPRs {
		if tp.Reason == "review_count" && tp.PRNumber == 42 && tp.ReviewCount == 3 {
			foundReviewTop = true
		}
	}
	if !foundReviewTop {
		t.Error("expected PR #42 as top by review_count")
	}

	// PR #10 issue 最多（12 个）
	foundIssueTop := false
	for _, tp := range repo.TopPRs {
		if tp.Reason == "issue_count" && tp.PRNumber == 10 && tp.IssueCount == 12 {
			foundIssueTop = true
		}
	}
	if !foundIssueTop {
		t.Error("expected PR #10 as top by issue_count")
	}
}
```

**Step 2: Run test to verify it fails**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/report/ -run TestReviewStatCollector -v`
Expected: FAIL — NewReviewStatCollector not defined

**Step 3: Write implementation**

```go
// internal/report/collector.go
package report

import (
	"context"
	"sort"
	"strings"
	"time"

	"otws19.zicp.vip/kelin/dtworkflow/internal/model"
)

// ReviewResultStore 是 StatCollector 对 Store 的最小依赖接口
type ReviewResultStore interface {
	ListReviewResultsByTimeRange(ctx context.Context, start, end time.Time) ([]*model.ReviewRecord, error)
}

// StatCollector 统计收集器接口
type StatCollector interface {
	Collect(ctx context.Context, tr TimeRange) (*DailyStats, error)
}

// ReviewStatCollector 评审统计收集器
type ReviewStatCollector struct {
	store ReviewResultStore
}

// NewReviewStatCollector 创建评审统计收集器
func NewReviewStatCollector(store ReviewResultStore) *ReviewStatCollector {
	return &ReviewStatCollector{store: store}
}

func (c *ReviewStatCollector) Collect(ctx context.Context, tr TimeRange) (*DailyStats, error) {
	records, err := c.store.ListReviewResultsByTimeRange(ctx, tr.Start, tr.End)
	if err != nil {
		return nil, err
	}

	stats := &DailyStats{
		Date: tr.Start.Format("2006-01-02"),
	}
	if len(records) == 0 {
		return stats, nil
	}

	// 按仓库分组
	repoMap := make(map[string][]*model.ReviewRecord)
	for _, r := range records {
		repoMap[r.RepoFullName] = append(repoMap[r.RepoFullName], r)
	}

	// 稳定排序：按仓库名排序
	repoNames := make([]string, 0, len(repoMap))
	for name := range repoMap {
		repoNames = append(repoNames, name)
	}
	sort.Strings(repoNames)

	for _, name := range repoNames {
		repoRecords := repoMap[name]
		repoStat := RepoStats{
			RepoFullName: name,
			Stats:        aggregateRecords(repoRecords),
			TopPRs:       findTopPRs(name, repoRecords),
		}
		stats.RepoStats = append(stats.RepoStats, repoStat)
	}

	stats.Total = aggregateRecords(records)
	return stats, nil
}

func aggregateRecords(records []*model.ReviewRecord) AggregatedStats {
	var s AggregatedStats
	var totalDuration int64
	for _, r := range records {
		s.ReviewCount++
		switch strings.ToLower(r.Verdict) {
		case "approve":
			s.ApproveCount++
		case "request_changes":
			s.RequestChanges++
		case "comment":
			s.CommentCount++
		}
		if r.ParseFailed {
			s.FailedCount++
		}
		s.CriticalCount += r.CriticalCount
		s.ErrorCount += r.ErrorCount
		s.WarningCount += r.WarningCount
		s.InfoCount += r.InfoCount
		totalDuration += r.DurationMs
		s.TotalCostUSD += r.CostUSD
	}
	if s.ReviewCount > 0 {
		s.AvgDurationMs = totalDuration / int64(s.ReviewCount)
	}
	return s
}

// findTopPRs 找出评审次数最多和 issue 数量最多的 PR，去重后最多 2 条
func findTopPRs(repoName string, records []*model.ReviewRecord) []TopPR {
	type prKey struct {
		PRNumber int64
	}
	type prAgg struct {
		ReviewCount   int
		MaxIssueCount int
	}
	m := make(map[prKey]*prAgg)
	for _, r := range records {
		k := prKey{PRNumber: r.PRNumber}
		agg, ok := m[k]
		if !ok {
			agg = &prAgg{}
			m[k] = agg
		}
		agg.ReviewCount++
		if r.IssueCount > agg.MaxIssueCount {
			agg.MaxIssueCount = r.IssueCount
		}
	}

	// 找评审次数最多的（必须 > 1 才有意义）
	var topByReview *TopPR
	maxReviewCount := 1
	for k, agg := range m {
		if agg.ReviewCount > maxReviewCount {
			maxReviewCount = agg.ReviewCount
			topByReview = &TopPR{
				PRNumber:    k.PRNumber,
				RepoName:    repoName,
				ReviewCount: agg.ReviewCount,
				Reason:      "review_count",
			}
		}
	}

	// 找 issue 最多的（必须 > 0）
	var topByIssue *TopPR
	maxIssueCount := 0
	for k, agg := range m {
		if agg.MaxIssueCount > maxIssueCount {
			maxIssueCount = agg.MaxIssueCount
			topByIssue = &TopPR{
				PRNumber:   k.PRNumber,
				RepoName:   repoName,
				IssueCount: agg.MaxIssueCount,
				Reason:     "issue_count",
			}
		}
	}

	var result []TopPR
	if topByReview != nil {
		result = append(result, *topByReview)
	}
	if topByIssue != nil {
		// 去重：如果是同一个 PR，只保留 review_count 那条
		if topByReview == nil || topByIssue.PRNumber != topByReview.PRNumber {
			result = append(result, *topByIssue)
		}
	}
	return result
}
```

**Step 4: Run test to verify it passes**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/report/ -v`
Expected: ALL PASS

**Step 5: Commit**

```bash
git add internal/report/collector.go internal/report/collector_test.go
git commit -m "feat(m2.7): StatCollector 接口与 ReviewStatCollector 实现"
```

---

### Task 4: 配置扩展 — DailyReportConfig

**Files:**
- Modify: `internal/config/config.go:17-29` — 新增 DailyReportConfig 字段和结构体
- Modify: `internal/config/config.go:213-244` — WithDefaults 新增默认值
- Modify: `internal/config/config.go:304-393` — Clone 深拷贝无需修改（DailyReportConfig 全是值类型）
- Modify: `internal/config/validate.go:33-248` — 新增 daily_report 校验
- Test: `internal/config/validate_test.go` — 新增校验测试

**Step 1: Write the failing test**

在 `internal/config/validate_test.go` 中追加（如该文件存在则追加，不存在则创建）：

```go
func TestValidate_DailyReport(t *testing.T) {
	base := validConfig() // 复用已有的 validConfig 辅助函数

	t.Run("disabled skips all checks", func(t *testing.T) {
		cfg := base
		cfg.DailyReport = DailyReportConfig{Enabled: false}
		if err := Validate(&cfg); err != nil {
			t.Errorf("disabled daily_report should pass: %v", err)
		}
	})

	t.Run("enabled requires feishu_webhook", func(t *testing.T) {
		cfg := base
		cfg.DailyReport = DailyReportConfig{
			Enabled:  true,
			Cron:     "0 9 * * *",
			Timezone: "Asia/Shanghai",
		}
		err := Validate(&cfg)
		if err == nil {
			t.Fatal("expected error for missing feishu_webhook")
		}
		if !strings.Contains(err.Error(), "feishu_webhook") {
			t.Errorf("error should mention feishu_webhook: %v", err)
		}
	})

	t.Run("invalid cron expression", func(t *testing.T) {
		cfg := base
		cfg.DailyReport = DailyReportConfig{
			Enabled:       true,
			Cron:          "not-a-cron",
			Timezone:      "Asia/Shanghai",
			FeishuWebhook: "https://open.feishu.cn/open-apis/bot/v2/hook/xxx",
		}
		err := Validate(&cfg)
		if err == nil {
			t.Fatal("expected error for invalid cron")
		}
		if !strings.Contains(err.Error(), "cron") {
			t.Errorf("error should mention cron: %v", err)
		}
	})

	t.Run("invalid timezone", func(t *testing.T) {
		cfg := base
		cfg.DailyReport = DailyReportConfig{
			Enabled:       true,
			Cron:          "0 9 * * *",
			Timezone:      "Invalid/Zone",
			FeishuWebhook: "https://open.feishu.cn/open-apis/bot/v2/hook/xxx",
		}
		err := Validate(&cfg)
		if err == nil {
			t.Fatal("expected error for invalid timezone")
		}
		if !strings.Contains(err.Error(), "timezone") {
			t.Errorf("error should mention timezone: %v", err)
		}
	})

	t.Run("valid full config", func(t *testing.T) {
		cfg := base
		cfg.DailyReport = DailyReportConfig{
			Enabled:       true,
			Cron:          "0 9 * * *",
			Timezone:      "Asia/Shanghai",
			FeishuWebhook: "https://open.feishu.cn/open-apis/bot/v2/hook/xxx",
			FeishuSecret:  "sec123",
		}
		if err := Validate(&cfg); err != nil {
			t.Errorf("valid config should pass: %v", err)
		}
	})
}
```

**Step 2: Run test to verify it fails**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/config/ -run TestValidate_DailyReport -v`
Expected: FAIL — DailyReportConfig not defined

**Step 3: Add DailyReportConfig struct and field**

在 `internal/config/config.go` 的 `Config` 结构体中追加字段：

```go
	DailyReport DailyReportConfig `mapstructure:"daily_report"`
```

在 `config.go` 末尾（WebhookConfig 后面）追加类型定义：

```go
// DailyReportConfig 每日评审统计报告配置
type DailyReportConfig struct {
	Enabled       bool   `mapstructure:"enabled"`
	Cron          string `mapstructure:"cron"`
	Timezone      string `mapstructure:"timezone"`
	SkipEmpty     bool   `mapstructure:"skip_empty"`
	FeishuWebhook string `mapstructure:"feishu_webhook"`
	FeishuSecret  string `mapstructure:"feishu_secret"`
}
```

**Step 4: Add defaults in WithDefaults**

在 `WithDefaults()` 函数的 `notify.default_channel` 行之前追加：

```go
		m.v.SetDefault("daily_report.enabled", false)
		m.v.SetDefault("daily_report.cron", "0 9 * * *")
		m.v.SetDefault("daily_report.timezone", "Asia/Shanghai")
		m.v.SetDefault("daily_report.skip_empty", false)
```

**Step 5: Add validation in Validate**

在 `internal/config/validate.go` 的 `Validate` 函数中，在 `repos[].review 校验` 块之后、`if len(errs) > 0` 之前追加：

```go
	// daily_report 校验（仅 enabled=true 时）
	if cfg.DailyReport.Enabled {
		// cron 表达式语法校验
		if _, cronErr := cron.ParseStandard(cfg.DailyReport.Cron); cronErr != nil {
			errs = append(errs, fmt.Errorf("daily_report.cron 表达式无效: %w", cronErr))
		}
		// timezone 合法性
		if _, tzErr := time.LoadLocation(cfg.DailyReport.Timezone); tzErr != nil {
			errs = append(errs, fmt.Errorf("daily_report.timezone 无效: %w", tzErr))
		}
		// feishu_webhook 必填
		if strings.TrimSpace(cfg.DailyReport.FeishuWebhook) == "" {
			errs = append(errs, fmt.Errorf("daily_report.feishu_webhook 启用每日报告时不能为空"))
		} else if u, urlErr := url.Parse(cfg.DailyReport.FeishuWebhook); urlErr != nil || (u.Scheme != "http" && u.Scheme != "https") || u.Host == "" {
			errs = append(errs, fmt.Errorf("daily_report.feishu_webhook 格式无效: %q", cfg.DailyReport.FeishuWebhook))
		}
	}
```

并在 `validate.go` 的 import 中追加：

```go
	"time"
	"github.com/robfig/cron/v3"
```

注意：`robfig/cron/v3` 已是 asynq 的间接依赖，无需额外 `go get`。

**Step 6: Run test to verify it passes**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/config/ -run TestValidate_DailyReport -v`
Expected: PASS

**Step 7: Run all config tests**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/config/ -v`
Expected: ALL PASS

**Step 8: Commit**

```bash
git add internal/config/config.go internal/config/validate.go internal/config/validate_test.go
git commit -m "feat(m2.7): 新增 DailyReportConfig 配置结构 + 校验逻辑"
```

---

### Task 5: 飞书统计卡片格式化

**Files:**
- Create: `internal/report/feishu_card.go`
- Test: `internal/report/feishu_card_test.go`

**Step 1: Write the failing test**

```go
// internal/report/feishu_card_test.go
package report

import (
	"strings"
	"testing"
)

func TestFormatDailyReportCard_WithData(t *testing.T) {
	stats := &DailyStats{
		Date: "2026-04-05",
		Total: AggregatedStats{
			ReviewCount:    12,
			ApproveCount:   8,
			RequestChanges: 3,
			CommentCount:   1,
			CriticalCount:  2,
			ErrorCount:     5,
			WarningCount:   18,
			InfoCount:      7,
			AvgDurationMs:  222000,
			TotalCostUSD:   1.24,
		},
		RepoStats: []RepoStats{
			{
				RepoFullName: "acme/backend",
				Stats:        AggregatedStats{ReviewCount: 8, ApproveCount: 6, RequestChanges: 2, CriticalCount: 1, ErrorCount: 3},
			},
		},
	}
	prevStats := &DailyStats{
		Date:  "2026-04-04",
		Total: AggregatedStats{ReviewCount: 9, CriticalCount: 1, ErrorCount: 7},
	}

	card := FormatDailyReportCard(stats, prevStats)

	// 验证顶层结构
	if card["msg_type"] != "interactive" {
		t.Errorf("msg_type = %v, want interactive", card["msg_type"])
	}

	cardContent, ok := card["card"].(map[string]any)
	if !ok {
		t.Fatal("missing card content")
	}
	header, ok := cardContent["header"].(map[string]any)
	if !ok {
		t.Fatal("missing header")
	}

	// 蓝色 header
	if header["template"] != "blue" {
		t.Errorf("header template = %v, want blue", header["template"])
	}

	// 标题包含日期
	titleMap, ok := header["title"].(map[string]string)
	if !ok || !strings.Contains(titleMap["content"], "2026-04-05") {
		t.Error("header title should contain date")
	}
}

func TestFormatDailyReportCard_Empty(t *testing.T) {
	stats := &DailyStats{Date: "2026-04-05"}

	card := FormatDailyReportCard(stats, nil)

	cardContent := card["card"].(map[string]any)
	header := cardContent["header"].(map[string]any)

	// 灰色 header
	if header["template"] != "grey" {
		t.Errorf("empty report should have grey header, got %v", header["template"])
	}
}

func TestTrendIndicator(t *testing.T) {
	tests := []struct {
		current, previous int
		hasPrev           bool
		want              string
	}{
		{10, 5, true, "(+5)"},
		{3, 8, true, "(-5)"},
		{5, 5, true, "(-)"},
		{10, 0, false, ""},
	}
	for _, tt := range tests {
		got := trendIndicator(tt.current, tt.previous, tt.hasPrev)
		if got != tt.want {
			t.Errorf("trendIndicator(%d, %d, %v) = %q, want %q", tt.current, tt.previous, tt.hasPrev, got, tt.want)
		}
	}
}
```

**Step 2: Run test to verify it fails**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/report/ -run TestFormatDailyReportCard -v`
Expected: FAIL

**Step 3: Write implementation**

```go
// internal/report/feishu_card.go
package report

import (
	"fmt"
	"strings"
	"time"
)

// FormatDailyReportCard 格式化每日统计报告为飞书交互卡片
func FormatDailyReportCard(stats, prevStats *DailyStats) map[string]any {
	if stats.IsEmpty() {
		return buildEmptyReportCard(stats.Date)
	}
	return buildFullReportCard(stats, prevStats)
}

func buildEmptyReportCard(date string) map[string]any {
	return buildCard(
		fmt.Sprintf("每日评审统计 - %s", formatDateWithWeekday(date)),
		"grey",
		[]any{
			mdElement("昨日无 PR 评审活动"),
		},
	)
}

func buildFullReportCard(stats, prevStats *DailyStats) map[string]any {
	hasPrev := prevStats != nil && !prevStats.IsEmpty()
	var prevTotal AggregatedStats
	if hasPrev {
		prevTotal = prevStats.Total
	}
	s := stats.Total

	var parts []string

	// 总览
	parts = append(parts, "**总览**")
	parts = append(parts, fmt.Sprintf("评审总数：**%d** %s", s.ReviewCount, trendIndicator(s.ReviewCount, prevTotal.ReviewCount, hasPrev)))
	parts = append(parts, fmt.Sprintf("通过：%d　需修改：%d　评论：%d",
		s.ApproveCount, s.RequestChanges, s.CommentCount))
	parts = append(parts, fmt.Sprintf("耗时均值：%s　总费用：$%.2f", formatDuration(s.AvgDurationMs), s.TotalCostUSD))
	parts = append(parts, "")

	// 问题分布
	parts = append(parts, "**问题分布**")
	parts = append(parts, fmt.Sprintf("CRITICAL: %d %s　ERROR: %d %s",
		s.CriticalCount, trendIndicator(s.CriticalCount, prevTotal.CriticalCount, hasPrev),
		s.ErrorCount, trendIndicator(s.ErrorCount, prevTotal.ErrorCount, hasPrev)))
	parts = append(parts, fmt.Sprintf("WARNING: %d %s　INFO: %d %s",
		s.WarningCount, trendIndicator(s.WarningCount, prevTotal.WarningCount, hasPrev),
		s.InfoCount, trendIndicator(s.InfoCount, prevTotal.InfoCount, hasPrev)))

	elements := []any{mdElement(strings.Join(parts, "\n"))}

	// 仓库明细
	if len(stats.RepoStats) > 0 {
		elements = append(elements, divider())

		var repoParts []string
		for _, repo := range stats.RepoStats {
			rs := repo.Stats
			repoParts = append(repoParts, fmt.Sprintf("**%s**（%d 次评审）", repo.RepoFullName, rs.ReviewCount))
			repoParts = append(repoParts, fmt.Sprintf("通过 %d　需修改 %d　评论 %d | CRITICAL: %d　ERROR: %d",
				rs.ApproveCount, rs.RequestChanges, rs.CommentCount, rs.CriticalCount, rs.ErrorCount))
		}
		elements = append(elements, mdElement(strings.Join(repoParts, "\n")))
	}

	// 关键 PR
	var topParts []string
	for _, repo := range stats.RepoStats {
		for _, tp := range repo.TopPRs {
			switch tp.Reason {
			case "review_count":
				topParts = append(topParts, fmt.Sprintf("评审最多：%s #%d（%d 次重新评审）", tp.RepoName, tp.PRNumber, tp.ReviewCount))
			case "issue_count":
				topParts = append(topParts, fmt.Sprintf("问题最多：%s #%d（%d 个 issue）", tp.RepoName, tp.PRNumber, tp.IssueCount))
			}
		}
	}
	if len(topParts) > 0 {
		elements = append(elements, divider())
		elements = append(elements, mdElement("**关键 PR**\n"+strings.Join(topParts, "\n")))
	}

	return buildCard(
		fmt.Sprintf("每日评审统计 - %s", formatDateWithWeekday(stats.Date)),
		"blue",
		elements,
	)
}

func buildCard(title, color string, elements []any) map[string]any {
	return map[string]any{
		"msg_type": "interactive",
		"card": map[string]any{
			"config": map[string]bool{"wide_screen_mode": true},
			"header": map[string]any{
				"title":    map[string]string{"tag": "plain_text", "content": title},
				"template": color,
			},
			"elements": elements,
		},
	}
}

func mdElement(content string) map[string]any {
	return map[string]any{
		"tag":     "markdown",
		"content": content,
	}
}

func divider() map[string]any {
	return map[string]any{"tag": "hr"}
}

// trendIndicator 计算趋势指示符
func trendIndicator(current, previous int, hasPrev bool) string {
	if !hasPrev {
		return ""
	}
	diff := current - previous
	switch {
	case diff > 0:
		return fmt.Sprintf("(+%d)", diff)
	case diff < 0:
		return fmt.Sprintf("(%d)", diff)
	default:
		return "(-)"
	}
}

func formatDuration(ms int64) string {
	d := time.Duration(ms) * time.Millisecond
	m := int(d.Minutes())
	s := int(d.Seconds()) % 60
	if m > 0 {
		return fmt.Sprintf("%dm %ds", m, s)
	}
	return fmt.Sprintf("%ds", s)
}

func formatDateWithWeekday(dateStr string) string {
	t, err := time.Parse("2006-01-02", dateStr)
	if err != nil {
		return dateStr
	}
	weekdays := []string{"周日", "周一", "周二", "周三", "周四", "周五", "周六"}
	return fmt.Sprintf("%s（%s）", dateStr, weekdays[t.Weekday()])
}
```

**Step 4: Run test to verify it passes**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/report/ -v`
Expected: ALL PASS

**Step 5: Commit**

```bash
git add internal/report/feishu_card.go internal/report/feishu_card_test.go
git commit -m "feat(m2.7): 飞书统计卡片格式化（含趋势指示、仓库明细、关键 PR）"
```

---

### Task 6: ReportGenerator 编排层

**Files:**
- Create: `internal/report/generator.go`
- Test: `internal/report/generator_test.go`

**Step 1: Write the failing test**

```go
// internal/report/generator_test.go
package report

import (
	"context"
	"fmt"
	"testing"
	"time"

	"otws19.zicp.vip/kelin/dtworkflow/internal/notify"
)

type mockNotifier struct {
	sent []notify.Message
	err  error
}

func (m *mockNotifier) Name() string { return "mock" }
func (m *mockNotifier) Send(_ context.Context, msg notify.Message) error {
	if m.err != nil {
		return m.err
	}
	m.sent = append(m.sent, msg)
	return nil
}

func TestReportGenerator_Generate_WithData(t *testing.T) {
	store := &mockReviewStore{
		results: testReviewRecords(),
	}
	notifier := &mockNotifier{}

	gen := NewReportGenerator(
		NewReviewStatCollector(store),
		notifier,
		"Asia/Shanghai",
		false, // skipEmpty
	)

	if err := gen.Generate(context.Background()); err != nil {
		t.Fatalf("Generate: %v", err)
	}

	if len(notifier.sent) != 1 {
		t.Fatalf("expected 1 message sent, got %d", len(notifier.sent))
	}
	msg := notifier.sent[0]
	if msg.EventType != notify.EventDailyReport {
		t.Errorf("EventType = %v, want %v", msg.EventType, notify.EventDailyReport)
	}
}

func TestReportGenerator_Generate_Empty_SkipFalse(t *testing.T) {
	store := &mockReviewStore{results: nil}
	notifier := &mockNotifier{}

	gen := NewReportGenerator(NewReviewStatCollector(store), notifier, "Asia/Shanghai", false)
	if err := gen.Generate(context.Background()); err != nil {
		t.Fatalf("Generate: %v", err)
	}
	if len(notifier.sent) != 1 {
		t.Fatalf("expected 1 message (empty report), got %d", len(notifier.sent))
	}
}

func TestReportGenerator_Generate_Empty_SkipTrue(t *testing.T) {
	store := &mockReviewStore{results: nil}
	notifier := &mockNotifier{}

	gen := NewReportGenerator(NewReviewStatCollector(store), notifier, "Asia/Shanghai", true)
	if err := gen.Generate(context.Background()); err != nil {
		t.Fatalf("Generate: %v", err)
	}
	if len(notifier.sent) != 0 {
		t.Fatalf("expected 0 messages (skip empty), got %d", len(notifier.sent))
	}
}

func TestReportGenerator_Generate_NotifierError(t *testing.T) {
	store := &mockReviewStore{results: testReviewRecords()}
	notifier := &mockNotifier{err: fmt.Errorf("webhook timeout")}

	gen := NewReportGenerator(NewReviewStatCollector(store), notifier, "Asia/Shanghai", false)
	err := gen.Generate(context.Background())
	if err == nil {
		t.Fatal("expected error from notifier")
	}
}
```

注意：`testReviewRecords` 辅助函数可放在 `collector_test.go` 或测试文件中：

```go
func testReviewRecords() []*model.ReviewRecord {
	now := time.Now()
	return []*model.ReviewRecord{
		{RepoFullName: "acme/backend", PRNumber: 1, Verdict: "approve", IssueCount: 2, DurationMs: 10000, CostUSD: 0.5, CreatedAt: now.Add(-2 * time.Hour)},
		{RepoFullName: "acme/backend", PRNumber: 2, Verdict: "request_changes", IssueCount: 5, CriticalCount: 2, DurationMs: 20000, CostUSD: 0.8, CreatedAt: now.Add(-4 * time.Hour)},
	}
}
```

**Step 2: Run test to verify it fails**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/report/ -run TestReportGenerator -v`
Expected: FAIL — NewReportGenerator not defined, EventDailyReport not defined

**Step 3: Add EventDailyReport to notify package**

在 `internal/notify/notifier.go` 的事件常量中追加：

```go
	EventDailyReport EventType = "report.daily_review"
```

**Step 4: Write ReportGenerator implementation**

```go
// internal/report/generator.go
package report

import (
	"context"
	"encoding/json"
	"fmt"
	"log/slog"
	"time"

	"otws19.zicp.vip/kelin/dtworkflow/internal/notify"
)

// ReportGenerator 每日报告生成器
type ReportGenerator struct {
	collector StatCollector
	notifier  notify.Notifier
	timezone  string
	skipEmpty bool
	logger    *slog.Logger
}

// NewReportGenerator 创建报告生成器
func NewReportGenerator(collector StatCollector, notifier notify.Notifier, timezone string, skipEmpty bool) *ReportGenerator {
	return &ReportGenerator{
		collector: collector,
		notifier:  notifier,
		timezone:  timezone,
		skipEmpty: skipEmpty,
		logger:    slog.Default(),
	}
}

// Generate 生成并发送每日报告
func (g *ReportGenerator) Generate(ctx context.Context) error {
	loc, err := time.LoadLocation(g.timezone)
	if err != nil {
		return fmt.Errorf("加载时区 %q 失败: %w", g.timezone, err)
	}

	// 计算"昨天"和"前天"的时间窗口（按配置时区）
	now := time.Now().In(loc)
	todayStart := time.Date(now.Year(), now.Month(), now.Day(), 0, 0, 0, 0, loc)
	yesterdayStart := todayStart.Add(-24 * time.Hour)
	dayBeforeStart := yesterdayStart.Add(-24 * time.Hour)

	yesterdayRange := TimeRange{Start: yesterdayStart, End: todayStart}
	dayBeforeRange := TimeRange{Start: dayBeforeStart, End: yesterdayStart}

	// 收集昨天的统计
	stats, err := g.collector.Collect(ctx, yesterdayRange)
	if err != nil {
		return fmt.Errorf("收集昨日统计失败: %w", err)
	}

	// 空数据处理
	if stats.IsEmpty() && g.skipEmpty {
		g.logger.InfoContext(ctx, "昨日无评审活动，跳过报告发送")
		return nil
	}

	// 收集前天的统计（用于趋势对比）
	var prevStats *DailyStats
	if !stats.IsEmpty() {
		prevStats, err = g.collector.Collect(ctx, dayBeforeRange)
		if err != nil {
			// 趋势数据查询失败不阻塞报告发送
			g.logger.WarnContext(ctx, "收集前日趋势数据失败，跳过趋势对比", "error", err)
		}
	}

	// 格式化卡片
	card := FormatDailyReportCard(stats, prevStats)
	cardJSON, err := json.Marshal(card)
	if err != nil {
		return fmt.Errorf("序列化卡片失败: %w", err)
	}

	// 通过 Notifier 发送（直接调用 FeishuNotifier，不经 Router）
	msg := notify.Message{
		EventType: notify.EventDailyReport,
		Severity:  notify.SeverityInfo,
		Title:     fmt.Sprintf("每日评审统计 - %s", stats.Date),
		Body:      string(cardJSON),
	}

	if err := g.notifier.Send(ctx, msg); err != nil {
		return fmt.Errorf("发送每日报告失败: %w", err)
	}

	g.logger.InfoContext(ctx, "每日报告发送成功", "date", stats.Date, "review_count", stats.Total.ReviewCount)
	return nil
}
```

**重要说明：** FeishuNotifier 现有的 `Send` 方法会调用 `FormatFeishuCard(msg)` 格式化。但日报卡片布局完全不同，需要绕过这一步。有两种处理方式：

a) 在 `FeishuNotifier.Send` 中检测 EventType，对 `EventDailyReport` 使用 `msg.Body` 中的预格式化 JSON
b) 创建一个专用的 `DailyReportFeishuNotifier`（包装 FeishuNotifier 的 HTTP 发送逻辑）

**推荐 b) 更干净**——创建 `ReportFeishuSender` 直接发送预格式化的卡片 JSON：

```go
// internal/report/feishu_sender.go
package report

import (
	"bytes"
	"context"
	"encoding/json"
	"fmt"
	"io"
	"log/slog"
	"net/http"
	"time"

	"otws19.zicp.vip/kelin/dtworkflow/internal/notify"
)

// ReportFeishuSender 发送预格式化的飞书卡片（不经过 FormatFeishuCard）
type ReportFeishuSender struct {
	webhookURL string
	secret     string
	httpClient *http.Client
	logger     *slog.Logger
}

// NewReportFeishuSender 创建日报飞书发送器
func NewReportFeishuSender(webhookURL string, opts ...notify.FeishuOption) (*ReportFeishuSender, error) {
	if webhookURL == "" {
		return nil, fmt.Errorf("webhookURL 不能为空")
	}
	s := &ReportFeishuSender{
		webhookURL: webhookURL,
		httpClient: &http.Client{Timeout: 10 * time.Second},
		logger:     slog.Default(),
	}
	// 注意：FeishuOption 操作的是 *FeishuNotifier，不能直接复用。
	// 改为接受显式参数。
	return s, nil
}

func (s *ReportFeishuSender) Name() string { return "feishu_report" }

// Send 对 EventDailyReport 消息，直接从 msg.Body 解析预格式化的卡片 JSON 并发送。
// 对其他消息类型，返回错误。
func (s *ReportFeishuSender) Send(ctx context.Context, msg notify.Message) error {
	if msg.EventType != notify.EventDailyReport {
		return fmt.Errorf("ReportFeishuSender 仅支持 EventDailyReport，收到 %s", msg.EventType)
	}

	var cardMap map[string]any
	if err := json.Unmarshal([]byte(msg.Body), &cardMap); err != nil {
		return fmt.Errorf("解析卡片 JSON 失败: %w", err)
	}

	body, err := marshalWithSign(cardMap, s.secret)
	if err != nil {
		return fmt.Errorf("序列化请求体失败: %w", err)
	}

	req, err := http.NewRequestWithContext(ctx, http.MethodPost, s.webhookURL, bytes.NewReader(body))
	if err != nil {
		return fmt.Errorf("创建请求失败: %w", err)
	}
	req.Header.Set("Content-Type", "application/json")

	resp, err := s.httpClient.Do(req)
	if err != nil {
		return fmt.Errorf("HTTP 请求失败: %w", err)
	}
	defer func() { _ = resp.Body.Close() }()

	respBody, _ := io.ReadAll(io.LimitReader(resp.Body, 1024))
	if resp.StatusCode < 200 || resp.StatusCode >= 300 {
		return fmt.Errorf("HTTP %d, response: %s", resp.StatusCode, string(respBody))
	}
	return nil
}
```

注意 `marshalWithSign` 需要复用签名逻辑。由于 `genSign` 在 `notify` 包中是非导出的，建议：将签名逻辑提取为内部使用，或者在 `report` 包中重新实现签名。**最简单的方式**是让 `ReportFeishuSender` 直接接受 secret 参数并内联签名：

```go
func marshalWithSign(cardMap map[string]any, secret string) ([]byte, error) {
	if secret != "" {
		timestamp := time.Now().Unix()
		stringToSign := fmt.Sprintf("%d\n%s", timestamp, secret)
		h := hmac.New(sha256.New, []byte(stringToSign))
		h.Write([]byte{})
		sign := base64.StdEncoding.EncodeToString(h.Sum(nil))
		cardMap["timestamp"] = fmt.Sprintf("%d", timestamp)
		cardMap["sign"] = sign
	}
	return json.Marshal(cardMap)
}
```

**由于这个 Task 比较复杂，实施时需要：**
1. 先创建 `feishu_sender.go`（含 `marshalWithSign`）
2. 修改 `generator.go` 使用 `notify.Notifier` 接口（`ReportFeishuSender` 实现该接口）
3. 然后跑测试

**Step 5: Run test to verify it passes**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/report/ -v`
Expected: ALL PASS

**Step 6: Commit**

```bash
git add internal/report/generator.go internal/report/generator_test.go internal/report/feishu_sender.go internal/notify/notifier.go
git commit -m "feat(m2.7): ReportGenerator 编排层 + ReportFeishuSender 日报专用发送器"
```

---

### Task 7: 任务类型常量与 Processor 路由

**Files:**
- Modify: `internal/model/task.go:8-12` — 新增 TaskTypeGenDailyReport
- Modify: `internal/model/task.go:38-43` — IsValid 追加 case
- Modify: `internal/queue/client.go:18-22` — 新增 AsynqTypeGenDailyReport
- Modify: `internal/queue/client.go:148-160` — taskTypeToAsynq 追加 case
- Modify: `internal/queue/processor.go:83-92` — 新增 reportGenerator 字段
- Modify: `internal/queue/processor.go:132-326` — ProcessTask 中新增 gen_daily_report case

**Step 1: Add constants**

在 `internal/model/task.go` 中：

```go
const TaskTypeGenDailyReport TaskType = "gen_daily_report"
```

在 `IsValid()` 的 switch 中追加：

```go
case TaskTypeReviewPR, TaskTypeFixIssue, TaskTypeGenTests, TaskTypeGenDailyReport:
```

在 `internal/queue/client.go` 中：

```go
const AsynqTypeGenDailyReport = "dtworkflow:gen_daily_report"
```

在 `taskTypeToAsynq` 的 switch 中追加：

```go
case model.TaskTypeGenDailyReport:
    return AsynqTypeGenDailyReport
```

**Step 2: Add ReportGenerator to Processor**

在 `internal/queue/processor.go` 中：

新增接口和注入函数：

```go
// ReportGenerator 每日报告生成接口
type ReportGenerator interface {
	Generate(ctx context.Context) error
}

// WithReportGenerator 注入每日报告生成器
func WithReportGenerator(gen ReportGenerator) ProcessorOption {
	return func(p *Processor) {
		p.reportGenerator = gen
	}
}
```

在 `Processor` 结构体中追加字段：

```go
	reportGenerator  ReportGenerator  // 每日报告生成器（可选）
```

**Step 3: Add gen_daily_report case in ProcessTask**

在 `ProcessTask` 方法的 `// 1. 反序列化 payload` 之前，添加对 `gen_daily_report` 任务类型的特殊处理。由于该任务没有 payload（Scheduler 入队时 payload 为空），需要单独处理：

在 `ProcessTask` 开头，反序列化之前追加：

```go
	// 每日报告任务：无 payload，不需要 Docker 容器，直接执行
	if task.Type() == AsynqTypeGenDailyReport {
		return p.handleDailyReport(ctx)
	}
```

新增方法：

```go
func (p *Processor) handleDailyReport(ctx context.Context) error {
	if p.reportGenerator == nil {
		p.logger.WarnContext(ctx, "收到每日报告任务但 reportGenerator 未注入，跳过")
		return fmt.Errorf("reportGenerator 未注入: %w", asynq.SkipRetry)
	}

	taskID := fmt.Sprintf("daily-report-%s", time.Now().Format("2006-01-02"))
	p.logger.InfoContext(ctx, "开始生成每日报告", "task_id", taskID)

	// 创建任务记录
	now := time.Now()
	record := &model.TaskRecord{
		ID:        taskID,
		TaskType:  model.TaskTypeGenDailyReport,
		Status:    model.TaskStatusRunning,
		CreatedAt: now,
		UpdatedAt: now,
		StartedAt: &now,
	}
	if err := p.store.CreateTask(ctx, record); err != nil {
		p.logger.WarnContext(ctx, "创建每日报告任务记录失败", "error", err)
		// 不阻塞执行
	}

	err := p.reportGenerator.Generate(ctx)

	record.UpdatedAt = time.Now()
	completedAt := time.Now()
	record.CompletedAt = &completedAt
	if err != nil {
		record.Status = model.TaskStatusFailed
		record.Error = err.Error()
	} else {
		record.Status = model.TaskStatusSucceeded
	}
	if updateErr := p.store.UpdateTask(ctx, record); updateErr != nil {
		p.logger.WarnContext(ctx, "更新每日报告任务状态失败", "error", updateErr)
	}

	if err != nil {
		p.logger.ErrorContext(ctx, "每日报告生成失败", "error", err)
		return fmt.Errorf("每日报告生成失败: %w", err)
	}
	p.logger.InfoContext(ctx, "每日报告生成成功", "task_id", taskID)
	return nil
}
```

**Step 4: Run all existing tests to confirm no regression**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/queue/ -v`
Expected: ALL PASS

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/model/ -v`
Expected: ALL PASS

**Step 5: Commit**

```bash
git add internal/model/task.go internal/queue/client.go internal/queue/processor.go
git commit -m "feat(m2.7): 新增 gen_daily_report 任务类型 + Processor 路由"
```

---

### Task 8: Scheduler 集成与 serve.go 装配

**Files:**
- Modify: `internal/cmd/serve.go:639-672` — asynq mux 注册后追加 Scheduler 启动
- Modify: `internal/cmd/serve.go:639-672` — processorOpts 追加 WithReportGenerator

**Step 1: Register daily report task type with mux**

在 `serve.go` 的 `mux.Handle(queue.AsynqTypeGenTests, processor)` 后追加：

```go
	mux.Handle(queue.AsynqTypeGenDailyReport, processor)
```

**Step 2: Wire ReportGenerator into Processor**

在 processorOpts 构建块中（`if deps.GiteaClient != nil && cfgManager != nil` 块之后），追加：

```go
	// M2.7: 每日报告装配
	if cfg.AppCfg != nil && cfg.AppCfg.DailyReport.Enabled {
		drCfg := cfg.AppCfg.DailyReport
		reportSender, reportErr := report.NewReportFeishuSender(drCfg.FeishuWebhook, drCfg.FeishuSecret)
		if reportErr != nil {
			return fmt.Errorf("初始化每日报告飞书发送器失败: %w", reportErr)
		}
		reportCollector := report.NewReviewStatCollector(deps.Store)
		reportGen := report.NewReportGenerator(reportCollector, reportSender, drCfg.Timezone, drCfg.SkipEmpty)
		processorOpts = append(processorOpts, queue.WithReportGenerator(reportGen))
	}
```

**Step 3: Start asynq Scheduler**

在 `mux.Handle` 之后、`deps.AsynqServer.Start(mux)` 之后追加 Scheduler 启动：

```go
	// M2.7: 启动每日报告 Scheduler
	var reportScheduler *asynq.Scheduler
	if cfg.AppCfg != nil && cfg.AppCfg.DailyReport.Enabled {
		drCfg := cfg.AppCfg.DailyReport
		loc, _ := time.LoadLocation(drCfg.Timezone) // 已在 Validate 校验
		redisOpt := asynq.RedisClientOpt{Addr: cfg.RedisAddr, Password: cfg.RedisPassword, DB: cfg.RedisDB}
		reportScheduler = asynq.NewScheduler(redisOpt, &asynq.SchedulerOpts{
			Location: loc,
		})
		entryID, schedErr := reportScheduler.Register(drCfg.Cron, asynq.NewTask(queue.AsynqTypeGenDailyReport, nil))
		if schedErr != nil {
			return fmt.Errorf("注册每日报告定时任务失败: %w", schedErr)
		}
		slog.Info("每日报告定时任务已注册", "cron", drCfg.Cron, "timezone", drCfg.Timezone, "entry_id", entryID)

		go func() {
			if runErr := reportScheduler.Run(); runErr != nil {
				slog.Error("每日报告 Scheduler 异常退出", "error", runErr)
			}
		}()
	}
```

**Step 4: Add Scheduler shutdown to graceful shutdown**

在 `gracefulShutdown` 或 `doGracefulShutdown` 中，在 `deps.AsynqServer.Shutdown()` 之后追加 Scheduler 关闭。

由于 `reportScheduler` 是 `runServeWithConfig` 的局部变量，需要通过闭包或在 `<-stopCh` 后关闭：

在 `<-stopCh` 之后、`gracefulShutdown` 调用之前追加：

```go
	// 关闭 Scheduler（在 gracefulShutdown 之前，确保不再入队新任务）
	if reportScheduler != nil {
		reportScheduler.Shutdown()
		slog.Info("每日报告 Scheduler 已关闭")
	}
```

**Step 5: Add import**

在 `serve.go` 的 import 中追加：

```go
	"otws19.zicp.vip/kelin/dtworkflow/internal/report"
```

**Step 6: Build to verify compilation**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go build ./...`
Expected: 编译成功

**Step 7: Commit**

```bash
git add internal/cmd/serve.go
git commit -m "feat(m2.7): serve.go 装配 Scheduler + ReportGenerator"
```

---

### Task 9: 示例配置更新

**Files:**
- Modify: `configs/dtworkflow.example.yaml` — 追加 daily_report 配置段

**Step 1: Append daily_report section**

在 `configs/dtworkflow.example.yaml` 末尾追加：

```yaml
# 每日评审统计报告
# 定时通过飞书发送前一天的 PR 评审统计摘要。
# 与实时通知的飞书 Webhook 独立配置，可发送到不同群。
daily_report:
  enabled: false                            # 启用开关
  cron: "0 9 * * *"                         # 定时触发表达式（默认每天 09:00）
  timezone: "Asia/Shanghai"                 # 触发时区
  skip_empty: false                         # 无评审活动时是否跳过发送（false = 发送简短心跳通知）
  feishu_webhook: ""                        # 飞书 Webhook URL
  feishu_secret: ""                         # 签名密钥（建议开启）
```

**Step 2: Verify example config can be loaded**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./internal/config/ -run TestExampleConfig -v`

如果没有现成的 TestExampleConfig 测试，可以直接编译确认。

**Step 3: Commit**

```bash
git add configs/dtworkflow.example.yaml
git commit -m "docs(m2.7): 示例配置补充 daily_report 配置段"
```

---

### Task 10: 全链路编译验证 + 全量测试

**Step 1: Full build**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go build ./...`
Expected: 编译成功

**Step 2: Full test suite**

Run: `cd /Users/kelin/Workspace/DTWorkflow && go test ./... -count=1`
Expected: ALL PASS

**Step 3: Lint**

Run: `cd /Users/kelin/Workspace/DTWorkflow && golangci-lint run ./...`
Expected: 无新增 lint 错误

**Step 4: 如有失败，修复后重新验证**

**Step 5: Final commit (if any fixes)**

```bash
git add -A
git commit -m "fix(m2.7): 全链路验证修复"
```

---

## 文件变更汇总

| 文件 | 操作 | Task |
|------|------|------|
| `internal/report/model.go` | 新增 | 1 |
| `internal/report/model_test.go` | 新增 | 1 |
| `internal/report/collector.go` | 新增 | 3 |
| `internal/report/collector_test.go` | 新增 | 3 |
| `internal/report/feishu_card.go` | 新增 | 5 |
| `internal/report/feishu_card_test.go` | 新增 | 5 |
| `internal/report/generator.go` | 新增 | 6 |
| `internal/report/generator_test.go` | 新增 | 6 |
| `internal/report/feishu_sender.go` | 新增 | 6 |
| `internal/store/store.go` | 修改 | 2 |
| `internal/store/sqlite.go` | 修改 | 2 |
| `internal/store/sqlite_test.go` | 修改 | 2 |
| `internal/config/config.go` | 修改 | 4 |
| `internal/config/validate.go` | 修改 | 4 |
| `internal/config/validate_test.go` | 修改 | 4 |
| `internal/notify/notifier.go` | 修改 | 6 |
| `internal/model/task.go` | 修改 | 7 |
| `internal/queue/client.go` | 修改 | 7 |
| `internal/queue/processor.go` | 修改 | 7 |
| `internal/cmd/serve.go` | 修改 | 8 |
| `configs/dtworkflow.example.yaml` | 修改 | 9 |
