# M5.3 E2E 模块拆分与并行执行 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 E2E 全量执行添加模块发现与拆分能力，按模块独立入队并行执行，各模块独立通知和报告。

**Architecture:** 宿主侧新增 `ScanE2EModules` 扫描 `e2e/` 目录发现模块，`EnqueueManualE2E` 改为多任务返回，全量模式下按模块拆分入队。Store 层泛化 gen_tests 专用查询为 task_type 参数化通用查询。Processor 层零改动。

**Tech Stack:** Go, SQLite (modernc.org/sqlite), asynq, Gin, Cobra

**设计文档:** `docs/plans/2026-05-11-m5.3-e2e-module-split-parallel-design.md`

---

## 文件结构总览

| 文件 | 操作 | 职责 |
|------|------|------|
| `internal/e2e/scan.go` | 新建 | `E2EModuleScanner` 接口 + `ScanE2EModules` 函数 |
| `internal/e2e/scan_test.go` | 新建 | scan 单元测试 |
| `internal/e2e/errors.go` | 修改 | 新增 `ErrNoE2EModulesFound` |
| `internal/store/store.go` | 修改 | 泛化接口：`FindActiveTasksByModule` + `ListActiveModules` |
| `internal/store/sqlite.go` | 修改 | SQL 实现 task_type 参数化 |
| `internal/store/sqlite_test.go` | 修改 | 泛化后回归测试 |
| `internal/queue/enqueue.go` | 修改 | `EnqueueManualE2E` 多模块拆分 + helpers + option |
| `internal/queue/enqueue_test.go` | 修改 | E2E 拆分单元测试 |
| `internal/api/e2e_handler.go` | 修改 | 响应适配 + 422 错误处理 |
| `internal/cmd/e2e.go` | 修改 | 服务端 CLI 适配多任务输出 + 注入 scanner |
| `internal/dtw/cmd/e2e.go` | 修改 | dtw 适配多任务 + 等待模式 |
| `internal/cmd/serve_deps.go` | 修改 | 装配层注入 `WithE2EModuleScanner` |

---

## Task 1: E2E 模块发现 — ScanE2EModules

**Files:**
- Create: `internal/e2e/scan.go`
- Create: `internal/e2e/scan_test.go`
- Modify: `internal/e2e/errors.go`

### 1.1 新增 sentinel error

- [ ] **Step 1: 在 `internal/e2e/errors.go` 添加 `ErrNoE2EModulesFound`**

```go
ErrNoE2EModulesFound = errors.New("e2e: 未发现包含 cases/ 的模块")
```

在现有 `var` 块内追加即可。

- [ ] **Step 2: 确认编译通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./internal/e2e/`
Expected: 无错误

### 1.2 实现 ScanE2EModules

- [ ] **Step 3: 创建 `internal/e2e/scan.go`，编写接口和函数**

```go
package e2e

import (
	"context"
	"log/slog"
)

// E2EModuleScanner 扫描仓库 E2E 模块的窄接口。
// giteaRepoFileChecker（internal/cmd/adapter.go）天然满足此接口。
type E2EModuleScanner interface {
	ListDir(ctx context.Context, owner, repo, ref, dir string) ([]string, error)
}

const maxE2EScanDirs = 30

// ScanE2EModules 扫描仓库 e2e/ 目录，发现包含 cases/ 子目录的模块。
// 返回模块名列表（如 ["order", "auth"]）。
func ScanE2EModules(ctx context.Context, scanner E2EModuleScanner,
	owner, repo, ref string) ([]string, error) {

	topDirs, err := scanner.ListDir(ctx, owner, repo, ref, "e2e")
	if err != nil {
		return nil, err
	}

	if len(topDirs) == 0 {
		return nil, ErrNoE2EModulesFound
	}

	if len(topDirs) > maxE2EScanDirs {
		slog.Warn("ScanE2EModules: 子目录数量超过上限，截断处理",
			"total", len(topDirs), "max", maxE2EScanDirs, "owner", owner, "repo", repo)
		topDirs = topDirs[:maxE2EScanDirs]
	}

	var modules []string
	for _, dir := range topDirs {
		subEntries, subErr := scanner.ListDir(ctx, owner, repo, ref, "e2e/"+dir)
		if subErr != nil {
			slog.Warn("ScanE2EModules: 子目录扫描失败，跳过",
				"dir", dir, "error", subErr)
			continue
		}
		for _, entry := range subEntries {
			if entry == "cases" {
				modules = append(modules, dir)
				break
			}
		}
	}

	if len(modules) == 0 {
		return nil, ErrNoE2EModulesFound
	}
	return modules, nil
}
```

- [ ] **Step 4: 确认编译通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./internal/e2e/`
Expected: 无错误

### 1.3 编写 scan 单元测试

- [ ] **Step 5: 创建 `internal/e2e/scan_test.go`**

```go
package e2e

import (
	"context"
	"errors"
	"testing"
)

type mockE2EScanner struct {
	dirs map[string][]string
	errs map[string]error
}

func (m *mockE2EScanner) ListDir(_ context.Context, _, _, _, dir string) ([]string, error) {
	if err, ok := m.errs[dir]; ok {
		return nil, err
	}
	return m.dirs[dir], nil
}

func TestScanE2EModules_Normal(t *testing.T) {
	scanner := &mockE2EScanner{
		dirs: map[string][]string{
			"e2e":         {"order", "auth", "docs"},
			"e2e/order":   {"cases", "fixtures"},
			"e2e/auth":    {"cases"},
			"e2e/docs":    {"readme.md"},
		},
	}
	modules, err := ScanE2EModules(context.Background(), scanner, "o", "r", "main")
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	if len(modules) != 2 {
		t.Fatalf("expected 2 modules, got %d: %v", len(modules), modules)
	}
	if modules[0] != "order" || modules[1] != "auth" {
		t.Errorf("unexpected modules: %v", modules)
	}
}

func TestScanE2EModules_Empty(t *testing.T) {
	scanner := &mockE2EScanner{dirs: map[string][]string{"e2e": {}}}
	_, err := ScanE2EModules(context.Background(), scanner, "o", "r", "main")
	if !errors.Is(err, ErrNoE2EModulesFound) {
		t.Fatalf("expected ErrNoE2EModulesFound, got: %v", err)
	}
}

func TestScanE2EModules_NoCasesDir(t *testing.T) {
	scanner := &mockE2EScanner{
		dirs: map[string][]string{
			"e2e":       {"order", "auth"},
			"e2e/order": {"fixtures"},
			"e2e/auth":  {"scripts"},
		},
	}
	_, err := ScanE2EModules(context.Background(), scanner, "o", "r", "main")
	if !errors.Is(err, ErrNoE2EModulesFound) {
		t.Fatalf("expected ErrNoE2EModulesFound, got: %v", err)
	}
}

func TestScanE2EModules_TopDirFails(t *testing.T) {
	scanner := &mockE2EScanner{
		errs: map[string]error{"e2e": errors.New("network error")},
	}
	_, err := ScanE2EModules(context.Background(), scanner, "o", "r", "main")
	if err == nil || errors.Is(err, ErrNoE2EModulesFound) {
		t.Fatalf("expected non-ErrNoE2EModulesFound error, got: %v", err)
	}
}

func TestScanE2EModules_SubDirFails_SkipsContinues(t *testing.T) {
	scanner := &mockE2EScanner{
		dirs: map[string][]string{
			"e2e":       {"order", "auth"},
			"e2e/auth":  {"cases"},
		},
		errs: map[string]error{"e2e/order": errors.New("timeout")},
	}
	modules, err := ScanE2EModules(context.Background(), scanner, "o", "r", "main")
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	if len(modules) != 1 || modules[0] != "auth" {
		t.Fatalf("expected [auth], got: %v", modules)
	}
}

func TestScanE2EModules_Truncation(t *testing.T) {
	dirs := make([]string, 35)
	scanner := &mockE2EScanner{dirs: map[string][]string{}}
	for i := range dirs {
		name := "mod" + string(rune('a'+i%26)) + string(rune('0'+i/26))
		dirs[i] = name
		scanner.dirs["e2e/"+name] = []string{"cases"}
	}
	scanner.dirs["e2e"] = dirs
	modules, err := ScanE2EModules(context.Background(), scanner, "o", "r", "main")
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	if len(modules) > maxE2EScanDirs {
		t.Fatalf("expected at most %d modules, got %d", maxE2EScanDirs, len(modules))
	}
}
```

- [ ] **Step 6: 运行测试验证全部通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/e2e/ -run TestScanE2E -v`
Expected: 全部 PASS

- [ ] **Step 7: 提交**

```bash
git add internal/e2e/scan.go internal/e2e/scan_test.go internal/e2e/errors.go
git commit -m "feat(m5.3): 新增 ScanE2EModules 模块发现功能"
```

---

## Task 2: Store 层泛化

**Files:**
- Modify: `internal/store/store.go`
- Modify: `internal/store/sqlite.go`
- Modify: `internal/store/sqlite_test.go`

### 2.1 接口变更

- [ ] **Step 1: 在 `internal/store/store.go` 添加新接口方法**

在 `Store` 接口中添加：

```go
// FindActiveTasksByModule 查找同一仓库 + module 粒度的活跃任务
// （pending/queued/running/retrying）。泛化自 FindActiveGenTestsTasks。
// module 为空时匹配 payload.module 字段缺失的记录。
FindActiveTasksByModule(ctx context.Context, repoFullName, module string, taskType model.TaskType) ([]*model.TaskRecord, error)

// ListActiveModules 返回指定仓库下活跃任务的 module 列表。泛化自 ListActiveGenTestsModules。
ListActiveModules(ctx context.Context, repoFullName string, taskType model.TaskType) ([]string, error)
```

暂时保留旧方法（`FindActiveGenTestsTasks` / `ListActiveGenTestsModules`），待调用方迁移后删除。

- [ ] **Step 2: 确认编译通过（接口暂未实现，先写签名）**

### 2.2 SQLite 实现

- [ ] **Step 3: 在 `internal/store/sqlite.go` 添加 `FindActiveTasksByModule` 实现**

```go
func (s *SQLiteStore) FindActiveTasksByModule(ctx context.Context, repoFullName, module string, taskType model.TaskType) ([]*model.TaskRecord, error) {
	query := `SELECT ` + taskColumns + `
	FROM tasks
	WHERE repo_full_name = ?
	  AND task_type = ?
	  AND COALESCE(json_extract(payload, '$.module'), '') = ?
	  AND status IN (?, ?, ?, ?)
	ORDER BY created_at ASC`

	rows, err := s.db.QueryContext(ctx, query,
		repoFullName,
		string(taskType),
		module,
		string(model.TaskStatusPending),
		string(model.TaskStatusQueued),
		string(model.TaskStatusRunning),
		string(model.TaskStatusRetrying),
	)
	if err != nil {
		return nil, fmt.Errorf("查找活跃 %s 任务失败: %w", taskType, err)
	}
	defer rows.Close()

	var records []*model.TaskRecord
	for rows.Next() {
		record, err := scanTaskRecord(rows)
		if err != nil {
			return nil, fmt.Errorf("扫描活跃 %s 任务失败: %w", taskType, err)
		}
		records = append(records, record)
	}
	if err := rows.Err(); err != nil {
		return nil, fmt.Errorf("遍历活跃 %s 任务失败: %w", taskType, err)
	}
	return records, nil
}
```

- [ ] **Step 4: 在 `internal/store/sqlite.go` 添加 `ListActiveModules` 实现**

```go
func (s *SQLiteStore) ListActiveModules(ctx context.Context, repoFullName string, taskType model.TaskType) ([]string, error) {
	query := `SELECT COALESCE(json_extract(payload, '$.module'), '') AS module
	FROM tasks
	WHERE task_type = ?
	  AND repo_full_name = ?
	  AND status IN (?, ?, ?, ?)
	ORDER BY created_at ASC`

	rows, err := s.db.QueryContext(ctx, query,
		string(taskType),
		repoFullName,
		string(model.TaskStatusPending),
		string(model.TaskStatusQueued),
		string(model.TaskStatusRunning),
		string(model.TaskStatusRetrying),
	)
	if err != nil {
		return nil, fmt.Errorf("查询活跃 %s module 列表失败: %w", taskType, err)
	}
	defer rows.Close()

	var modules []string
	for rows.Next() {
		var m string
		if err := rows.Scan(&m); err != nil {
			return nil, fmt.Errorf("扫描 %s module 失败: %w", taskType, err)
		}
		modules = append(modules, m)
	}
	if err := rows.Err(); err != nil {
		return nil, fmt.Errorf("遍历 %s module 失败: %w", taskType, err)
	}
	return modules, nil
}
```

- [ ] **Step 5: 将旧方法改为委托调用新方法**

```go
func (s *SQLiteStore) FindActiveGenTestsTasks(ctx context.Context, repoFullName, module string) ([]*model.TaskRecord, error) {
	return s.FindActiveTasksByModule(ctx, repoFullName, module, model.TaskTypeGenTests)
}

func (s *SQLiteStore) ListActiveGenTestsModules(ctx context.Context, repoFullName string) ([]string, error) {
	return s.ListActiveModules(ctx, repoFullName, model.TaskTypeGenTests)
}
```

- [ ] **Step 6: 确认编译通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./internal/store/`
Expected: 无错误

### 2.3 测试验证

- [ ] **Step 7: 更新所有 mock Store 以满足扩展后的接口**

以下 4 个 mock Store 必须添加 `FindActiveTasksByModule` 和 `ListActiveModules` 的 stub 实现，否则编译失败：

- `internal/queue/enqueue_test.go` 的 `mockStore`
- `internal/queue/recovery_test.go` 的 `mockStoreForRecovery`
- `internal/api/helpers_test.go` 的 `mockStore`
- `internal/cmd/task_test.go` 的 `mockTaskStore`（如存在）

每个 mock 添加：

```go
func (m *mockStore) FindActiveTasksByModule(_ context.Context, _, _ string, _ model.TaskType) ([]*model.TaskRecord, error) {
	return nil, nil
}
func (m *mockStore) ListActiveModules(_ context.Context, _ string, _ model.TaskType) ([]string, error) {
	return nil, nil
}
```

确认全量编译通过：

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./...`
Expected: 无错误

- [ ] **Step 8: 在 `internal/store/sqlite_test.go` 添加泛化方法测试**

添加测试用例验证：
1. `FindActiveTasksByModule` 按 `TaskTypeGenTests` 查询 → 结果与旧 `FindActiveGenTestsTasks` 一致
2. `FindActiveTasksByModule` 按 `TaskTypeRunE2E` 查询 → 正确过滤
3. `ListActiveModules` 按 `TaskTypeGenTests` / `TaskTypeRunE2E` 分别查询 → 正确隔离

```go
func TestFindActiveTasksByModule_GenTests(t *testing.T) {
	s := newTestStore(t)
	ctx := context.Background()

	// 创建 gen_tests 任务
	r1 := newTestRecord("repo/a", model.TaskTypeGenTests, model.TaskStatusRunning)
	r1.Payload.Module = "backend"
	must(t, s.CreateTask(ctx, r1))

	// 创建 run_e2e 任务（同 module，不应被 gen_tests 查询命中）
	r2 := newTestRecord("repo/a", model.TaskTypeRunE2E, model.TaskStatusRunning)
	r2.Payload.Module = "backend"
	must(t, s.CreateTask(ctx, r2))

	tasks, err := s.FindActiveTasksByModule(ctx, "repo/a", "backend", model.TaskTypeGenTests)
	must(t, err)
	if len(tasks) != 1 || tasks[0].ID != r1.ID {
		t.Fatalf("expected only gen_tests task, got %d tasks", len(tasks))
	}
}

func TestFindActiveTasksByModule_RunE2E(t *testing.T) {
	s := newTestStore(t)
	ctx := context.Background()

	r1 := newTestRecord("repo/a", model.TaskTypeRunE2E, model.TaskStatusRunning)
	r1.Payload.Module = "order"
	must(t, s.CreateTask(ctx, r1))

	r2 := newTestRecord("repo/a", model.TaskTypeRunE2E, model.TaskStatusRunning)
	r2.Payload.Module = "auth"
	must(t, s.CreateTask(ctx, r2))

	tasks, err := s.FindActiveTasksByModule(ctx, "repo/a", "order", model.TaskTypeRunE2E)
	must(t, err)
	if len(tasks) != 1 || tasks[0].ID != r1.ID {
		t.Fatalf("expected 1 task for order module, got %d", len(tasks))
	}
}

func TestListActiveModules_IsolatesByTaskType(t *testing.T) {
	s := newTestStore(t)
	ctx := context.Background()

	r1 := newTestRecord("repo/a", model.TaskTypeGenTests, model.TaskStatusRunning)
	r1.Payload.Module = "backend"
	must(t, s.CreateTask(ctx, r1))

	r2 := newTestRecord("repo/a", model.TaskTypeRunE2E, model.TaskStatusRunning)
	r2.Payload.Module = "order"
	must(t, s.CreateTask(ctx, r2))

	genModules, err := s.ListActiveModules(ctx, "repo/a", model.TaskTypeGenTests)
	must(t, err)
	if len(genModules) != 1 || genModules[0] != "backend" {
		t.Fatalf("unexpected gen_tests modules: %v", genModules)
	}

	e2eModules, err := s.ListActiveModules(ctx, "repo/a", model.TaskTypeRunE2E)
	must(t, err)
	if len(e2eModules) != 1 || e2eModules[0] != "order" {
		t.Fatalf("unexpected e2e modules: %v", e2eModules)
	}
}
```

- [ ] **Step 9: 运行 store 测试验证通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/store/ -v -count=1`
Expected: 全部 PASS（包括现有回归测试）

- [ ] **Step 10: 提交**

```bash
git add internal/store/store.go internal/store/sqlite.go internal/store/sqlite_test.go
git commit -m "feat(m5.3): 泛化 Store 查询接口支持按 task_type 参数化"
```

---

## Task 3: 入队层 E2E 多模块拆分

**Files:**
- Modify: `internal/queue/enqueue.go`
- Modify: `internal/queue/enqueue_test.go`

### 3.1 添加 E2EModuleScanner 依赖注入

- [ ] **Step 1: 在 `internal/queue/enqueue.go` 的 `EnqueueHandler` 结构体添加字段和 option**

在 `EnqueueHandler` struct 中添加：

```go
e2eModuleScanner e2e.E2EModuleScanner // M5.3: 可选，nil 时全量模式退化为单任务
```

添加 option 函数：

```go
// WithE2EModuleScanner 注入 E2EModuleScanner 用于 M5.3 全量模式模块发现。
func WithE2EModuleScanner(s e2e.E2EModuleScanner) EnqueueOption {
	return func(h *EnqueueHandler) { h.e2eModuleScanner = s }
}
```

需要在文件头部 import 添加 `e2esvc "otws19.zicp.vip/kelin/dtworkflow/internal/e2e"`（注意已有别名约定，与 serve.go 一致）。

- [ ] **Step 2: 确认编译通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./internal/queue/`
Expected: 无错误

### 3.2 添加 E2E 私有 helpers

- [ ] **Step 3: 添加 `listActiveE2ETasks` helper**

```go
// listActiveE2ETasks 查找同模块活跃 E2E 任务（fail-open）。
func (h *EnqueueHandler) listActiveE2ETasks(ctx context.Context, repoFullName, module string) []*model.TaskRecord {
	tasks, err := h.store.FindActiveTasksByModule(ctx, repoFullName, module, model.TaskTypeRunE2E)
	if err != nil {
		h.logger.WarnContext(ctx, "查找活跃 run_e2e 任务失败,跳过取消",
			"repo", repoFullName, "module", module, "error", err)
		return nil
	}
	return tasks
}
```

- [ ] **Step 4: 添加 `cleanupAllActiveE2ETasks` helper**

```go
// cleanupAllActiveE2ETasks 全量拆分前清理同仓库所有活跃 run_e2e 任务。
func (h *EnqueueHandler) cleanupAllActiveE2ETasks(ctx context.Context, repoFullName string) {
	modules, err := h.store.ListActiveModules(ctx, repoFullName, model.TaskTypeRunE2E)
	if err != nil {
		h.logger.WarnContext(ctx, "cleanupAllE2E: 查询活跃 run_e2e module 列表失败",
			"repo", repoFullName, "error", err)
		return
	}
	for _, mod := range modules {
		tasks, tErr := h.store.FindActiveTasksByModule(ctx, repoFullName, mod, model.TaskTypeRunE2E)
		if tErr != nil {
			h.logger.WarnContext(ctx, "cleanupAllE2E: 查询活跃 run_e2e 任务失败",
				"repo", repoFullName, "module", mod, "error", tErr)
			continue
		}
		h.cancelTasks(ctx, tasks)
	}
}
```

- [ ] **Step 5: 添加 `enqueueSingleE2E` helper**

```go
// enqueueSingleE2E 封装单个 E2E 任务的入队 + Cancel-and-Replace。
func (h *EnqueueHandler) enqueueSingleE2E(ctx context.Context, payload model.TaskPayload, triggeredBy string) (EnqueuedTask, error) {
	payload.TaskType = model.TaskTypeRunE2E
	if payload.DeliveryID == "" {
		payload.DeliveryID = generateManualDeliveryID()
	}

	activeTasks := h.listActiveE2ETasks(ctx, payload.RepoFullName, payload.Module)

	record := &model.TaskRecord{
		TaskType:     model.TaskTypeRunE2E,
		Priority:     model.PriorityNormal,
		RepoFullName: payload.RepoFullName,
		DeliveryID:   payload.DeliveryID,
		TriggeredBy:  triggeredBy,
	}

	if err := h.enqueueTask(ctx, payload, record); err != nil {
		return EnqueuedTask{}, err
	}
	h.cancelTasks(ctx, activeTasks)

	h.logger.InfoContext(ctx, "run_e2e 任务已入队",
		"task_id", record.ID, "repo", payload.RepoFullName,
		"module", payload.Module, "triggered_by", triggeredBy)
	return EnqueuedTask{TaskID: record.ID, Module: payload.Module}, nil
}
```

- [ ] **Step 6: 确认编译通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./internal/queue/`
Expected: 无错误

### 3.3 改造 EnqueueManualE2E

- [ ] **Step 7: 重写 `EnqueueManualE2E` 签名和实现**

将现有 `EnqueueManualE2E` 改为返回 `([]EnqueuedTask, error)`：

```go
// EnqueueManualE2E 手动入队 E2E 测试任务。
// M5.3：全量模式（module 空 + e2eModuleScanner 已注入 + baseRef 非空）时
// 自动扫描 e2e/ 目录拆分子任务。
func (h *EnqueueHandler) EnqueueManualE2E(ctx context.Context, payload model.TaskPayload, triggeredBy string) ([]EnqueuedTask, error) {
	if payload.RepoFullName == "" || payload.CloneURL == "" {
		return nil, fmt.Errorf("payload 数据不完整: RepoFullName 或 CloneURL 为空")
	}

	// 全量模式：module 空 + scanner 已注入 + baseRef 非空
	if payload.Module == "" && h.e2eModuleScanner != nil && payload.BaseRef != "" {
		modules, err := e2esvc.ScanE2EModules(ctx, h.e2eModuleScanner,
			payload.RepoOwner, payload.RepoName, payload.BaseRef)
		if err != nil {
			if errors.Is(err, e2esvc.ErrNoE2EModulesFound) {
				return nil, err
			}
			h.logger.WarnContext(ctx, "ScanE2EModules 失败，回退到单任务逻辑",
				"repo", payload.RepoFullName, "error", err)
			result, singleErr := h.enqueueSingleE2E(ctx, payload, triggeredBy)
			if singleErr != nil {
				return nil, singleErr
			}
			return []EnqueuedTask{result}, nil
		}

		if len(modules) >= 2 {
			h.cleanupAllActiveE2ETasks(ctx, payload.RepoFullName)
		}

		var results []EnqueuedTask
		for _, mod := range modules {
			subPayload := payload
			subPayload.Module = mod
			subPayload.DeliveryID = ""
			result, subErr := h.enqueueSingleE2E(ctx, subPayload, triggeredBy)
			if subErr != nil {
				h.logger.WarnContext(ctx, "E2E 子任务入队失败",
					"repo", payload.RepoFullName, "module", mod, "error", subErr)
				continue
			}
			results = append(results, result)
		}
		if len(results) == 0 {
			return nil, fmt.Errorf("所有 E2E 子任务入队均失败")
		}

		h.logger.InfoContext(ctx, "E2E 全量拆分入队完成",
			"repo", payload.RepoFullName, "discovered", len(modules),
			"enqueued", len(results), "triggered_by", triggeredBy)
		return results, nil
	}

	// 非全量模式：单任务入队
	result, err := h.enqueueSingleE2E(ctx, payload, triggeredBy)
	if err != nil {
		return nil, err
	}
	return []EnqueuedTask{result}, nil
}
```

- [ ] **Step 8: 确认编译通过（此时调用方签名不匹配，可能有编译错误）**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./internal/queue/`
Expected: 编译通过（queue 包内部无调用方问题）

### 3.4 编写入队层测试

- [ ] **Step 9: 在 `internal/queue/enqueue_test.go` 添加 E2E 拆分测试**

添加以下测试：
1. 全量拆分路径：scanner 返回 2 模块 → 2 个子任务入队
2. scanner 未注入 → 单任务入队
3. 扫描失败 → 回退单任务
4. `ErrNoE2EModulesFound` → 返回 error
5. 单模块 Cancel-and-Replace 正确性

（具体测试代码参考现有 `TestEnqueueManualGenTests_*` 测试风格）

- [ ] **Step 10: 运行 queue 包测试验证通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./internal/queue/ -v -count=1 -run TestEnqueueManualE2E`
Expected: 全部 PASS

- [ ] **Step 11: 暂不提交（与 Task 4/5 合并提交）**

> 此时 `go build ./...` 失败（API handler / CLI 调用方签名不匹配），
> 等 Task 4 和 Task 5 完成后一起提交。单包验证：
> Run: `go build ./internal/queue/`
> Expected: 通过

---

## Task 4: API Handler 适配

> **编译顺序注意**：Task 3 修改了 `EnqueueManualE2E` 签名（返回 `[]EnqueuedTask` 而非 `string`），
> 此时 `go build ./...` 会失败（调用方未更新）。Task 4 和 Task 5 修复调用方后才能全量编译通过。
> **建议**：Task 3/4/5 可合并为一次提交，或按 `go build ./internal/queue/` 验证单包编译。

**Files:**
- Modify: `internal/api/e2e_handler.go`

### 4.1 适配多任务返回

- [ ] **Step 1: 修改 `triggerE2E` 处理 `EnqueueManualE2E` 新签名**

关键改动：
1. `EnqueueManualE2E` 返回 `([]EnqueuedTask, error)` → 签名适配
2. `errors.Is(err, e2e.ErrNoE2EModulesFound)` → 返回 HTTP 422
3. `len(results) > 1` → 返回 `split/tasks` 结构
4. `len(results) == 1` → 保持现有单任务响应格式

```go
results, err := h.deps.EnqueueHandler.EnqueueManualE2E(ctx, payload, triggeredBy)
if err != nil {
	if errors.Is(err, e2esvc.ErrNoE2EModulesFound) {
		Error(c, http.StatusUnprocessableEntity, ErrCodeBadRequest,
			"仓库中未发现 E2E 测试模块（需 e2e/{module}/cases/ 目录结构）")
		return
	}
	h.deps.Logger.Error("入队 e2e 失败",
		"owner", owner, "repo", repo, "error", err)
	Error(c, http.StatusInternalServerError, ErrCodeInternalError, "入队 e2e 失败")
	return
}

if len(results) == 1 {
	Success(c, http.StatusAccepted, gin.H{
		"task_id": results[0].TaskID,
		"repo":    repoInfo.FullName,
		"module":  results[0].Module,
		"env":     req.Env,
		"status":  "pending",
	})
	return
}

type taskInfo struct {
	TaskID string `json:"task_id"`
	Module string `json:"module"`
}
tasks := make([]taskInfo, 0, len(results))
for _, r := range results {
	tasks = append(tasks, taskInfo{TaskID: r.TaskID, Module: r.Module})
}
Success(c, http.StatusAccepted, gin.H{
	"split": true,
	"tasks": tasks,
})
```

需要在文件头部添加 import：`e2esvc "otws19.zicp.vip/kelin/dtworkflow/internal/e2e"`

- [ ] **Step 2: 确认编译通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./internal/api/`
Expected: 无错误

- [ ] **Step 3: 暂不提交（与 Task 3/5 合并提交）**

---

## Task 5: 服务端 CLI 适配

**Files:**
- Modify: `internal/cmd/e2e.go`

### 5.1 适配多任务输出 + 注入 scanner

- [ ] **Step 1: 修改 `runE2E` 函数**

关键改动：
1. 在构造 `EnqueueHandler` 时注入 `queue.WithE2EModuleScanner(&giteaRepoFileChecker{client: gc})`
2. `EnqueueManualE2E` 返回 `[]EnqueuedTask` → 适配输出
3. 单任务时调用现有 `printE2EResult`
4. 多任务时输出汇总表格

```go
// 4. 构造 EnqueueHandler（注入 scanner）
handler := queue.NewEnqueueHandler(qClient, qClient, s, slog.Default(),
	queue.WithE2EModuleScanner(&giteaRepoFileChecker{client: gc}),
)

// 5. 入队
results, err := handler.EnqueueManualE2E(ctx, payload, buildCLITriggeredBy())
if err != nil {
	if errors.Is(err, e2esvc.ErrNoE2EModulesFound) {
		return &ExitCodeError{Code: 1, Err: fmt.Errorf("仓库中未发现 E2E 测试模块（需 e2e/{module}/cases/ 目录结构）")}
	}
	return &ExitCodeError{Code: 1, Err: fmt.Errorf("入队失败: %w", err)}
}

if len(results) == 1 {
	printE2EResult(results[0].TaskID, payload)
} else {
	printE2ESplitResult(results, payload)
}
```

添加 `printE2ESplitResult` 函数：

```go
func printE2ESplitResult(results []queue.EnqueuedTask, payload model.TaskPayload) {
	type e2eSplitTask struct {
		TaskID string `json:"task_id"`
		Module string `json:"module"`
	}
	type splitResult struct {
		Split bool           `json:"split"`
		Repo  string         `json:"repo"`
		Env   string         `json:"env,omitempty"`
		Tasks []e2eSplitTask `json:"tasks"`
	}
	tasks := make([]e2eSplitTask, 0, len(results))
	for _, r := range results {
		tasks = append(tasks, e2eSplitTask{TaskID: r.TaskID, Module: r.Module})
	}
	data := splitResult{Split: true, Repo: payload.RepoFullName, Env: payload.Environment, Tasks: tasks}
	PrintResult(data, func(v any) string {
		r := v.(splitResult)
		var sb strings.Builder
		fmt.Fprintf(&sb, "E2E 测试已拆分为 %d 个模块任务：\n\n", len(r.Tasks))
		fmt.Fprintf(&sb, "  %-16s %s\n", "模块", "任务 ID")
		for _, t := range r.Tasks {
			fmt.Fprintf(&sb, "  %-16s %s\n", t.Module, t.TaskID)
		}
		return sb.String()
	})
}
```

- [ ] **Step 2: 确认编译通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./internal/cmd/`
Expected: 无错误

- [ ] **Step 3: 合并提交 Task 3/4/5（签名变更 + 所有调用方适配）**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./...`
Expected: 全量编译通过

```bash
git add internal/queue/enqueue.go internal/queue/enqueue_test.go internal/api/e2e_handler.go internal/cmd/e2e.go
git commit -m "feat(m5.3): EnqueueManualE2E 多模块拆分 + API/CLI 调用方适配"
```

---

## Task 6: dtw 瘦客户端适配

**Files:**
- Modify: `internal/dtw/cmd/e2e.go`

### 6.1 适配多任务返回 + 逐任务等待

- [ ] **Step 1: 修改 `e2eRunCmd` 的 `RunE`**

关键改动：
1. 解析响应时兼容 `split` 字段
2. `--no-wait` 模式：拆分时输出汇总表格
3. 等待模式：逐个轮询所有子任务

```go
var result struct {
	TaskID string `json:"task_id"`
	Split  bool   `json:"split"`
	Tasks  []struct {
		TaskID string `json:"task_id"`
		Module string `json:"module"`
	} `json:"tasks"`
}

path := fmt.Sprintf("/api/v1/repos/%s/%s/e2e", owner, repo)
if err := client.Do(cmd.Context(), "POST", path, body, &result); err != nil {
	return fmt.Errorf("提交 E2E 任务失败: %w", err)
}

// 收集需要等待的 taskID 列表
type taskEntry struct {
	ID     string
	Module string
}
var taskEntries []taskEntry

if result.Split && len(result.Tasks) > 0 {
	for _, t := range result.Tasks {
		taskEntries = append(taskEntries, taskEntry{ID: t.TaskID, Module: t.Module})
	}
} else {
	taskEntries = append(taskEntries, taskEntry{ID: result.TaskID})
}

if e2eNoWait {
	if result.Split {
		if !flagJSON {
			printer.PrintHuman("E2E 测试已拆分为 %d 个模块任务：", len(taskEntries))
			for _, t := range taskEntries {
				printer.PrintHuman("  %s  %s", t.Module, t.ID)
			}
		} else {
			return printer.PrintJSON(result)
		}
	} else {
		return printer.Print(fmt.Sprintf("E2E 任务已创建: %s", taskEntries[0].ID), result)
	}
	return nil
}

// 等待模式：逐个轮询
if !flagJSON && result.Split {
	printer.PrintHuman("E2E 测试已拆分为 %d 个模块任务，逐个等待完成...", len(taskEntries))
}

opts := dtw.DefaultWaitOptions()
if e2eTimeout > 0 {
	opts.Timeout = e2eTimeout
}

var failed int
for _, entry := range taskEntries {
	if !flagJSON {
		if entry.Module != "" {
			printer.PrintHuman("等待模块 %s (task: %s)...", entry.Module, entry.ID)
		} else {
			printer.PrintHuman("等待任务 %s...", entry.ID)
		}
	}
	status, err := dtw.WaitForTask(cmd.Context(), client, entry.ID, opts)
	if err != nil {
		if !flagJSON {
			printer.PrintHuman("等待任务 %s 失败: %v", entry.ID, err)
		}
		failed++
		continue
	}
	if !flagJSON {
		printer.PrintHuman("  task_id: %s  status: %s", status.ID, status.Status)
		if status.Error != "" {
			printer.PrintHuman("  错误: %s", status.Error)
		}
	}
	if status.Status == "failed" {
		failed++
	}
}

if flagJSON {
	return printer.PrintJSON(map[string]any{"total": len(taskEntries), "failed": failed})
}

if failed > 0 {
	return fmt.Errorf("E2E 任务失败: %d/%d", failed, len(taskEntries))
}
return nil
```

- [ ] **Step 2: 确认编译通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./internal/dtw/...`
Expected: 无错误

- [ ] **Step 3: 提交**

```bash
git add internal/dtw/cmd/e2e.go
git commit -m "feat(m5.3): dtw 瘦客户端适配 E2E 多模块拆分"
```

---

## Task 7: 装配层注入

**Files:**
- Modify: `internal/cmd/serve_deps.go`

### 7.1 注入 WithE2EModuleScanner

- [ ] **Step 1: 在 `BuildServiceDeps` 的 enqueueOpts 中添加 E2E scanner 注入**

在构造 `enqueueOpts` 的 `if giteaGenTestsClient != nil` 块中添加：

```go
// M5.3: E2E 模块发现复用同一 giteaRepoFileChecker
queue.WithE2EModuleScanner(&giteaRepoFileChecker{client: giteaGenTestsClient}),
```

位置：与 `queue.WithModuleScanner(...)` 相邻。

注意：`giteaRepoFileChecker` 已实现 `ListDir` 方法，天然满足 `e2e.E2EModuleScanner` 接口。需要在 `internal/cmd/adapter.go` 中添加编译时接口验证（与现有 `var _ testgen.RepoFileChecker = (*giteaRepoFileChecker)(nil)` 并列）：

```go
var _ e2e.E2EModuleScanner = (*giteaRepoFileChecker)(nil)
```

此处使用 `e2e` 而非 `e2esvc`，因为 `adapter.go` 中已有 `import "otws19.zicp.vip/kelin/dtworkflow/internal/e2e"` 无别名（与 `serve.go` 的 `e2esvc` 别名不同）。如果 `adapter.go` 尚未 import `e2e` 包，需要添加。

- [ ] **Step 2: 确认编译通过**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./...`
Expected: 无错误

- [ ] **Step 3: 提交**

```bash
git add internal/cmd/serve_deps.go internal/cmd/adapter.go
git commit -m "feat(m5.3): 装配层注入 E2E 模块扫描能力"
```

---

## Task 8: 全量回归验证

**Files:** 无新改动，仅验证

- [ ] **Step 1: 全量构建**

Run: `cd /Users/kelin/Work/DTWorkflow && go build ./...`
Expected: 无错误

- [ ] **Step 2: 全量测试**

Run: `cd /Users/kelin/Work/DTWorkflow && go test ./... -count=1`
Expected: 全部 PASS（现有 gen_tests / e2e 路径不受影响）

- [ ] **Step 3: 验证 `e2eModuleScanner=nil` 退化行为**

确认在不注入 scanner 的情况下（如 CLI 的 `queue.NewEnqueueHandler(...)` 不传 `WithE2EModuleScanner`），`EnqueueManualE2E` 退化为单任务逻辑，与 M5.1 行为完全一致。现有 CLI 调用路径（`internal/cmd/e2e.go` 改造后）和 API handler 都应正常工作。

- [ ] **Step 4: 静态检查**

Run: `cd /Users/kelin/Work/DTWorkflow && golangci-lint run ./...`
Expected: 无新 lint 错误

- [ ] **Step 5: 最终提交（如有 lint 修复）**

```bash
git add -A && git commit -m "fix(m5.3): lint 修复"
```

---

## 实施注意事项

1. **import 别名一致性**：`e2e` 包在 `queue/enqueue.go` 中使用 `e2esvc` 别名（因 `e2e` 是 Go 合法标识符但容易与变量冲突），与 `serve.go` 已有约定一致。
2. **Store 旧方法保留**：`FindActiveGenTestsTasks` / `ListActiveGenTestsModules` 保留为委托方法，避免一次性改动所有调用方（gen_tests 路径已验证稳定），后续可按需清理。
3. **`EnqueuedTask.Framework` 字段**：E2E 场景该字段留空，与设计文档一致。
4. **错误脱敏**：`ErrNoE2EModulesFound` 是业务错误，直接暴露给用户无安全风险；非此错误的 scan 失败走 warn 日志 + 回退，不向用户暴露内部错误。
5. **Processor 零改动**：拆分后各子任务仍通过 `TaskTypeRunE2E` 路由到 `e2e.Service.Execute`，与 M5.1 完全一致。
