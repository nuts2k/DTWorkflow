# DTWorkflow 本机部署配置模板
# 复制此文件为 deploy/local.env（已被 .gitignore 排除，不会提交）
# scripts/deploy.sh 和 scripts/rollback.sh 会自动 source 此文件
#
# 用途：记录本机到测试服务器的 SSH 入口，避免每次手动传参

# 测试服务器 SSH Host（对应 ~/.ssh/config 中的 Host 条目）
DEPLOY_HOST=companytest
